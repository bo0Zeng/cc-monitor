# 项目全面审阅报告 —— cc-monitor / Phase G 最终验收

- **日期**：2026-07-29
- **分支**：`account-ux`，审阅基线 HEAD = `2e2d010`
- **范围**：整个项目当前状态。重点是 `unify-launch`（F01-F11 + R 段 9/9 + B 段 B01-B04 + L2）
  与 `integrate-toolchain`（T01/T02/T03/T04/T07）这两个工作区落地后的整体健康度。
- **做法**：四个视角的独立 agent 并行排查（整体设计 / 文档工程 / 代码工程 / 实现细节），
  主线程另做**文档-代码交叉对比**。四个 agent 全程只读、不跑 tmux、不写用户文件；
  代码工程视角自行做了 4 个变异实验并全部还原（收工时 `git status --short` 空）。
- **审阅 ≠ 修复**：本报告是清单 + 优先级。**只有阻塞级真 bug 才动了代码**，
  且每一条都由主线程独立复现后才修。未修项全部转登记到 `.claude/planned-build/BACKLOG.md` §E（E1-E32）。

---

## 摘要

**整体健康度：底层扎实，上层文档失真。**

代码侧的核心防线是真的有牙的——这一点被独立变异实验证实（见「代码工程」§做对了）。
四个视角合计报出 **6 条阻塞**、约 30 条重要、约 20 条建议。我逐条独立复现后的处置：

| | 数量 | 说明 |
|---|---|---|
| **阻塞：已修** | **5** | 2 条远端 profile 数据丢失 · 1 条 e2e 零断言报绿 · 1 条恢复入口失准 · 1 条假签收 + 遗留项未登记 |
| **阻塞：复现后降级** | **1** | 「两渲染器对 base 不等价」→ 降为重要（理由见下，不是驳回，是补了审阅没看到的一层事实） |
| **重要 / 建议：未修，已登记** | **32 条**（E1-E32） | 按「阻塞级真 bug 才动代码」的界线留给用户拍板 |

**最要紧的一句话**：这一轮**代码质量高于文档质量**。
代码里那套「反向自检 + 计数自检 + 变异验证」的纪律是真在执行的；
而**文档的「单一事实来源」全线失真**——两份被指定为「恢复入口」的 `STATUS.md` 落后实际 3-4 个功能，
两份 `MASTERPLAN.md` 的功能表把已交付的功能全标「待做」，`planned-build/README.md` 索引里
缺的正是当时唯一在动的工作区。**一个接手者按文档恢复，会重做已完成的工作、漏掉已落地的行为。**

---

## 一、整体设计

### 阻塞（复现后降级为重要 → BACKLOG E15）

**两个渲染器对「未选账号（base）」态不等价。**
CLI 渲染器（`tryRenderCli`）对 base 恒吐 `--base`，而 `shared/ccm:572` 收到 `--base` 就
`unset CLAUDE_CONFIG_DIR`；兜底渲染器（`renderFallback`）对 base 态**零 env op**
（`ACCOUNT_DIMENSION.apply` 在 `kind !== "account"` 时直接 return），载荷经
`bash -lic`（`launch.rs:133`）跑，**登录 shell 里继承/设置的 `CLAUDE_CONFIG_DIR` 原样生效**。
分支条件是「远端装没装 ccm」。

**我复现后补的一层**：审阅把它归为 R11 同型（「静默落 manifest 默认账号」）。
实测 `shared/ccm:320` 是 `elif [ "$use_base" != 1 ] && [ -z "${CLAUDE_CONFIG_DIR:-}" ]`
——**落 manifest 默认账号只在「无继承值」时触发**，而兜底路径没有 ccm、没有 manifest，
**R11 那个病灶在那条路上不存在**。剩下的暴露面是「用户自己在远端 profile 里 export 的值被继承」。

**那算 bug 还是用户本意？** 取决于「未选账号」的语义是「不注入」还是「强制基座」。
CLI 侧选了后者（F05，为修 R11），兜底侧沿用前者（legacy 逐字节等价是它的契约）。
**这是产品语义决定，不是明确的 bug**，所以我没有在验收轮里单方面改远端启动语义。→ **E15，待拍板**。

### 重要

| # | 发现 | 登记 |
|---|---|---|
| 1 | **「IR / CLI / UI 三处同一个模型」实际达成约一处半。** IR 对**远端**路径是真承重的（6 个 executor 全经 `planXxx → renderLaunchCommand`）；但 **UI 零消费注册表**（`container`/`agent` 轴不进注册表，账号取值走 `fetchAccounts`，`MODEL_DIMENSION` 无 UI 入口），**本地路径完全在 IR 之外** | 见下「文档-代码漂移」——这一条我交叉核实过，`MASTERPLAN` 的 F06 标题是过度声明，已订正 |
| 2 | **IR 落地后的新功能仍绕开它**：`remote-launch.ts:70` 的 `buildUsageProbePayload` 手拼 `export CLAUDE_CONFIG_DIR=…; unset <nested>; claude`，是 `account` + `nested-env-reset` 两个维度的手工复刻，第 6 个绕过 IR 的 builder，由 F10（晚于 F03）新增 | E23 |
| 3 | **IR 的类型设计被一条生产零调用点的测试专属路径塑造**：`remote-launch.ts` 五个导出生产零调用点，而 `LaunchAccount.name` 可选 + `cliFlags` 返回 `null` 强制降级这条分支，唯一理由是服务它。**一个没有生产消费者的兼容面换来一条永久降级分支** | E24 |
| 4 | **`tool_registry` + `config_surface` 是「为一张多半答不出的表造了一个注册表」**：注册表**不驱动任何行为**（装/探/卸仍是 5 套各写各的，没有一处读 `TOOLS`），唯一消费者产出一个只读报告；约 1043 行生产 + 1525 行测试换来 6 个工具 10 行 touches；而那 10 行里 7 行的 `observe()` 一律返回 `Undetermined`（本页明写不连 SSH），生产平台 Windows 上剩下 3 行的 `Either` 大概率也只能给「本机没找到」 | 讨论见「优先级建议」 |
| 5 | `ccm` 已长出 IR 表达不了的词汇：自报 `capabilities` 含 `agent`/`detach`/`tmux-size`，`tryRenderCli` 一个都不发 ⇒ **app 远端只能起 claude**；`@ccm_agent` 有写者零读者；`probe.version` 解析后零消费者 | E-未编号，见报告末 |
| 6 | **会话身份是渲染器相关的**：`shared/ccm` 写 `@ccm_sid_expect`（由 poller 提升），`session-backend.ts:113` 在 create 分支直写裸 `@ccm_sid`。`sftp.rs:959` 明写这是故意的、理由（兜底路径没有 poller，写 expect 会永不提升 ⇒ 会话不可 kill）成立。**但结果是「这个会话可不可被识别/kill」取决于它当初由哪个渲染器起的**，而那正是 F04 立项要消灭的那类根因 | 记录 |
| 7 | Rust 分层：**底层干净，上半是网**。`lib.rs` 同时是顶（119 项 `invoke_handler`）和底（`load_remote_config_by_label` 被 11 个模块 21 处向上调，共 29 处上行引用）；`ssh_source.rs` 入度 14；3 个真环（`search↔remote_history`、`ssh_source↔sftp`、`ssh_source↔tmux`）。9 个模块出度为 0，是真底层 | 与代码工程视角的结论**部分冲突**，见下 |
| 8 | **Rust↔TS 边界：命令名零漂移，payload 形状零门禁**。119 个命令名声明=注册=引用，双向 diff 空——但这是**人工纪律**：全仓唯一契约门禁覆盖 3/119 个命令。128 个 Rust struct 对 209 个 TS type，22 个文件靠手工 `camelCase`。`sftp_pool.rs:33` ↔ `src/sftp/paths.ts:6` 的 `u64→number` 是静默有损 | 记录 |
| 9 | **「Claude 配置目录在哪」有 4 份独立实现**（`hooks_diag.rs:415` / `paths.rs:33` / `mcp.rs:31` / `config_surface.rs:600`），而 `hooks_diag.rs:356` 自己写着「若它自己再写一遍判定，两处就会各自漂移」——`config_surface` 遵守了，另两处没有 | E22 |

### 我核实后认为做对了的架构决定（摘要）

- **`verified_write` / `fenced_block` / `paste-block` 是真提炼 + 真收编，不是并存**——旧实现被换掉而非留着。
  「这是本轮唯一达到『抽象吃掉了既有实现』标准的部分，与第 4 条的注册表形成对照——
  **同一批工作里两种结果，说明尺子是有的，只是没对注册表用**。」
- **把 `ccm` 做成纯优化而非依赖**：兜底渲染器保持逐字节旧行为、`tryRenderCli` fail-closed
  （`null` 就是拿不到命令，而不是静默丢修饰）、降级只 `console.debug`（没装 ccm 是正常态）。
- **拒绝为假想消费者建抽象，有可查的执行记录**：`ProbeStatus` 删掉、`--bus-id` 加了又删、
  `launch-menu.ts` 的 container 组删掉（附零消费者证据）、`WriteVerdict::is_ok` 删掉。
- **`cc_bus.rs` 只读 + 编排，没在 Rust 里重写 bash 总线**；配合地盘表（自己的地盘读写、
  `~/.bashrc` 与 `~/.claude/settings.json` 只读+生成待贴文本）是本项目最清晰的一条产品级边界。
- **`HostScope` / `EitherHost` 的引入理由是真的**：不加它，Windows 上审计页会对 cc-bus 三行
  说「不存在」，而驾驶舱正从远端读得好好的。

---

## 二、文档工程

### 阻塞（3 条，全部已修）

**B1. 两个活跃工作区的「恢复入口」都落后实际 3-4 个功能。**
`integrate-toolchain/STATUS.md` 写「当前功能：T03（Phase D 审计进行中）」，而 T04/T07 都已交付；
`unify-launch/STATUS.md` 相邻两行自相矛盾（`当前功能：B02 完成` 紧接 `下一个功能：B01`，
而 B01 早已签收），§B 表仍标 `B03 | 待做` / `B04 | 待做`，而 B03/B04 已有 7 个 commit。
**严重性**：这两份文件是接手者唯一被指定的入口。按它恢复 = 重做 B01、漏掉 B03/B04 已落地的行为。
→ **已修**（两份订正，并在 `unify-launch` 里留下「此前写着什么」）。

**B2. 本轮遗留项没有一条登进 BACKLOG，违反 BACKLOG 自己立的规矩。**
`BACKLOG.md:9` 明文：「凡在 feature/STATUS/MASTERPLAN 里写下『顺延到 X』的，**同时**在这里登一行。」
实测 `grep -n "R15\|R16\|B03\|void async" BACKLOG.md` **零命中**。
而 BACKLOG 的建档动因写的正是这种断链（U6 明写「顺延 U8」，U8 收官时它无声蒸发）。
**同一失效模式在本轮完整复现**，且 R16 挂着一个可能的生产 bug。
→ **已修**：新增 §E，登记 **32 条**（E1-E32）。

**B3. 五份新增 feature 文档章节号重复 + 一处假签收。**
`T02`/`T03`/`T04`/`T07` 各有两套 §5-§8（第一套「（待填）」、第二套才是真内容；`T01` 是唯一做对的）。
更要紧的是 `T07-settings-panel-ia.md` §6 勾了 `[x] 通过代码审计（待审计闭环后回填）`，
而同文件 §4 写「待开对抗性审计」、§9 记录审计查出 **2 个阻塞**。
**勾选框在审计之前打上、审计出阻塞后也没退回**——这与本工作区自己引的纪律
「**不伪造第二次验收**」直接冲突。
→ **假签收已修**（订正并留下原文与成因）；**章节号重复未修** → E25
（实测今天没有实际引用踩上去，现有跨文档引用都指向续编号的 §9/§12/§13，二义性是 latent）。

### 重要

- **I1 MASTERPLAN 已经不是它自称的「单一事实来源」**：`integrate-toolchain/MASTERPLAN.md` §1
  功能表 T01-T09 + G01-G03 **状态列全是「待做」**，而其中 5 个 T + 3 个 G 早已交付；
  订正写在 `P-READINESS.md` 里，从未回写。`unify-launch/MASTERPLAN.md` §1 表
  **完全没有 R00-R09 / B01-B04 的行**——三分之二的在跑工作只存在于 STATUS 的两张表里。
  → **已修**（两份功能表订正，并在表头写明此前状况）。
- **I2 文档间事实矛盾六处**。其中一处我用代码判定了归属（见「文档-代码漂移」①）。
  产品层那处：README 三处写 v3.2.0 而 `CHANGELOG`/`package.json`/`tauri.conf.json`/`Cargo.toml`
  都是 **3.3.0**；README 的测试数（cargo 365 / DOM 595）也过期。
  → **版本号与两个测试数已修**（改为 v3.3.0 / cargo 536 / DOM 814，均为本轮实测）；
  那个 1400 字单段落与「零账号功能覆盖」→ E30。
- **I3 悬空 / 误指引用**。最坏的一类：**4 处把 `doc/INVARIANTS.md` 的行号 37 写成了节号 §37**。
  那段内容（tmux 名跨语言白名单 + `enter?`）实际在 **§1**（恰好位于第 37 行）；
  而 §37 后来**真的被建出来了**（2026-07-27 `9531ef3`），讲的是维度 `applies`。
  **跟着引用走会落到一节看起来很正经、其实完全无关的内容上，而读者不会察觉——比 404 更坏。**
  → **已修**（4 处 + 在 MASTERPLAN 末尾记下成因与纪律）。
  其余：已删的 `shared/ccm-wrapper.sh` 仍被 4 处当现存文件引用（含对一个已删文件下达未来指令）→ E28；
  全仓 234 个 `file:line` 引用绝大多数是裸 basename，而 `panel.ts` 仓内有两个 → 记录。
- **I4 结论所依赖的数字已漂移**：`PHASE-G-DRAFT.md` 与 `T07` 的结论「826 行不是 god file」
  建立在 `panel.ts` = 826 行上，**实测 1011 行**（T07 自己加的分区块隔离就在那 185 行里）。
  **承重数字在写下终账的那一刻就已经不成立。**
  → **已修，且改的不只是数字**：以行数立论本身违反「拆分看缺陷不看行数」那条纪律
  （「N 行不算 god file」是同一把行数尺子的另一头），结论不变但理由里的行数删掉。
- **I5 索引表缺 integrate-toolchain + 七行失准** → **已修**（补一行 + 订正 7 行 +
  写明「以各区 STATUS 为准」这句免责当时救不了，因为被指向的 STATUS 自己也过期）。
- **I6 已完成/已废弃但未标注**：`auto-e2e` 的 E2-E5 有完整计划、零落地记录、无收官语；
  `codex-phase2/STATUS.md` 落后自己正文 6 个功能，暂停只写在 sibling 里 → 索引里已注明。

### 关于「自省记录是资产还是噪音」

文档视角给了一个我认为准确的三层判断，值得原样保留：

- **资产**：`PHASE-G-DRAFT.md` 节一（三次「计划预设了抽象点，数清现实后发现不需要」，
  每次带实测数字，其中两次「不做」直接换来真缺陷：吃掉用户 PowerShell profile / 整页白屏）
  + 「由这些错固化下来的纪律」。可迁移、不依赖上下文。
- **半资产**：节二那 32 条清单。它抽出了单条 bug 记录做不到的**跨条目模式**
  （同一个认知偏差反复四次：以为在本机、其实在远端）。
  **但 32 条没有一条带 commit / 文件 / 测试名，接手者无法定位、无法复核，只能选择相信。**
- **真正有害的不是自省，是自省被排在时间线上、而全篇没有一处回答「所以现在的事实是什么」**。
  三个实证：`T01` 从「结果统一成 `ProbeStatus`」到「**删掉 `ProbeStatus`**」跨 169 行，
  前者无任何指向后文的标记；`T04` 标题和前 93 行讲「五套机制收编」，而 §11 结论是
  「统一部署器不该建」；`T02` 承诺「不新增字段并写测试钉住」，后来把那条测试删了、`T04` 把字段加上。
  **读一半停下的人会拿走一个已被推翻的结论。**

**处置**：`PHASE-G-DRAFT.md` 已转正并在开头写明「被审阅推翻的三处一律照改」。
每份 T 文档加一节「当前事实」→ E25（与章节号一起做）。

---

## 三、代码工程

### 阻塞（1 条已修，2 条登记）

**1（已修）`e2e/cc-spawn-uplift.sh:22` 缺 tmux 时 `exit 0`，20 条断言一条不跑仍绿。**
审阅用不含 tmux 的 PATH 实证（第 22 行在任何 tmux 调用之前退出）：`SKIP: 未装 tmux` / `EXIT=0`。
同类另外 7 套全是 `exit 1`。
**严重性理由（原话值得保留）**：CI 里为这批套件写的立项理由是
「cargo 369 + npm 全绿 + tsc 0，仍放行过一个让 send-keys 完全失效的改动，
因为那些门禁只断言『我写出了打算写的字符串』」——**一套能在零断言下报绿的套件，
正好把这个唯一可信的门禁抵消掉。**
→ **已修**：改 `exit 1`；**顺带补上审阅另外点出的 `tmux-target-acceptance.sh:18`
完全没有守卫**那处。两处都实测（无 tmux 的 PATH → `需要 tmux` / `EXIT=1`）。

**2（登记 E11）八套 CI 真机套件全部没有「最小断言数」地板。**
每套收尾只有 `[ "$FAIL" -eq 0 ]`，加上套件内部 3 处静默 SKIP 分支，
**任何一处环境退化都是「少跑若干条 + 绿」**。
这跟本仓自己的教义直接矛盾：`structural_scan.rs:41` 的 `require(min_checked, …)`
把计数自检做成**调用方想忘都忘不掉**（`min_checked == 0` 直接硬失败）。
**Rust 侧做到了，价值更高的 shell 侧没做。**
→ **未做，但已把本轮实测的 8 个数字写进 E11**（26/44/12/15/13/21/14/7，合计 152），
下一个人不必重新测。**我没做的理由如实说**：这是 8 个异构文件的新工程 + 需要第二次全套真机运行才能验，
超出「审阅轮只修阻塞级真 bug」的界线；而阻塞 1 的修复已经关掉了唯一被实证过「零断言报绿」的那个口子。

**3（已修）`ci.yml` 断言条数标签与实际不符——而它是唯一记录期望条数的地方。**
→ **已修**：按实测改为 `ccm-cli` 44、`cc-spawn-uplift` 21，表头由「7 套 / 126 条」改为
「**8 套 / 152 条**」并逐套列出。

### 重要

| # | 发现 | 登记 |
|---|---|---|
| 4 | `vendor/cc-acct-iso/scripts/` **1348 行 bash 进了二进制、部署到远端执行，却在 shellcheck 门禁之外**。而当初把 `shared/ccm` 纳入的理由原话是「它被 `include_str!` 进二进制，是生产件」——**同一条论证一字不改地适用**。审阅实测今天 `shellcheck --severity=error` **零告警** ⇒ 扩进来零成本 | E13 |
| 5 | 6 套已自动化的 e2e 套件同时不在 `package.json` 也不在 `ci.yml` | E14 |
| 6 | `ssh_source.rs` 4847 行、fan-in 14——**但公开面很窄，不该按行数拆**。审阅逐个查了 14 个依赖方，只用到 6 个符号 ⇒ 是内聚的 SSH facade。**真正的异味只有一处**：`shell_quote` 是与 SSH 无关的纯字符串工具，却是「必须依赖这个 4847 行模块」的第三大原因（5 个模块只为它进来）。搬去 `utils.rs` 即可，一处改动 | E31。**这一条与整体设计视角的「上半是网」不矛盾**：网在 `lib.rs` 的上行引用，不在 `ssh_source` |
| 7 | 覆盖率地板只量 `*.vitest.ts` 而分母含 `*.test.ts` 覆盖的文件 ⇒ 只能设到 40/34/36/41；**Rust 侧 31570 行 / 536 测试，零覆盖率门禁** | E32 |
| 8 | `*.test.ts` 需手工登记进 `package.json` 才会跑（今天 16/16 没漏，但新增不登记就永远不跑）；`*.vitest.ts` 是 glob 自动纳入 | 记录 |

### 我核实后认为做对了的（这一节是本报告最该被读的部分）

**代码工程视角做了 4 个变异实验，4/4 全被杀**——这是「旗舰门禁真的工作」的证据：

1. **`sftp.rs:1032` 对 `shared/ccm` 的结构性扫描是真门禁**。把 `shared/ccm:355` 的
   `-t "=$attach_name:"` 改成裸 `-t "$attach_name"`（**编译得过**，因为它走 `include_str!`），
   `cargo test` 立刻红，报文精确到行号和原因。逃生口钉死（`pin_definition` 断言 `$t`
   只被赋值一次）和计数地板（`require(10)` vs 实际 11）都是承重的。**全仓最好的一处门禁。**
2. **`structural_scan.rs:47` 让 `require(min_checked=0)` 直接硬失败**——把「计数自检」
   从约定变成 API 层面无法绕过。位置选得对，这正是 shell 侧缺的那一层。
3. **Rust 模块图实际是干净的**：44 个模块只有 2 个二节点环。
   审阅第一遍跑出 12 个环，逐个核验后发现 **10 个是文档注释造成的假阳性**
   （散文里提到别的模块的函数名）。大量跨模块文档交叉引用而不产生实际耦合，是好现象。
4. **`src/settings/` 是干净的星形**：`panel.ts` → 8 个 section，零 section 反向 import、
   零 section 互相 import。在两个 section 里各植一处变异，**各自被本 section 的测试杀掉**。
5. **`build.rs` 五件事全部「缺席即 no-op」**，CI 与本地不会分叉成失败；
   唯一真实差异（本地有上游仓、CI 没有）是刻意的且方向正确。
6. **`ci.yml` 顶层 `defaults.run.shell: bash` 显式指定就为拿到 `-eo pipefail`**，
   注释直接点名本仓踩过的「裸管道掩盖编译失败误报全绿」。
7. **vendored 脚本在 git 里是 100644，但上传时显式给 `0o755`**——
   那个在 `shared/**` 咬过两次的 exec-bit 类 bug 在这条路径上**结构性不可能复发**。
   修在写入点而不是加一条 lint，是对的。
8. **安慰剂测试是在被主动猎杀，不是被否认**。占比实测很低：
   Rust 文本扫描类约 17/536（≈3%）、TS 约 10/814（≈1.2%），弱断言 38 处/814。
   而 `tool_registry.rs` 那 7 条源码扫描测试**每条都自带反向控制**
   （先断言「变异真落位」再判色，注释写明「本会话两次『全绿』其实是变异没写进文件」）。

---

## 四、实现细节

### 阻塞（2 条，均已修）

这两条是**本轮唯一动了生产代码的地方**，都由我独立复现后才修。

**① 远端 profile 的读-改-写全程在「有损字符串空间」里，非 UTF-8 的 `.bashrc` 会被静默改坏，
且备份也是坏的。**

`read_optional` 返回 `Option<Vec<u8>>`，两个 profile 命令都立刻
`String::from_utf8_lossy(&b).into_owned()`。任何非 UTF-8 字节（GBK 注释、latin-1 人名、
误粘的 `\xa0`）在这一步变成 U+FFFD，然后：
① **备份写的是已经有损的 `existing`，原字节从此不可恢复**；
② `merged`/`stripped` 带着 U+FFFD 被写回；
③ 读回校验拿 `from_utf8_lossy(readback)` 与同样有损的期望比 → **逐字节相同、校验通过**,
**整套「备份+读回+回滚」为这次损坏出具了合格证**。

**为什么是阻塞**：本机侧同一操作用 `read_to_string`，遇非 UTF-8 是 `Err` 中止（fail-safe）；
远端侧是 fail-destructive，写的还是「下次开终端就炸」的文件。
而 `verify_uploaded_bytes` 的头注自己写着「按字节而不是按字符串」——
**那条纪律只落到了 daemon 二进制那条路，profile 这条路没落。**

**② 远端「读失败」= 「空文件」，于是不备份 + 整份覆盖 / 谎报无需卸载。**

`read_optional(...).unwrap_or_default()` 把「读不到」和「内容是空」并成一个值
（`read_optional` = `sftp.read().ok()`，无权限/被占用/传输失败全压成 `None`）。
install 分支随后 `if !existing.is_empty() { 备份 }` ⇒ **读失败时跳过备份**，
`merged` 只含 ccm 块，`upload_atomic` **把用户整份 `.bashrc` 换成那个块，无任何可恢复副本**。
uninstall 分支则 `stripped == existing` 成立 → 返回「远端 … 里没有 ccm 块，无需卸载」。

**为什么是阻塞**：本机侧专门有一道对称的防线（`profile_installer.rs:191-201`，
`on_disk_size > 0 && raw.is_empty()` → 直接 `Err`，注释写明是 **v1.7.9 事故的修法**），
远端侧完全没有——而 `sftp` 手里有 `try_exists`/`metadata` 可以问，只是没问。
第二条恰好是 `strip_profile_block` 头注亲自定义为 bug 的形态（「它主动告诉用户没问题」），
**上一轮只修到纯函数一层，根因在这个读取行**。

**修法**（`sftp.rs`）：新增 `interpret_profile_read`，把读取拆成 fail-safe 三态——
`Ok(None)` = `try_exists` 明确说不存在；`Ok(Some(s))` = 读到了且是合法 UTF-8；
`Err` = 读不出来 / 非 UTF-8 / 有字节数却读到空。异步取样 `read_profile_text`
**只在需要时**才补问 `try_exists`/`metadata`，不给常见路径加往返。
顺带把 install 两个失败分支那句无条件的「已尝试回滚原文件」改成如实
（`rollback_note`：`existing` 为空时那个 `if` 一条也不执行，**机制不许声称做了它没做的事**）。

**验证**：新增 5 条测试（含每条的反向自检）；**3 个变异全部编译得过并按预期变红**——
① 把初始读取改回 lossy+`unwrap_or_default` → 结构性守卫红；
② 把「读失败且问不出是否存在」放宽成当新建 → 行为测试红；
③ 把纯函数里的 `from_utf8`换回 `from_utf8_lossy` → 非 UTF-8 那条红。
`cargo test` 531 → **536**。

> **一条留给后来人的记录**：那个结构性守卫**收窄了两次才对上，两次都是自己抓自己**。
> 初版扫整个函数体 → 撞上写后读回校验那处**正当的** `read_optional(sftp, &profile)`；
> 收到「文本变换之前」后仍假红 → `install` 的读取段里还有一处正当的 `read_optional`，
> 读的是刚部署的 **ccm CLI 脚本**，不是 profile。
> **守卫范围必须恰好等于性质范围**——这是本会话第三次栽在同一形状上。

### 重要（全部登记，未修）

`profile_installer` 首次安装失败时 rollback 是空操作而文案说「已尝试回滚」（**文案已改如实，
删文件未做**）→ E16 · 远端 hooks 诊断读死 `$HOME/.claude/settings.json` 不认
`CLAUDE_CONFIG_DIR`（本机侧刚在 B04 修成尊重它）→ E17 · 远端 `classify_command` 用 basename
回答精确路径问题，`PathMissing` 这一态对任何「装在别处」的程序**永远不可达**→ E18 ·
`shared/ccm` 相对 `--cwd` 被应用两次且会无限产孤儿会话 → E19 ·
`upload_atomic` 会把符号链接形态的 `.bashrc` 换成普通文件（dotfiles/stow 用户）→ E20 ·
`config.json` 的丢失更新 + 固定 tmp 名 → E21。

### 建议（未登记为独立项）

`find_conflicting_functions` 遇悬空 BEGIN 会让 `inside_ccm_block` 永久为 true ·
`scan_profile` 对 UTF-16LE 的 `$PROFILE` 会报「存在、0 字节、未安装」并隐藏卸载按钮 ·
`cc_integration_install(path)` 对 path 零校验（远端侧同一功能严格到只收 home 下裸文件名）·
备份/临时文件从不回收 · `shared/ccm` 用 `jq > tmp; mv` 重写 `~/.claude.json` 不保留 mode ·
`upload_atomic` 最后一次 rename 失败时目标已不存在而错误文案没提 `.bak` ·
SFTP 连接池以用户起的标签为键（模块头注已自行登记）· `--tmux=` 空值被静默当成 `--tmux`。

### 我核实后认为做对了的（摘要）

- **`fenced_block::find_pair` 的抽取门槛与语义取向**：拒绝建「统一部署器」而只抽真有 ≥2 消费者
  且藏着数据丢失的那一族；「有 BEGIN 无 END 一律 `Err` 中止」取两侧最强档，
  且 `split_inclusive('\n')` 与 `.lines()` 的下标对齐经逐边界核验（空串/无尾换行/`\n\n`/CRLF/纯 CR）一致。
- **`damaged_fence_leaves_the_file_byte_identical` 换掉代理指标**：从「扫自身源码里 fence 与 write
  的字节顺序」换成「造坏文件、调真函数、断言 `Err` 且文件字节逐字未变 + 不留任何临时/备份文件」，
  并把两个「编译得过且真写盘」的变异如何骗过旧测试写进注释。审阅评为
  「**本次抽样里质量最高的一条测试，也是唯一挡得住我上面找的那类『看起来中止了其实写了』的门禁**」。
- **`shared/ccm` 的三条「不静默吞修饰」**：`need_val` 拦 `--account --print` 吞参 ·
  `attach` 与 `--detach` 矛盾直接 die · 「已在 tmux 内且无显式名」时 `--detach/--tmux-size`
  会落空所以 die。**阻塞语义与「静默生效假象」之间选了前者。**
- **信任框轮询末尾的 `|| true`**：`for` 循环 rc = 最后一条 `grep -q` 的 rc，
  在 `--detach` 摘掉尾部 attach 之后会变成整条链的退出码 ⇒
  「良性路径恒返回 1 → 调用方判失败 → 真在跑的孤儿会话」。审阅核对了 seq 的实际拼装，推理链成立。
- **`upload_atomic` 的三条有代价的正确取舍**：tmp 用 `EXCLUDE` 创建 · 旧目标先 rename 成 `.bak`
  而不是先删 · rename 后坚决不 `set_metadata`（那条 OpenSSH setstat 把文件截成 0 字节的真机诊断结论）。

---

## 五、文档-代码漂移（主线程交叉对比）

这一节是四个 agent 之外的增量——文档视角按分工不读代码，另三个不系统核文档声明。

**① 判定了文档视角报的一处矛盾（用代码定归属）。**
`integrate-toolchain/STATUS.md:124` 说「`INVENTORY.md` 冻结在 F01 ⇒ 成功标准① 从未验收」，
而 `P-READINESS.md:9` 说「R06 已重写（符号名 + 可跑 grep 锚点）」。
**实测**：把 `INVENTORY.md` 里所有 5 字符以上的反引号符号名逐个 grep `src/ src-tauri/src/ shared/`，
**34 个锚点 33 个命中**，唯一未命中的是 commit hash `ef1310b`（不是符号）。
⇒ **`P-READINESS.md` 那边对，`STATUS.md:124` 已过期。R06 的成功标准① 站得住。**

**② 证伪了一条 MASTERPLAN 声明（已订正）。**
`unify-launch/MASTERPLAN.md:66` 把 **F06「本地路径并入 IR」标为完成**。实测：
- `planLocal` 在生产代码里**零调用点**（`src/launch-requests.ts:139` 只剩一句注释记录 R07 删了它）；
- `src-tauri/src/history.rs:930` `build_local_ps_command` **不引用任何 IR 类型**
  （`grep -n "LaunchPlan\|launch_plan" src-tauri/src/history.rs` 为空）。

⇒ **本地路径完全在 IR 之外，且 R07 是有意把它移出去的**（理由「接了也拿不到新东西」成立）。
F06 真正交付的东西是实在的——两套 PowerShell 拼装收成一个函数——**但那不叫「并入 IR」**。
后果：`§0.1`「IR、CLI、UI 三处是同一个模型」这条在本地这一格上**声明的架构与建成的架构相反**。
→ **已订正**（标题改为「两套 PowerShell builder 收成一个」+ 附实测记录）。

**③ 承重数字全部重取。**
`panel.ts` **1011**（文档记 826）· `tabs.ts` **3309**（记 3178）· `ssh_source.rs` **4847**
（`README.md` 记 4756、`spine-split/STATUS.md` 记 ~4512，**两个都不对**）·
`shared/ccm` **636**（各处文档记 542）· `remote-section.ts` 1743。
→ 已在各处订正或加「别照抄本表」的警示。

**④ 版本/测试数对账。**
`package.json` / `Cargo.toml` / `tauri.conf.json` / `CHANGELOG` 一致为 **3.3.0**，
只有 `README.md` 三处写 v3.2.0 → **已修**。测试数 cargo 365→**536**、DOM 595→**814** → 已修。

**⑤ 命令名契约实测无漂移。** 119 个 Tauri 命令名的「声明数 = 注册数 = TS 引用数」，双向 diff 空。
这是好结果，但它靠人工纪律维持（唯一契约门禁覆盖 3/119），**payload 形状零门禁** → 记录。

---

## 六、优先级建议

### 立即修（我判断有真实用户可见风险，本轮未做的部分）

1. **E15 两渲染器 base 语义分歧**——需要你拍板「未选账号」=「不注入」还是「强制基座」。
   两个答案都能实现，但**必须两边一致**，否则同一个 UI 动作落在哪个账号上取决于目标机装没装 ccm。
2. **E20 `upload_atomic` 打断符号链接的 `.bashrc`**——dotfiles/stow 用户会静默失去关联，
   且备份只存内容、链接不可恢复。
3. **E19 `shared/ccm` 相对 `--cwd`**——会起错目录**且**每次调用都产一个新孤儿会话。
4. **E17/E18 远端诊断的两处「看着很确定的错答案」**——两处都是本机侧刚修好、远端侧没跟上，
   改法都很小（一处定值字符串、一处改用已有的精确行）。

### 近期处理（机制性，防复发）

5. **E11 八套真机套件加断言数地板**——实测数字已备好（26/44/12/15/13/21/14/7）。
   这是本会话反复被烧的那个失效模式（伪造的绿）唯一的结构性答案，
   而本仓 `structural_scan::require` 已经证明团队认这个做法。
6. **E13 把 vendored 的 1348 行 bash 纳入 shellcheck**——实测今天零告警，扩进来零成本。
7. **E22 「Claude 配置目录在哪」4 份实现收成 1 份**——已有一份带完整解释的实现可复用。
8. **E25 五份 T 文档各加一节「当前事实」+ 章节号续编**——
   代价很小，而它治的是「读一半停下的人会拿走一个已被推翻的结论」。
9. **E26/E29 已在本轮修完，但机制没修**：MASTERPLAN 功能表与索引 README
   没有任何东西迫使它们跟上。建议把「Phase F 必须回写功能表状态列」写成硬步骤。

### 可选优化

10. **E31 `shell_quote` 搬去 `utils.rs`**——一处改动，砍掉 5 个模块对 4847 行模块的伪依赖。
11. **E32 Rust 侧加覆盖率地板**；TS 侧地板的分子分母口径不一致也值得一次修。
12. **E14 6 套已自动化的 e2e 进 CI**。
13. **`tool_registry` + `config_surface` 的定位**（整体设计视角重要 4）。
    我的看法：那台机器**本身设计得对**（`EitherHost` 包内层使「Either 绝不说 Absent」一句可证），
    问题是**它服务的那张表在不连 SSH 的前提下多半答不出话**。
    两条路：要么给这一页接上 `origin`（让远端行真能回答），要么把它的产品承诺收窄成
    「本机这几行 + 远端的申报清单」，别让 UI 上出现 7 行「未确定」。
    **不建议就这么放着**——一个大多数行说「不知道」的审计页，用户第二次就不会再打开。

### 明确不做（已记档，附理由）

- **不拆 `panel.ts` / `tabs.ts` / `ssh_source.rs`**——三次都由具体耦合面实测证成，
  且 Phase G 代码工程视角独立复核认同 `ssh_source.rs` 是内聚 facade。
- **不建统一部署器**——五件事形状不同，套一层分派器只会把它们装进一个盒子。
- **E9 24 处构造期 I/O 改懒加载**——实测只有约 11 处真在构造器里，收益是启动延迟不是正确性。
- **不做全局 MCP、不写用户的 `~/.claude/settings.json` / `~/.bashrc` / PowerShell profile**——
  产品级边界，已写进地盘表。

---

## 七、本轮门禁（CI 原样命令，输出落文件后 grep 核实）

| 门禁 | 结果 |
|---|---|
| `cargo test --all` | **536 passed**, 0 failed, 3 ignored（rc=0） |
| `cargo test -p code-picture-core` | **25 passed**（rc=0） |
| `cargo fmt --check` | 干净 |
| `cargo clippy --all-targets` | **0 error**（rc=0） |
| `npx tsc --noEmit` | **0**（rc=0） |
| `npm test` | **814 passed / 53 files**（rc=0） |
| `npm audit --omit=dev --audit-level=high` | rc=0（余 1 条 low） |
| `shellcheck --severity=error e2e/*.sh shared/cc-bus/scripts/* shared/ccm` | 0 |
| `bash e2e/exec-bit-guard.sh` | rc=0 |
| **8 套真机套件** | 全部 rc=0，实测 **26/44/12/15/13/21/14/7 = 152 条断言** |
| tmux 隔离 | 守卫 shim（无 `-L`/`-S` 一律拒，rc=97）+ 起飞前双向 canary 自检通过 |
| 默认 socket 三个会话 | 名称与创建时间**逐字未变**。唯一差异是我自己那个会话的 `(attached)` 标记（客户端附着状态，非会话变动） |
| `/tmp/ccm-fence*` 残留 | 无 |
| `git diff --stat HEAD -- e2e/` | **9 行**（不是 0）——本轮**有意**改了两个 e2e 守卫（阻塞 1 的修复），仅此两文件 |

---

## 附：本报告的边界（我没能验证的）

- **Windows 生产平台的代码路径未执行**。本机是 Linux，`#[cfg(windows)]` 全部没跑
  （`ReplaceFileW` 的 ACL 保留、`discover_profiles` 依赖的 `dirs::document_dir()`
  在 OneDrive 重定向下的取值、`launch_powershell_window`）。CI 的 windows-latest 会跑。
- **两条阻塞的修复没有在真远端验证**——修的是纯函数 + 读取路径，
  验证靠 5 条单测 + 3 个变异，**没有对着一台真机跑一次 install/uninstall**。
- **`config_surface` 在 Windows 上到底渲染出几行「未确定」是推断**（按分支走的），没实测。
- **E19 的确切症状未验**（是「起错目录」还是「叠加两次」取决于 tmux 对 `new-session -c <相对路径>`
  的解析侧）。审阅只验到了渲染出的命令串（`--print`），没跑 tmux。
- **真正的变异分数没测**。本轮 + 审阅合计 7 个手挑变异全部被杀，
  这是「旗舰门禁真的工作」的证据，**不是全仓 kill rate 的证据**——后者要跑 `cargo-mutants`。
- **`.claude/planned-build/` 12 个工作区只深读了 2 个**（`unify-launch` / `integrate-toolchain`），
  其余与本轮的交叉约束没查。
- **`release.yml` 未审**；`CHANGELOG.md`（1784 行）只核了最新版本号与日期。
- **前端并发实况未验**（E21 只确认了代码形状是 load→改→整份写、Rust 侧无合并/无版本）。
