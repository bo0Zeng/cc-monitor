# K-R77 · 拆掉最后那道绕道 ＋ 给它上恒零棘轮 —— 刀与读数

> 件计划 `features/K-R77-拆掉最后那道绕道并给它上棘轮.md`（计划仓）· 来路 `DECISIONS.md#R36` 裁定一 ＋ 裁定二。
> 工作树 `/.claude/worktrees/k-r77`（分支 `track/k-r77`，基点 `d2b21cc`，**本树未铺 `embedded-daemons/`**）。
> 量于 **2026-09-12**。量具与日志一律落会话 scratchpad（纪律 ㉑），**工作树里零残留**。
> 变异台 `…/scratchpad/kr77/cut.py`（每刀断言锚点命中数、印「变异已落地」、`os.utime` 刷 mtime）·
> 沙箱跑手 `…/scratchpad/kr77/sbx.sh`（与 `.claude/devbox/gate` 同挂载 · 同 `CARGO_TARGET_DIR=…/pm-targets/k-r77`）。
> 两份量具的**被测对象都写死指向 `.claude/worktrees/k-r77` 这一棵树**（`WT` 常量），住址里带 `kr77` 子目录，不与别的 agent 共名。

## §1 门禁四格（沙箱 `.claude/devbox/gate`，`PB_WS=backend-consolidation`）

| 格 | 基线（PM 预登记 @ `d2b21cc`） | 本件收工 | 差 | 押注 |
|---|---|---|---|---|
| cargo（8 包合计） | 1531 | **1535** | **+4** | 押 `+2 ~ +5` ✅ |
| daemon（`remote-daemon-proto` 单包） | 686 | **686** | **±0** | `KR77D1` 死值验 ✅ |
| npm | 1688 | **1688** | **±0** | ✅ |
| e2e（print-parity / rbind-title / cli / contract-parity） | 12 / 8 / 46 / 45 | **12 / 8 / 46 / 45** | 恒等 | ✅ |
| `pb check` | — | `FAIL=0 BROKEN=0` | — | — |
| 另有 fmt · winchk · generated 三格 | — | 全绿 | — | — |

⚠ **上表量于写件文件 `§8` 之前那一趟（`GATE: OK`）。** 写完 `§8` 之后又跑了一趟：
**代码那十格逐格同值、全绿**，而 `pb check` 变成 **`FAIL=1 BROKEN=0`**，唯一那条是
`[J3 陈账] INDEX.md 比源文件旧`。因果现打：`find . -name '*.md' -newer INDEX.md`（排 `.briefs/`）
**只有件文件一份** ⇒ 是「写 `§8`」这个动作本身造的。派工单逐字「收工前不要跑 `pb index`」，
`DECISIONS.md#R36` 裁定三已把这一族判给 PM。**我没跑 `pb index`。**

**`+4` 落在哪一包 —— 这次是量出来的，不是推的**：`cargo test -p monitor --lib` 本件收工 **1415 passed**，
基点 `d2b21cc` 上同一把尺子 **1411 passed**（`4 passed; 1421 filtered out` 那一趟的 `1421 + 4 = 1425 = 1415 + 10 ignored`，两头对得上）。
四条新判据全部住 `src-tauri/src/structural_scan.rs`（monitor 那一包）。

另两格非数量的押注：

- **剥法逐字不动**：`git diff --name-only d2b21cc..HEAD -- src-tauri/crates/` ⇒ **0 行**；
  `src-tauri/crates/guard-core/src/lib.rs` 的 md5 两头相同 —— 盘上 `441fc5c4e1d15c660b9595bf54e5688f`，
  `git show d2b21cc:…` 同值。
- **`DIAL_SITES` 未搬处数仍 6**：现算（口径 = `moved == false` 那几行的处数合计）⇒ **6**；
  盘上那条判据 `ssh_source::…::six_of_the_seven_dial_sites_are_still_in_this_process` 本趟门禁里绿。

## §2 做了什么（两件同拍）

**先拆后立** —— 中间那一段没有闸，但盘上也没有真阳（`§0b` 那条论证），无害。

### 拆（`KR77D1`）

- `remote-daemon-proto/src/readonly_guard.rs`：`mod g6_doctrine {` → `pub(crate) mod g6_doctrine {`，
  顶层那两行 `#[cfg(test)]` ＋ `pub(crate) use g6_doctrine::{cell_names, is_cell};` 删掉。
- 引用者 `remote-daemon-proto/src/no_timer_guard.rs` 两处取名改走
  `crate::readonly_guard::g6_doctrine::{is_cell, cell_names}`。
- 🔴 **`no_timer_guard.rs` 不在派工单写区里** —— 越界申报与「只有它一个引用者」的**编译器权威读数**见 §5。

### 立（`KR77D2` ＋ `KR77D3`）

四条新判据，全住 `src-tauri/src/structural_scan.rs` 的 `mod tests`：

| 判据 | 它是哪一半 |
|---|---|
| `the_cfg_test_reexport_detour_stays_extinct` | 正题：**恒零棘轮** ＋ 两条地板（人群第一步 · 活体正控） |
| `the_detour_reader_can_tell_a_new_one_from_none` | **反向那半**（合成读数钉 `detour_over_ceiling` 的方向） |
| `the_detour_scanner_sees_every_visibility` | **失效方向那半**（六形可见性 ＋ 五形反面） |
| `the_detour_ratchet_reaches_both_trees` | `KR77D3`：逐棵树地板 ＋ 点名三份真长过绕道的文件 |

**落点为什么是 `structural_scan.rs`**（题面要求现打之后说清）：

1. 它是本仓「结构性扫描」这一族的家，头注四要件（枚举 · 逐个断言 · 计数自检 · 钉死逃生口）就是本条要的形状；
2. 它已经有一份**跨两棵树**的语料面 `addr_corpus()`（`src-tauri/src` ＋ `src-tauri/crates` ＋ `remote-daemon-proto/src` ＋ `build.rs`
   ＋ 用 `include_str!` 把自己补回来），`KR77D3` 要的「够得着 daemon」现成；
3. 同一份文件里已经住着这一族病的**第二个住址**（`no_two_test_attributes_land_on_the_same_function` 里那句
   `strip_visibility(t).starts_with("mod ")`，`K-R75` 刚接上）—— 判据与它治的病同住一处，读的人一眼看得到两半。

**为什么可以恒零**（三条，缺一条都不该恒零；正文住判据头注，这里只给索引）：
① 今天真的是 0（本条自己就是读数）· ② 那道绕道**没有合法用途了**（唯一理由是剥法的缺陷，`K-R75` 已修）·
③ **0 是这个量的下界** ⇒「必须比历史最低档低」在这里退化成「等于 0」。
⇒ 恒零比「历史最低档」**更强**：后者允许「降下去再涨回来」那一格，前者不允许。
重新裁定的出路也写死在头注：剥法又认不出某种可见性 ⇒ 先修 `guard_core::strip_visibility`；
真要开口子 ⇒ 走 `DECISIONS.md`，**不许**在这里抬 `DETOUR_CEILING`。

## §3 变异表（逐行：锚点 · 命中 · 活体信号 · 判定行 · 新红 · 定性）

> 🔴 纪律 ㉒ 的活体信号，本表按刀分两种，**都写出来**：
> · 动**编译单元**的刀 —— 那一趟 cargo 真的印出来的 `Compiling …` 行数（`cut.py` 每刀 `os.utime` 强刷 mtime）；
> · 动**被扫的盘上文本**的刀（判据运行期读盘，本来就不必重编）—— 信号是**报错里逐字回显了我刚写进去的那一行**。
> 分母：`cargo test -p monitor --lib`（monitor 那一格）= **1415** · `cd remote-daemon-proto && cargo test`（daemon 那一格）= **686**。
> 「4 tests」那几趟跑的是 `cargo test -p monitor --lib detour`（**只过滤，不换台**：同一个测试二进制，`1421 filtered out`）。

| 刀 | 锚点（住址 · 命中次数） | 改成什么 | 活体信号 | 判定行 | 新红 | 定性 |
|---|---|---|---|---|---|---|
| **M1** | `&& strip_visibility(mod_line).starts_with("mod ")` @ `src-tauri/crates/guard-core/src/lib.rs` · **1** | 退回 `&& mod_line.starts_with("mod ")` | `Compiling` × 2 | `FAILED. 685 passed; 1 failed; 2 ignored` | **1** | 🔴 **`KR77D1` 死值验兑现，且是最小面**：红的恰是 `guard_support::tests::every_daemon_file_strips_clean`，逐字「**`readonly_guard.rs`：剥完仍残留 2 个测试属性**」——**红名单里正是那一族** |
| **M2** | `#[cfg(test)]\npub(crate) mod g6_doctrine {` @ `readonly_guard.rs` · **1** | 可见性收回私有（**不**加回再导出） | `Compiling` × 2 | **编译错，见 §3a（CRASH 单列）** | — | 🔴 `pub(crate)` 是**承重件**不是装饰：编译器给出 **恰好 2 处** `E0603`，都在 `no_timer_guard.rs` ⇒ 这就是「还有没有别的引用者」的权威分母 |
| **M3a** | `#[cfg(test)]\npub(crate) mod g6_doctrine {` @ `readonly_guard.rs` · **1**（在它前面插回那道再导出） | **daemon 树**新写一道同形绕道（＋3 行） | 报错逐字回显我刚写的那一行 ＋ `Compiling` × 2 | `FAILED. 3 passed; 1 failed`（4 tests） | **1** | 🔴 **`KR77D2` 死值验 ① 兑现**：正题红并**点名住址 ＋ 行号 ＋ 原文** —— `remote-daemon-proto/src/readonly_guard.rs（第 1183 行）—— \`pub(crate) use g6_doctrine::{cell_names, is_cell};\``。★ **这正是 `K-R76` 的 `M3` 那一刀**，它当时 **0 红** |
| **M3b** | `#[cfg(test)]\npub(crate) mod tests {` @ `src-tauri/src/local_daemon.rs` · **1** | **monitor 树**新写一道同形绕道（＋3 行） | 报错逐字回显那一行 ＋ `Compiling` × 1 | `FAILED. 3 passed; 1 failed`（4 tests） | **1** | 🔴 同一条判据在**另一棵树**上也点得出名：`src-tauri/src/local_daemon.rs（第 1764 行）—— \`pub(crate) use tests::demand_tmux_shim;\`` |
| **M4** | `if today > ceiling {` @ `structural_scan.rs` · **1** | 比较写反成 `if today < ceiling {` | `Compiling` × 1 | `FAILED. 3 passed; 1 failed`（4 tests） | **1** | 🔴 **`KR77D2` 死值验 ② 兑现，最小面**：红的只有 `the_detour_reader_can_tell_a_new_one_from_none`，**正题照样绿** —— `K-R38` 那条道理原样复现：反向那半承重 |
| **M5** | `guard_core::strip_visibility(next).strip_prefix("use ")` @ `structural_scan.rs` · **1** | 收窄成 `next.strip_prefix("pub(crate) use ")` | `Compiling` × 1 | `FAILED. 3 passed; 1 failed`（4 tests） | **1** | 🔴 **失效方向兑现**：红的只有 `the_detour_scanner_sees_every_visibility`（逐字「可见性写成 `` 时逮不到」），**正题照样绿** —— 人群缩水在真树上是静默的，牙全在那一条 |
| **M6** | `addr_corpus()` 那个 `for sub in […]` 三根列表 @ `structural_scan.rs` · **1** | 把 `"remote-daemon-proto/src"` 整根摘掉 | `Compiling` × 1 | `FAILED. 1410 passed; 5 failed; 10 ignored` | **5** | 语料面塌一棵树 ⇒ `addr_corpus` **自己那条 ≥180 的地板先响**（`只收到 119 份`）；红名单含本件两条新判据 ＋ 本区已有三条地址判据 |
| **M6b** | 同 M6 ＋ `out.len() >= 180,` → `>= 100,` · **1** | 摘掉一棵树，**再顺手把地板调下来**（判据挡路时现实里就是这么干的） | `Compiling` × 1 | `FAILED. 2 passed; 2 failed`（4 tests） | **2** | 🔴 **`KR77D3` 死值验兑现，且两条地板独立**：`the_detour_ratchet_reaches_both_trees` 逐字「**daemon 那棵树只收到 0 份 .rs**」；正题也红（人群第一步那条地板：`只认出 159 个`，基线 250） |
| **M7** | `"remote-daemon-proto/src/readonly_guard.rs",`（`want` 列表首项）@ `structural_scan.rs` · **1** | 换成一个盘上不存在的住址 | `Compiling` × 1 | `FAILED. 3 passed; 1 failed`（4 tests） | **1** | 🔴 **点名那半有牙，最小面**；顺带把两棵树的读数印出来：**monitor 118 份 · daemon 88 份** |

### §3a CRASH 单列 —— M2

`M2` 不是读数，是**编译错**，按纪律单列：

```
error[E0603]: module `g6_doctrine` is private
  --> src/no_timer_guard.rs:611   crate::readonly_guard::g6_doctrine::is_cell(cell),
error[E0603]: module `g6_doctrine` is private
  --> src/no_timer_guard.rs:615   crate::readonly_guard::g6_doctrine::cell_names().join(" / ")
error: could not compile `cc-monitor-remote` (bin "cc-monitor-remote" test) due to 2 previous errors
```

它**是刻意打出来的**（不是台子炸了）：这一刀问的是「把可见性收回私有，谁编不过」，
而编译器给的答案就是这一件要点名的东西 —— **恰好 2 处，都在 `no_timer_guard.rs`，别处一处都没有**。

### §3b 一处锚点纪律的现打记录（记下来，下一轮别再撞）

`M2` 复原时按 `mod g6_doctrine {` 单行锚点切 ⇒ **命中 3 次**（另两处是 `K-R76` 留档的旧文里逐字引的那两行），
台子当场拒切。改成两行锚点 `#[cfg(test)]\nmod g6_doctrine {` ⇒ 命中 1，切成。
⇒ **留档旧文会让单行锚点变不唯一**，这一族在本件里真发生了一次。

## §4 `7u`：逐处掏空实现、签名留着（**没有用 `git checkout`**，一律反向文本替换，每趟 `git status --porcelain` 复核为空）

| 掏空谁 | 怎么掏 | 新红 | 仍绿的是哪几条 · 它们的牙在哪一刀 |
|---|---|---|---|
| `cfg_test_reexport_sites` | 体首插 `return Vec::new();`（签名留着） | **2** | `the_detour_reader_can_tell_a_new_one_from_none`（纯合成读数，不经过扫描器 ⇒ 牙在 `M4`）· `the_detour_ratchet_reaches_both_trees`（只数语料面 ⇒ 牙在 `M6b`/`M7`） |
| `cfg_test_module_names` | 体首插 `return BTreeSet::new();` | **2** | 同上两条，同样的理由 |
| `detour_over_ceiling` | 体首插 `return None;`（恒答「没越线」） | **1** | 🔴 **正题绿是空真**：今天真树上 0 处 ⇒ 恒 `None` 与判对了输出一模一样。这正是这条棘轮为什么必须有一条**合成读数**的反向判据 —— 牙全在 `the_detour_reader_can_tell_a_new_one_from_none`。另两条不经过这个算子 |
| `detour_next_item` | 体首插 `return None;` | **2** | 同第一行两条 |
| **拆本身**（把那道绕道整个装回去） | 见 `M3a` | **1** | — |

**0 红一处都没有** —— 每一处掏空至少有一条红点名。
两条地板（人群第一步 `>= 200` · 活体正控「合成语料必须逮到 1 处」）是把「扫描器坏了」与「盘上真的没有」分开的那一半，
`7u` 前三行里它们各响过一次。

## §5 🔴 越界申报：`no_timer_guard.rs` 不在派工单写区里

**写区逐字**（`.dispatch.json`）：件文件 · `readonly_guard.rs` · `structural_scan.rs` ·
`ssh_source.rs` · `local_daemon.rs` · `backend/control/local_backend.rs` · `dial_home_registry.rs`。
后四份是 **`K-R76` 那一拍的引用者**（PM 按纪律 ⑰ 第十八个变体圈进来的），
而**本件那道绕道的引用者不在其中** —— 本件一份都没动那四份（`git diff --name-only d2b21cc..HEAD` 只有 3 个名字）。

- **真引用者只有一个：`remote-daemon-proto/src/no_timer_guard.rs`，两处**。
  分母不是 `grep`，是**编译器**：`M2` 那一刀（把可见性收回私有）给出恰好 2 条 `E0603`，都在这一份文件里。
  另有一处**文档引用** `no_timer_guard.rs` 头注逐字写着 `crate::readonly_guard::g6_doctrine::is_cell`
  —— 那句话在本件**之前是假的**（`g6_doctrine` 当时私有，那条路径解析不了），拆完之后它才成真，本件没改它。
- **为什么我做了而不是停下来交回**：`R36` 裁定一已经把「拆」拍了板，而引用者跟着改是那条裁定的**机械后果**；
  派工单同一段逐字写着「**若还有别的引用者，交回时点名**」。⇒ 我按最小面做完（2 行，纯取名路径）并在这里点名。
  **这一格由 PM 追认**，我不自批。

## §6 🔴 交回 PM 的两件事（都不自批）

1. **`daemon` 那一格今天没有这道闸 —— `KR77D3` 的诚实答案**。
   本件这条棘轮住在 monitor 那棵树（门禁 `cargo` 那一格），靠**读盘上的 `remote-daemon-proto/src`** 够到 daemon。
   ⇒ 只跑门禁 `daemon` 那一格（`cd remote-daemon-proto && cargo test`）的人，新写一道绕道**不会红**。
   两棵树是两个 workspace（`remote-daemon-proto/Cargo.toml` 头注逐字 standalone）。
   **不许把「monitor 侧立了一条」读成两棵树都守住了。** 要不要在 daemon 那一格也放一条同形的，归 PM。
   （这一段同样逐字写在 `the_detour_ratchet_reaches_both_trees` 的头注里，不是只写在这份记录里。）
2. **`every_dead_name_named_in_the_prose_is_declared_dead` 有一个结构性盲区，本件又撞了一次**。
   它的语料 `dead_name_corpus()` 按构造摘掉调用者自己（`structural_scan.rs`）⇒ **凡是别处散文点名住在这份文件里的判据，
   它一律读成「代码里根本不存在」**。今天登记表里同形的已有三行
   （`doc/INVARIANTS.md` × `every_monitor_file_strips_clean` · `guard-core/src/lib.rs` × 同一个名字 ×2），
   本件加第四行（`readonly_guard.rs` × `the_cfg_test_reexport_detour_stays_extinct`）。
   ⚠ **那句话真的有人核**：它写成住址形，由同一份文件的 `every_symbol_address_in_the_sources_still_resolves` 判得了
   （那条的语料用 `include_str!` 把本文件补了回来）。
   ⇒ 根因治法（要不要把本文件的**声明**单独补进 `in_code`，而不把 `INVENTORY` 的字面量一起喂进去）**归 PM**，本件不碰。

## §7 改动面

`git diff --stat d2b21cc..HEAD` ⇒ 3 份文件 · +414 / −8。

| 文件 | 改了哪几个函数 | 说明 |
|---|---|---|
| `remote-daemon-proto/src/readonly_guard.rs` | **零函数体改动** | 两个 hunk：一段注释（旧的「别在这里自批」换成「拆了、别再写回来」）＋ `mod g6_doctrine` 那一行的可见性。生产段净删 2 行（属性行 ＋ 再导出行） |
| `remote-daemon-proto/src/no_timer_guard.rs` | `no_timer_guard::tests::registered_uses_all_have_reasons` | 一个 hunk · 2 行，只改取名路径；断言、人群、报错文案一个字没动 |
| `src-tauri/src/structural_scan.rs` | 新增 6 个私有辅助 ＋ 1 个 `const` ＋ **4 条 `#[test]`**；改 1 条既有判据 | 新增块整块住 `mod tests`（＋394 行）；既有判据 `every_dead_name_named_in_the_prose_is_declared_dead` 的 `INVENTORY` ＋1 行 ＋ 一段说明（＋15 行） |

**三份文件的改动全部落在测试段**：`readonly_guard.rs` 那一处是 `#[cfg(test)]` 模块的声明行 ＋ 注释 ·
`no_timer_guard.rs` 那两行在 `#[test]` 函数体内 · `structural_scan.rs` 全在 `mod tests` 里。
⇒ **零生产函数体改动**，与 `daemon` 那一格逐格 686 对得上。

## §8 三处 `git status --porcelain`

| 处 | 开跑前 | 收工后 |
|---|---|---|
| 计划仓 `.claude/planned-build`（**仓根在这里**，不是项目根） | 🔴 **没量到** —— 那一趟我把尺子架在项目根 `/home/zbl/文档/claudecode-frontend` 上（`fatal: 不是 Git 仓库`），据此写下「不在任何 git 仓里」。**那是量具作用域对不上事实**，本区最高频的那一族，本件自己犯了一次 | `M backend-consolidation/features/K-R77-….md`（件文件 `§8` ＋ 三条 `mutation:`，**不提交**）＋ **9 个 `?? backend-consolidation/.briefs/*.md`**（`K-R48b` `K-R48c` `K-R53`–`K-R58` `K-R60`）。⚠ 那 9 个「不是本件动的」是**转述**（`K-R76#§8` 收工那趟记着同样一批），**不是**我量的 |
| 代码仓 `cc-monitor`（主树） | 未动（本件全部动作在工作树里） | 未动（`git status --porcelain` 空） |
| 工作树 `.claude/worktrees/k-r77` | **空**（干净停在 `d2b21cc`） | **空**（4 份文件全部点名提交，尖 `6dc1004`） |

每一刀切完都复原并复核一次 `git status --porcelain` 为空，全程 **9 刀 ＋ 4 趟 `7u`，一次残留都没有**。
