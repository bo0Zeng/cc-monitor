# K-R87 死值验原文（`C` 阶段填）

三条 dod 各自的刀、逐刀**完整原文读数**住这里；件文件 `§3` 只放摘要。

## 规矩（缺一条这份证据不算数）

- **全部在沙箱里跑**：`PB_WS=backend-consolidation .claude/devbox/gate <本工作树绝对路径> <tag>`。
  **不许直接在本机跑**（`K31` / `R15`）。
- **一趟一刀**，下一刀前还原干净，贴 `git status --porcelain` 自证盘上没留变异。
- 🔴 **还原不许用 `cp -a`**（连 mtime 一起还原 ⇒ cargo 判「没变」、沿用上一刀产物，
  你会读到**上一刀的红**。`K-R88` 真踩过）。用 `cp` ＋ `touch`。
- **点名**：每刀写清**哪一条判据红了**，不是只写「红了」；阴性对照也贴读数。
- ⚙ **本树已铺 `node_modules` 软链**（→ 主树），不铺 `M0` 就是红的（`tsx: not found`）；
  它被 `.gitignore` 盖着、不进提交，**不是写区项**（`K-R86` 判过，照此办）。

## `M0` 基线

〔填：`GATE: OK —— 13 格全绿`（cargo N · daemon N · npm N · e2e a/b/c/d），量于哪个基点〕
⚠ 参照：主干 `995724c` 上 `K-R86` 收官读数是 cargo **1595** · daemon **728** · npm **1714**
（**未铺 `embedded-daemons`** 的那棵树上量的）。**你自己重打一趟，别抄。**

## `M1` …（逐刀往下写）
