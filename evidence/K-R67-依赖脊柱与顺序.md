# K-R67 摸底产出 —— **依赖脊柱与顺序**

> 用户逐字：「**把这些做了后看看之前还有什么没做。思考怎么根据架构把之前的东西做完。**」
> 它有两半，**第二半才是正题**：一张没做完的清单谁都列得出来，值钱的是**顺序与依赖**。
> 本件**零产品代码**；**不替用户裁「要不要做」** —— 下面只给顺序与依赖，砍不砍是用户的。

---

## §A 读数与量具（先给住址，后面每个数都指回这里）

| 量 | 读数 | 量于 · 用什么量的 |
|---|---|---|
| 件总数 · 已签收 · 未签收 | **106 · 62 · 44** | 量具 `evidence/K-R67-census.py`，输出落 `evidence/K-R67-census.out`；被测对象是**计划仓** `.claude/planned-build/backend-consolidation`（不是代码仓）。分母 = `features/` 下带 `kind: item` 那一行的件文件 |
| PM 起点（件 `§0a`，量于 `2e10f7b`） | 103 · 61 · 42 | 差额来历现算：其后新立 `K-R66`/`K-R67`/`K-R68` 三件、`K-R65` 一件签收 ⇒ 103+3=106、61+1=62、42+3−1=44 |
| `.dispatch.json` | md5 `a3c1297df562aee64928c4cb302a13f2` · 170340 B · mtime 2026-09-11 23:34:41 | ⚠ 本件开跑期间它**变过一次**（第一趟读到 `parked` 45 键、第二趟 46 键，`K-R68` 是这中间加的）⇒ 上面这个 md5 是**第二趟之后**的，后面所有关于这本账的数都以它为准 |
| 工作树 | `HEAD = 5be9737` · `HEAD^{tree} = 52ed3a92c9db3332582fbf0773f18fb9133c4af1` == `5be9737^{tree}` · `git status --porcelain` 空 | 开跑前现打 |
| **代码仓那一侧的每一个数** | 量具 `evidence/K-R67-code-readings.py`（只读，一条命令重打全部），输出落 `evidence/K-R67-code-readings.out` | 每一行自己印分母怎么数的；量不到就印 `?` 当场喊，不静默算 0 |

⚠ **口径**：下面「类」「层」「停车理由今天」三列是**人写的判定**，不是脚本推导出来的读数；
每一条的依据逐条住 `evidence/K-R67-census.out` 第七节 TSV 的「依据」列。
把它们读成读数就是 `C17`（诊断不许比它证得出的说得更死）。

---

## §B 第 1 问 —— 那 44 件各属哪一类（逐件给住址，名单与数同句）

分类规则写死在量具 `VERDICTS` 的注释里，四类之外多一类 **戊 = 终态**（`K-R41` 已裁「判定不做」，
**它不该算进「还有什么没做」**）。

| 类 | 数 | 名单 |
|---|---|---|
| **甲 已交付未签收**（整件交付，只差手续） | **8** | `K-R19` `K-R38` `K-R42` `K-R52` `K-R53` `K-R55` `K-R56` `K-R58` |
| **乙 部分交付**（本拍已合入主干，件内仍有明写没做的阶段） | **20** | `K-P2` `K-P3` `K-P4` `K-R1` `K-R2` `K-R8` `K-R9` `K-R12` `K-R16` `K-R22` `K-R23` `K-R24` `K-R25` `K-R43` `K-R48` `K-W1B` `K-W1C` `K-W2D` `K-W2E` `K-W4` |
| **丙 摸底件**（`exit: 待摸底`） | **6** | `K-R51` `K-R54` `K-R57` `K-R66` `K-R67` `K-R68` |
| **丁 真没做**（零交付） | **9** | `K-P6` `K-R11` `K-R35` `K-R39` `K-R40` `K-R44` `K-R47` `K-R50` `K-R64` |
| **戊 终态**（不该算进「没做」） | **1** | `K-R41` |

⇒ 🔴 **「42 件要做」这句话，按盘上现算是错的**：44 件里 **8 件只差手续**、**1 件已有终局**、
**6 件是摸底件（其中 3 件的摸底本身已经交回：`K-R51` `K-R54` `K-R57`）**。
**真正「零交付、题面还在」的是 9 件**，而这 9 件里 5 件的停车理由今天已经不成立（见 §C）。

---

## §C 第 2 问 —— `parked` 理由的时态审计（本区第二次，上一次是 `K-R27`）

### C-1 这本账自己的三处对不上（脚本现算，不是判定）

| | 数 | 名单 | 说明 |
|---|---|---|---|
| **A 停车条挂着，而件已签收** | **4** | `K-R59` `K-R61` `K-R62` `K-R63` | 结构性已馊 —— 件都签收了，停车理由还在那里说「本轮不推它」 |
| **B 未签收，而 `parked` 里没有它** | **2** | `K-P6` `K-R41` | `K-R41` 是终态可以不写；🔴 **`K-P6` 是用户 09-05 逐字开的件，今天盘上没有任何一处写着「为什么不推它」** |
| **C `parked` 已改写、`parked_全文` 没跟** | **3** | `K-R48` `K-R50` `K-R55` | `parked_全文_注` 里那句「**要还原：把 `parked_全文` 拷回 `parked` 即可**」对这三条是**倒退** —— 拷回去会把它们的停车理由改回旧版本，而 `K-R55` 的旧版本逐字是「本轮不派 —— 并发档：场上 `K-R53` 在跑」，那是一句今天完全为假的话 |

### C-2 逐条判（分母写清楚）

| 判 | 数 | 名单 |
|---|---|---|
| **已馊** | **8** | `K-P3` `K-R9` `K-R22` `K-R35` `K-R39` `K-R40` `K-R44` `K-R51` |
| **半馊**（一半仍成立，一半已翻） | **7** | `K-P4` `K-R24` `K-R25` `K-R50` `K-R64` `K-W2E` `K-W4` |
| **成立** | **27** | 见 `evidence/K-R67-census.out` 第六节 |
| **无理由** | **2** | `K-P6` `K-R41` |

⚠ **这四个数的分母不一样，别把它们读成同一层的判断**：
**我现打核过的是 17 条**（8 已馊 + 7 半馊 + `K-R11` + `K-P6`，逐条读数在下面与 TSV 的「依据」列）；
其余 **27 条我只核了一层** —— 「它自陈的那几项未做，今天有没有被别的件带走」。
**「27 条全都成立」这句话我给不出分母，所以不写** —— 只写「我核过的这一层里没发现翻掉的」。

### C-3 现打逐条（八条已馊）

1. **`K-P3`** —— 理由逐字「`record_death` 还没有生产调用点，账上恒空；三处接线归下一拍单独一件」。
   现打：`K-P3b`（**已签收**）已把三处接上，住 `src-tauri/src/daemon_policy.rs` 的
   `DEATH_RECORD_SITES`（`note_detached_death` / `daemon_supervise_events` / `note_never_started`），
   并配了一条**相等**断言 `the_death_ledger_is_wired_at_exactly_these_sites`（不是地板）。
2. **`K-R9`** —— 「那个洞要另立件 —— PM 下一拍立」。09-05 已注明被 `K-R25` 在 `3aab95a` 关上。
3. **`K-R22`** —— 两处数都馊了：① `race_watchdog_times_out_on_blackhole` 09-05 已注明被 `K-R24 D2`
   在 `ab48245` 修掉；② 「`gate.sh` 478→627 行」09-05 订正成 891，而**今天现打 1023 行**
   （`wc -l scripts/gate.sh` @ `5be9737`）⇒ **同一个数第三次过期**。
4. **`K-R35`** —— 「并发上限 1，`K-R36` 在跑」。`K-R36` 今天 `state: 已签收`。
5. **`K-R39`** —— 「并发上限 1，`K-R37` 在跑」。`K-R37` 今天 `state: 已签收`。
6. **`K-R40`** —— 「并发上限 1，`K-R38` 在跑」。`K-R38` 的代码 09-09 已合入 `2c9c9cc`、
   没有任何 agent 在对应它；它占的是**签收**那一格，**不是档位**。
7. **`K-R44`** —— 「前提是 `P2d` 给出「谁还在跑」，而 `P2d` 没做」。两笔现打：
   ① `P2d` 住**另一个工作区** `control-parity`，`state: 已签收` / `exit: 改件`（不是交付，是改了计划出的口）；
   ② `P2d` 当年判「结构性做不到」的**依据**是「`setsid` / `process_group` / `DETACHED_PROCESS` /
   `pre_exec` 命中数 = 0」，而**今天 `src-tauri/src/local_daemon.rs` 的 `spawn_detached` 就带
   `process_group(0)`、收尸走 `reap_detached`** ⇒ **那条依据被推翻，`K-R44` 的阻塞要重判。**
   ⚠ 我推翻的只是**依据**；`P2d` 另半句「做成了也没人受益」我**没有**重做。
8. **`K-R51`** —— 「解锁：四路交回、PM 合并成一张图之后再派 D2」。四路**都已交回**
   （`LEDGER.md`「四路只读审计交回三路」与「第四路（分层）也回来了」两节）⇒ 解锁条件已到。

### C-4 现打逐条（七条半馊，只写翻掉的那半）

- **`K-P4`**：「要真机跑 Windows，而 win11 VM 归用户 ⇒ 归排期」——「**真机拿不到**」那半今天不成立：
  `K-R42` 09-10 夜就在 win11 上取到过读数（`features/K-R42-…#§9` · `audits/K-R42-真机读数-PM.md`）。
  剩下的是**排期与许可**，不是可得性。
- **`K-R24`**：`accounts_query.rs:2236` 那个行号 09-05 已注明漂到 `:2351`（本条自己就是「行号要带校验位」的活体）。
- **`K-R25`**：「`tool_registry.rs:577` 私有剥法进没进登记表未核」09-06 已核并关掉。
- **`K-R50`**：「排在 `K-R55` 之后」那半**已满足**（`K-R55` 09-11 已合入 `6bb9399`）。
- **`K-R64`**：「摸底那一问今天没有读数」那半 09-11 第三十拍 PM 已给口径（`features/K-R64-…#§0b-2`）。
- **`K-W2E`**：那条 R4 暗路（`invoke::run` 不 `env_clear` ⇒ 插件继承 `ENV_PORT`/`ENV_TOKEN` 可回连宿主）
  **已被 `K-R26`（已签收）关掉**，件住 `features/K-R26-插件继承整份环境能回连宿主.md`。
- **`K-W4`**：「另归 PM 落」那笔 09-06 已现打订正为**早就做完了**。

### C-5 一条不是「馊」而是「一直成立」的，也记下来（免得读者以为审计只会翻案）

**`K-R11`** —— 现打复核：`find /home/zbl/文档/claudecode-frontend -name pb.py -not -path '*/node_modules/*'` = **0**；
`pb.py` 只住上线位（`~/.claude/skills/planned-build/bin/pb.py`，558567 B）。
⇒ 「这个工作区做不了它的一半」**今天仍然为真**，它的解锁只能靠用户点甲/乙/丙。

---

## §D 🔴 PM 名单外的五样（盘上普查逮到的 —— 这一件的正题就是「PM 漏了什么」）

### D-1 **本机根本没有 `ccm` 这个入口** —— `K34` 正题的真前置，而它不在任何一件里

`K34` 逐字：「装了新版后 `~/.local/bin/ccm` 可以干净退役」。PM 把这一格记成
「**唯一零读数、只能真机上买**」⇒ 读起来像「代码都好了，等一次真机验证」。**现打四笔说它不是**：

| # | 现打 | 住址 |
|---|---|---|
| ① | **远端有**：推过去的 `~/.local/bin/ccm` 已经是三行入口 shim `exec <daemon> ccm "$@"` | `src-tauri/src/sftp.rs::ccm_entry_shim` · `CCM_CLI_REMOTE_PATH = ".local/bin/ccm"` |
| ② | **本机没有**：闭集 `TOOLS` 里显式写 `destination` 的 **8** 条声明中，落点是 `…/ccm` 的只有 **1** 条，而它是 **`RemoteHomeRelative(".local/bin/ccm")`** ⇒ **本机落点 0 条**。本机侧往 `~/.local/bin` 落东西的只有 cc-bus 那一族 `cc-*` | `src-tauri/src/tool_registry.rs:330`；分母 = `grep -n 'destination: ToolDestination::' src-tauri/src/tool_registry.rs` 的 8 行；旁证 `config_surface.rs:1369` `("cc-bus", "~/.local/bin/cc-*", Either)` |
| ③ | **产品自己还在生产旧 `ccm` 的使用者**：别名生成器逐字吐 `名字() { ccm … "$@"; }` | `src/launcher-diagnostics.ts::buildAliasLine`，返回行逐字 `` `${trimmedName}() { ccm${flagStr ? ` ${flagStr}` : ""} "$@"; }` `` |
| ④ | 用户机器 PATH 上那个 `ccm` 就是那份 **2026-07-27 的旧 bash**（31 364 B）；`cc-spawn` 的查找末档也回落到它 | `K-R57` `§8.4`（`evidence/K-R57-A-env-census.out` 第【八】【九】节） |

⇒ **「装了新版之后旧的变得没人再用」今天在结构上做不到** —— 缺的不是一次真机读数，是**一个口**：
把后端装成本机的 `ccm`（`K26` 逐字：「`ccm` 就是后端的原生命令行入口」），
并让别名生成器指向它而不是指向 PATH 上碰巧是谁的那个 `ccm`。
⇒ 🔴 **这一件盘上没有。** 它是 `K-R44` / `K-R57` 下一步 / `K-R48` 的 `KR48D3` / 「旧 ccm 干净退役」
四条线的**共同下游根**。
⇒ ⚠ **它不需要用户的机器就能建**（`K31` 管的是「开发测试不许在本机跑」与「不偷改用户机器」，
不是「产品不许有安装能力」—— `STATUS.md` 09-11 第二十二拍已经把这条重读过一遍）；
**只有「验」那一步要用户点。**

⚠ **诚实边界**：我**没有**验「装了这个口之后旧的真的没人用了」——那还是要真机。
我买到的只是「今天挡着它的不是读数，是一个不存在的产品能力」。

### D-2 **`observe/` 那件活盘上没有件**（PM 点了这条前置，但它不在 44 件里的任何一件）

现打：`src-tauri/src/backend/` 下**只有 `control/`**（`find` 18 项，无 `observe/`）；
而 `backend/mod.rs` 自己把下一步写成了一句可执行的话（逐字）：
「**建 `observe/`，把 `control/local_query.rs` 挪成 `observe/local_query.rs`**，
连 `BACKEND_FILES` 那条登记的能力线一起改成 `observe`」，
并说明**两条判据已经在那儿等着**（`every_file_under_backend_is_registered_with_a_reason` ·
`every_file_under_backend_lives_on_a_capability_line`，后者只认 `control`/`observe` 两条线）。
⇒ 便宜、无前置、可并行 —— **而 44 件里没有它。**

### D-3 🔴 **`observe/` 的贵那一半，与 `K-W2D` 是同一道门**（PM 名单上这两条没连起来）

- `backend/control/local_query.rs` 头注逐字：「加读命令要动 hello 的 `commands` 集 =
  动**仓外 aterm 也在读的协议面**，而对面今天有通报闸门 —— **在没法沟通的时候改共享契约是最坏的时机**。」
- `K-W2D` 停车理由逐字：「`panorama` 族要进 `inbound::REGISTRY`（PM 持有），
  它会同时改 hello 帧的 `commands` 集 ⇒ **跨仓契约面**（仓外 aterm 是消费方）。」
- 两处指的是同一个字段：`remote-daemon-proto/src/wire.rs:102` 逐字「取值空间与 `Hello.commands`
  **同一套**（`inbound::COMMANDS`）」。

⇒ **「把 8 条 reader 退役搬进 `observe/`」与「`panorama` 出 sidecar」要动同一张表、走同一次跨仓通报。**
**⇒ 它们必须同拍设计**（一次通报，不是两次）。

### D-4 **11 条要用户点的，只有 2 条在待决区**

现打：全盘 `kind: question` 节点 **13** 条，未闭合 **2** 条（`ROADMAP.md#KU11` · `#KU12`）。
而下面这些「要用户点 / 要产品决定」的，**只住在 `.dispatch.json` 的停车理由、`STATUS.md`、`audits/` 里**，
**一条都没有登记成 question** ⇒ 攒批端给用户的那个口**看不见它们**：

| # | 要用户点什么 | 今天住在哪 | 点了会解锁 |
|---|---|---|---|
| 1 | `K-R19` 甲/乙（`state` 永久停「未开」，还是账本上显式记「已交付但被历史改写挡住」） | `parked_全文.K-R19` | 整件 |
| 2 | `K-R38` 要不要追认成签收 | `parked_全文.K-R38` | 整件 |
| 3 | `K-R8` 排期（它推荐 B′，要动生产段） | `parked_全文.K-R8` | 整件 |
| 4 | `K-R50` 第 3 条（立唯一入口，与 `K-P4` 射程重叠） | `parked.K-R50` | 整件 |
| 5 | `K-R11` 甲/乙/丙（授权动上线位 / 靠人记得跑 / 降成不判红的读数） | `parked_全文.K-R11` | 整件 |
| 6 | `K-R16` 五条路线选一（A statusUpdatedAt 上线 · B busy 才存疑 · C 归档 · D 心跳对账 · E messaging socket） | `parked_全文.K-R16` | 整件 |
| 7 | `K-R44` 「先给个手动清理的口子就行」算不算数 | `parked_全文.K-R44` | 整件 |
| 8 | 真机验「旧 `ccm` 干净退役」的许可（`K31`） | `ROADMAP.md:141` · `STATUS.md:312` | `K34` 正题 |
| 9 | Windows 上 `is_executable` 该不该按 `PATHEXT` 真实现 | `audits/K-R52-PM.md` · `STATUS.md:1246` | `K-R55` 跟进 |
| 10 | 「不给 `--account`」今天落 manifest 默认号 = 静默换号，含义是什么 | `audits/K-R53-PM.md` · `STATUS.md:1153` | `K-R53` 跟进 · `K37` |
| 11 | `capabilities/default.json` 的 `{"path":"**"}` 收不收窄 | `STATUS.md:1270` | 安全面 |

外加已在待决区的 **`KU11`**（拨号走哪条路）与 **`KU12`**（界面侧 7 处拨号搬不搬）——
`KU11` 是 **`K-P6` 的唯一阻塞**，而 `K-P6` 是**用户 09-05 逐字开的件**。

⇒ 🔴 **不把这 11 条一次问完，下面的顺序里有 7 个位置是空的。**

### D-5 **这本账的语汇缺两个形状**（`K-R54` 自陈过一次，本件再逮到一次同族）

- `K-R54` 逐字：「这本账今天**没有一个键能表达「零代码/只读的活」** —— 写进 `parked` 是唯一能让判据闭嘴的地方，
  而 `parked` 的语义是「本轮不推」。」
- `STATUS.md`「顺带逮到一格」逐字：「**写区这本账表达不了「删掉这个文件」**」。
- 后果现打：`.dispatch.json` 顶层有 **9** 个键逐条记着「**手动摘出（不是签收）**」
  （`K-R38` · `K-R41` · `K-R42` · `K-R45` 甲 · `K-R45` 乙 · `K-R48 Bx` · `K-W2D` 真做拍 · `K-R49` · `K-R52`），
  另有 `手动摘除记录` 键记着更早的几笔（`K-A1` · `K-H1` …）；
  而 skill 铁律逐字「**摘除只此一条路 `pb accept`**，手动摘是老债复发」。

⇒ 这不是一条「顺手记一笔」的账：**上面「甲类 8 件」正是这个缺口的产物**，
而每多一次手动摘出，「还剩多少件」这个数就多一分假。

---

## §E 第 3 问 —— 按架构分层（这一步定的是「谁和谁必须同拍」）

先把今天的层现打出来，免得按名字猜：

| 层 | 盘上是什么 | 现打 |
|---|---|---|
| **后端本体（daemon）** | `remote-daemon-proto/src/` | 九条线：`platform/` `observe/` `control/` `dial/` `relay/` `plugin/` `sidecars/` `agents/` `common/` |
| **本机后端边界** | `src-tauri/src/backend/` | 🔴 **只有 `control/`**（18 项）；`observe/` **不存在** |
| **中转** | daemon 的 `relay/` | 11 份 |
| **适配层** | daemon 的 `platform/` | 10 项（`K-R52`/`K-R55` 刚把这条线立起来并守住） |
| **界面** | `src/` | —— |
| **环境/部署** | `tool_registry` · `config_surface` · `profile_installer` · `sftp` · 本机后端自释放 · 三种载体 | —— |
| **判据卫生** | 各 `*_guard.rs` / `*_registry.rs` / `scripts/gate.sh` | `gate.sh` 现打 1023 行 |

44 件按**主层**（同拍层另列，两个数不相加）：

| 层 | 数 | 名单 |
|---|---|---|
| 判据卫生 | **12** | `K-R9` `K-R11` `K-R19` `K-R22` `K-R24` `K-R25` `K-R35` `K-R38` `K-R39` `K-R40` `K-R41` `K-R64` |
| 后端本体 | **9** | `K-P2` `K-R8` `K-R23` `K-R48` `K-R51` `K-R56` `K-W1B` `K-W2D` `K-W2E` |
| 本机后端 | **5** | `K-P3` `K-P4` `K-P6` `K-R43` `K-R53` |
| 环境/部署 | **5** | `K-R42` `K-R44` `K-R57` `K-R58` `K-R68` |
| 界面 | **4** | `K-R16` `K-R47` `K-R50` `K-R66` |
| 跨层 | **3** | `K-R54` `K-R67` `K-W4` |
| 适配层 | **3** | `K-R12` `K-R52` `K-R55` |
| 中转 | **3** | `K-R1` `K-R2` `K-W1C` |

🔴 **这张分层表最要紧的一行是第一行**：**44 件里 12 件（27%）是判据卫生** ——
它们**与用户 `K33`–`K38` 那条线没有写区交集**（唯一例外见 §F 第 6 波）。
PM 09-11 自己记过同一笔（「元工作占比过高，`K-R63`/`K-R64` 都不在用户那条线上」）。
⇒ **它们该整层并行、不该抢用户那条线的档**，但也不该继续挂着**已经馊了**的停车理由。

**必须同拍的四对（依赖不是排序，是「分开做要付两份」）**：

1. **`observe/` 的贵那一半 × `K-W2D`** —— 同一张 `inbound::COMMANDS` 表、同一次跨仓通报（§D-3）。
2. **`K-R68`（载体闭集） × `K34` 的「装」那一档** —— `K-R57` 交回时给的下一步逐字
   「**先做那张清单的闭集、让聚合视图去读它，不是先做装**」。
3. **`K-R55` 跟进② × `K-R64`** —— `RELAY_KEEPS_THE_OLD_PATH` 的退役条件逐字指着**已删的** `shared/ccm`，
   那正是 `K-R64`（101 处「删掉的东西还在被正文指着」）的同一形，而 `K-R64` 的分类口径 09-11 已给。
4. **Windows 真机那一格 ×4** —— `K-P4`（`#[cfg(windows)] const _` 本门禁零读数）·
   `K-W1C` D4 甲 · `K-R43` 一格 · `K-R47`（`K32` 把 Linux 前端往后排 ⇒ 界面验收面在 Windows）。
   ⇒ **一次开机把这四格一起买**，不是开四次。
   ⚠ 与它**不是**同一台机器的是 `K34` 正题（要的是**用户这台 Linux**，`K31` ⇒ 要许可）。

---

## §F 第 4/5 问 —— **依赖脊柱与顺序**（正题）

口径：**「A 是 B 的前置」= 不做 A，B 要么做不了、要么做完要返工、要么它的验收面是空的。**
并发上限今天是 **1 路**（`MASTERPLAN §2 K3`）⇒ 下面是一条线，`可并行` 标的是「档一旦放开先并这几件」。

### 第 0 波 · 让分母是真的（PM 一拍，零 agent，半小时量级）

| 做什么 | 为什么在这个位置 |
|---|---|
| 摘掉 4 条已签收却还挂着的 `parked`（`K-R59` `K-R61` `K-R62` `K-R63`） | 这本账今天**多报 4 个阻塞** |
| 同步 `K-R48` `K-R50` `K-R55` 的 `parked_全文` | `parked_全文_注` 写着的还原路对这三条是**倒退**（§C-1 C） |
| 给 `K-P6` 补一条停车理由 | 44 件里唯一一件「连为什么不推都没有」的用户件 |
| 修 §C 那 8 已馊 + 7 半馊 | 其中 `K-R35` `K-R39` `K-R40` 三件的理由**只有档位一条**，改完当场可派 |
| 八件甲类补账（`K-R19` `K-R38` `K-R42` `K-R52` `K-R53` `K-R55` `K-R56` `K-R58`） | 缺的是：`K-R52` 缺 dod 节点 · `K-R53`/`K-R55`/`K-R56`/`K-R58` 缺 `mutation:` 住址 · `K-R42` 缺登记 + `§8` 三十条重排版 · `K-R19`/`K-R38` 要用户裁（进第 1 波） |

**位置理由（这一波为什么必须第一）**：后面每一步都要读这两本账。
今天它们**多报 4 件阻塞、少记 1 件、错记 3 件**，而 8/44 = **18%** 的「未签收」其实是手续。
不补，「还剩多少」这个数在整条路径上都是假的 —— 而 `KU13` 那一问（循环收敛不了）正是靠这个数问出来的。

### 第 1 波 · 一次问完（PM 一拍，产物是 `ROADMAP §3` 的一批 question 节点，不是代码）

把 §D-4 那 11 条**登记成 question 再攒批端给用户**。
**位置理由**：其中 4 条一答就解锁整件（`K-R19` `K-R38` `K-R8` `K-R50`）、
2 条解锁整棵树（`KU11` → `K-P6`；`KU12`）、1 条是 `K34` 正题的许可。
**不问，第 2 波之后的顺序里有 7 个位置是空的**；而问它们**不占实现档**。

### 第 2 波 · **本机的 `ccm` 入口**（新件 —— `K34` 正题的真前置，§D-1）

- **前置**：第 1 波第 8 条（真机许可）只挡「验」，**不挡「建」** ⇒ 建这一件不用等。
- **同拍**：`K-R48` 的 `KR48D3`（旧副本）—— 它的形状已被 `K34` 改成「**退役，不是更新**」，归的就是这条线。
- **下游**：`K-R44`（旧版本不删）· `K-R57` 的下一步 · 「旧 `ccm` 干净退役」那一格。
- **为什么排在第 2 而不是更后**：它是四条线的共同下游根，而且**它是唯一一件「用户逐字要的东西
  今天在结构上做不到」** —— 其余的都是「做了一半」或「手续没走」。

### 第 3 波 · **载体闭集 `K-R68`**（摸底）

- **前置**：无（`K-R65` 已签收，它就是从那件的盘上普查里长出来的）。
- **它是谁的前置**：`K34` 的「装」那一档（`K-R57` 逐字给的次序）。
- **它自己欠一格**：三种载体**是不是同一次构建的产物**没验过（件 `§0c-2` 自陈）——
  摸底第一刀就该买这个，别把 `R24` 裁定一（按代码同源裁的）当成二进制同源的结论。

### 第 4 波 · **能力线归位**

- **4a（新件，便宜，无前置，档一放开第一个并行）**：建 `src-tauri/src/backend/observe/`，
  把 `control/local_query.rs` 挪过去、改 `BACKEND_FILES` 的能力线。两条判据已经在等（§D-2）。
- **4b（贵，硬前置）**：8 条 `reader` 退役搬进 `observe/`。
  `backend/mod.rs` 明写两条拦路：① **daemon 侧查询集缺口**（`--list-projects` 缺会话 sid 清单 ·
  无 codex 项目枚举 · `--search` 无索引 · 缺 `--list-marketplaces` · 删会话无对侧）；
  ② **宿主耦合**（`tasks.rs` 带 `AppHandle`/`Emitter`，`search.rs` 三处 `tauri::State<`）。
  🔴 **4b 与 `K-W2D` 同拍**（§D-3：同一张 `inbound::COMMANDS`、同一次跨仓通报）。
  ⚠ `backend/mod.rs` 同段逐字警告「**今天没有触发器，不许再在这里编一个** —— 一个不会响的闹钟
  比一句过期的话更贵」⇒ **4b 的启动信号只能是「有人排它」，不许再造一条判据来假装有人看着。**

### 第 5 波 · **`K33` 主堆：所有命令只许一处**

- 起点是 `K-R54` 已交回的 **17 行逐处裁定表**（`features/K-R54-…#§8` · 锚点 `evidence/K-R54-摸底-锚点表.tsv`）。
- 第一刀按它收窄后的两小步走：**先让两条路等价（`K-R56` 已铺完并合入 `94cfafa`），再谈删** ——
  真删那条回落的解锁条件**没变**：那条路要先有一个**运行期计数**。
- **同拍**：`K-R53` 跟进①（那条中转判据自己抄了一份被测逻辑，生产那道闸翻掉一个字都不响）。
- **位置理由**：它是用户 `K33` 的主堆，但它的每一刀都要读 `K-R54` 的表 ⇒ **表在前，刀在后**；
  而表已经在盘上了，所以它排在第 5 而不是更后。

### 第 6 波 · **适配层收尾 + 悬空引用**

- `K-R55` 跟进①：`control/ccm/mod.rs` 那条 `Command::new("sh")` 搬进适配层。
- `K-R55` 跟进② **并进 `K-R64`**：`RELAY_KEEPS_THE_OLD_PATH` 的退役条件逐字指着已删的 `shared/ccm` ——
  与 `K-R64` 那 101 处同一形，而 `K-R64` 的分类口径（历史留痕 vs 活指针，给留痕一个显式记号）09-11 已给。
- **位置理由**：这是本区唯一一处**判据卫生与用户线有写区交集**的地方 ⇒ 它必须从第 8 波里拎出来单排。

### 第 7 波 · **界面**（`K-R66` 先摸底）

- `K-R66`（`exit: 待摸底`）—— 它自己的停车理由逐字「排在 `K-R67` 之后 —— **先有顺序再动界面**」⇒ 本件一交回即解锁。
  它的摸底第一问该是「**每一项能不能说出「改坏了长什么样」**」（`STATUS.md` 已写死这条口径）。
- `K-R16`（红绿灯）等第 1 波第 6 条；`K-R50` 等第 1 波第 4 条；`K-R47` 等 Windows 真机窗口。
- **位置理由**：界面这一层**三件里两件在等用户点**，第三件的验收面按 `K32` 在 Windows 上
  ⇒ 它天然排在「问完 + 真机窗口」之后。

### 第 8 波（**整层可并行**）· 判据卫生 11 件（`K-R41` 是终态，不算）

`K-R9` `K-R11` `K-R19` `K-R22` `K-R24` `K-R25` `K-R35` `K-R38` `K-R39` `K-R40`（`K-R64` 已提到第 6 波）。
- 与用户线**零写区交集**（例外已拎走）⇒ **档一放开，这一层可以整层并行**。
- 内部次序：先第 0 波修完账 ⇒ `K-R35`/`K-R39`/`K-R40` 当场可派（它们的理由**只有档位一条**）；
  `K-R19`/`K-R38` 等第 1 波；`K-R11` 等第 1 波第 5 条；`K-R22`/`K-R24`/`K-R25` 的剩项已知，随时可排。
- **位置理由**：PM 自己 09-11 记了「元工作占比过高」那一笔 ⇒ **它不该抢用户那条线的档**；
  但它也不该继续挂着五条已经馊掉的停车理由（那会让盘面读起来像「有人在等什么」，其实没有）。

### 第 9 波 · 旧波次遗产（08-24 那一波 + `K-P` 族）

`K-P2` `K-P3` `K-R1` `K-R2` `K-R12` `K-R23` `K-W1B` `K-W1C` `K-W2E` `K-W4`（`K-W2D` 已提到第 4 波）。
- 共同形状：**本拍买到的那一格已在主干上，剩下的是当时就明写没做的那几格**
  ⇒ 每一件的下一拍**能直接从它自己的停车理由里读出题面，不用重新摸底**。这是它们的便宜之处。
- 共同位置：**它们不在用户 `K33`–`K38` 那条线上** ⇒ 排在后面。
- **两个例外要拎出来**：
  · **`K-W2D` 提到第 4 波**（跨仓契约面同拍）。
  · **`K-P4` 与 `K-W1C` D4 甲进 Windows 真机窗口**（与 `K-R43`/`K-R47` 一起，一次开机买四格）。

### 位置由用户答案决定的两件（不排死，写清依赖）

- **`K-P6`**（用户 09-05 逐字开的件）—— 盘上**写得出来**的阻塞只有 `ROADMAP.md#KU11`（未闭合）；
  它的 `needs: K-P7` 已满足（`K-P7` 今天 `state: 已签收`）。
  ⚠ 「唯一」这个词我给不出分母 —— 我核的是它 meta 行的 `needs` 与 `links`，**没核**别的件有没有暗着挡它。
  ⚠ 它自己量过一条要端给用户的：**Windows 上本机后端只有 stdio 监护、没有常驻监听口
  ⇒ 整件搬完，Windows 上也买不到用户要的那个「独立跑」。** 这句话该跟 `KU11` 一起端上去。
- **`K-R44`** —— 停车理由的依据已被推翻（§C-3 第 7 条）⇒ **要重判一次，不是照旧挂着**。

---

## §G 一句话的路径（如果只读一行）

**先把账修对（第 0 波）→ 一次问完那 11 条（第 1 波）→ 把「本机的 `ccm` 入口」建出来（第 2 波，
`K34` 正题的真前置，盘上没有这件）→ 载体闭集（第 3 波）→ `observe/` 归位、贵那一半与 `K-W2D` 同拍
（第 4 波）→ `K33` 主堆按 `K-R54` 的表逐刀（第 5 波）→ 适配层收尾并进 `K-R64`（第 6 波）→
界面（第 7 波）→ 判据卫生整层并行（第 8 波）→ 旧波次遗产（第 9 波）。**

**这条路径与「一直做」的关系**：`KU13` 的读数是「今天立 5 签 5，循环在原地滚」。
上面的第 0/1 波**不产生新件**，而它们一次性把 **8 件甲类**从「未签收」里摘出去、
把 **15 条**理由已馊/半馊的停车条改对 ⇒ **循环第一次会出现净减。**
之后每一波的入口条件都是**上一波的产物**，不是「谁想起来谁做」。

---

## §H 判不了的（逐条写清缺什么才判得了）

| # | 判不了什么 | 缺什么才判得了 |
|---|---|---|
| ① | **四格门禁今天的数** | 本件零产品代码、沙箱线 `DECISIONS.md#R21` 不许在宿主跑测试 ⇒ 我给的是**树等同性**（`HEAD^{tree}` == `5be9737^{tree}`、`git status` 空）而不是重跑读数。缺：一次沙箱门禁 |
| ② | **那 27 条判「成立」的停车理由里，有没有第 N 条其实也馊了** | 我现打核过的只有 17 条（§C-2 已写分母）。缺：对其余 27 条各自的解锁条件逐条现打 —— 那是 27 次独立的读数，不是一次 grep |
| ③ | **`K-R68` 三种载体是不是同一次构建的产物** | 要真去比三份二进制的指纹（`build.rs` 的 `NATIVE_DAEMON_*` · `embedded-daemons/` · `externalBin`）。件 `§0c-2` 自陈同一条 |
| ④ | **8 条 `reader` 里哪几条的 daemon 对侧补起来最便宜** | 要读 daemon `observe/` 那 7 份文件的查询集并逐条对上退役条件。本件射程没进到那里 |
| ⑤ | **`K-R44` 的正题（值不值得自动删）** | 我只推翻了它停车理由的**依据**（`P2d` 那条零命中读数今天为假），**没有**重做那一问。缺：按今天的 `spawn_detached`/`reap_detached` 重算「谁还在跑」判不判得出来 |
| ⑥ | **「本机装了 `ccm` 入口之后，旧的真的没人用了」** | 那一步仍然要**用户这台 Linux** 的真机读数（`K31` ⇒ 要许可）。§D-1 买到的只是「今天挡着它的不是读数，是一个不存在的产品能力」 |
| ⑦ | **这 44 件里哪几件用户其实想砍** | 件 `§0d` 写死了不替用户裁。缺：用户看过这张表之后的话 |
