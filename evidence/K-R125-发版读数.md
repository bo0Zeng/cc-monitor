# K-R125 发版读数 —— **①②③ 过了，停在第 ④ 步：不推 tag**

> 本文件只装读数，不装结论。每个数带「量于哪一刻 · 用什么量的 · 在哪棵树上量的」。
> 被测树：`/home/zbl/文档/claudecode-frontend/.claude/worktrees/k-r125`
> （分支 `track/k-r125`，基点 `92de10c7028210ba30508fb32d2651a2c8275dc1`）。
>
> 前两趟的账（住址，不重打）：`K-R119` 只跑演练就推 tag ⇒ `ci-gate` 拦下，零字节发出去；
> `K-R123` 开 PR 跑 `ci.yml` ⇒ 7 绿 1 红 ⇒ 停在第一步、关 PR、不推 tag，读数住
> `evidence/K-R123-发版读数.md`（**那两个数我没重打**，住址在那儿）。
>
> 🔴 **本件的结果一句话**：`KR125D1` 要的 8/8 **拿到了**（PR **#89**，run `34939025122`，
> 8 个 job 全 success —— **第一次**）· `KR125D2` 的重打做完了（**266 / 407**，量于 `92de10c`）·
> `KR125D3` 的 ②（合 PR）与 ③（演练，`build-linux` 真跑到）都做了，
> **第 ④ 步（推 tag）没做** —— `origin/main`（`078a987`）上 `ci.yml` 连跑两趟都是 `failure`
> （两趟红在 `relay::server::tests` 同族的两条不同测试、**同一处 panic**，§10），
> 而 `ci-gate` 判的正是「目标 commit 上有没有一条绿的 CI run」⇒ 推 tag 必被拒。
> **零 tag · 零 release · Release 正文一个字节没发出去。**
> `origin/main` **动了**（这是本件唯一不可逆的一次对外动作，§3 / §11）。

---

## 一 · 第 ① 步：推 track 分支 ＋ 开 PR（`KR125D1`）

**对外动作两次**（`git push origin track/k-r125` · `gh pr create`）。

| 项 | 现打 |
|---|---|
| 分支 | `track/k-r125` → `origin`，`git ls-remote --heads origin track/k-r125` = `755d9c1d55ec1deabec1d1b6f556425e74293999` |
| PR | **#89** `https://github.com/bo0Zeng/cc-monitor/pull/89`，base `main`，head `track/k-r125` |
| PR 标题 | `track/k-r125 -> main`（中性，不表达任何东西 —— `R82`） |
| CI run | **`34939025122`** · `https://github.com/bo0Zeng/cc-monitor/actions/runs/34939025122` |
| event / ref / head | `pull_request` / `track/k-r125` / `755d9c1d55ec1deabec1d1b6f556425e74293999` |
| 墙钟 | 2026-09-15T06:53:50Z → 07:00:45Z |
| **run 终态** | **`completed / success`** |

### 1.1 逐 job 结论 —— **8 个全部点名，8/8 全绿**

分母 = `ci.yml` 在**目标 sha 上**现打 8 个 job（尺子逐字照抄 `K-R119` §6 那把，
只把被测对象换成目标 sha：`git show 755d9c1:.github/workflows/ci.yml | sed -n '/^jobs:/,$p' | grep -cE '^  [a-z0-9-]+:$'` = **8**）。
云端侧同一个数另量一次：`gh run view 34939025122 --json jobs --jq '.jobs|length'` = **8** ⇒ 两侧对得上。
读数量具 = `gh run view 34939025122 --json jobs`，量于 2026-09-15T07:0x UTC。

| # | job | 结论 | 墙钟（UTC） |
|---|---|---|---|
| 1 | `Rust lint + test` | **success** | 06:53:52 → 06:58:45 |
| 2 | `Frontend typecheck + build` | **success** | 06:53:52 → 06:58:32 |
| 3 | `Remote daemon (Linux) lint + test` | **success** | 06:53:52 → 06:55:06 |
| 4 | `Linux app build (no bundle)` | **success** | 06:53:52 → 06:55:35 |
| 5 | `E2E scripts health (shellcheck + py compile)` | **success** | 06:54:28 → 06:54:56 |
| 6 | `E2E real-machine (tmux, no Rust)` | **success** | 06:53:54 → 06:55:04 |
| 7 | `E2E real-machine (tmux + Rust builder emit)` | **success** | 06:53:53 → 07:00:44 |
| 8 | `E2E weak-net rig (docker + netem, no tmux/Rust)` | **success** | 06:53:52 → 06:54:46 |

**8 绿 0 红**（分母 = 上表 8 行）。

### 1.2 🔴 步 8 单独点名 —— `K-R124` 那条守卫**在云端真的绿了**

`E2E scripts health (shellcheck + py compile)`（job id `104283191858`）逐步现打
（量具 `gh run view 34939025122 --json jobs`）：

```
1 Set up job                                                      success
2 Run actions/checkout@v4                                         success
3 shellcheck (errors only)                                        success
4 vendored cc-acct-iso self-tests (sandboxed, 294 assertions)     success
5 python syntax compile                                           success
6 G-A/G-C 覆盖面地板（20 套真机套件都必须带断言数地板）             success
7 exec-bit guard (shared/** shebang files must be 100755 in git)  success
8 release.yml 发版守卫（KR114D1 ＋ KR124D2）                       success   ← 上一趟红的就是这一步
16 Post Run actions/checkout@v4                                    success
17 Complete job                                                    success
```

**步 8 自己印的**（从 job 日志逐字抄，不转述；量具
`gh api repos/bo0Zeng/cc-monitor/actions/jobs/104283191858/logs`，落
`…/scratchpad/kr125-job-scripts-health.log` 第 356-379 行）：

```
PASS  地板·触发器解析得出来 :: on 的键 = ['push', 'workflow_dispatch']
PASS  地板·job 数 :: 4 个 job（分母 = release.yml 顶层 jobs 的键数）
PASS  地板·step 总数 :: 37 步（分母 = 全部 job 的 steps 逐条）—— 切块器坏了这个数会塌
PASS  ①触发得了（有 workflow_dispatch） :: …
PASS  ②有 publish 这个输入 :: inputs 的键 = ['publish']
PASS  ②publish 是 boolean :: type='boolean'
PASS  ②publish 默认 false :: default=False（这一格红 = 随手点一下就发一个版）
PASS  ③env.PUBLISH 与钉住的字面逐字相同 :: 盘上="${{ github.event_name == 'push' || inputs.publish == true }}"
PASS  ④地板·找得到「往 Release 上写」的步骤 :: 2 处（分母 = 全部 job 的全部 step 共 37 个）
PASS  ④闸·build-windows / Create / update GitHub Release :: if="env.PUBLISH == 'true'"
PASS  ④闸·build-linux / Append Linux artifacts to the release :: if="env.PUBLISH == 'true'"
PASS  ⑤地板·找得到 CI 门那一步 :: 1 处
PASS  ⑤CI 门挂在同一个闸上 :: if="env.PUBLISH == 'true'"
PASS  ⑥正文来源·build-windows / Create / update GitHub Release :: body_path='RELEASE_BODY.md' · body=None
PASS  ⑥不回落自动生成·build-windows / Create / update GitHub Release :: generate_release_notes=None
PASS  ⑥正文来源·build-linux / Append Linux artifacts to the release :: body_path='RELEASE_BODY.md' · body=None
PASS  ⑥不回落自动生成·build-linux / Append Linux artifacts to the release :: generate_release_notes=None
PASS  ⑥地板·每一处发布步骤都有 body_path :: 2/2 处（分母 = 上面数出来的发布步骤）
PASS  ⑥正文只有一个住址 :: body_path 的取值集合 = ['RELEASE_BODY.md']
PASS  ⑦生成器排在发布步骤前面·build-windows :: `scripts/release-notes.mjs` 在本 job 第 14 步之前被第 13 步调用，且那一行点名了 `RELEASE_BODY.md`
PASS  ⑦生成器排在发布步骤前面·build-linux :: `scripts/release-notes.mjs` 在本 job 第 13 步之前被第 12 步调用，且那一行点名了 `RELEASE_BODY.md`
PASS  ⑧地板·生成器在盘上 :: /home/runner/work/cc-monitor/cc-monitor/scripts/release-notes.mjs
PASS  ⑧生成器吐得出本版正文 :: release-notes: v3.8.0 的正文 131 行 / 99 个非空行（取自 CHANGELOG.md 的 [3.8.0] 段，地板 20）（rc=0）
release-gate: 23 passed（分母 = 上面逐行印出来的 PASS 条数；被测对象 `/home/runner/work/cc-monitor/cc-monitor/.github/workflows/release.yml`。…）
```

🔴 **`K-R123` 里那条 `FAIL  ③env.PUBLISH 与钉住的字面逐字相同` 这一趟是 `PASS`** ——
逐字对照：上一趟盘上那一侧印的是 `"false"`（runner 先把 `run:` 块里的 `${{ … }}` 求值），
这一趟印的是 `"${{ github.event_name == 'push' || inputs.publish == true }}"` 原样。
差别来自 `K-R124` 把整段判据从 `run:` 块搬进 `evidence/K-R124-ruler.py`（`.py` 不经 GitHub 的表达式渲染器）。
⇒ **`K-R124` 自陈「云端真会绿它验不了」那一格，本趟拿到了实测读数。**
⚠ 诚实边界：本趟实打的触发器只有 **`pull_request`** 一个（`K-R123` §1.3 那张三行表里的另外两行
`push`→`main` 与 `push`→tag `v*`，在本文 §3 与 §4 里各补了一次实打，见那两节）。

---

## 二 · `KR125D2`：CHANGELOG 那段数发版前重打

### 2.1 复量 —— **自己打的，没照抄 `K-R124` 的 265/407**

量于 2026-09-15T06:4x UTC · 被测树 `92de10c`（= `track/k-r125` 的基点，也是这次改动的父提交）：

```
$ git rev-list --count v3.7.0..92de10c          266
$ git diff --name-only v3.7.0..92de10c | wc -l  407
```

三个量点摆在一起（分母 = 同一把尺子 `git rev-list --count` / `git diff --name-only | wc -l`，
只换被测提交）：

| 量点 | commit | 文件 | 谁量的 |
|---|---|---|---|
| `0a92892`（盘上原来写的那个） | **253** | **392** | 原文那一行，本趟重打复现得到同样两个数 ⇒ 它当时没写错，是**量点旧了** |
| `e5e48b7`（`K-R124` 量的） | **265** | **407** | `K-R124`；本趟重打复现，两个数都对得上 |
| **`92de10c`（本趟写进去的）** | **266** | **407** | 本件 |

### 2.2 改了哪一行 —— **只改那两个数 ＋ 原地留旧值与旧量点**

`CHANGELOG.md` 第 13-15 行（改后），逐字：

```
距 3.7.0 共 **266 个 commit / 407 个文件**（`git rev-list v3.7.0..HEAD` 与
`git diff --name-only v3.7.0..HEAD`，量于 `92de10c`
〔K-R125 09-15 重打：旧值 **253 个 commit / 392 个文件**，旧量点 `0a92892`〕）。
```

改动面：`git diff --stat CHANGELOG.md` = `1 file changed, 3 insertions(+), 2 deletions(-)`。
整份文件 md5：改前 `ca09d09052f920fde9f71df60613d44c` → 改后 `18444370a99ba37c968e3a2ec4ef677e`
（量具 `md5sum CHANGELOG.md`，在被测树里跑）。**`CHANGELOG.md` 别处一个字节没动** ——
证法不是 `grep` 数加行，是那份 `git diff` 只有这一个 hunk（`@@ -10,8 +10,9 @@`）。
提交 `755d9c1`，**只点名一个文件**（`git add CHANGELOG.md`）。

### 2.3 🔴 现打确认「那一段真的是 Release 正文会取的那一段」

在沙箱里跑（`K31`：宿主不跑开发动作），被测树 = 改后的 `k-r125`：

```
$ node scripts/release-notes.mjs --check
release-notes: v3.8.0 的正文 131 行 / 99 个非空行（取自 CHANGELOG.md 的 [3.8.0] 段，地板 20）
rc=0
$ node scripts/release-notes.mjs /tmp/RELEASE_BODY.md
release-notes: 写出 /tmp/RELEASE_BODY.md（v3.8.0，131 行 / 99 个非空行）
rc=0
```

`RELEASE_BODY.md` 头 6 行逐字（`head -6`）：

```
# cc-monitor v3.8.0


距 3.7.0 共 **266 个 commit / 407 个文件**（`git rev-list v3.7.0..HEAD` 与
`git diff --name-only v3.7.0..HEAD`，量于 `92de10c`
〔K-R125 09-15 重打：旧值 **253 个 commit / 392 个文件**，旧量点 `0a92892`〕）。
```

⇒ **改的那一行就在生成器吐出来的正文里**（`md5sum /tmp/RELEASE_BODY.md` = `95cbbbacbffe0b0e403893cc2e5bc8b4`，
量于容器内 `/tmp`）。真发出去的那一份另在 §4 从 Release 页上现打一次。

### 2.4 死值验（一刀）—— **这一形今天没人守**

刀具住 `…/scratchpad/kr125-cut.py`（本会话私有目录，只属于本角色；
被测对象逐字写在文件头注：`/home/zbl/文档/claudecode-frontend/.claude/worktrees/k-r125`）。

| 刀 | 锚点（切在哪一处 · 命中几次） | 变异 | 读数 |
|---|---|---|---|
| d1 | `CHANGELOG.md` 里逐字 `距 3.7.0 共 **266 个 commit / 407 个文件**` —— 切前断言**恰好命中 1 次**（实打 1） | 改回旧值 `253 个 commit / 392 个文件`（只动正文那一处，量点 `92de10c` 与原地留的旧值都不动） | **没有任何东西红** |

切后复核「变异已落地」：新值命中 **0** · 旧值命中 **1**（量具 `python3 -c` 数字面量）。
门禁两趟（同一棵树、同一个沙箱镜像、同一个 `CARGO_TARGET_DIR`）：

| 趟 | 命令 | 判定行 | 逐格 |
|---|---|---|---|
| 入场（未切） | `PB_WS=backend-consolidation .claude/devbox/gate …/k-r125 k-r125` | `GATE: OK —— 20 格全绿` · `GATE_RC=0` | 落 `…/scratchpad/kr125-gate.log` |
| d1（切了） | 同上 | `GATE: OK —— 20 格全绿` · `GATE_RC=0` | 落 `…/scratchpad/kr125-gate-cut.log` |

两趟判定行数同为 **20 格**（量法 = 数门禁自己印的 `^  (ok|FAIL) ` 行，两趟都是 20）⇒ 不是台子塌了，
是真的**一格都没红**。逐格读数也逐个相同（`release-gate 23` · `cargo 1616` · `daemon 762` ·
`tsc 366` · `npm 1726` · `pb check FAIL=0 BROKEN=0`）。

🔴 **如实登记：「CHANGELOG 里那个 commit/文件计数写错了」这一形，今天没有任何东西在守。**
分母 = 本地门禁 20 格 ＋ 云端 `E2E scripts health` 步 8 那 23 条判据（后者按构造也够不着：
它判的是 `release.yml` 的结构与「生成器吐得出本版正文」，
`release-notes.mjs` 自己的头注逐字「**不判正文写得对不对**」）。
**没顺手补闸** —— 那是另一件的活，本件正在发版路上（dod 逐字要求）。
⚠ 边界：这个「没人守」只盖我跑过的这两处；别的面（真机、别人的脚本）本行盖不到。

**刀已回退**：`kr125-cut.py revert`（`git checkout -- CHANGELOG.md`）⇒
`md5sum CHANGELOG.md` = `18444370a99ba37c968e3a2ec4ef677e`（与 2.2 改后那一份逐字相同）·
`git status --porcelain` 里 `CHANGELOG.md` 零命中。

---

## 三 · 第 ② 步：合 PR 进 `main`（`KR125D3` 前半）

`R82` 逐字「**绿了就合**」⇒ `gh pr merge 89 --merge`（**没改标题 · 没 at 任何人 · 没合到别的分支 ·
PR 上一条评论都没留**）。

| 项 | 现打 |
|---|---|
| PR 终态 | `gh pr view 89 --json state,mergedAt,mergeCommit` = `MERGED` · `2026-09-15T07:03:31Z` · `078a987357f8f3e943950bf5c7f136f359c75a9d` |
| `origin/main` | 合前 `14e0f0555b623fd96375f4df547f15b0a5519301` → 合后 **`078a987357f8f3e943950bf5c7f136f359c75a9d`** |
| 合并形状 | **真合并提交，两个父**：`git log --format='%P' -1 078a987` = `14e0f055… 755d9c1d…`（不是 squash、不是 rebase） |
| 合进去多少 | `git rev-list --count 14e0f05..755d9c1` = **246** 个 commit · `git diff --name-only 14e0f05..755d9c1 \| wc -l` = **405** 个文件（云端侧 `gh pr view 89 --json changedFiles` = **405**，两侧对得上；⚠ 同一份 json 里的 `commits` 字段是 **100**，那是 GraphQL 单页上限，**不是提交数**，别用它） |

⚠ **这一趟合的不只是本件那一个提交，要说清**：`origin/main` 在合之前停在 `14e0f05`（09-10，`v3.7.0` 之后），
而本地主干已经走到 `92de10c`（`K-R120`…`K-R124`）。`track/k-r125` 的基点就是 `92de10c`
⇒ 这一次 PR 把**那 246 个提交连同本件的 `755d9c1` 一起**推上了 `origin/main`。
这正是发版要的（`main` 上必须有 3.8.0 那棵树），但它不是「只合了一个 CHANGELOG 改动」。

**合出来那棵树与 PR 头那棵树逐字相同**（`git rev-parse 078a987^{tree}` = `git rev-parse 755d9c1^{tree}`
= `8988b4b43d0393320e45e489346c0e5adca4a0be`）⇒ CI 在 `755d9c1` 上量到的 8/8，
量的就是 `078a987` 的那棵树；`14e0f05` 是 `92de10c` 的祖先（`git merge-base --is-ancestor` 退出码 0），
合并没有引入任何第三方内容。

**打 tag 那一刻的计数**（与 §2.1 写进正文的那两个数**不是同一个量点**，写出来别混）：

```
$ git rev-list --count v3.7.0..078a987          268
$ git diff --name-only v3.7.0..078a987 | wc -l  407
```

差 **+2 个 commit**（= 本件的 `755d9c1` ＋ 合并提交 `078a987` 自己）· 文件数**不变**（`CHANGELOG.md`
本来就已经在 `v3.7.0..` 的差集里）。正文里写 `266 / 407 量于 92de10c` 是**刻意的**：
那个量点在写进去那一刻就存在、读者照它重打得到同样两个数；
而「打 tag 那一刻」的 sha 在正文落笔时还不存在，写进去就是一个**没法复打的预判**。

---

## 四 · 第 ③ 步：演练（`workflow_dispatch` ＋ `publish=false`）—— **`build-linux` 真的跑到了**

`gh workflow run release.yml --ref main -f publish=false`（**这是本件第四次对外动作**，
它不往 Release 上写任何东西，见 §11）。

| 项 | 现打 |
|---|---|
| run | **`34939884120`** · `https://github.com/bo0Zeng/cc-monitor/actions/runs/34939884120` |
| event / ref / head | `workflow_dispatch` / `main` / `078a987357f8f3e943950bf5c7f136f359c75a9d` |
| 墙钟 | 2026-09-15T07:04:4x → 07:29:47Z |
| **run 终态** | **`completed / success`** |

逐 job（分母 = `release.yml` 顶层 4 个 job，量具 `gh run view 34939884120 --json jobs`）：

| # | job | 结论 | 墙钟（UTC） |
|---|---|---|---|
| 1 | `Gate on CI (same commit)` | **success** | 07:04:53 → 07:04:56（3 秒 —— 那一步 `if: env.PUBLISH == 'true'`，**演练里被跳过**） |
| 2 | `Cross-compile remote daemons (musl)` | **success** | 07:04:59 → 07:08:53 |
| 3 | `Build Windows artifacts` | **success** | 07:08:55 → 07:23:03 |
| 4 | **`Build Linux artifacts (deb)`** | **success** | 07:23:06 → 07:29:47 | 

🔴 **`KR125D3` 点名要确认的那一格：`build-linux` 真的跑到了、而且跑完了**（第 4 行，6m41s）。

⚠ **单子/件文件 `§1` 那句括号里的话与盘上对不上，如实记下来**：
逐字「`K-R119` 那趟卡在 `build-windows`，Linux 那半一次没跑过；`K-R123` 那趟跑到了」——
**两半都与住址里的读数矛盾**：
- `K-R119` 的演练 `34917750361` 里 `Build Linux artifacts (deb)` 是 **success**（01:53:10 → 02:01:00），
  读数逐字住 `evidence/K-R119-发版读数.md § 3.5`（**这个数我没重打，住址在那儿**）。
- `K-R123` **一趟演练都没跑**（它停在第 ① 步），读数住 `evidence/K-R123-发版读数.md § 二` 那张表的 ③ 行。
⇒ 这句括号话大概是把两趟记串了。**本件不改单子、不改 dod 字面**，只把差报上来。

### 4.1 两个构建 job 自己印的（逐字，不转述）

`build-windows`（job `104287024644`，落 `…/scratchpad/kr125-dryrun-windows.log`）：

```
embedded x86_64 (7573832 bytes)
embedded aarch64 (6729680 bytes)
cc-monitor-remote-x86_64-pc-windows-msvc.exe 7921664
Version self-consistent: 3.8.0 (package.json / tauri.conf / Cargo.toml / Cargo.lock all agree)
演练（publish=false）：跳过「与 tag 一致」那一半；ref_name=main 不是版本号
Backend identity OK: remote-daemon-proto version=0.0.0 (deliberate), BUILD_ID=p2j-bus-state
    Built application at: D:\a\cc-monitor\cc-monitor\src-tauri\target\release\monitor.exe
    Finished 2 bundles at:
        D:\a\cc-monitor\cc-monitor\src-tauri\target\release\bundle\nsis\cc-monitor_3.8.0_x64-setup.exe
        D:\a\cc-monitor\cc-monitor\src-tauri\target\release\bundle\msi\cc-monitor_3.8.0_x64_en-US.msi
```

`build-linux`（job `104290770445`，落 `…/scratchpad/kr125-dryrun-linux.log`）：

```
embedded x86_64 placed
embedded aarch64 placed
    Built application at: /home/runner/work/cc-monitor/cc-monitor/src-tauri/target/release/monitor
 Bundling cc-monitor_3.8.0_amd64.deb (/home/runner/work/cc-monitor/cc-monitor/src-tauri/target/release/bundle/deb/cc-monitor_3.8.0_amd64.deb)
6b61d2d3dc10a5f78fdc9544edf1ecfaaab51fc718a4a3721d881344cedc7387  cc-monitor_3.8.0_amd64.deb
a65ad515f85e2c01ca98aa6013f953be4b38918f6894a5bcaabea163db2ea3f0  monitor
```

⚠ **两个 job 里「往 Release 上写」那两步都 `skipped`**（`Render the Release body` 与
`Create / update GitHub Release` / `Append Linux artifacts to the release`，
逐字 `if: env.PUBLISH == 'true'`，而 runner 环境里现打 `PUBLISH: false`）
⇒ **这一趟一个字节都没发到 Release 上**。

---

## 五 · 🔴 第 ④ 步：**没推 tag**，停在演练之后

**停的理由是现打的读数，不是保守**：`ci-gate` 判的是「**目标 commit 上有没有一条绿的 CI run**」
（`release.yml:126` 逐字 `green=$(printf '%s\n' "$runs" | grep -c '^completed success ')`，
`green > 0` 才放行），而 `078a987` 上现打：

```
$ gh api "repos/bo0Zeng/cc-monitor/actions/runs?head_sha=078a987…&per_page=20"
34939884120  Release  workflow_dispatch  completed/success  attempt=1
34939805688  CI       push               completed/failure  attempt=2     ← ci-gate 只认名字/路径是 CI 的那一条
```

`ci-gate` 的过滤器逐字 `select(.path == ".github/workflows/ci.yml" or .name == "CI")`
⇒ 它看得见的只有 `34939805688` 一条，而那条是 **failure**，且**已经跑完**（`pending = 0`）
⇒ 按 `release.yml:143-145` 那一支，推 tag **会当场被拒**（`commit … 上的 CI 全部跑完、没有一条是绿的`），
只多出一个要删的孤儿 tag。⇒ **`KR125D3` 的第 ④ 步本件没做，判「没做到」，不是「过了」。**
那条红是什么、为什么它不是一次抖动，逐字读数在 §10。

**收工读数（`KR125D3` 点名的那几条，现打于 2026-09-15T07:3x UTC）**：

```
$ gh release view v3.8.0            release not found
$ gh release list --limit 3         v3.7.0  Latest  2026-09-10T13:30:36Z   ← Latest 仍是它
$ git ls-remote --tags origin v3.8.0   （空）
$ git tag -l 'v3.8*'                   （空）
```

⇒ **Release 正文是不是我们那一段 —— 本件判不了，因为没有 Release。**
能给的只有**它会取哪一段**的实打（§2.3：生成器在本树上吐出来的 `RELEASE_BODY.md` 头一段
就是我们改过的那一行，`md5` = `95cbbbacbffe0b0e403893cc2e5bc8b4`，
本地那一份落 `…/scratchpad/kr125-RELEASE_BODY-local.md`），
以及 `release.yml` 两处发布步骤都挂着 `body_path: RELEASE_BODY.md`、
`generate_release_notes` 两处都不存在（§1.2 步 8 的 ⑥ 那四条 PASS）。
**「云端真发出来的那一份正文长什么样」这一格，本件没有对象可量。**

---

## 六 · `KR125D4` 前半：产物逐个核 —— **能核的核了，核不到的写「判不了」**

演练（`publish=false`）只留下**一份**可下载的产物（`gh api …/runs/34939884120/artifacts` 现打
**1 个**：`embedded-daemons`，6914777 B，未过期）。Windows 安装包与 `.deb` 只活在 runner 上
（两处上传 Release 的步骤都 skipped，§4.1）⇒ **拿不到字节。**

### 6.1 内嵌后端 —— **从字节里扫 stamp，不读配置**（扫到了）

`gh run download 34939884120 -n embedded-daemons -D …/scratchpad/kr125-dryrun-art/`。
量具 = `…/scratchpad/kr125-stamp-scan.py`（头注逐字写着被测对象；**只读命令行点名的那几份字节**，
不读任何配置文件、不跑那个二进制、不解包），正则 `<<ccm-build-id:([!-~]{1,64}?):ccm-build-id>>`：

| 文件 | 字节数 | sha256 | 扫到的 stamp | 命中 | 完整戳在不在 |
|---|---|---|---|---|---|
| `cc-monitor-remote-x86_64` | 7573832 | `4c96f6f936baeeaaf7f30524b443d781db3bc4e929ca3d25527c271d5b08cd78` | `p2j-bus-state` | **1** | True |
| `cc-monitor-remote-aarch64` | 6729680 | `b189f165ec63b164f1a489e308c442e1046c090c147bc3e39963cc7421469f9b` | `p2j-bus-state` | **1** | True |

主干源逐字：`git show 078a987:remote-daemon-proto/src/main.rs | sed -n '239p'`
= `const BUILD_ID: &str = "p2j-bus-state";` ⇒ **字节里那个戳与主干源逐字相同**。
⚠ 顺带一个读数：这两份的 sha256 与 `K-R119` 那趟演练扫出来的**逐字相同**
（`evidence/K-R119-发版读数.md § 3.4`，**那两个哈希我没重打，住址在那儿** —— 我重打的是本趟这两份）。

### 6.2 Windows 安装包 —— 🔴 **构建到了，但字节拿不到 ⇒ 扫不了，判不了**

`build-windows` 自己印的两份 bundle 名（§4.1 逐字）：`cc-monitor_3.8.0_x64-setup.exe` ·
`cc-monitor_3.8.0_x64_en-US.msi`，另有裸 `monitor.exe`。
它那一步算过 SHA256（`Compute SHA256 checksums`，`success`），但结果 `Out-File` 进了
`SHA256SUMS.txt`、**没有印进日志**（现打：日志里 `^[0-9A-F]{64}  ` 零命中）
⇒ **连哈希都拿不到**。**从字节里扫 stamp 这一格：判不了**（没有对象）。

### 6.3 Linux `.deb` —— 🔴 **同上：判不了**，但有它自己印的哈希

`build-linux` 印的逐字（§4.1）：`cc-monitor_3.8.0_amd64.deb` sha256
`6b61d2d3dc10a5f78fdc9544edf1ecfaaab51fc718a4a3721d881344cedc7387` ·
`monitor`（Linux app 二进制）`a65ad515f85e2c01ca98aa6013f953be4b38918f6894a5bcaabea163db2ea3f0`。
⚠ 那是 **runner 自己印的数**，不是我在字节上量的 —— **我没有那两份字节**。
**从字节里扫 stamp 这一格：判不了。**
⚠ 顺带：`monitor` 那个哈希与 `K-R119` 演练印的**逐字相同**（住址同 6.1 那条）。

---

## 七 · `KR125D4` 后半：「这棵树自称的版本」—— **人眼逐处过了一遍**（`R79`，不是「那条判据绿了」）

量于 2026-09-15T06:5x UTC · 量具 = `sed -n '<行>p'` / `grep -n` · 被测树 = `k-r125` 工作树
（内容等同 `755d9c1`，而 `git rev-parse 078a987^{tree}` 与 `755d9c1^{tree}` 逐字相同
⇒ **同一份读数也就是 `origin/main` 此刻那棵树的读数**，不必再扫一遍）。
分母 = `K-R119` §1.1 那 7 处 ＋ `R79` 点名的 2 处 ＋ `KR122D3` 收的 `package-lock.json` 顶层 2 处 = **11 行**。

| # | 住址 | 现打那一行（逐字） | 结论 |
|---|---|---|---|
| 1 | `package.json:4` | `  "version": "3.8.0",` | ✅ |
| 2 | `src-tauri/Cargo.toml:27` | `version = "3.8.0"` | ✅ |
| 3 | `src-tauri/tauri.conf.json:4` | `  "version": "3.8.0",` | ✅ |
| 4 | `src-tauri/Cargo.lock:2828`（`name = "monitor"` 在 `:2827`，紧随其后那一行） | `version = "3.8.0"` | ✅ |
| 5 | `README.md:11` 抬头行 | `… \| 当前版本: v3.8.0` | ✅ |
| 6 | `README.md:328` 「项目当前状态」块 | `- **版本**：v3.8.0（Released）` | ✅ |
| 7 | `README.en.md:5` 抬头行 | `… \| Current: v3.8.0` | ✅ |
| 8 | `README.md:15` | `当前发布 **v3.8.0**` | ✅ |
| 9 | `README.en.md:9` | `current release **v3.8.0**` | ✅ |
| 10 | `package-lock.json:3` | `  "version": "3.8.0",` | ✅ |
| 11 | `package-lock.json:9` | `      "version": "3.8.0",` | ✅ |

**11/11。** ⚠ 行号带校验位：上表每一行都把**那一行的逐字内容**抄在旁边了。

### 7.1 全树反扫旧号（这是「人眼」那一半，不是上表那 11 行）

尺子逐字（**照抄 `K-R123` §3.2 那把改对之后的版本** —— 那把尺子的第一版漏了 `sed 's#^\./##'`，
两条排除一条都没生效、命中印成 110；这里用的是改对之后的）：

```
grep -rn "3\.7\.0" --include='*.json' --include='*.toml' --include='*.md' \
  --include='*.ts' --include='*.rs' --include='*.sh' --include='*.yml' . \
  | sed 's#^\./##' | grep -v '^CHANGELOG\.md:' | grep -v '^evidence/' \
  | grep -v node_modules | grep -v '/target/'
```

命中 **31 行**（落 `…/scratchpad/kr125-old-version-scan.txt`），按文件分组现打：
`src-tauri/src/doc_claim_registry.rs` 6 · `src-tauri/src/backend/control/local_backend.rs` 6 ·
`src-tauri/src/lib.rs` 5 · `src-tauri/src/local_daemon.rs` 2 · `src-tauri/src/dial_home_registry.rs` 2 ·
`doc/RELEASING.md` 2 · 其余 8 份各 1（`ssh_source.rs` · `shared_crate_registry.rs` ·
`local_read_surface_registry.rs` · `backend/mod.rs` · `backend/control/agent_profile_parity.rs` ·
`doc/IPC-PROTOCOL.md` · `README.md` · `README.en.md`）。
⇒ 与 `K-R123` 那趟的 31 行**逐文件同分布**（`K-R124` 没有动这一面）。

**逐行看过，31 行全是带日期的历史指称** —— 09-10 那一批「F05b 已随 v3.7.0 发出去」的订正注 ·
`doc/RELEASING.md` 拿旧号举例（`:14` 讲 09-09 那次漂移、`:41` 讲 v3.7.0 补过一行）·
`doc_claim_registry.rs` 的 docstring 在讲**这条判据从哪来** ·
`README.md:164` / `README.en.md:136` 那两句「v3.6.0 与 v3.7.0 的实际产物是 `en-US`」。
⇒ **没有一行是「这棵树此刻自称 3.7.0」。**

⚠ 三条边界，写出来别读宽：
① 这一扫**只盖那 7 个扩展名**；`CHANGELOG.md` 与 `evidence/` 是**主动排除**的（前者是墓碑、
后者是读数留档），别的扩展名本行盖不到。
② 「全是历史指称」是**我人眼逐行判的**，没有机检在守这句话。
③ 顺手再记一次**不影响发版、但确实已经馊了的引文**（`K-R123` §3.2 也记过，本件只核不改、不在写区）：
`src-tauri/src/dial_home_registry.rs:21` 与 `:79` 里那段 `cargo tree` 输出逐字写着
`russh v0.61.1 └── monitor v3.7.0` —— 作为一次带日期（09-12）的快照它没写错，
但今天这棵树的 monitor 是 `3.8.0`。

---

## 八 · `R80` 点名必须逐字带上的那两段（`KR125D4`）

两段的**唯一住址**是 `DECISIONS.md#R80`（`…/planned-build/backend-consolidation/DECISIONS.md:5456`），
下面**逐字照抄**，后面各跟一行「今天还成不成立」的现打。

### 8.1 第一段 —— 这一版没上过真机

> **这一版没上过真机。最后一次真机装机验证是 09-10 的 `v3.7.0`（干净 Windows 机）。
> 其后 230 个提交一次没上过真机。**

**今天仍然成立**，逐处现打：

- 本件**一次真机都没上**（分母 = 本件跑过的全部动作：一趟 PR CI · 一次合并 · 一趟演练 · 一次推 tag ·
  几趟沙箱门禁 —— 没有任何一步把产物装到任何一台机器上）。
- 「最后一次真机装机验证是 09-10 的 `v3.7.0`」这一句**我没重打**（那是 09-10 PM 的读数，
  住 `evidence/K-R114-真机清单.md` 与 `K-R119` 的交回）；本件能重打的是它的旁证：
  推 tag 之前 `gh release list` 的 `Latest` 仍是 `v3.7.0`（2026-09-10T13:30:36Z）。
- ⚠ **那一段里「其后 230 个提交」这个数馊了**（`K-R123` §4.4 已点过一次，今天更大）：
  `git rev-list --count v3.7.0..078a987` = **268**（量于 2026-09-15，打 tag 那一刻那棵树）。
  **我没有去改 `DECISIONS.md`** —— 那份文件是冻结面，明令一个字节都不许碰（固定项第 2 条）。

### 8.2 第二段 —— 那条 `ccm` PATH 预判

`R80` 第二节逐字（住 `DECISIONS.md#R80` 「二、🔴 那条预判没有因此消失，它只是没被验」）：

> `evidence/K-R114-真机清单.md` 的 **R2**：
> `ccm` 入口落 `~/.cc-monitor/bin/`（`install_local_ccm_entry` 逐字「不碰 PATH」），
> 而写进用户 rc 的别名块（`shared/ccm-aliases.sh`）**只把 `~/.local/bin` 加进 PATH**，
> 第 3 行还写着「唯一实现在 `~/.local/bin/ccm`」
> ⇒ **预判：干净机器上 `ccm` / `cc` / `cct` 全是 command not found。**

🔴 **今天仍然既没被证实也没被证伪。**
理由是结构性的：证实 / 证伪它**唯一的路是在一台干净机器上真装一次**，
而本件**没上过真机**（同 7.1 那个分母）。
⇒ 它今天的状态与 `R80` 落笔那一刻**一个字没变**：**没验 ≠ 没有**。
⚠ `R80` 第三节那条登记（「一旦有人在干净机器上装了这一版，第一件事就是敲一下 `ccm`」）
**落点不是本件** —— 本件只把它原样带出来，不代做、也不改它的归属。

---

## 九 · 本地门禁（20 格）—— **不是任何一条 dod 的落点，是旁证**

命令（从项目根起跑，`K31`：宿主不跑 cargo / npm / tsc）：

```
PB_WS=backend-consolidation .claude/devbox/gate \
  /home/zbl/文档/claudecode-frontend/.claude/worktrees/k-r125 k-r125
```

落点 `…/f80a7aea-0ccf-4854-9b7a-12961a9c55aa/scratchpad/kr125-gate.log`（本会话私有目录，
不与别的 agent 共用同名文件）· 被测对象 = `k-r125` 工作树（内容 = `755d9c1`）· 量于 2026-09-15T06:4x–07:0x UTC。

```
GATE: OK —— 20 格全绿（hooks · copy2 · shellcheck · ci-e2e-prereq · release-gate · fmt · fmt-daemon ·
winchk · winchk-daemon · cargo · deadcode · generated · daemon · tsc · npm · 四套 ccm e2e · pb check），可以出货
GATE_RC=0
```

逐格（分母 = 门禁自己印的 `^  (ok|FAIL) ` 那 20 行）：hooks 11 · copy2 11 · shellcheck 57 ·
ci-e2e-prereq 20 · **release-gate 23** · fmt 1 · fmt-daemon 1 · winchk 1 · winchk-daemon 1 ·
cargo **1616**（9 个包合计）· generated 与 Rust 源一致 · deadcode 41 · **daemon 762** · tsc 366 ·
npm 1726 · ccm e2e 四套 12/8/46/45 · pb check FAIL=0 BROKEN=0。

⚠ **`K-R123` §五那条红（`relay::server::tests::a_launch_command_…_with_that_accounts_key`）
本地这两趟都没复现**（daemon 762 passed，入场那趟与死值验那趟都是）。
**但它今天在云端红了两趟**（第二趟红的是同一族里的另一条）—— 见 §10。

---

## 十 · 🔴 挡住第 ④ 步的那条红：`Remote daemon (Linux) lint + test`，**两趟、两条不同的测试、同一处 panic**

合并之后 `ci.yml` 在 `main` 上自己跑了一趟（`push` 事件，不是我触发的），
`attempt=2` 是 **PM 重跑的**（不是我；我这一轮没跑过任何 rerun）。

| run · attempt | event / ref | head_sha | 终态 | 红的那一条 |
|---|---|---|---|---|
| `34939025122` | `pull_request` / `track/k-r125` | `755d9c1` | **success**（8/8，§1.1） | —— |
| `34939805688` attempt 1 | `push` / `main` | `078a987` | 🔴 **failure** | `relay::server::tests::a_launch_command_carrying_the_relay_env_prefix_reaches_the_relay_with_that_accounts_key` |
| `34939805688` attempt 2 | 同上（重跑） | `078a987` | 🔴 **failure** | `relay::server::tests::a_credentials_file_produced_by_the_write_side_routes_that_account_to_the_upstream` |

两趟都只红一发（`test result: FAILED. 761 passed; 1 failed; 2 ignored`，两趟同数），
其余 7 个 job 两趟全绿。

**两条都 panic 在同一处**（逐字，从两份 job 日志各自抄；量具
`gh api repos/bo0Zeng/cc-monitor/actions/jobs/<id>/logs`，
落 `…/scratchpad/kr125-main-daemon-fail.log`（attempt 1，job `104285647930`）与
`…/scratchpad/kr125-main-daemon-fail-a2.log`（attempt 2，job `104289810112`））：

```
attempt 1 ---- relay::server::tests::a_launch_command_carrying_the_relay_env_prefix_reaches_the_relay_with_that_accounts_key stdout ----
thread '<unnamed>' (3910) panicked at src/relay/server.rs:1185:43:
end: Os { code: 32, kind: BrokenPipe, message: "Broken pipe" }
thread '…' (3909) panicked at src/relay/server.rs:3879:13:
这一发没走完一条转发（账号 acct-b）："HTTP/1.1 502 Bad Gateway\r\n" / ""

attempt 2 ---- relay::server::tests::a_credentials_file_produced_by_the_write_side_routes_that_account_to_the_upstream stdout ----
thread '<unnamed>' (4556) panicked at src/relay/server.rs:1185:43:
thread '…' (4555) panicked at src/relay/server.rs:4045:13:
中转 stderr："… [relay] connection ended: Broken pipe (os error 32)\n[relay] upstream connect failed: Connection refused (os error 111)\n"
```

⇒ **同一个 panic 住址 `src/relay/server.rs:1185:43` ＋ 同一对 stderr 形状
（`Broken pipe (os error 32)` ＋ `upstream connect failed: Connection refused (os error 111)`）**
⇒ 这是 `relay::server::tests` 那一族**共用的一个根因**，不是两次互不相干的抖动。
**这一条我自己重打过**（两份 job 日志各自 `gh api` 现拉、逐字比对，不是转述 PM 的话）。

**它与 `K-R123` §五那条本地红同族**（同一个测试名出现在 attempt 1；同一句 `502 Bad Gateway`）。

⚠ **仍然不许读宽**，逐条写清我**没**做的：
- **我没有查 1185:43 那一处在争什么**（分母 = 本件跑过的动作：我只读了两份日志，
  没有打开 `src/relay/server.rs` 去读那一段、也没有在本地复现它）。
- **我一个字节都没改** —— 根因住 `remote-daemon-proto/src/relay/server.rs`，
  **不在本件写区**（`§2` 逐字「明令不在写区：任何生产代码」），PM 已说另立一件。
- **本地那两趟门禁都没复现它**（`daemon 762 passed`，§9）⇒ 它在沙箱里与在云端 runner 上表现不同，
  **这个差本件没有查**。

---

## 十一 · 写区外逐处 · 对外动作逐处

**对外动作，本件一共四次**（分母 = 我这一轮跑过的、**会改动 `origin` 或发到别人手上**的命令；
只读的 `gh run view` / `gh api …/logs` / `gh run download` / `gh release list` / `git ls-remote` 不算）：

| # | 动作 | 在四样许可里吗 | 可逆吗 |
|---|---|---|---|
| 1 | `git push origin track/k-r125` | ✅ 第一样 | 可（删分支）—— **我没删** |
| 2 | `gh pr create --base main --head track/k-r125` | ✅ 第二样 | 可 |
| 3 | `gh pr merge 89 --merge` | ✅ 第三样 | **不可**（`origin/main` 已从 `14e0f05` 走到 `078a987`） |
| 4 | `gh workflow run release.yml --ref main -f publish=false` | 第 ③ 步「演练」本身，单子逐字点名要跑；`publish=false` ⇒ 它一个字节都没往 Release 上写（§4.1 两处上传步骤 skipped） | 不适用（只是跑了一趟构建） |

**没做的对外动作，逐处写清**：

- **没推 tag**（第四样，**没用上** —— 停的理由见 §5 / §10）。
- **没推 `main`** —— `origin/main` 的变动**只经 PR #89 的合并**，没有一次 `git push origin main`。
- **没删任何 tag / 分支**（`v3.8.0` 本来就不存在，现打 `git ls-remote --tags origin v3.8.0` 为空）。
- **没建、没改任何 GitHub Release**（没有 Release 可改）。
- **没在 PR 上留任何评论、没改标题、没 at 任何人**（`R82`）。
- **没跑过任何 `gh run rerun`** —— `34939805688` 的 `attempt=2` 是 PM 跑的，不是我。

**写区外一个字节没碰**：

- **没碰任何生产代码** —— 包括 §10 那条 relay 红的根因所在 `remote-daemon-proto/src/relay/server.rs`，
  **一个字没改**（`§2` 逐字「明令不在写区：任何生产代码」）。
- **没碰 `.github/**`**（`ci.yml` / `release.yml` 只用不改）· **没碰 `scripts/**`** ·
  **没碰那 11 处版本号**（§7 只核不改）· **没碰 `evidence/K-R124-ruler.py`**。
- **`CHANGELOG.md` 只改了 `[3.8.0]` 段那两个数 ＋ 原地留旧值那一行**（§2.2，一个 hunk）。
- **计划仓只写了件文件那一份** `features/K-R125-重新打v3.8.0第二次.md`，**没提交**（提交归 PM）。
- **没碰 `K-R119-发版说明-v3.8.0.md`**（`K-R124` 之后它已不是正文来源，本件连读都只读了它的住址）。
- **没碰上线位 `~/.claude/skills/…`**（本轮题目与它无关，一条路都没走）。

---

## 十二 · 三处 `git status`（交回前最后一次现打，2026-09-15T07:4x UTC）

量具 = `git status --porcelain`，**在每一棵树里各跑一次**（`brief` 第 5 条：量之前先确认自己站在哪儿）。

**① 计划仓** `/home/zbl/文档/claudecode-frontend/.claude/planned-build`：

```
 M "backend-consolidation/features/K-R125-重新打v3.8.0第二次.md"
?? backend-consolidation/.briefs/*.md        （26 行，全是派工单，进场时就在）
```

分母现打：`M` **1 行**（= 本件的件文件，我写的）· `??` **26 行**，
且 `git status --porcelain | grep '^??' | grep -v '\.briefs/'` **零命中**
⇒ 26 个未跟踪文件全在 `.briefs/` 下。
⚠ **进场那一刻这棵树上 `M` 是 0 行**（现打过，`?? ` 26 行同样）⇒ 那一行 `M` 是本轮我落的。
**本件在计划仓一次 `git commit` 都没跑**（铁律 3：提交归 PM 独占）。

**② 代码仓主树** `/home/zbl/文档/claudecode-frontend/cc-monitor`（分支 `main`）：

```
（空 —— `git status --porcelain | wc -l` = 0）
```

⇒ **我一个字节没碰它**（它同时是 `pb deploy` 的部署源，铁律 4 明令不许在它上面干活）。

**③ 我的工作树** `/home/zbl/文档/claudecode-frontend/.claude/worktrees/k-r125`（分支 `track/k-r125`）：

提交前只有 `?? evidence/K-R125-发版读数.md` 一份；提交时**只点名文件**（铁律 20）：
`git add evidence/K-R125-发版读数.md` ＋ `git commit`（**零 emoji · 无 `Co-Authored-By` · 不 `amend`**）。
提交后的 sha 与 `git status` 由交回报告给出。
