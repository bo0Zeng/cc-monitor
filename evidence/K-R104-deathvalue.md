# K-R104 死值验原文（`C` 阶段填）

四条 dod ＋ `7u` 各自的刀、逐刀**完整原文读数**住这里；件文件 `§3` 只放摘要。

## 规矩（缺一条这份证据不算数）

- **全部在沙箱里跑**：`PB_WS=backend-consolidation .claude/devbox/gate <本工作树绝对路径> <tag>`。**不许直接在本机跑**（`K31`/`R15`）。
  🔴 **`K-R101` 那轮实现方自陈破过这条**（宿主上跑过 tsc/vitest/cargo test/fmt）。PM 实测无害，但**违规已登记**。**别重蹈。**
- **一趟一刀**，下一刀前还原干净，贴 `git status --porcelain` 自证。
- 🔴 **还原不许用 `cp -a`**（连 mtime 一起还原 ⇒ cargo 判「没变」⇒ **你读到的是上一刀的红**）。用 `cp` ＋ `touch`。
- 🔴 **刀具取不到该取的东西必须拒跑（fail-closed）** —— `K-R87` 的 `cut.sh` 只认第 2 行 `# FILES:`，某变异写在第 4 行 ⇒ **还原静默跳过**。
- 🔴 **门禁在跑的时候，那棵工作树是它的** —— `K-R101` 自己撞出来的：门禁跑到一半在同一棵树上切了刀，**整趟作废重打**。
- ⚙ **「变异存活」两种成因别混**：判据瞎 ⇒ 补判据 · 变异没生效 ⇒ 重切刀（`K-R87` 的 `new-session -A` 在无终端 daemon 里 rc=1，是后者）。
- ⚙ 本树 `node_modules` 软链已铺（不铺 `M0` 必红）；gitignore 盖着，**不是写区项**。
- ⚠ 写 shell 刀具会被 `shell_lint_registry` 拦（全仓每个 `.sh` 要么进 CI shellcheck 表要么进豁免表）。`K-R101` 的做法是**折成 `.py`**，别去改那两张表。

## `M0` 基线

〔填：`GATE: OK —— 13 格全绿`（cargo N · daemon N · npm N · e2e a/b/c/d），量于哪个基点〕
⚠ 参照：主干 `19581ba` 上 `K-R101` 收官 1598 / 742 / 1722 / 12·8·46·45。**自己重打，别抄。**

## `M1` …（逐刀往下写）
