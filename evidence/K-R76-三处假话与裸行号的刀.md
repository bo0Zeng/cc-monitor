# K-R76 · 三处假话与那个裸行号 —— 刀与读数

> 件计划 `features/K-R76-剥法修好之后那三处正文成了假话.md`（计划仓）· 来路 `DECISIONS.md#R35` 裁定一。
> 工作树 `/.claude/worktrees/k-r76`（分支 `track/k-r76`，基点 `208cd21`，**本树未铺 `embedded-daemons/`**）。
> 量于 **2026-09-12**，量具与日志一律落会话 scratchpad（纪律 ㉑），**工作树里零残留**（收工 `git status --porcelain` 只有 6 个 `M`）。

## §1 门禁四格（沙箱 `.claude/devbox/gate`，`PB_WS=backend-consolidation`）

| 格 | 基线（PM 预登记 @ `208cd21`） | 本件收工 | 差 | 押注 |
|---|---|---|---|---|
| cargo（8 包合计） | 1530 | **1531** | **+1** | 押 `+0 ~ +3` ✅ |
| daemon（`remote-daemon-proto` 单包） | 686 | **686** | **±0** | `KR76D3` 死值验 ✅ |
| npm | 1688 | **1688** | **±0** | ✅ |
| e2e（print-parity / rbind-title / cli / contract-parity） | 12 / 8 / 46 / 45 | **12 / 8 / 46 / 45** | 恒等 | ✅ |
| `pb check` | — | `FAIL=0 BROKEN=0` | — | — |

另两格非数量的押注：**`DIAL_SITES` 里 `moved == false` 的处数合计仍是 6**（现算，量法与 `dial_home_registry::unmoved_dial_sites_in` 同口径）·
**剥法逐字不动**（`git diff --name-only -- src-tauri/crates/` 输出 **0 行**）。

⚠ **`+1` 落在哪一包我没有在 `208cd21` 上单独重打**：`-p monitor --lib` 今天是 `1411 passed`，
而「基线 1410」是从 8 包合计 1530→1531 ＋「本件只新增 1 条判据、住 monitor」**推出来的**，不是量出来的。

## §2 第一趟门禁红了 2 条 —— 那不是变异，是真盘上的账

`gate-1`（改完接线、还没打这两处）逐字：

```
test result: FAILED. 1409 passed; 2 failed; 10 ignored
    structural_scan::tests::every_dead_name_named_in_the_prose_is_declared_dead
    structural_scan::tests::line_number_addresses_stay_in_range_and_never_grow
```

1. **`line_number_addresses_stay_in_range_and_never_grow`** ——
   逐字「存量登记里这几条盘上已经没有了：`local_daemon.rs → src-tauri/crates/guard-core/src/lib.rs 的第 141 行`」。
   ⇒ 本区**早就有一条裸行号的递减棘轮**，那个裸行号在它的存量表里登记着；正文里删掉之后**表要同拍变短**。
   这一条是 `KR76D2` 真正的机检落点，不是我立的 —— 我做的是**让那张表少一行**。
2. **`every_dead_name_named_in_the_prose_is_declared_dead`** ——
   我新写的判据头注里点了 `every_monitor_file_strips_clean`，而那个名字**按构造**不在语料里
   （`dead_name_corpus` 摘掉调用者自己那一份，而它的定义就住 `structural_scan.rs`）。
   ⇒ 按它给的出路 ①「把话改对」，改成指 `guard_core::assert_tree_strips_clean`（那个符号在语料里，真伪判得了）。

★ 这两条正是本件治的那一族**自己长出来的守门人**：一处指着旧判断的正文，被机器当场逮住。

## §3 变异表（逐行：锚点 · 命中 · 活体信号 · 判定行 · 新红 · 定性）

> 🔴 纪律 ㉒：每一趟都核过「它真的重编过吗」—— 台子还原用 `shutil.copy` ＋ `os.utime(p, None)`（**不是 `copy2`**），
> 读数栏里的「活体信号」是那一趟 cargo 输出里真的印出来的 `Compiling …` 行。
> 台子住 scratchpad `…/kr76/mut.py`，被测对象**只指 `.claude/worktrees/k-r76` 这一棵树**（写死在 `WT`）。
> 分母：`cargo test -p monitor --lib`（monitor 那一格）= **1411**；`cd remote-daemon-proto && cargo test`（daemon 那一格）= **686**。

| 刀 | 锚点（住址 · 命中次数） | 改成什么 | 活体信号 | 判定行 | 新红 | 定性 |
|---|---|---|---|---|---|---|
| **M1** | `&& strip_visibility(mod_line).starts_with("mod ")` @ `src-tauri/crates/guard-core/src/lib.rs` · **1** | 退回 `&& mod_line.starts_with("mod ")` | `Compiling guard-core` ＋ `Compiling monitor` | `FAILED. 1382 passed; 29 failed; 10 ignored` | **29** | 🔴 **`KR76D1` 死值验兑现**（见 §3a 红名单：`ssh_source.*` 4 条 ＋ `local_daemon::tests::*` 15 条都在里面） |
| **M2** | `const PIN: &str = r#"…"#;` @ `local_daemon.rs` · **1** | `"mod "` → `"modx "`（形状照旧是一句 Rust 代码，不是拆台） | `Compiling monitor` | `FAILED. 1410 passed; 1 failed` | **1** | 🔴 **`KR76D2` 死值验兑现**，且是**最小面**：红的恰是 `local_daemon::tests::the_strip_rule_this_file_leans_on_is_still_on_disk` |
| **M3** | 4 份文件 5 处锚点（`ssh_source.rs` 1 · `dial_home_registry.rs` 1 · `local_backend.rs` **3** · `local_daemon.rs` 1）＋ 文件末尾追加再导出 | 把 `K-R74`/`K-R7` 那道绕道**整个装回去**，新判据留着 | `Compiling monitor` | `ok. 1411 passed; 0 failed` | **0** | 〔`7u`〕**`KR76D1` 今天没有独立的牙** —— 见 §4 |
| **M4** | 那一行正文 @ `local_daemon.rs` · **1** | 住址改回**裸行号** `…/guard-core/src/lib.rs:234`（今天还对的那个数） | `Compiling monitor` | `FAILED. 1410 passed; 1 failed` | **1** | 🔴 `KR76D2` **失效方向 ①「同病换个数字」被机器挡住**：`line_number_addresses_stay_in_range_and_never_grow` 逐字「这几处是**新写的行号地址**」 |
| **M5** | `remote-daemon-proto/src/readonly_guard.rs` 整份退回 `208cd21`（本件在这份文件上**只改了那一段正文**） | 那段假话原样回来 | `Compiling cc-monitor-remote` | `ok. 686 passed; 0 failed; 2 ignored` | **0** | 〔`7u`〕`KR76D3` 是**纯正文**，天然没牙；同时坐实 daemon 那一格**逐格 686、动不动那段话都一样** |
| **M6** | `("local_daemon.rs", "structural_scan.rs", 425),` @ `structural_scan.rs` · **1**（在它前面插回那条存量行） | 裸行号存量行放回表里（正文已经改对） | `Compiling monitor` | `FAILED. 1410 passed; 1 failed` | **1** | 删那一行是**承重的**，不是顺手：逐字「存量登记里这几条盘上已经没有了」 |
| **M7** | `fn the_strip_rule_this_file_leans_on_is_still_on_disk() {` @ `local_daemon.rs` · **1** | 函数体**整个掏空**（掏掉 1124 字节），签名留着 | `Compiling monitor` | `ok. 1411 passed; 0 failed` | **0** | 〔`7u`〕本件唯一那条新断言，**别处无人接**（如实报） |

**CRASH：0 条。** 七刀全部编译通过、全部印出判定行；没有一趟出现「判定行掉了 / 异常退出」。
每一趟还原之后都核过 `git status --porcelain` 与 `git diff --stat`，六份 `M` 与刀前逐格相同。

### §3a M1 的红名单（29 条，逐条）

`ssh_source.rs`（**4**）：`dial_move_judge::six_of_the_seven_dial_sites_are_still_in_this_process` ·
`write_half_guard::ssh_source_never_splits_a_stream_itself` · `write_half_guard::ssh_source_never_writes_to_a_stream_itself` ·
`write_half_guard::the_shared_stripper_keeps_the_part_this_guard_must_scan`

`local_daemon.rs`（**15**）：`detached_reads_the_path_that_was_taken_not_a_guess` · `relay_running_really_reads_the_handle_table` ·
`starting_twice_does_not_spawn_a_second_local_daemon` · `the_auto_start_refusal_is_not_only_a_log_line` ·
`the_detach_landing_is_the_host_layer_and_the_injection_is_really_used` · `the_detached_handshake_is_derived_from_the_hello_gate` ·
`the_listen_port_is_deterministic_and_inside_the_dynamic_range` · `the_listen_token_file_is_pinned_cell_by_cell` ·
`the_local_backend_only_takes_a_binary_this_platform_can_run` ·
`the_relay_has_a_named_starter_and_it_runs_before_the_detached_branch_returns` · `the_stop_command_really_calls_this_module` ·
`the_strip_rule_this_file_leans_on_is_still_on_disk` · `the_timer_registry_names_every_caller_of_the_one_wait_here` ·
`the_two_resolution_paths_still_agree_on_the_order` · `the_user_actionable_start_failures_all_reach_the_user`

其余（**10**，本件不认领，只如实登记）：`backend::control::payload::…::nobody_reaches_the_relay_take_points_without_going_through_the_seam` ·
`byte_cap_registry::…` · `cross_half_edge_registry::…::no_cross_half_edge_lives_in_production_code` ·
`exec_site_registry::…` · `local_read_surface_registry::…`（2）· `rust_timer_registry::…`（2）·
`structural_scan::tests::every_monitor_file_strips_clean` · `write_site_registry::…`

⇒ **`KR76D1` 那句「红的必须包含这两份文件」兑现**：两份文件各自的判据都在名单里，不是只有一条树级反向自检点了名。

## §4 `7u` —— 本件天然弱，写清为什么弱

`7u` 问的是「把实现整个退掉，还有多少条新断言仍绿」。本件的实现有三块，**三块的形状不一样**：

- **`KR76D1`（拆两处绕道）**：它的产出是**接线本身**，**没有随之新增任何断言**。
  ⇒ 把绕道装回去（`M3`）**0 红**。这不是「刀不够狠」，是**今天盘上没有任何东西在守「别再写这道绕道」** ——
  逼它出声的唯一办法是件计划自己指定的那把尺子（`M1`：退回剥法 ⇒ 29 红）。
  🔴 **说得准的话**：`M1` 证的是「**剥法今天真的接得住这两处**」，`M3` 证的是「**绕道回来了没人吭声**」。两句都是真的，不许拿前者盖后者。
  ⇒ 「要不要给这道绕道立一条棘轮」**归 PM**，写在件文件 `§8` 的待决里，我不自批。
- **`KR76D2`（去裸行号 ＋ 换校验位）**：它有两半，**两半各有一刀**——
  判据那一半掏空 ⇒ **0 红**（`M7`，本件唯一那条新断言别处无人接，如实报）；
  正文＋存量表那一半 ⇒ **两刀各 1 红**（`M4` 写回裸行号 · `M6` 存量行放回）。
- **`KR76D3`（只改那段过期的话）**：**纯正文**，`§0d` 写死不许动接线。
  ⇒ 退回旧假话（`M5`）**0 红**，且 daemon 那一格**动不动都是 686**。这一条**结构上就不可能有牙**，别硬凑。

## §5 改动面（最小面）

`git diff -U0` 里**所有非注释**的增删行，逐行列全（分母 = 6 份文件的整个 diff）：

| 面 | 行 | 归哪 |
|---|---|---|
| 生产段 **删 4 行**、增 0 行 | `ssh_source.rs`：`#[cfg(test)]` ＋ `pub(crate) use dial_move_judge::DIAL_SITES;`；`local_daemon.rs`：`#[cfg(test)]` ＋ `pub(crate) use tests::demand_tmux_shim;` | `KR76D1` —— **这四行就是那两道绕道本身** |
| 模块声明 2 处各改一个词 | `mod dial_move_judge {` → `pub(crate) mod …`；`mod tests {` → `pub(crate) mod tests {` | `KR76D1` |
| 测试段 4 处取名改路径 | `local_backend.rs` 3 处 `crate::local_daemon::demand_tmux_shim(` → `…::tests::demand_tmux_shim(`；`dial_home_registry.rs` 1 处 `crate::ssh_source::DIAL_SITES` → `…::dial_move_judge::DIAL_SITES` | `KR76D1` |
| 测试段 **删 5 行** | `structural_scan.rs` 裸行号存量表那一条 | `KR76D2` |
| 测试段 **增 1 个 `fn`**（26 行） | `local_daemon.rs::tests::the_strip_rule_this_file_leans_on_is_still_on_disk` | `KR76D2` |
| 纯注释 | 三处正文 ＋ `dial_home_registry.rs` 头注那一句 | `KR76D1` / `KR76D2` / `KR76D3` |

**零生产函数体改动** —— 上表「生产段」那一档只有删行，没有任何 `fn` 的体变过。

## §6 一条如实登记的诚实边界

新判据 `the_strip_rule_this_file_leans_on_is_still_on_disk` 买到的是「**那段散文指得着**」，
**买不到「剥法对不对」**（那归 `guard-core` 自己那几条）、也**买不到「本文件测试段真被剥干净了」**
（那归 `structural_scan.rs` 里走 `guard_core::assert_tree_strips_clean` 的树级反向自检）。
它的反空真那半格（`GUARD_CORE.len() > 10_000`）**本件没有单独切刀验**——
要验它得让 `include_str!` 读到空文件，而那会同时把 `matches` 那半也弄红 ⇒ 分不开。**如实记成没验。**
