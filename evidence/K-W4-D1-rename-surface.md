# K-W4 · D1 · 改名射程重打

> 件：`K-W4`（`features/K-W4-仓库级重命名加全局PhaseG.md`，DoD `KW4D1`）
> 量于：代码仓 **`d305ffa`**（`d305ffacf84e2a045befdd54a7f0ad2db2aa387f`，本树 `track/k-w4` 基点）
> 对照：件文件 `§0b` 量于 **`4eb271f`**（`4eb271fd5dab07c44071204043e15fbb4a0aa84f`，`git merge-base --is-ancestor` = YES，是 `d305ffa` 的祖先）
> 打于：2026-09-04 夜（宿主，只读命令；无测试、无网络）
> 量具：本文件正文里逐条写着的那几条 `git grep` / `grep -r`。**没有另开脚本** —— 每条都是一行命令，
> 抬进正文比藏在一个 `.py` 里更好复打（且 `evidence/` 里不许有 `.sh`）。

## 0 · 尺子（三把，**不许相加**）

与 `§0b` 逐字同源，这里把命令原样写出来供复打：

| 尺 | 命令 | 作用域 | 分母 |
|---|---|---|---|
| **甲**（代码仓·处数） | `git grep -oF '<字面量>' <commit> -- .` | `cc-monitor` git 仓 **tracked** 文件；同一行两次算两处 | `git ls-tree -r --name-only d305ffa \| wc -l` = **775**（`4eb271f` 上是 **770**） |
| **乙**（代码仓·文件数） | 同上换 `-lF` | 同上 | 同上 |
| **丙**（计划仓·处数） | `grep -roF --include=*.md '<字面量>' .`，cwd = `.claude/planned-build` | **另一个 git 仓**（`git rev-parse --show-toplevel` = `/home/zbl/文档/claudecode-frontend/.claude/planned-build`） | `.md` **652**（find 652 = `git ls-files '*.md'` 652 ⇒ 今天没有未跟踪的 `.md`；`.git` 下 `.md` = 0，不进分母） |

🔴 **分母自己动了一格**：`§0b` 写「分母 770」，今天是 **775**（+5 个 tracked 文件，本波 13 路在跑）。
凡是「占分母百分之几」那种读数，两版的分母不同 ⇒ **百分比不许跨版本直接比**。

## 1 · 五个面，逐面重打

### 面① 目录名 `remote-daemon-proto/` → `backend/`

| 读数 | `4eb271f`（`§0b`） | `d305ffa`（今天） | 差 |
|---|---|---|---|
| 字面量出现（尺甲） | 440 | **442** | **+2** |
| 涉及文件（尺乙） | 121 | **123** | **+2** |
| 路径本身要动的 tracked 文件 | 77 | **77** | 0 |

`§0b` 那三个数在 `4eb271f` 上**逐字复打成功**（440 / 121 / 77），⇒ 尺子对得上，差值是真漂移不是量法不同。

**差值定位**（`diff` 两版的 `-lF` 文件名集合，不是按数猜）：

```
> evidence/K-R24-home-axis-ab.py            1 处
> evidence/K-R9-R2-fallback-watch-scope.md  1 处
```

⇒ **+2 处 / +2 文件，两个都落在 `evidence/`**，是本波 K-R24 / K-R9 两道新落的量具与读数。
★ 这一格本身是个结论：**面① 的漂移方向是「禁令区在涨」** —— `§2` 划为不许动的那一块每加一份
带日期的量具就 +N，而**活代码面这一版一个字节都没动**（下面的分解表）。

**442 处按顶层目录**（合计 442）：

| 目录 | `4eb271f` | `d305ffa` |
|---|---|---|
| `src-tauri/` | 230 | 230 |
| **`evidence/`** | 118 | **120** |
| `doc/` | 28 | 28 |
| `e2e/` | 25 | 25 |
| `.github/` | 14 | 14 |
| `remote-daemon-proto/`（自指） | 8 | 8 |
| `src/` | 7 | 7 |
| `scripts/` | 4 | 4 |
| 仓根文件 | 4 | 4 |
| `shared/` | 2 | 2 |

**123 文件按后缀**：`rs` 53 · **`py` 29**（`§0b` 28）· `sh` 17 · `md` 10（`§0b` 9）· `ts` 5 · `yml` 2 ·
`toml` 2 · `tsv` 1 · `snapshot` 1 · `json` 1 · `js` 1 · 无后缀 1。

🔴 **`KW4D2` 要先声明的那个分母，现打**（`§0b` 只说了「禁令区不进分母」，没给数）：

| 人群 | 处 | 文件 | 口径（pathspec 逐字） |
|---|---|---|---|
| 全仓 | 442 | 123 | `-- .` |
| **禁令区 `evidence/`** | 120 | 33（其中 `.py` **29**） | `-- evidence` |
| **活代码面 = 全仓 − `evidence/`** | **322** | **90** | `-- . ':(exclude)evidence'` |
| 禁令区 `CHANGELOG.md` | 0 | 0 | `-- CHANGELOG.md` |

⇒ `KW4D2` 说「旧名归零」时，**分母是 322 处 / 90 文件**（面①这一侧），不是 442。
⚠ `CHANGELOG.md` 在面① 上是 **0 处** —— `§2` 那条禁令（`:443` / `:499` / `:513` / `:522` 写着旧路径）
指的是**面②的 `cc-monitor-remote`**（现打 2 处），不是目录名。**`§2` 那一条把两个面并在一句话里说了。**

### 面② crate / 包名 `cc-monitor-remote` → `cc-monitor-backend`

| 读数 | `4eb271f`（`§0b`） | `d305ffa` | 差 |
|---|---|---|---|
| 字面量出现（尺甲） | 170 | **170** | 0 |
| 涉及文件（尺乙） | 51 | **51** | 0 |

**文件名集合两版 `diff` 为空**（不是只比了两个数）。按顶层目录与后缀也逐格同值：

- 目录：`e2e/` 46 · `src-tauri/` 36 · **`.github/` 33** · `doc/` 21 · `remote-daemon-proto/` 13 ·
  `shared/` 11 · `src/` 5 · 仓根 3 · `evidence/` 2（合计 170）
- 后缀：`rs` 18 · **`sh` 16** · `md` 6 · `yml` 2 · `ts` 2 · `toml` 2 · `py` 2 · `lock` 1 · `json` 1 · 无后缀 1

⇒ `§0b` 拿这两个数坐实的那个 lockstep 风险（`.github/` 33 处正则副本 + 16 个 `sh` + `e2e/` 46 处夹具）
**今天一格没变，`KW4D3` 的题面仍然成立**。

**面② 的分母分解**（同 `KW4D2` 用）：禁令区 `evidence/` **2 处 / 2 文件** · `CHANGELOG.md` **2 处** ·
**活代码面 168 处 / 49 文件**（`':(exclude)evidence'`）。

### 面③ 可执行文件名 `monitor` → `cc-monitor`

🔴 **不用字面量数**（`monitor` 是普通词），照 `§0b` 的锚点表切。
🔴 **锚点之间没去重，下表不许求和** —— 现打了这句话的证据：五个非零锚点的处数直接相加是
**62**，而它们命中的 `文件:行号` 并集只有 **56** ⇒ **6 处是同一行被两个锚点各数了一遍**
（量法：`git grep -n` 各锚点 → `awk` 取 `文件:行号` → `sort -u | wc -l`）。

| 锚点（口径） | `4eb271f`（`§0b`） | `d305ffa` 处 | `d305ffa` 文件 |
|---|---|---|---|
| `monitor\.exe`（`-oE` 正则；`-oF 'monitor.exe'` 同值 39） | 39 / 17 文件 | **39** | 17 |
| `-p monitor`（`-oF`） | 11 / 6 | **11** | 6 |
| `target/debug/monitor`（`-oF`） | 5 / 5 | **5** | 5 |
| `target/release/monitor`（`-oF`） | 3 / 1 | **3** | 1 |
| `name = "monitor"`（`-oF`） | 4 / 3 | **4** | 3 |
| `mainBinaryName`（`-oF`） | 0 / 0 | **0** | 0 |

⇒ **六个锚点两版逐格同值，面③ 零漂移。**

**两条结构事实重核，带行号校验位（逐字抄在旁边）：**

1. 二进制名的唯一真源是 **`src-tauri/Cargo.toml:25`** 逐字 `name = "monitor"`
   （`[package]` 那一行在 `:24`）。
   ⚠ **`§0b`「两条结构事实」第 1 条写的是 `Cargo.toml:24`** —— 24 是 `[package]` 头，
   `name = "monitor"` 在 **25**。差一行，结论（「改 cargo 包名就够」）不受影响。
   `name = "monitor"` 那 4 处的另外三处：`src-tauri/Cargo.lock:2827` ·
   `.github/workflows/release.yml:132`（注释）· `:134`（PowerShell 正则 `^name = "monitor"\r?\n`）。
   ★ **`release.yml:134` 那一处是面② 那条 lockstep 风险在面③ 上的活体**：它是个**正则副本**，
   改包名不同拍改它 ⇒ `$lock` 取到 `'<missing>'`。
2. `src-tauri/tauri.conf.json` 只有两行命名，**都在「不动」那一栏**，两处逐字复核：
   `:3` `  "productName": "cc-monitor",` · `:5` `  "identifier": "com.ccmonitor.app",`
   且 `mainBinaryName` 全仓 **0 处** ⇒ 面③ 不该碰这份文件（`§2` 的禁令仍然成立）。

### 面④ 文档面 —— 跨两个 git 仓

| 面 | `remote-daemon-proto` | `cc-monitor-remote` | 尺 | 分母 |
|---|---|---|---|---|
| `cc-monitor` 仓内 `.md`（`§0b`：9 / 6 文件） | **10 文件** | **6 文件** | 乙 | 775 tracked |
| **计划仓全区**（`§0b`：1205 处 / 209 文件 · 187 / 53） | **1210 处 / 210 文件** | **188 处 / 54 文件** | 丙 | **652** `.md`（`§0b` 写 648 / tracked 641） |
| 只算本工作区 `backend-consolidation`（`§0b`：956 / 109 · 122 / 30） | **961 处 / 110 文件** | **123 处 / 31 文件** | 丙 | **178** `.md`（`§0b` 写 174） |

**差值解释，逐条**：

- 仓内 `.md` 面① 9 → 10 文件：新增的是 `evidence/K-R9-R2-fallback-watch-scope.md`（面① 那 +2 里的一个）。
- 计划仓 +5 处 / +1 文件（面①）与 +1 处 / +1 文件（面②）· 分母 648 → 652（+4 `.md`）·
  本工作区分母 174 → 178（+4）：**四个 `.md` 全落在本工作区**，是本波（09-04 夜）13 路的件文件与
  PM 产物；`§0b` 那一版量于同一天更早的一刻。
- ⚠ **`§0b` 写「分母 648（tracked 641）」，今天 find 与 `git ls-files` **同为 652**** ⇒ 那 7 份
  当时未跟踪的（`§0d` 记的 `drafts/` 七件）已经进版本控制了。**「648 / 641」那一对数今天不存在了。**
- ⚠ **`worktrees/` 那一大坨仍然不进任何分母**：`.claude/worktrees` 是 `.claude/planned-build` 的
  **兄弟目录**，不在计划仓根以内 ⇒ 尺丙**结构上**够不到它，不是靠「记得排除」。
  （`§0b` 那条自罚订正说的就是这件事：作用域取 `.claude/` 会把同一份文件的 55 份工作树副本算进来，虚高约 4.5 倍。）

🔴 **210 文件仍然不是待办量，是「不许动」的证据**（`§0b` 同结论，重打后基数变了 1）。
其中 **110 个在本工作区**（占 210 的 52.4%）—— 旧名主要堆在本区。
⚠ 例外只有一份：**本件自己的件文件**（`features/K-W4-仓库级重命名加全局PhaseG.md`，
现打面① **10 处** · 面② **6 处**），它本来就在写区里。

### 面⑤ 用户机器上已经铺下去的那份

**现打（2026-09-04 21:16 PDT，只 `ls -l` / `cat` 那个 18 字节的标记，没有写、没有删、没有 exec）：**

```
drwxrwxr-x 2 zbl zbl    4096 Sep  4 06:15  /home/zbl/.cc-monitor/bin/
-rw------- 1 zbl zbl      18 Sep  4 06:15  /home/zbl/.cc-monitor/bin/.build_id
-rwx------ 1 zbl zbl 2374712 Sep  4 06:15  /home/zbl/.cc-monitor/bin/cc-monitor-remote
-rw-rw-r-- 1 zbl zbl      99 Jul  8 21:14  /home/zbl/.cc-monitor/bin/wrap.log
```

- **2374712 字节（2.3 MB）**，与 `§0b` 面⑤ **逐字节同值、mtime 同值**（`Sep 4 06:15`）⇒ 这一版没动过。
- 🔴 **`§0b` 没给的那一格，今天补上**：`.build_id` **确实在**，内容逐字 **`p1r-event-liveness`**
  （18 字节 = 那 18 个字符，无尾随换行）。**它与二进制同一个 mtime** ⇒ 是同一次自部署写下的一对。
  ★ 而它是个**人取的标签**，不是内容哈希 ⇒「标记相符」在结构上**不蕴含**「字节相同」，
  更不蕴含「那个文件还在」。**这就是 `§0c` 那条断裂的第一手读数**（D4 接着用）。
- 仓里写死 `.cc-monitor/bin` 的地方：**84 处 / 27 文件**（尺甲/乙），
  与 `§0b` 逐格同值，文件名集合 `diff` 为空。
  按顶层目录：`src-tauri/` 30 · `e2e/` 23 · `doc/` 13 · 仓根 7 · `src/` 5 · `shared/` 5 · `remote-daemon-proto/` 1。

## 2 · 一条本文件自己造成的漂移（不写就是伪造分母）

**本文件一落盘，面① 与面② 的数就变** —— 它住在 `evidence/`，正文里反复出现两个旧名。
上面全部读数**量于 `d305ffa`，即本文件与 D4 那笔改动进仓之前**。

★ 结论不是「所以别写」，而是：**面① 的 `evidence/` 那一格是个只涨不跌的棘轮**，
每一份带日期的量具/读数都往里加。所以 `KW4D2` 的「归零」**必须**按上面那张分母表说的
「活代码面 322 处 / 90 文件」判，否则这一格会让「归零」永远不可达 —— 而不可达的判据等于没有判据。

## 3 · 判不了 / 没做的

- **面④ 的「活的指路文档是哪一小撮」没重打** —— 那是 `KW4D5` 的射程，本波不做（PM 补充：本波只 D1 + D4）。
- **合并会不会让面① 那 77 / 442 腐掉**（`§0e ㈢`）：判不了 —— `K-W3` 本波在产拆件表，
  今天盘上一个产出都没有。要判得了，需要那张表落地。
- **`§0b` 面⑤ 说 `doc/REMOTE-PHASE0-DEPLOY.md` 有 8 处旧路径** —— 本波没核（同属 `KW4D5`）。
