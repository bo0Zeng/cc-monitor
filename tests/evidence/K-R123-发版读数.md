# K-R123 发版读数 —— **停在第 ① 步：`ci.yml` 不是 8/8**

> 本文件只装读数，不装结论。每个数带「量于哪一刻 · 用什么量的 · 在哪棵树上量的」。
> 被测树：`/home/zbl/文档/claudecode-frontend/.claude/worktrees/k-r123`
> （分支 `track/k-r123`，基点 = HEAD = `4cf2ec2a4067abe603d5dd7301cc17add8b63ee8`，与本地 `main` 同尖）。
>
> 🔴 **本件的结果一句话**：`KR123D1` 要的 8/8 **没拿到**（**7 绿 1 红**）
> ⇒ 按单子逐字「**绿不了就停在这一步，回来报我，不许推 tag**」，`KR123D2` 的 ①②③ 一步都没做。
> **零 tag · 零 release · `origin/main` 一个字节没动。**

---

## 一 · 第 ① 步：推 track 分支 ＋ 开 PR（`KR123D1`）

**对外动作两次**（`git push origin track/k-r123` · `gh pr create`）。

| 项 | 现打 |
|---|---|
| 分支 | `track/k-r123` → `origin`，`git ls-remote --heads origin track/k-r123` = `4cf2ec2a4067abe603d5dd7301cc17add8b63ee8` |
| PR | **#88** `https://github.com/bo0Zeng/cc-monitor/pull/88`，base `main`，head `track/k-r123` |
| PR 标题 | `track/k-r123 -> main`（中性，不表达任何东西 —— `R82`） |
| CI run | **`34928839471`** · `https://github.com/bo0Zeng/cc-monitor/actions/runs/34928839471` |
| event / ref / head | `pull_request` / `track/k-r123` / `4cf2ec2a4067abe603d5dd7301cc17add8b63ee8` |
| 墙钟 | 2026-09-15T04:26:33Z → 04:32:39Z |
| **run 终态** | **`completed / failure`** |

### 1.1 逐 job 结论 —— **8 个全部点名**

分母 = `ci.yml` 现打 8 个 job（尺子逐字照抄 `K-R119` §6：
`sed -n '/^jobs:/,$p' .github/workflows/ci.yml | grep -cE '^  [a-z0-9-]+:$'` = **8**，量于 `4cf2ec2`）。
读数量具 = `gh run view 34928839471 --json jobs`，量于 2026-09-15T04:3x UTC。

| # | job | 结论 | 墙钟（UTC） |
|---|---|---|---|
| 1 | `Rust lint + test` | **success** | 04:26:35 → 04:31:02 |
| 2 | `Frontend typecheck + build` | **success** | 04:26:35 → 04:30:34 |
| 3 | `Remote daemon (Linux) lint + test` | **success** | 04:26:35 → 04:28:01 |
| 4 | `Linux app build (no bundle)` | **success** | 04:26:35 → 04:28:37 |
| 5 | `E2E scripts health (shellcheck + py compile)` | 🔴 **failure** | 04:27:11 → 04:27:38 |
| 6 | `E2E real-machine (tmux, no Rust)` | **success** | 04:26:35 → 04:27:40 |
| 7 | `E2E real-machine (tmux + Rust builder emit)` | **success** | 04:26:35 → 04:32:39 |
| 8 | `E2E weak-net rig (docker + netem, no tmux/Rust)` | **success** | 04:26:35 → 04:27:31 |

**7 绿 1 红**（分母 = 上表 8 行）。

### 1.2 `K-R122` 修的那五条：**五条全部翻绿了**（对照 `K-R119` §6 那张表）

对照口径 = 按 **job 名**认（不按标签字面），两张表都在同一份 `ci.yml` 的 8 个 job 上。
上一趟读数住 `evidence/K-R119-发版读数.md` §6（那个数我没重打，住址在那儿）；本趟是我自己打的。

| job | `K-R119`（`6bba991`） | 本趟（`4cf2ec2`） |
|---|---|---|
| `Rust lint + test` | success | success |
| `Linux app build (no bundle)` | success | success |
| `E2E weak-net rig` | success | success |
| `Remote daemon (Linux) lint + test` | **failure**（Windows target 10 个编译错） | **success** |
| `E2E scripts health` | **failure**（shellcheck SC1081 ×6） | **failure（换了一条，见 1.3）** |
| `E2E real-machine (tmux, no Rust)` | **failure**（缺 daemon 二进制） | **success** |
| `E2E real-machine (tmux + Rust builder emit)` | **failure**（缺 daemon 二进制） | **success** |
| `Frontend typecheck + build` | **failure**（vitest 路径分隔符） | **success** |

⇒ **`K-R122` 那五条逐条兑现了**：四条 job 整个翻绿；第五条（shellcheck）那一**步**也翻绿了
（`E2E scripts health` 的 **步 3 `shellcheck (errors only)` = success**，量具
`gh api repos/bo0Zeng/cc-monitor/actions/jobs/104252591682 --jq '.steps[]'`）。
**红的是同一个 job 里的另一步。**

### 1.3 🔴 唯一那条红，逐字定性 —— **第六条，`K-R122` 在构造上看不见它**

`E2E scripts health (shellcheck + py compile)`（job id `104252591682`）逐步现打：

```
1 Set up job                                                      success
2 Run actions/checkout@v4                                         success
3 shellcheck (errors only)                                        success   ← K-R122 修的那条，绿了
4 vendored cc-acct-iso self-tests (sandboxed, 294 assertions)      success
5 python syntax compile                                           success
6 G-A/G-C 覆盖面地板（20 套真机套件都必须带断言数地板）              success
7 exec-bit guard (shared/** shebang files must be 100755 in git)   success
8 release.yml 手工触发守卫（KR114D1）                              failure   ← 红在这里
16 Post Run actions/checkout@v4                                    success
17 Complete job                                                    success
```

步 8 自己印的（从 job 日志逐字抄，不转述）：

```
PASS  地板·触发器解析得出来 :: on 的键 = ['push', 'workflow_dispatch']
PASS  地板·job 数 :: 4 个 job（分母 = release.yml 顶层 jobs 的键数）
PASS  ①触发得了（有 workflow_dispatch） :: …
PASS  ②有 publish 这个输入 :: inputs 的键 = ['publish']
PASS  ②publish 是 boolean :: type='boolean'
PASS  ②publish 默认 false :: default=False（这一格红 = 随手点一下就发一个版）
FAIL  ③env.PUBLISH 与钉住的字面逐字相同 :: 盘上="${{ github.event_name == 'push' || inputs.publish == true }}"
PASS  ④地板·找得到「往 Release 上写」的步骤 :: 2 处（分母 = 全部 job 的全部 step 共 35 个）
PASS  ④闸·build-windows / Create / update GitHub Release :: if="env.PUBLISH == 'true'"
PASS  ④闸·build-linux / Append Linux artifacts to the release :: if="env.PUBLISH == 'true'"
PASS  ⑤地板·找得到 CI 门那一步 :: 1 处
PASS  ⑤CI 门挂在同一个闸上 :: if="env.PUBLISH == 'true'"（这一格红 = 真发版那条路上 CI 门可能被跳过）
---- 红 1 条（分母 = 上面逐行 PASS/FAIL 打出来的那几条）----
##[error]release.yml 手工触发守卫红了：③env.PUBLISH 与钉住的字面逐字相同
##[error]Process completed with exit code 1.
```

**病在哪 —— 决定性的一行就在日志里**。runner 会把 `run:` 块里的 `${{ … }}` **先求值再交给 shell**，
而这个判据把「要比的那个字面」**写在 `run:` 块里**（`.github/workflows/ci.yml:723`，盘上逐字）：

```
          CANON_ENV = "${{ github.event_name == 'push' || inputs.publish == true }}"
```

runner 渲染之后，日志里那一行逐字变成（量具 =
`gh api repos/bo0Zeng/cc-monitor/actions/jobs/104252591682/logs | grep -n 'CANON_ENV'`，
现打第 364 行）：

```
CANON_ENV = "false"
```

⇒ 判据拿 **`"false"`** 去比盘上那份 **`"${{ github.event_name == 'push' || inputs.publish == true }}"`**，
**必不相等**。

**它在哪些事件下会过**（分母 = `ci.yml` 现打的三个触发器 `push:branches[main]` / `push:tags['v*']` / `pull_request`）：

| 事件 | `${{ … }}` 求值成 | `CANON_ENV` 渲染成 | 与盘上字面相等吗 |
|---|---|---|---|
| `pull_request` | false | `"false"` | **否**（本趟实打） |
| `push` → `main` | true | `"true"` | **否**（推演，本件未实打） |
| `push` → tag `v*` | true | `"true"` | **否**（推演，本件未实打） |

⇒ **这一格在三个触发器上都过不了**。⚠ 诚实边界：三行里**只有第一行是我实打的**，
后两行是按同一段求值规则推的 —— 要实打就得真推 `main` 或真推 tag，而那两样正是本件被拦住的动作。

**为什么直到今天才看见它**（三条现打，各带住址）：

1. 这段守卫是 **`K-R114`（`d1a0552`）加的** —— 量具 `git log --oneline -S'CANON_ENV' -- .github/workflows/ci.yml`，
   命中 **1 条**：`d1a0552 K-R114 release.yml 加 workflow_dispatch，并把「发不发布」收成一个闸`。
2. **最后一次 8/8 全绿的 CI（`34507187447` @ `14e0f05`，2026-09-10）上它根本不存在** ——
   `git show 14e0f05:.github/workflows/ci.yml | grep -c 'CANON_ENV'` = **0**；
   而 `git merge-base --is-ancestor 14e0f05 d1a0552` 退出码 0 ⇒ 守卫加在那趟绿之后。
3. **`K-R119` 那趟（`34919687825` @ `6bba991`）这一步压根没跑到** —— 同一个 job 的**步 3 shellcheck 先红**，
   步 8 被 `-e` 带着一起没执行。⇒ shellcheck 一修好，它才第一次露面。

★ 形状：**一条红把它后面的红挡住了**。`K-R122` 的 `ci-e2e-prereq` 是静态判据（只读盘上三份文本），
它守的是「前置齐了」；而**前置齐 ≠ 那一套会绿** —— 单子里写死的那句话，这一趟就是它的实例。

### 1.4 PR 的处置：**关了**

`R82` 逐字「**绿了就合，不绿就关**」⇒ `gh pr close 88`，现打
`gh pr view 88 --json number,state,mergedAt` = `PR#88 state=CLOSED mergedAt=null`。
**没合、没改标题、没 at 任何人、没合到别的分支、没在 PR 上留任何评论。**

---

## 二 · 第 ②③④ 步（`KR123D2`）：**一步都没做**

| 步 | 单子要的 | 实际 | 为什么 |
|---|---|---|---|
| ② 合 PR 进 `main` | 合 | **没合**（PR 关了） | `KR123D1` 没过 |
| ③ 演练（`workflow_dispatch` ＋ `publish=false`），确认 `build-linux` 跑到 | 跑 | **没跑** | 顺序不许反 |
| ④ 推 tag ⇒ `ci-gate` 放行 | 推 | **没推** | 单子逐字「不许推 tag」 |

**收工读数（`KR123D2` 点名的那两条，现打于 2026-09-15T04:4x UTC）**：

```
$ gh release view v3.8.0
release not found

$ gh release list --limit 3
v3.7.0   Latest   v3.7.0   2026-09-10T13:30:36Z      ← Latest 仍是它，没变成 v3.8.0
v3.6.0            v3.6.0   2026-08-01T02:26:55Z
v3.5.0            v3.5.0   2026-07-31T19:51:24Z

$ git ls-remote --tags origin v3.8.0
（空 —— 远端没有这个 tag）

$ git tag -l 'v3.8*'
（空 —— 本地也没有）

$ git ls-remote --heads origin main
14e0f0555b623fd96375f4df547f15b0a5519301                ← 与开工前逐字相同，没动过
```

⇒ **`KR123D2` 判「没做到」，不是「过了」。**

---

## 三 · `KR123D3`：产物 ＋ 「这棵树自称的版本」

### 3.1 产物 —— 🔴 **本趟判不了，因为本趟一个产物都没构建**

演练没跑（见上表 ③）⇒ 没有 `tauri build`、没有 `embedded-daemons` artifact、没有 `.deb`、没有安装包。
**从字节里扫 stamp 这件事本趟没有对象可扫。**

本件能给的只有**主干源那一侧**（在 `4cf2ec2` 上现打，量具 `grep -n`）：

| 要核的 | 现打 | 住址 |
|---|---|---|
| 主干源里的 `BUILD_ID` | `const BUILD_ID: &str = "p2j-bus-state";` | `remote-daemon-proto/src/main.rs:239` |
| 与 `K-R119` 那趟扫出来的 stamp 同不同 | **同**（那趟两份 musl daemon 字节里各扫出 1 次 `p2j-bus-state`） | 上一趟的读数住 `evidence/K-R119-发版读数.md` §3.4，**这个数我没重打** |

⚠ **别把上一行读宽**：`K-R119` 扫的是 `6bba991` 那趟演练的产物字节；本树是 `4cf2ec2`。
两棵树的 `BUILD_ID` **源码字面相同**，但「**本树构建出来的产物字节里有那个戳**」这一格
**本趟没有产物、判不了**。

### 3.2 「这棵树自称的版本」—— **人眼逐处过了一遍**（`R79`，不是「判据绿了」）

量于 2026-09-15T04:1x UTC · 量具 = `grep -n` · 被测树 `4cf2ec2`。
分母 = `K-R119` §1.1 那 7 处 ＋ `R79` 点名的 2 处 ＋ `KR122D3` 新收的 `package-lock.json` 顶层 2 处 = **11 行**。

| # | 住址 | 现打那一行（逐字） | 结论 |
|---|---|---|---|
| 1 | `package.json:4` | `  "version": "3.8.0",` | ✅ |
| 2 | `src-tauri/Cargo.toml:27` | `version = "3.8.0"` | ✅ |
| 3 | `src-tauri/tauri.conf.json:4` | `  "version": "3.8.0",` | ✅ |
| 4 | `src-tauri/Cargo.lock:2828`（`name = "monitor"` 紧随其后那一行） | `version = "3.8.0"` | ✅ |
| 5 | `README.md:11` 抬头行 | `… | 当前版本: v3.8.0` | ✅ |
| 6 | `README.md:328` 「项目当前状态」块 | `- **版本**：v3.8.0（Released）` | ✅ |
| 7 | `README.en.md:5` 抬头行 | `… | Current: v3.8.0` | ✅ |
| 8 | `README.md:15` | `当前发布 **v3.8.0**` | ✅ |
| 9 | `README.en.md:9` | `current release **v3.8.0**` | ✅ |
| 10 | `package-lock.json:3` | `  "version": "3.8.0",` | ✅ **（`K-R119` 那趟是 `3.7.0`，`KR122D3` 修了）** |
| 11 | `package-lock.json:9` | `      "version": "3.8.0",` | ✅ **（同上）** |

**全树反扫一遍旧号**（这是「人眼」那一半，不是上表那 11 行）。尺子逐字：

```
grep -rn "3\.7\.0" --include='*.json' --include='*.toml' --include='*.md' \
  --include='*.ts' --include='*.rs' --include='*.sh' --include='*.yml' . \
  | sed 's#^\./##' | grep -v '^CHANGELOG\.md:' | grep -v '^evidence/' \
  | grep -v node_modules | grep -v '/target/'
```

⚠ **这把尺子我第一版写错过，读数一起记下来**：头一版漏了 `sed 's#^\./##'`
⇒ 两条排除（`^CHANGELOG\.md:` · `^evidence/`）**一条都没生效**，
命中数印成 **110**，而我差点把它当成「已排除后的数」。**改对之后是 31。**
（这正是 `brief` 第 12 条那句「报的每一个数，分母怎么数的都要写明」要挡的东西。）

命中 **31 行**，按文件分组现打：
`src-tauri/src/doc_claim_registry.rs` 6 · `src-tauri/src/backend/control/local_backend.rs` 6 ·
`src-tauri/src/lib.rs` 5 · `src-tauri/src/local_daemon.rs` 2 · `src-tauri/src/dial_home_registry.rs` 2 ·
`doc/RELEASING.md` 2 · 其余 8 份各 1（`ssh_source.rs` · `shared_crate_registry.rs` ·
`local_read_surface_registry.rs` · `backend/mod.rs` · `backend/control/agent_profile_parity.rs` ·
`doc/IPC-PROTOCOL.md` · `README.md` · `README.en.md`）。

**逐行看过，31 行全是带日期的历史指称** —— 09-10 那一批「F05b 已随 v3.7.0 发出去」的订正注 ·
`doc/RELEASING.md` 拿旧号举例 · `doc_claim_registry.rs` 的 docstring 在讲**这条判据从哪来**
（它复述的正是 `K-R119` 逮到 `package-lock` 旧号那件事，是历史，不是现状）·
`README.md:164` / `README.en.md:136` 那两句「v3.6.0 与 v3.7.0 的实际产物是 `en-US`」。
⇒ **没有一行是「这棵树此刻自称 3.7.0」。**

⚠ 三条边界，写出来别读宽：
① 这一扫**只盖那 7 个扩展名**；`CHANGELOG.md` 与 `evidence/` 是**主动排除**的
（前者是墓碑、本件只核不改；后者是读数留档）；别的扩展名本行盖不到。
② 「全是历史指称」是**我人眼逐行判的**，没有机检在守这句话。
③ 顺手记一处**不影响发版、但确实已经馊了的引文**：`src-tauri/src/dial_home_registry.rs:21` 与 `:79`
里那段 `cargo tree` 输出逐字写着 `russh v0.61.1 └── monitor v3.7.0` ——
它标着「09-12 PM 现打」，**作为一次带日期的快照它没写错**，但今天这棵树的 monitor 是 `3.8.0`
⇒ 谁照那两行重打会看到 `monitor v3.8.0`。**本件只核不改**（不在写区）。

⇒ **`KR123D3` 的「自称版本」这一半：过了，11/11。产物那一半：判不了（没有产物）。**

---

## 四 · `KR123D4`：文案逐条复核（只读只核，一个字节没改）

被核对象：`/home/zbl/文档/claudecode-frontend/.claude/planned-build/backend-consolidation/K-R119-发版说明-v3.8.0.md`
（**计划仓那一份，明令不在写区**）· 现打 `wc -l` = **136 行**（含 §0 那 9 行留档抬头；
单子与件文件写的「127 行」指的是抬头之后的正文那一段，两个数不矛盾，量的不是同一段）。

### 4.1 breaking 段那六条，逐条在 `4cf2ec2` 上现打复核

| # | 文案说的 | 现打（量具 `grep -rn`，被测树 `4cf2ec2`） | 还准不准 |
|---|---|---|---|
| 1 | `daemonless` 删了；旧配置里那个开关还是 `true` 的机器收到一条**有名字的告知**，下次保存把旧键写没 | `src/ src-tauri/src/ remote-daemon-proto/src/ shared/` 里命中 96 行；**剔掉注释行与 `.md` 之后剩 27 行**，逐行看过只有两处是活代码，而且正好就是文案描述的那两件事：`src/remote-config.ts:62` `export const LEGACY_NO_BACKEND_KEY = "daemonless";`（旧键迁移）· `src/settings/readiness.ts:83` 那句告知逐字「这台机器此前是按「不装后端」配的（旧的 daemonless 开关），而今天没有那一档了 —— 」。其余 25 行是判据/测试在**反向**守它不回潮（`src-tauri/src/backend/control/launch_wire.rs:834` 逐字断 `!cfg.contains(r#""daemonless","#)`） | ✅ 准 |
| 2 | `cch` 别名删了；`ccm` 不再猜目录；`$CCM_WORKSPACE` 删掉 | `src-tauri/src/account_aliases.rs:802` 逐字断 `!crate::sftp::builtin_alias_names().contains(&"cch")`；`CCM_WORKSPACE` 6 处**全在注释/墓碑里**，`remote-daemon-proto/src/control/ccm/plan.rs:41` 逐字「这个环境变量**不再被读**」 | ✅ 准 |
| 3 | `ccm` 收成后端原生命令，`shared/ccm` 那套 bash 删了 | `ls shared/` = `cc-bus/` ＋ `ccm-aliases.sh` 两项，**没有 `ccm`**；转发壳形状钉在 `src-tauri/src/sftp.rs:1553` 逐字「它只许有一行 `exec <后端> ccm "$@"`」 | ✅ 准 |
| 4 | tmux 会话名 `<sid8>-cc` → `<项目名>-cc` | `src/ipc/local-tmux-name.ts:14-15` 逐字「基名从 `<sid8>-cc` 换成从 **cwd** 派生的 `<项目名>-cc`」 | ✅ 准 |
| 5 | 后端二进制自带身份 ⇒ 旧远端会被判旧重装一次 | `remote-daemon-proto/src/main.rs:239` `const BUILD_ID: &str = "p2j-bus-state";`；`src-tauri/src/ssh_source.rs:101` 逐字「跳的条件**只有一个**：记忆里那台机器的 build_id 恰好等于 `EXPECTED_DAEMON_BUILD_ID`」；`main.rs:179` 逐字「`--ccm-probe` 多吐一行 `build=<BUILD_ID>`」 | ✅ 准 |
| 6 | 桌面侧两条 SSH 回落（送键 · 杀会话）删净 | `src-tauri/src/tmux_daemon_gate_guard.rs:467` / `:592` 两条判据分别守 `kill_remote_tmux` / `tmux_send_keys` 里**不许再出现** `connect_and_exec_cmd`；`:239` 逐字「要恢复它先回 `K-R54` 重新裁定」 | ✅ 准 |

⇒ **六条一条都不用改。**

### 4.2 `K-R122` 要不要加第七条？—— **不要**

`K-R122`（`a93b79e`）改了 12 个文件（量具 `git show --stat a93b79e`）。逐个定性「用户看得见吗」：

| 文件 | 性质 | 用户看得见吗 |
|---|---|---|
| `.github/workflows/ci.yml` | CI 编排 | 否 |
| `scripts/gate.sh` · `evidence/K-R122-cut.py` · `evidence/K-R122-ruler.py` · `evidence/K-R80-gate-cell-coverage.py` | 门禁与量具 | 否 |
| `e2e/usage-probe-acceptance.sh` | e2e 脚本里一个函数改名 | 否 |
| `src/ipc/commands.vitest.ts` | 前端**测试**里 `walk()` 吐路径时规范化 `path.sep` | 否 |
| `remote-daemon-proto/src/sidecars/codepicture/acquire.rs` | 三条**测试**加 `#[cfg(unix)]` | 否 |
| `src-tauri/src/doc_claim_registry.rs` | 文档对拍判据（新增一条 `the_npm_lockfile_claims_the_version_we_ship`） | 否 |
| `package-lock.json` | 顶层两处 `3.7.0` → `3.8.0` | 否（装出来的东西不变） |
| `remote-daemon-proto/src/observe/search_query.rs` | **唯一一处生产段**：`set_mtime` 从 `libc::utimensat` 换成 `std` 的 `File::set_times` | **否** —— 同一件事换了一个 API，Linux 上语义相同；远端后端今天只在 Linux 上跑 |

⇒ **`K-R122` 没有引入任何会改变已有行为的用户可见变化，breaking 段不用加第七条。**
⚠ 诚实边界：这个判断是**逐文件读 diff 得出的**，不是跑出来的 —— 我没有为「换 API 前后行为相同」造过一条活体对照。

### 4.3 🔴 **文案里有几行数已经馊了** —— 都在「这一版验到哪为止」那一节

那一节自己写着（逐字）：「上面这几行是 **2026-09-15 那一刻的读数** —— 这份说明若在之后才真正发出去，**每一行都要重打一遍**。」
**我重打了。四处对不上：**

| 文案里那一行（逐字） | 现打（`4cf2ec2`，2026-09-15） | 差在哪 |
|---|---|---|
| 「距 `v3.7.0` 共 **257 个 commit / 397 个文件**（… 量于 `6bba991`）」 | `git rev-list --count v3.7.0..4cf2ec2` = **261** · `git diff --name-only v3.7.0..4cf2ec2 \| wc -l` = **402** | 量点变了（多了 `K-R120`…`K-R122` 四个提交） |
| 「**出货门禁 16 格全绿** —— 量于树尖 `6bba991`… cargo 1615 · daemon 762 · tsc 366 · npm 1726」 | 门禁已是 **19 格**（`K-R122` 加了 shellcheck / ci-e2e-prereq / winchk-daemon 三格）· cargo **1616** | 格数与 cargo 数都变了 |
| 「**同一棵树上 `ci.yml` 8 个 job 有 5 个红**（… 逐条列的那五条）」 | 本趟 `4cf2ec2` 上是 **7 绿 1 红**，而且**红的不是那五条里的任何一条** | 整句失效 |
| 「其后 **230** 个提交一次没上过真机」 | `git rev-list --count v3.7.0..4cf2ec2` = **261**（那个 230 量于 `d1a0552`） | 数变了 |

🔴 **我没有改那份文件**（`§2` 写区逐字「`K-R119-发版说明-v3.8.0.md`（**只读只核，要改先回来说**）」）
⇒ **这四行原样留在那儿，交回 PM/用户裁**。真到发版那一拍，这四行必须重打。

### 4.4 `R80` 点名必须带的那两段：**原文里在，逐字复核过，今天仍然成立**

- 「**这一版没上过真机。最后一次真机装机验证是 09-10 的 `v3.7.0`（干净 Windows 机）。**」
  ⇒ **今天仍然成立**：`gh release list` 现打 `Latest` 仍是 `v3.7.0`（2026-09-10），
  而 `v3.7.0..4cf2ec2` 之间 **261** 个提交没有任何一次上过真机（本件也没上）。
  ⚠ 那一段里跟着的「其后 **230** 个提交」这个数馊了，见 4.3。
- 那条 `ccm` PATH 预判（原文逐字）：
  「`ccm` 入口落 `~/.cc-monitor/bin/`（装它那个函数的文档逐字「**不碰 PATH、不碰任何 rc、不碰用户的 `~/.local/bin`**」），
  而写进你 rc 的别名块**只把 `~/.local/bin` 加进 PATH** ⇒ **预判：干净机器上 `ccm` / `cc` / `cct` 全是 command not found。**」
  ⇒ **今天仍然既没被证实也没被证伪** —— 证实/证伪它唯一的路是**在一台干净机器上装一次**，
  而本件没有产物、没有发版、没上真机（分母 = 本件跑过的全部动作）。

### 4.5 🔴 文案根本没有落到任何对外位置 —— 而且按本件的边界，**它本来也落不了**

`release.yml` 现打两处「往 Release 上写」都是
`uses: softprops/action-gh-release@v2` ＋ **`generate_release_notes: true`**，**没有 `body` / `body_path`**
（住址 `.github/workflows/release.yml:426-435` 与 `:571-580`）。
⇒ 就算这一趟真发出去了，Release 正文也是 **GitHub 自动生成的那一份**，不是这 136 行。
要换成这份文案，得跑 `gh release edit --notes-file` —— 那是**第五样对外动作**，
而单子逐字「**对外动作只许四样**：推 track 分支 · 开 PR · 合 PR · 推 tag。**别的一律没有。**」
⇒ **我没做，交回 PM/用户裁**（`K-R119` 在它的 §8 里也点过同一件事）。

---

## 五 · 本地门禁（19 格）—— **不是任何一条 dod 的落点，是旁证**

命令（从项目根起跑，`K31`：宿主不跑 cargo/npm/tsc）：

```
PB_WS=backend-consolidation .claude/devbox/gate \
  /home/zbl/文档/claudecode-frontend/.claude/worktrees/k-r123 k-r123
```

落点 `…/f80a7aea-0ccf-4854-9b7a-12961a9c55aa/scratchpad/kr123-gate.log`（本会话私有目录，
不与别的 agent 共用同名文件）· 被测对象 = `4cf2ec2` 那棵树 · 量于 2026-09-15T04:2x–04:5x UTC。

```
GATE: FAIL —— daemon（退出码 101）
GATE_RC=1
```

逐格（分母 = 门禁自己印的那几行）：hooks 11 · copy2 11 · **shellcheck 57** · **ci-e2e-prereq 20** ·
fmt 1 · fmt-daemon 1 · winchk 1 · **winchk-daemon 1** · cargo **1616**（9 个包合计）·
generated 与 Rust 源一致 · deadcode 41 · **daemon 761 passed / 1 failed** · tsc 366 · npm 1726 ·
ccm e2e 四套 12/8/46/45 · pb check FAIL=0 BROKEN=0。

🔴 **那一条红，如实定性成「本件没复现、也没定性成真缺陷」**：

```
thread 'relay::server::tests::a_launch_command_carrying_the_relay_env_prefix_reaches_the_relay_with_that_accounts_key'
panicked at src/relay/server.rs:3879:13:
这一发没走完一条转发（账号 acct-b）："HTTP/1.1 502 Bad Gateway\r\n" / ""
test result: FAILED. 761 passed; 1 failed; 2 ignored; 0 measured; 0 filtered out
```

**单跑三趟，三趟全绿**（同一个沙箱镜像、同一棵树、`--network none`，量于 2026-09-15T04:5x UTC）：

```
cargo test --offline --bin cc-monitor-remote relay::server::tests::a_launch_command_carrying_the_relay_env_prefix_reaches_the_relay_with_that_accounts_key
第 1 趟 ok. 1 passed; 0 failed; 763 filtered out   rc=0
第 2 趟 ok. 1 passed; 0 failed; 763 filtered out   rc=0
第 3 趟 ok. 1 passed; 0 failed; 763 filtered out   rc=0
```

⇒ **三趟单跑全绿、全量并发那一趟红一次** ⇒ 形状像**并发下的不稳定**，
**而且云端同一个 commit 上 `Remote daemon (Linux) lint + test` 是 success**（本文 1.1 第 3 行）。
⚠ **但我没有证明它是 flake**：我没有把全量并发那一趟复跑到再红一次，也没查 502 的成因
（分母 = 我跑过的这四趟）。**如实记成「判不了它是 flake 还是真缺陷」，不写成「过了」。**
⚠ 另一条边界：`761 + 1 + 2 = 764`，与单跑那趟 `1 + 763 = 764` 对得上 ⇒ **人群没变，只是那一发的结论变了。**

---

## 六 · 写区外逐处 · 对外动作逐处

**对外动作，本件一共三次**（分母 = 我这一轮跑过的、会改动 `origin` 或发到别人手上的命令；
只读的 `gh run view` / `gh api …/logs` / `gh release list` / `git ls-remote` 不算）：

| # | 动作 | 在四样许可里吗 | 可逆吗 |
|---|---|---|---|
| 1 | `git push origin track/k-r123` | ✅ 第一样 | 可（删分支）—— **我没删**，`K-R114` / `K-R119` 两条 track 分支今天也都还在 `origin` 上 |
| 2 | `gh pr create --base main --head track/k-r123` | ✅ 第二样 | 可 |
| 3 | `gh pr close 88` | `R82` 逐字「**不绿就关**」 | 可（`gh pr reopen 88`） |

**没做的对外动作，逐处写清**：

- **没合 PR**（第三样，没用上）· **没推 tag**（第四样，没用上）。
- **没推 `main`** —— `origin/main` 现打仍是 `14e0f05`，与开工前逐字相同。
- **没删任何 tag / 分支**（`v3.8.0` 本来就不存在，`git ls-remote --tags origin v3.8.0` 现打为空）。
- **没改 GitHub Release 的正文**（没有 Release 可改；而且那是第五样对外动作，见 4.5）。
- **没在 PR 上留任何评论、没改标题、没 at 任何人**（`R82`）。

**写区外一个字节没碰**：

- **没碰任何生产代码** —— 包括上面那条 `release.yml 手工触发守卫`，**我一个字没改**
  （`§0c` 逐字「不改任何生产代码」；`ci.yml` 属 `.github/**`，逐字「只用不改」）。
- **没碰 `.github/**`** · **没碰 `CHANGELOG.md`** · **没碰那 11 处版本号**（只核不改）。
- **没碰 `K-R119-发版说明-v3.8.0.md`**（只读只核；4.3 逮到的四行馊数原样留着）。
- **计划仓只写了件文件那一份** `features/K-R123-重新打v3.8.0.md`，**没提交**（提交归 PM）。

**三处 `git status`（现打于 2026-09-15T04:5x UTC）**：见件文件 `§8`。

---

## 七 · 三处 `git status`（交回前最后一次现打，2026-09-15T05:0x UTC）

量具 = `git status --porcelain`，**在每一棵树里各跑一次**（`brief` 第 5 条：量之前先确认自己站在哪儿）。
⚠ 下面三份都是**提交本文件之前**那一刻的读数；提交动作本身见本节末。

**① 计划仓** `/home/zbl/文档/claudecode-frontend/.claude/planned-build`：

```
 M "backend-consolidation/features/K-R123-重新打v3.8.0.md"
?? backend-consolidation/.briefs/K-R102.md
…（`?? backend-consolidation/.briefs/*.md` 共 24 行，全是派工单，进场时就在，不是我落的）
```

分母现打：`M` **1 行**（= 本件的件文件，我写的）· `??` **24 行**，
且 `git status --porcelain | grep '^??' | grep -v '\.briefs/'` **零命中**
⇒ **24 个未跟踪文件全在 `.briefs/` 下**。
⚠ **进场时这棵树上还另有两行 `M`**（`backend-consolidation/INDEX.md` 与
`backend-consolidation/features/K-R122-把云端CI那五条红修掉.md`）—— **现在没有了**，
说明本轮期间 PM 在这棵树上提交过。**那两处不是我碰的，本节只如实记这个差**。
**本件在计划仓一次 `git commit` 都没跑**（铁律 3：提交归 PM 独占）。

**② 代码仓主树** `/home/zbl/文档/claudecode-frontend/cc-monitor`（分支 `main`）：

```
（空 —— `git status --porcelain | wc -l` = 0）
```

⇒ **我一个字节没碰它**（它同时是 `pb deploy` 的部署源，铁律 4 明令不许在它上面干活）。

**③ 我的工作树** `/home/zbl/文档/claudecode-frontend/.claude/worktrees/k-r123`（分支 `track/k-r123`）：

```
?? "evidence/K-R123-发版读数.md"
```

⇒ 只有本文件一份。提交时**只许点名文件**（铁律 20）：
`git add evidence/K-R123-发版读数.md` ＋ `git commit`（**零 emoji · 无 `Co-Authored-By` · 不 `amend`**）。
提交后的 sha 与 `git status` 由交回报告给出。
