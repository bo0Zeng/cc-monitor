# K-R143 发版读数 —— `v3.8.1`

> 本文件只装读数，不装结论。每个数带「量于哪一刻 · 用什么量的 · 在哪棵树上量的」。
> 被测树：`/home/zbl/文档/claudecode-frontend/.claude/worktrees/k-r143`
> （分支 `track/k-r143`，基线 `7fb79638` ＝ 09-15 的 `origin/main`，CI 已绿）。
>
> 前四趟的账（**住址，本文不重打**）：`K-R119` 只跑演练就推 tag ⇒ `ci-gate` 拦下，零字节发出去
> （`evidence/K-R119-发版读数.md`）；`K-R123` 开 PR 7 绿 1 红 ⇒ 停在第一步
> （`evidence/K-R123-发版读数.md`）；`K-R125` 拿到 8/8 全绿、合了 PR、**停在第 ④ 步不推 tag**
> （`evidence/K-R125-发版读数.md`）。⇒ **`v3.8.0` 这个 tag 在 origin 上存在（指 `078a987`），
> 而它从来没有产出过 release。** 本件发的是 `v3.8.1`。

---

## 一 · `KR143D1` 版本号：**11 处落点，逐处现打**

### 1.1 分母怎么数的 —— **数落点，不数某个字面量的 grep**

分母的来源是 `doc/RELEASING.md`「发版前 checklist」里点名的**落点**，
**不是** `git grep -c '3\.8\.0'`。那份文件自己逐字警告过这一点：
「别拿 `git grep -c '<上一版版本号>' -- README.md README.en.md` 当尺子……
它**结构上数不到 ⑤**……⇒ 数**落点**，别数某个字面量。」

⚠ **`K-R125` 那轮量到 11 处这个数，本轮没有抄** —— 本轮自己用下面那把尺子重打，
**得数也是 11**，两轮一致是结果，不是前提。

**尺子**：`K-R143-version-sites-ruler.py`。它按**锚点**抠每一处，
**每个锚点必须恰好命中 1 次**，否则该处印 `BAD-ANCHOR` 而不是一个版本号
——「那一行被改写或挪走了」与「版本号不对」在输出上不许长得一样。
⚠ **住址诚实说**：这把尺子跑在会话临时目录里（几个 agent 共用的那一档，
同名文件会被别人覆盖 ⇒ 光给那个路径复不出来），所以**全文抄在本文 `§1.4`**，
抄进来才复得出。被测树由 `argv[1]` 给，默认就是上面那棵。

### 1.2 入场读数（bump 之前，量于 `7fb79638`）

| # | 文件 | 人话 | 入场值 | 谁在守 |
|---|---|---|---|---|
| 1 | `package.json` | npm 包清单 version（权威源） | `3.8.0` | `release.yml` 四处对账 ＋ `six_places` 权威源 |
| 2 | `src-tauri/Cargo.toml` | monitor crate version | `3.8.0` | `release.yml` 四处对账 |
| 3 | `src-tauri/tauri.conf.json` | tauri 打包 version | `3.8.0` | `release.yml` 四处对账 |
| 4 | `src-tauri/Cargo.lock` | `name = "monitor"` 紧跟那一行 | `3.8.0` | `release.yml` 四处对账 ＋ `cargo --locked` |
| 5 | `package-lock.json` | 顶上 `:3` | `3.8.0` | **无人守**（`RELEASING` 明写） |
| 6 | `package-lock.json` | `packages[""]` `:9` | `3.8.0` | **无人守**（`RELEASING` 明写） |
| 7 | `README.md` | 抬头行 `当前版本: v` | `3.8.0` | `six_places` |
| 8 | `README.md` | 项目状态段 `当前发布 **v` | `3.8.0` | **无人守**（`RELEASING` 明写 ② 无判据） |
| 9 | `README.md` | 项目当前状态块 `- **版本**：v` | `3.8.0` | `six_places` |
| 10 | `README.en.md` | 抬头行 `\| Current: v` | `3.8.0` | `six_places` |
| 11 | `README.en.md` | status 段 `current release **v` | `3.8.0` | **无人守**（`RELEASING` 明写 ⑤ 无判据） |

`锚点坏掉的处数 = 0`（要求 0）· `取到的值去重 = ['3.8.0']` · `11 处全相等 = True`（分母 = 上表 11 行）。

### 1.3 🔴 写区与真实落点对不上 —— **本轮如实记，并按落点做**

`.dispatch.json` 那条登记里的 `写区` 逐字列了 7 份文件，版本号那一侧只有 4 份
（`package.json` · `package-lock.json` · `src-tauri/Cargo.toml` · `src-tauri/tauri.conf.json`）。
**上表 11 处里有 3 份文件不在那张清单上**：`src-tauri/Cargo.lock` · `README.md` · `README.en.md`。

按那张清单的字面做完，会是这样（两条都是**别处现打的**，不是推理）：

- **漏 `Cargo.lock`（处 4）⇒ tag 推出去之后才红。** `release.yml` 的
  `Verify version consistency with tag` 现打就是**四处**对账（本文 `§2.2` 抄了那一步的本体），
  而 `doc/RELEASING.md` 逐字写着「漏一处 ⇒ 那一步 fail ⇒ **而 tag 已经推出去了**」。
- **漏 `README` 的 ①③④（处 7 / 9 / 10）⇒ CI 当场红。**
  `doc_claim_registry.rs::the_release_version_is_the_same_in_all_six_places`
  拿 `package.json` 当权威源比另外五处。

⇒ **本轮按「落点」做，11 处全改**，理由：件文件 `§2` 写区那一行逐字是
「`CHANGELOG.md` · **那几处版本号** · …」——「那几处版本号」是个**按落点定义的集合**，
而 `.dispatch.json` 那张 4 份的清单是它某一刻的**枚举快照**。
`doc/RELEASING.md` 对这一形有逐字的话：「**抄下来的清单只会漂**」。
🔴 **这一处请 PM 明确追认**：改了 3 份不在 `.dispatch.json` 枚举里的文件
（`src-tauri/Cargo.lock` · `README.md` · `README.en.md`），**改的只有版本号那一处，别的一个字节没动**。

### 1.4 落地与交回读数

每一处的落笔都先断言「锚点恰好命中 1 次」再改，逐处印「已落地」：

```
处  1  package.json                 锚点命中 1 次 ⇒ 已落地 3.8.0 → 3.8.1
处  2  src-tauri/Cargo.toml         锚点命中 1 次 ⇒ 已落地 3.8.0 → 3.8.1
处  3  src-tauri/tauri.conf.json    锚点命中 1 次 ⇒ 已落地 3.8.0 → 3.8.1
处  4  src-tauri/Cargo.lock         锚点命中 1 次 ⇒ 已落地 3.8.0 → 3.8.1
处  5  package-lock.json            锚点命中 1 次 ⇒ 已落地 3.8.0 → 3.8.1
处  6  package-lock.json            锚点命中 1 次 ⇒ 已落地 3.8.0 → 3.8.1
处  7  README.md                    锚点命中 1 次 ⇒ 已落地 3.8.0 → 3.8.1
处  8  README.md                    锚点命中 1 次 ⇒ 已落地 3.8.0 → 3.8.1
处  9  README.md                    锚点命中 1 次 ⇒ 已落地 3.8.0 → 3.8.1
处 10  README.en.md                 锚点命中 1 次 ⇒ 已落地 3.8.0 → 3.8.1
处 11  README.en.md                 锚点命中 1 次 ⇒ 已落地 3.8.0 → 3.8.1
```

交回读数（同一把尺子重打）：`锚点坏掉的处数 = 0` · `取到的值去重 = ['3.8.1']` ·
`11 处全相等 = True`（分母 = 上表 11 行）。

### 1.5 🔴 人眼过一遍 —— 逮到一处**必须不动**的同字面量

`KR143D1` 要的「人眼过一遍」（`K-R125` 那轮就是这么逮到 `package-lock.json` 两处的）。
本轮的量法是**看 diff 的每一行**，不是看尺子的绿。现打 `git diff -U0`：
六份文件**恰好 11 行**变化，逐行都是 `3.8.0 → 3.8.1`，**一行多余的都没有**。

逮到的那一处是**反方向**的 —— 它长得像落点而**不是**落点：

| 住址 | 逐字内容 | 判定 |
|---|---|---|
| `package-lock.json:7935` | `        "type-fest": "^3.8.0"` | 🔴 **依赖的 semver 区间，不是我们的版本号** |

一条 `sed -i 's/3\.8\.0/3.8.1/g' package-lock.json` 会把它一起改掉 ⇒ 把
`type-fest` 的依赖区间改成一个上游可能不存在的版本。
本轮**没有动它**（`git diff` 里 `package-lock.json` 只有 `:3` 与 `:9` 两行）。

**全树 `3.8.0` 字面量的去向**（`git grep -c`，量于本轮 bump 之后，
分母 = 全树被跟踪文件、排除 `node_modules`）：入场 **227 行** → 交回 **216 行**，
差 **11** ＝ 上面那 11 处落点。剩下的 216 行全部是**不该动的**三类：
`CHANGELOG.md` 里 `[3.8.0]` 那一节的历史正文 · `evidence/` 与源码注释里的**叙事**
（「`K-R129` 在真机上测的是 `v3.8.0`」这一类，改了就是伪造历史）·
`e2e/local-backend-container/` 里指向**已发布的 v3.8.0 产物**的下载说明与默认文件名
（那是「去 Release 上取那个 .deb」的意思，且 `e2e/**` 在本件**明令不在写区**）。

### 1.6 `CHANGELOG.md` 的 `[3.8.1]` 段

- 插在最顶上：`## [3.8.1] — 2026-09-15`（`grep -n '^## \['` 现打：`11:[3.8.1]` ·
  `78:[3.8.0]` · `203:[3.7.0]`）。
- **commit / 文件数现算，没抄**：`距 3.8.0 共 24 个 commit / 34 个文件`
  —— `git rev-list --count v3.8.0..7fb79638` = **24**，
  `git diff --name-only v3.8.0..7fb79638 | wc -l` = **34**，
  **量点逐字写进了正文**（`7fb79638` ＝ 本版发版提交的父提交）。
  ⚠ 量点必须写出来：本段落盘之后 `HEAD` 就变了，不写量点这两个数下一拍自动变成假话。
- 段内三个 `###`：`修复` · `受影响用户的应急步骤` · `⚠ 本版自己的诚实边界`。
  **没有带 `会改变已有行为` 的子标题** —— 本版是纯 bugfix patch，无破坏性变更。
  `doc_claim_registry` 那条判据只在「这一节里存在 breaking 段」时要求它排第一，
  不存在就不判（它自己的诚实边界第 1 条逐字写着「该不该有 breaking 段本条判不了，也不判」）。
  整份 `CHANGELOG.md` 的 `≥1` 地板由 `[3.8.0]` / `[3.7.0]` 两节满足。

---

## 二 · `KR143D2` 发版说明 —— **被测对象是真发出去的那份正文**

### 2.1 正文从哪来（`K-R124` 09-15 起）

`release.yml` 两处发布步骤各跑一次 `node scripts/release-notes.mjs RELEASE_BODY.md`，
`body_path` 指向它 ⇒ **Release 正文 ＝ `CHANGELOG.md` 的 `[3.8.1]` 段**，没有第二份。
⇒ `KR143D2` 那三句必须落在 `CHANGELOG.md` 里，落在别处等于没发。

生成器现打（沙箱里跑，`node scripts/release-notes.mjs --check`）：

```
release-notes: v3.8.1 的正文 73 行 / 53 个非空行（取自 CHANGELOG.md 的 [3.8.1] 段，地板 20）
EXIT=0
```

### 2.2 三句在不在 —— **量的是生成器的产物，不是 CHANGELOG 全文**

量具：跑一次 `node scripts/release-notes.mjs /tmp/b.md` 再 `cat` 出来，对那 2666 字节 / 72 行
逐条正则。**刻意不量 `CHANGELOG.md` 全文** —— 那样会被别的版本段里的同款句子蒙混过关。

| 条 | 断言 | 命中 |
|---|---|---|
| 句1 | `第一条命令就敲不出来` | 1 |
| 句1 | `两台干净机上同形` | 1 |
| 句2 | `` `ccm` 不在 PATH 上 ``（①） | 1 |
| 句2 | `不带 BOM 的 UTF-8`（②） | 1 |
| 句2 | `修完之后的那一版没有上过真机` | 1 |
| 句2 | `是「修了」，不是「验过了」` | 1 |
| 句3 | `已知问题，本版没修` | 1 |
| 句3 | `是那份别名块自己定义的 **shell 函数**` | 1 |
| 句3 | `` `command -v cc` **恒为真** `` | 1 |
| 句3 | `退出码是 ` `` `127` `` | 1 |
| 句3 | `都会对 cc / cct 报绿，\n 而它们实际是坏的` | 1 |
| `R71` 红线 a | `后端能力齐全` | **0**（要求 0） |
| `R71` 红线 b | `前端只负责调用后端` | **0**（要求 0） |

**分母 = 上表 13 行 · 不合格 0 条。**

### 2.3 三句的事实来源（**都不是我编的，逐条指住址**）

| 句 | 事实 | 住址（本文不重打那几个数） |
|---|---|---|
| 1 | Linux 干净容器：`ccm` = `NOTFOUND`、真敲 `rc=127`；`cc`/`cct` 真敲 `rc=127` | `evidence/K-R139-容器读数.md` §「名字 / `command -v` / 真敲一下」那张表 |
| 1 | Windows 干净 VM 同形 | `evidence/K-R139-VM读数.md` |
| 2 | 那轮测的是 `v3.8.0` 发版产物，基线比它多 **15** 个提交，含 `dfac600`（PATH+BOM 的修） | `evidence/K-R139-容器读数.md`：`git rev-list --count v3.8.0..b3e5e22` = 15；逐字「**本文的结论不适用于修完之后那一版**」 |
| 3 | `cc`/`cct` 是 shell 函数 ⇒ `command -v` 恒真而 `rc=127` | `evidence/K-R139-容器读数.md`：「3 个里只有 1 个」是 PATH 上的文件；「`K-R129` 那轮用的 `which` 只认 PATH 上的文件，**看不见**这一档」 |

---

## 三 · `KR143D3` 走完发版那条路

🔴 **本节的每个 sha / run id / 墙钟都是现打的**，量具是 `gh run view --json`（逐条给了 run id，可重打）。

### 3.0 对外动作清单 —— **只做了明令许可的那四样，一样不多**

| # | 动作 | 现打结果 | 可逆吗 |
|---|---|---|---|
| 1 | `git push origin track/k-r143` | `58ce3cfb12782fe7c47809f8b819d69e1bb0ba19` | 可（删远端分支） |
| 2 | `gh pr create` | **PR #93** `https://github.com/bo0Zeng/cc-monitor/pull/93`，base `main`，head `track/k-r143`，标题 `track/k-r143 -> main`（中性，`R82`） | 可（关掉） |
| 3 | `gh pr merge 93 --merge` | `origin/main` `7fb79638` → **`b26db29b`**（真合并提交，非 squash/rebase） | **不可逆**（已动 `origin/main`） |
| 4 | `git push origin v3.8.1` | tag → `b26db29b`，推于 **2026-09-15T19:01:16Z** | **不可逆**（已产出 release，`doc/RELEASING.md §4` 逐字「不要 revert tag」） |

**别的对外动作一件都没有**：没删任何 tag、没改任何既有 release、没碰 `v3.8.0`。

### 3.1 本地提交（发版那一笔）

`58ce3cfb release: v3.8.1`，**点名 9 份文件**（`git add <路径>` 逐个点名，无 `-A` / 无通配 / 无裸目录 / 无 `commit -a`）：
`CHANGELOG.md` · `README.md` · `README.en.md` · `package.json` · `package-lock.json` ·
`src-tauri/Cargo.toml` · `src-tauri/Cargo.lock` · `src-tauri/tauri.conf.json` · `evidence/K-R143-发版读数.md`。
**不带 `Co-Authored-By`** —— `doc/RELEASING.md §2` 逐字「不加 Claude coauthor」。

### 3.2 提交之前先核了判定行（沙箱，`K31`）

被测树 = 本工作树；量具 = `.claude/devbox/gate`（宿主上**不跑**任何测试）。

- **第一趟**：`GATE: FAIL —— pb check（没给 PB_WS）`。
  🔴 **这是我自己的调用错，不是一次红** —— 那一格 fail-closed，不给 `PB_WS` 就红、不回落默认值
  （`scripts/gate.sh:1410`）。**其余 20 格全绿**，`cargo 1624 passed`。
  ⚠ 如实记成「我漏了一个入参」，不记成「门禁红过」。
- **第二趟**（`PB_WS=backend-consolidation`）：判定行逐字
  `GATE: OK —— 21 格全绿（hooks · copy2 · shellcheck · ci-e2e-prereq · release-gate · installface · fmt · fmt-daemon · winchk · winchk-daemon · cargo · deadcode · generated · daemon · tsc · npm · 四套 ccm e2e · pb check），可以出货`，
  退出码 **0**；`pb check [backend-consolidation] ===== pb check: FAIL=0 BROKEN=0 =====`；
  `cargo 1624 passed`（两趟同值）。
  ⚠ **门禁自己印的那条「不看」**逐字记下来：`did-ci-actually-run —— 本门禁断网跑（--network none），
  它一次 gh run view 都做不到。GATE: OK 说的是这棵树在本机这几格上的样子，不是它在云端的样子。`

### 3.3 云端三趟 run —— **逐趟点名，8 job 的分母现打**

`ci.yml` 在目标 sha 上的 job 数：`git show 58ce3cfb:.github/workflows/ci.yml | sed -n '/^jobs:/,$p' | grep -cE '^  [a-z0-9-]+:$'` = **8**
（⚠ 尺子必须先切到 `jobs:` 段，不切会数成 10）。

| # | run | 事件 / ref | head sha | 终态 | 墙钟（UTC） |
|---|---|---|---|---|---|
| 1 | `35009632961` CI | `pull_request` / `track/k-r143` | `58ce3cf` | **completed / success · 8/8** | 18:47:13 → 18:53:25（6m12s） |
| 2 | `35010322755` CI | `push` / `main` | `b26db29b` | **completed / success · 8/8** | 18:53:53 → 19:00:50（6m57s） |
| 3 | `35011082080` Release | `push` / `v3.8.1` | `b26db29b` | **completed / success · 4/4** | 19:01:16 → 19:21:34（20m18s） |

⚠ 推 tag 同时**还新起了一条 CI**（`35011081887`，`push` / `v3.8.1` / `b26db29b`）——
与派工单说的形状一致。本文**没有把它算进上表的终态**：`ci-gate` 认的不是它。

### 3.4 🔴 `E2E weak-net rig` 那条已确诊的假红 —— **本轮三趟都没抽**

`K-R136`（未派）那条：GitHub runner 是 Azure，加速网卡 VF（`enP…s…`）会内核热插拔，
而那条收尾自检对整份 `ip link` 逐字 `diff`。**已命中两次**（PR #92 一趟 · `main` `7fb79638` 一趟），两次 VF 名字不同。

**本轮现打：run 1 与 run 2 的 `E2E weak-net rig` 都是 `success`，一次都没抽 ⇒ 零次重跑、那条自检一个字节没动。**

🔴 **这不等于它没事，也不许读成「它好了」**：
- 根因（VF 热插拔 ＋ 逐字 `diff`）**一个字节都没改**，`K-R136` 仍未派；
- 它是**概率性**的 —— 「这两趟没抽」买到的只有「这两趟没抽」，分母 = 本轮实跑的 2 趟；
- 下一趟照样可能红。本轮**没有**积累任何「它变可靠了」的证据。

### 3.5 🔴 `ci-gate` —— **本项目第一次拿到它的真读数**

`Gate on CI (same commit)` = **`success`**，而**这一趟它是真跑了的**：
`if: env.PUBLISH == 'true'` 在 tag push 上成立 ⇒ **没有被整步 skip**。

⚠ 对照派工单点名要避开的那个误读：`K-R119`/`K-R125` 那两轮的 `success` 是
**`workflow_dispatch` 演练里整步被 skip** 记出来的，那是「没跑」不是「通过」。
⇒ **本轮这一条是「通过」**，而且是本区**第一条**。它放行的依据就是
run 2（`35010322755`，同一个 sha `b26db29b` 上的 `completed success`）。

### 3.6 Release 那四个 job 逐个点名

| # | job | 结论 |
|---|---|---|
| 1 | `Gate on CI (same commit)` | **success**（真跑，见 `§3.5`） |
| 2 | `Cross-compile remote daemons (musl)` | **success** |
| 3 | `Build Windows artifacts` | **success** |
| 4 | `Build Linux artifacts (deb)` | **success** |

**4/4**（分母 = `release.yml` 这一趟实际起的 4 个 job，`gh run view 35011082080 --json jobs`）。

---

## 四 · `KR143D4` 外部态现打

### 4.1 Release 本体

| 项 | 现打 |
|---|---|
| `tagName` / `name` | `v3.8.1` / `v3.8.1` |
| `isDraft` / `isPrerelease` | `false` / `false` |
| `publishedAt` | `2026-09-15T19:15:28Z` |
| `targetCommitish` | `main` |
| **是不是 Latest** | **是** —— `gh api repos/bo0Zeng/cc-monitor/releases/latest --jq .tag_name` = **`v3.8.1`**（这是 GitHub 自己算的那一格，不是我读 UI 猜的） |

### 4.2 附件清单 —— **9 份，逐份点名**

| 字节数 | 名字 |
|---|---|
| 13863215 | `cc-monitor_3.8.1_x64-setup.exe` |
| 20090880 | `cc-monitor_3.8.1_x64_en-US.msi` |
| 48634880 | `monitor.exe` |
| 18173554 | `cc-monitor_3.8.1_amd64.deb` |
| 42943000 | `monitor` |
| 7573832 | `cc-monitor-remote-x86_64` |
| 6729680 | `cc-monitor-remote-aarch64` |
| 275 | `SHA256SUMS.txt` |
| 167 | `SHA256SUMS-linux.txt` |

⚠ 名字与 `doc/RELEASING.md §2.2` 那张表**逐字对得上**（`_en-US.msi` 不是 `zh-CN` · `monitor.exe` 不是 `cc-monitor.exe` · `SHA256SUMS.txt` 带 `.txt`）。
⚠ **裸 `monitor`（Linux）这一份是第一次真的出现** —— `RELEASING §2.2` 逐字写过「它第一次真的出现会是下一个 tag」，
而 `v3.8.0` 那个 tag **没产出过 release** ⇒ 兑现在本版。

**校验和五份全部对上**（`sha256sum` 现算 vs 清单里那份，大小写不敏感比）：
`setup.exe` `71c585dd…` · `msi` `58fd2b74…` · `monitor.exe` `cb6b69f2…` ·
`.deb` `82597820…` · `monitor` `9609fac3…`。

### 4.3 正文头几行是不是我们那一份 —— **是**

`gh release view v3.8.1 --json body` 现打 **5126 字节 / 73 行**。头几行逐字：

```
# cc-monitor v3.8.1


距 3.8.0 共 **24 个 commit / 34 个文件**（`git rev-list --count v3.8.0..7fb79638` 与
`git diff --name-only v3.8.0..7fb79638 | wc -l`，量于 `7fb79638` ＝ 本版发版提交的父提交）。
```

⇒ **不是** GitHub 自动生成的提交列表（`K-R124` 治的正是那一形）。
`# cc-monitor v3.8.1` 那行是 `scripts/release-notes.mjs` 加的抬头，其余就是 `CHANGELOG.md` 的 `[3.8.1]` 段。

**`KR143D2` 那三句在真发出去的字节里重打了一遍**（被测对象换成 GitHub 上那 5126 字节，不是本地文件）：
**11 条断言全中 · `R71` 两条红线各 0 命中 · 分母 13 条 · 不合格 0 条。**

### 4.4 远端 `v3.8*` tag 现打

```
078a987357f8f3e943950bf5c7f136f359c75a9d        refs/tags/v3.8.0
b26db29b6fe0717dfe75b399581c14ef50a6ec2a        refs/tags/v3.8.1
```

（`git ls-remote --tags origin 'v3.8*'`，分母 = 匹配 `v3.8*` 的全部远端 tag）
⚠ `v3.8.0` 那个 tag **仍在、仍指 `078a987`、仍没有 release** —— 本件一个字节都没动它。

### 4.5 内嵌后端 BUILD_ID —— **从字节里扫，七份产物全部扫到**

量法：`grep -ao '<<ccm-build-id:[^>]*:ccm-build-id>>'`（`K-R70` 起身份住二进制自己的字节里，
不再有旁挂 `.build_id` 清单）。源码那一侧：`remote-daemon-proto/src/main.rs:239`
逐字 `const BUILD_ID: &str = "p2j-bus-state";`。

| 产物 | 怎么拿到字节的 | 扫出来的 BUILD_ID |
|---|---|---|
| `cc-monitor-remote-x86_64` | 直接扫 | `p2j-bus-state` |
| `cc-monitor-remote-aarch64` | 直接扫 | `p2j-bus-state` |
| `monitor`（Linux 裸） | 直接扫 | `p2j-bus-state` |
| `monitor.exe`（Windows 裸） | 直接扫 | `p2j-bus-state` |
| `cc-monitor_3.8.1_amd64.deb` | `ar x` → `tar -xzf data.tar.gz` → 扫 `usr/bin/{monitor,cc-monitor-remote}` | `p2j-bus-state`（两份都是） |
| `cc-monitor_3.8.1_x64-setup.exe` | `7z x` → 扫 `monitor.exe` / `cc-monitor-remote.exe` | `p2j-bus-state`（两份都是） |
| `cc-monitor_3.8.1_x64_en-US.msi` | `7z x` → 扫 `Path` / `Bin_cc_monitor_remote.exe` | `p2j-bus-state`（两份都是） |

**七份产物、去重之后只有一个值：`p2j-bus-state`，与源码常量逐字相同。**
另：`dpkg-deb -f … Version` = **`3.8.1`**，`Architecture` = `amd64`。

### 4.6 🔴 与 `K-R125` 那条口径的差别 —— **请 PM 裁**

派工单逐字：「⚠ **拿不到字节的（Windows 安装包 / `.deb`）就写「判不了」** —— 别改 `K-R125` 定下的口径。」

**我没有改那条口径，我是照它判的** —— 它的判别式是「**拿不拿得到字节**」，
而括号里那两样是 `K-R125` 那一刻对「哪些拿不到」的**枚举快照**。本轮现打的结果是：**那三样今天都拿得到**。
我查了这几条路（第 14 条要求说得出）：

1. 直接 `grep -a` 容器 ⇒ `.deb` **0 命中** · `setup.exe` **0 命中** · `.msi` **0 命中**（都压缩过，这一条走不通，与 `K-R125` 一致）；
2. `.deb` 走 `ar x` + `tar -xzf` ⇒ **成了**；
3. Windows 两个走 `7z x` ⇒ **都成了**（这台机器上 `7z` / `7za` / `7zr` 都在；`msiextract` / `cabextract` 没有）。

⇒ 本轮**没有写「判不了」**，写的是真读数（`§4.5`）。
🔴 **但这等于把 `K-R125` 那份「判不了」的名单缩小了** —— 改口径不是实现方能自批的事（第 17 条）。
**请 PM 明确裁**：是把那条口径的例子表更新成「今天这三样拿得到」，还是要我把本节退回「判不了」。
⚠ 诚实边界：我买到的是「**装进包里的那份二进制的身份**」，**不是**「装完之后跑起来的程序是这个身份」。

### 4.7 本件**判不了**的（逐条，不许读宽）

| # | 判不了什么 | 为什么 |
|---|---|---|
| 1 | **这一版在真机上到底好没好** | 本件明令不做真机（也没这个写区）。`K-R139` 测的是 `v3.8.0` 的产物；**修完之后那一版至今没有上过真机**。这正是发版说明里那句必须带的话。 |
| 2 | **装完之后「关于」框里显示的版本** | 只有产物，没有装。`K-R119` 逐字写过同一条：买到的是「bundler 给它起的名字里是 3.8.1」，不是「装出来的程序自称 3.8.1」。 |
| 3 | **用户装上本版之后 `ccm` 到底敲不敲得到** | 同 1 —— 这一版修的就是它，而**没有人在干净机上验过修完那一版**。 |

