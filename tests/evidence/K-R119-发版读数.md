# K-R119 发版读数

> 本件唯一的对外动作是**推 tag**（用户 09-14 逐字「发3.8」＝ `DECISIONS.md#R77`、「不验，直接推 tag」＝ `DECISIONS.md#R80`）。
> 本文件只装读数，不装结论。每个数带「量于哪一刻 · 用什么量的 · 在哪棵树上量的」。
> 被测树：`/home/zbl/文档/claudecode-frontend/.claude/worktrees/k-r119`（分支 `track/k-r119`，HEAD `6bba991`，与主干 `main` 同尖）。

## 一 · 推 tag 之前的现打（`R79` 加的那一条：不是「判据绿了」，是真的 grep 一遍）

量于 2026-09-15 01:2x UTC · 量具 = `grep -n` · 被测树 = 上面那棵（`git rev-parse HEAD` = `6bba99101e6966dfc2a9b23a92ecc26fedc70cfe`）。

### 1.1 七处版本号（分母 = `doc_claim_registry::the_release_version_is_the_same_in_all_six_places` 的人群 6 处 ＋ `Cargo.lock` 的 monitor 包 1 处 = 7）

| # | 住址 | 现打那一行（逐字） |
|---|---|---|
| 1 | `package.json:4` | `  "version": "3.8.0",` |
| 2 | `src-tauri/Cargo.toml:27` | `version = "3.8.0"` |
| 3 | `src-tauri/tauri.conf.json:4` | `  "version": "3.8.0",` |
| 4 | `src-tauri/Cargo.lock:2828`（`name = "monitor"` 紧随其后那一行） | `version = "3.8.0"` |
| 5 | `README.md:11` 抬头行 | `… | 当前版本: v3.8.0` |
| 6 | `README.md:328` 「项目当前状态」块 | `- **版本**：v3.8.0（Released）` |
| 7 | `README.en.md:5` 抬头行 | `… | Current: v3.8.0` |

### 1.2 `R79` 点名的两处「这份文档此刻自称的版本」（判据人群此前不含它们，逮到它的是人眼）

| 住址 | 现打 |
|---|---|
| `README.md:15` | `当前发布 **v3.8.0**`（`grep -n '当前发布 \*\*v'` 命中 1 次） |
| `README.en.md:9` | `current release **v3.8.0**`（`grep -n 'current release \*\*v'` 命中 1 次） |

### 1.3 CHANGELOG

量具 = 一段 python（读 `CHANGELOG.md`，按 `## [` 切节、按 `### ` 数子标题），量于同一棵树：

- 最上一节标题逐字：`## [3.8.0] — 2026-09-14`
- 该节 `### ` 子标题 6 个，顺序：`六条会改变已有行为的` → `新增 — 用户看得见的` → `修复` → `改进 — 本机与远端收成同一条路` → `项目管理` → `升级须知`
  ⇒ **breaking 段是第一个 `###`**（`R77` 要的那个形状）。
- breaking 段顶层条目 **6 条**，逐条首行逐字：
  1. `- **\`daemonless\`（「不装后端也能看」那个模式）删了。**`
  2. `- **\`cch\` 这个别名删了；\`ccm\` 默认也不再替你猜目录。**`
  3. `- **\`ccm\` 变成后端的原生命令，\`shared/ccm\` 那套 1592 行 bash 删掉了。**`
  4. `- **tmux 会话名从 \`<sid8>-cc\` 改成 \`<项目名>-cc\`。**`
  5. `- **后端二进制自己带身份 ⇒ 每台已经装过后端的远端，连上之后会被判旧并自动重装一次。**`
  6. `- **桌面侧那两条 SSH 回落（送键 · 杀会话）删净了。**`

### 1.4 这一版距上一版多少（发版说明里要用的那个数，自己重打的）

- `git rev-list --count v3.7.0..6bba991` = **257**
- `git diff --name-only v3.7.0..6bba991 | wc -l` = **397**
- `git rev-parse v3.7.0^{commit}` = `640917e6dd1c10c52a5fbb7e9c6d95a68b51cecd`

⚠ `CHANGELOG.md` 最上一节里写的是「**253 个 commit / 392 个文件**（量于 `0a92892`）」——
两个数不一样是因为**量点不同**（那份量于 `0a92892`，本行量于 `6bba991`，中间还有几个提交）。
`CHANGELOG` 那一份是墓碑，本件**只核不改**（`K-R120` 的活）。

## 二 · 门禁 16 格（自己在沙箱里重打的，不是抄上一件的）

命令（从项目根起跑，`K31`：宿主不跑 cargo/npm/tsc）：

```
PB_WS=backend-consolidation .claude/devbox/gate \
  /home/zbl/文档/claudecode-frontend/.claude/worktrees/k-r119 k-r119
```

落点 `…/scratchpad/kr119-gate.log`（本会话私有目录，不与别的 agent 共用同名文件）·
被测对象 = 上面那棵树（`6bba991`）· 量于 2026-09-15 01:33–01:5x UTC。

```
GATE: OK —— 16 格全绿（hooks · copy2 · fmt · fmt-daemon · winchk · cargo · deadcode ·
generated · daemon · tsc · npm · 四套 ccm e2e · pb check），可以出货
GATE_RC=0
```

逐格：hooks 11 · copy2 11 · fmt 1 · fmt-daemon 1 · winchk 1 · cargo **1615**（9 个包合计）·
generated 与 Rust 源一致 · deadcode 41 · daemon 762 · tsc **366** · npm 1726 ·
ccm e2e 四套 12/8/46/45 · pb check FAIL=0 BROKEN=0。

门禁自己印的那条分母边界，照抄不改写：
> 分母 cargo　本树未铺 `src-tauri/embedded-daemons/` ⇒ `embedded_daemons` cfg 不置 ⇒
> 上面那个合计里少了「本地后端真的能起来吗」那一族（4 条）。

⇒ **「16 格全绿」买到的边界就摆在这儿**：那 4 条在本地这一趟没跑；云端 `build-windows`
那一步是真铺了 `embedded-daemons/` 才编的（见下一节），但它编的不是 `cargo test`。

## 三 · 第一趟：演练（`workflow_dispatch` ＋ `publish=false`）

命令（在 `track/k-r119` 上，与 `K-R114` 同一条路）：

```
git push origin track/k-r119          # 演练在构造上要求这个 ref 在远端；K-R114 推的是 track/k-r114
gh workflow run release.yml --ref track/k-r119 -f publish=false
```

**run `34917750361`** · `https://github.com/bo0Zeng/cc-monitor/actions/runs/34917750361` ·
event `workflow_dispatch` · branch `track/k-r119` · head `6bba991` · 起于 2026-09-15T01:33:39Z。

### 3.1 逐 job 结论

| job | 结论 | 墙钟 |
|---|---|---|
| `Gate on CI (same commit)` | **success** | 01:33:43 → 01:33:45（2s） |
| `Cross-compile remote daemons (musl)` | **success** | 01:33:47 → 01:37:45（3m58s） |
| `Build Windows artifacts` | **success** | 01:37:49 → 01:53:07（15m18s） |
| `Build Linux artifacts (deb)` | （见下节，起于 01:53:10） | |

`ci-gate` 那 2 秒是**它自己的设计**，不是「查过了 CI 是绿的」：那一步逐字
`if: env.PUBLISH == 'true'` ⇒ 演练时整步 skipped，job 因此 success。
**别把这一格读成「CI 绿过」** —— 演练这一趟没有任何测试门。

### 3.2 `K-R114` 那趟没走到 `build-linux`，这一趟走到了（自己重打的对照）

`gh run view 34861383050 --json jobs` 现打（量于 2026-09-15）：

```
run=34861383050 workflow_dispatch track/k-r114 d1a0552 completed/failure
  Gate on CI (same commit)            completed/success
  Cross-compile remote daemons (musl) completed/success
  Build Windows artifacts             completed/failure
  Build Linux artifacts (deb)         completed/skipped     ← 没走到
```

本趟 `34917750361`：`Build Windows artifacts` **success** ⇒ `build-linux` 的 `needs` 满足，
**01:53:10 真的起来了**（不是 skipped）。⇒ `K-R118` 修的那条 `tsc` 是承重的：
`tauri build` 的第一段就是 `npm run build`。

### 3.3 演练现打出来的几行（从 job 日志里逐字抄，不转述）

`Place + verify embedded daemons`（从字节里扫，不读配置）：

```
embedded x86_64 (7573832 bytes)
embedded aarch64 (6729680 bytes)
expecting stamp: <<ccm-build-id:p2j-bus-state:ccm-build-id>>
stamp x86_64 = p2j-bus-state (从字节里扫出来的，不是从旁边抄的)
stamp aarch64 = p2j-bus-state (从字节里扫出来的，不是从旁边抄的)
```

`Verify version consistency with tag`：

```
Version self-consistent: 3.8.0 (package.json / tauri.conf / Cargo.toml / Cargo.lock all agree)
演练（publish=false）：跳过「与 tag 一致」那一半；ref_name=track/k-r119 不是版本号
Backend identity OK: remote-daemon-proto version=0.0.0 (deliberate), BUILD_ID=p2j-bus-state
```

`tauri build`（**这是「安装包自己说的版本」，`R79` 要的那一半，量在推 tag 之前**）：

```
Finished `release` profile [optimized] target(s) in 9m 11s
Built application at: D:\a\cc-monitor\cc-monitor\src-tauri\target\release\monitor.exe
Running makensis to produce …\bundle\nsis\cc-monitor_3.8.0_x64-setup.exe
Running light to produce …\bundle\msi\cc-monitor_3.8.0_x64_en-US.msi
Finished 2 bundles at:
    …\bundle\nsis\cc-monitor_3.8.0_x64-setup.exe
    …\bundle\msi\cc-monitor_3.8.0_x64_en-US.msi
```

### 3.4 演练那份 `embedded-daemons` artifact，自己下下来再扫一遍字节

`gh run download 34917750361 -n embedded-daemons`，落 `…/scratchpad/kr119-dryrun-art/`：

| 文件 | 字节数 | sha256 | 字节里扫到的 stamp | 命中次数 |
|---|---|---|---|---|
| `cc-monitor-remote-x86_64` | 7573832 | `4c96f6f936baeeaaf7f30524b443d781db3bc4e929ca3d25527c271d5b08cd78` | `p2j-bus-state` | 1 |
| `cc-monitor-remote-aarch64` | 6729680 | `b189f165ec63b164f1a489e308c442e1046c090c147bc3e39963cc7421469f9b` | `p2j-bus-state` | 1 |

量具 = 一段 python（正则 `<<ccm-build-id:([!-~]{1,64}?):ccm-build-id>>` 扫**整份字节**，
不读任何配置文件、不跑那个二进制），并另断言主干那个完整戳
`<<ccm-build-id:p2j-bus-state:ccm-build-id>>` 逐字**在**字节里 ⇒ 两份都 True。
主干源逐字：`remote-daemon-proto/src/main.rs:239` `const BUILD_ID: &str = "p2j-bus-state";`

### 3.5 演练那趟 `build-linux` 的现打（`K-R114` 没走到的那一段）

逐 job 终态（`gh run view 34917750361`，量于 2026-09-15T02:0x UTC）：

```
34917750361 workflow_dispatch track/k-r119 6bba991 completed/success
  Gate on CI (same commit)            success  01:33:43 -> 01:33:45
  Cross-compile remote daemons (musl) success  01:33:47 -> 01:37:45
  Build Windows artifacts             success  01:37:49 -> 01:53:07
  Build Linux artifacts (deb)         success  01:53:10 -> 02:01:00   ← 真的跑到了
```

`build-linux` 那一趟自己印的（逐字）：

```
embedded x86_64 placed
embedded aarch64 placed
Built application at: /home/runner/work/cc-monitor/cc-monitor/src-tauri/target/release/monitor
Bundling cc-monitor_3.8.0_amd64.deb (…/src-tauri/target/release/bundle/deb/cc-monitor_3.8.0_amd64.deb)
f24eccd9e4e998d7318860f547dec3e05a181d58d27e4506dc75cb1d610f6df6  cc-monitor_3.8.0_amd64.deb
a65ad515f85e2c01ca98aa6013f953be4b38918f6894a5bcaabea163db2ea3f0  monitor
```

⚠ 演练里**两处 `action-gh-release` 都 skipped**（`if: env.PUBLISH == 'true'`）⇒ 这一趟
**一个字节都没发到 Release 上**；上面那些产物只活在 runner 上，本地拿不到（能下载的只有
`embedded-daemons` 那份内部 artifact，见 3.4）。

## 四 · 推 tag 之前最后一次现打（`R79` 第四节要的那一遍，量于 2026-09-15T02:02:40Z）

被测树 `6bba991`。**不是「判据绿了」，是逐处 grep：**

```
package.json                 = 3.8.0
src-tauri/tauri.conf.json    = 3.8.0
src-tauri/Cargo.toml         = 3.8.0
src-tauri/Cargo.lock[monitor]= 3.8.0
README.md 抬头               = 3.8.0
README.md 项目当前状态        = 3.8.0
README.en.md 抬头            = 3.8.0
全部等于 3.8.0 ： True （分母 = 上面 7 处）

README.md      →  当前发布 **v3.8.0**
README.en.md   →  current release **v3.8.0**
```

**安装包自己说的版本**（这一格量的是演练那一趟 bundler 的真实输出，**在推 tag 之前**）：

```
cc-monitor_3.8.0_x64-setup.exe        (NSIS)
cc-monitor_3.8.0_x64_en-US.msi        (MSI)
cc-monitor_3.8.0_amd64.deb            (deb)
```

🔴 **一处现打逮到的、判据盖不到的旧号**：`package-lock.json:3` 与 `:9` 仍是 `"version": "3.7.0"`。
它**不在那七处里**，也不在 `R79` 收的那两处里；`doc/RELEASING.md § 1` 自己记着这一条
（逐字「**没有任何东西卡它**」）。演练那趟 `npm ci` 与 `npm install` 两步都 **success**
⇒ 它**不会让构建红**，但它是**这棵树自称的版本**里仍然写着 3.7.0 的一处。
**本件只核不改**（版本号那一族是 `K-R120` 的写区）。

## 五 · 第二趟：推 tag（`v3.8.0`）—— **被它自己的第一道门拦下了，版没发出去**

```
git tag v3.8.0 6bba99101e6966dfc2a9b23a92ecc26fedc70cfe    # 轻量 tag，doc/RELEASING.md §2 的口径
git push origin v3.8.0                                      # 2026-09-15T02:03:12Z
```

一次 tag push 起了**两条**流水线（`ci.yml` 的触发器含 `tags: ['v*']`）：

| run | workflow | event | ref | head | 结论 |
|---|---|---|---|---|---|
| **`34919684488`** | Release | push | `v3.8.0` | `6bba991` | **failure** |
| **`34919687825`** | CI | push | `v3.8.0` | `6bba991` | **failure** |

### 5.1 Release run `34919684488` 逐 job

| job | 结论 | 墙钟 |
|---|---|---|
| `Gate on CI (same commit)` | **failure** | 02:03:21 → 02:08:49（5m28s） |
| `Cross-compile remote daemons (musl)` | **skipped** | — |
| `Build Windows artifacts` | **skipped** | — |
| `Build Linux artifacts (deb)` | **skipped** | — |

`ci-gate` 自己印的最后三行（逐字）：

```
completed failure https://github.com/bo0Zeng/cc-monitor/actions/runs/34919687825
##[error]commit 6bba99101e6966dfc2a9b23a92ecc26fedc70cfe 上的 CI 全部跑完、没有一条是绿的 —— 发版拒绝。
上面每行是 status / conclusion / run 链接。先把 CI 修绿再重跑这一个 job。
```

⇒ **这不是流水线坏了，是它按设计拦住了一棵红树**（`release.yml` 那段头注逐字：
「**CI 红 ⇒ 发不出去，这是设计意图，不是故障**」，`doc/RELEASING.md § 2.1`）。

### 5.2 发出去了什么：**零**

量于 2026-09-15T02:1x UTC：

```
$ gh release view v3.8.0
release not found

$ gh release list --limit 4
v3.7.0   Latest   v3.7.0   2026-09-10T13:30:36Z      ← Latest 仍是它
v3.6.0            v3.6.0   2026-08-01T02:26:55Z
...

$ git ls-remote --tags origin v3.8.0
6bba99101e6966dfc2a9b23a92ecc26fedc70cfe        refs/tags/v3.8.0   ← tag 在，release 不在
```

**盘面现状**：`origin` 上有一个 `v3.8.0` 的 tag，指着 `6bba991`；
**没有 Release 对象、没有任何资产、没有一个字节到过用户手上**。

## 六 · CI 那五条红，逐条定性（量于 run `34919687825`，`6bba991`）

**先给分母**：`ci.yml` 现打 8 个 job（尺子 `sed -n '/^jobs:/,$p' .github/workflows/ci.yml | grep -cE '^  [a-z0-9-]+:$'`）。
**3 绿 5 红**。

| job | 结论 | 红在哪一步 | 现打的定性 |
|---|---|---|---|
| `Rust lint + test` | success | — | — |
| `Linux app build (no bundle)` | success | — | — |
| `E2E weak-net rig` | success | — | — |
| `Remote daemon (Linux) lint + test` | **failure** | 步 7 `cargo check --all-targets --target x86_64-pc-windows-msvc` | **10 个编译错**，全在 **test 那一档**（`… (bin "cc-monitor-remote" test) due to 10 previous errors`）：`src/sidecars/codepicture/acquire.rs` 8 处（`os::unix` / `Permissions::from_mode` / `.mode()`）＋ `src/observe/search_query.rs:503` 2 处（`libc::utimensat` · `libc::AT_FDCWD`）。**生产段不在其中** —— 同一份源码在 `build-windows` 里原生编出过 `cc-monitor-remote.exe`（7921664 字节，演练那趟） |
| `E2E scripts health (shellcheck + py compile)` | **failure** | 步 3 `shellcheck --severity=error` | **SC1081 ×6**，同一份文件 `e2e/usage-probe-acceptance.sh`（84 / 135 / 142 / 148 / 153 / 161 行）：那里定义并调用了一个叫 `FOR` 的函数，shellcheck 按「大小写写错的 `for`」判 error |
| `E2E real-machine (tmux, no Rust)` | **failure** | 步 7 `ccm CLI 契约（F02）` | 套件自己印的逐字：`找不到原生入口 …/remote-daemon-proto/target/debug/cc-monitor-remote —— 先 cd remote-daemon-proto && cargo build`。⇒ `ccm` 收成后端原生命令（`K-R48`）之后，这个「**no Rust**」的 job **在构造上跑不了这套 e2e 了**；`ccm-cli` 前 26 格全 PASS，卡在 `assert-pass-floor ccm-cli 46` |
| `E2E real-machine (tmux + Rust builder emit)` | **failure** | 步 8 `用量探针（F10）` | 同一族：`需要先 build daemon：cd remote-daemon-proto && cargo build` |
| `Frontend typecheck + build` | **failure** | 步 9 `unit tests` | vitest **`1 failed | 1725 passed (1726)`**，唯一那条是 `src/ipc/commands.vitest.ts:543` 的「铸名只有一个算法口」。**它红在路径分隔符上**：期望 `["src/tabs.ts"]`，windows runner 上收到 `["src\\ipc\\local-tmux-name.ts", "src\\remote-launch.ts", "src\\tabs.ts"]` —— 判据拿 `src/ipc/local-tmux-name.ts` 这个**正斜杠字面量**去剔算法口自己，反斜杠那一份剔不掉 |

### 6.1 这五条不是「一直就红着」—— 是这 257 个提交里长出来的

`ci.yml` 在 `14e0f05`（2026-09-10，主干最后一次进 CI）上是 **8/8 全绿**
（`gh run view 34507187447`，逐 job 现打，量于 2026-09-15）。
中间**再没有东西进过 main**（`origin/main` 至今仍是 `14e0f05`，落后本地 236 个提交）
⇒ **这 257 个提交没有任何一次上过 `ci.yml`**，红是在这段窗口里攒出来的。

### 6.2 为什么本地门禁 16 格全绿而它红 —— 每一条都落在门禁自己印出来的射程之外

| CI 红的那条 | 本地门禁对应的那一格 | 门禁自己印的射程（逐字） |
|---|---|---|
| daemon 跨 target Windows check | `winchk` | 「射程 = `-p monitor` 一个包…**`remote-daemon-proto` 那 17 处与 `creds-core` 那 2 处本行盖不到**」 |
| shellcheck | **没有这一格** | 16 格里没有 shellcheck：`grep -c -i shellcheck scripts/gate.sh` = **0**（量于 `6bba991`），gate 那趟输出里 shellcheck 也是 0 命中 |
| 两套 e2e 缺 daemon 二进制 | 四套 `ccm e2e` | 门禁跑之前有一步「**e2e 前置　build 后端二进制（四套 ccm e2e 的被测对象）**」——**门禁替它建了，CI 那两个 job 没建** |
| vitest 那条路径分隔符 | `npm` 1726 | 本地跑在 Linux 上 ⇒ 分隔符恒是 `/`，**这一形在 Linux 上不可能红** |

★ 形状：**四条里有三条是「尺子的作用域对不上事实」**，而其中两条**门禁自己把射程印在了那一行上** ——
读数在那儿，没人把它读成「所以这三件事没人管」。

## 七 · 产物这一档（`KR119D2`）今天核到了什么、核不到什么

**核到了（有读数）**：

| 要核的 | 读数 | 住址 |
|---|---|---|
| 内嵌后端的 `BUILD_ID` 与主干一致 | 两份 musl daemon 的**字节里**各扫出 1 次 `p2j-bus-state`；完整戳 `<<ccm-build-id:p2j-bus-state:ccm-build-id>>` 逐字命中 | 本文 3.4（我自己下下来扫的）＋ 3.3（CI 自己在 Windows 上扫的） |
| 主干源里那个 `BUILD_ID` | `remote-daemon-proto/src/main.rs:239` `const BUILD_ID: &str = "p2j-bus-state";` | 被测树 `6bba991` |
| Windows 安装包**会叫什么名字、自称什么版本** | `cc-monitor_3.8.0_x64-setup.exe` · `cc-monitor_3.8.0_x64_en-US.msi` | 演练 run 的 `tauri build` 步，本文 3.3 |
| Linux 产物 | `cc-monitor_3.8.0_amd64.deb`（sha256 `f24eccd9…`）· 裸 `monitor`（sha256 `a65ad515…`） | 演练 run 的 `Compute SHA256 checksums (Linux)` 步，本文 3.5 |
| 四处版本号在 CI 那一侧自己对过账 | `Version self-consistent: 3.8.0 (package.json / tauri.conf / Cargo.toml / Cargo.lock all agree)` | 本文 3.3 |

🔴 **核不到（如实登记为「判不了」，不写成「过了」）**：

1. **发出去的那一份安装包**，因为**没有发出去的那一份** —— Release 不存在（本文 5.2）。
   上表 Windows / Linux 两行量的是**演练那一趟 runner 上的产物**：
   `publish=false` ⇒ 两处 `action-gh-release` 全 skipped ⇒ **那些文件我一个字节都下载不到**，
   读的是 job 日志里 bundler 与 `sha256sum` 自己印的行。
   ⇒ 「**安装包里显示的版本**」这一格今天买到的是「**bundler 给它起的名字里是 3.8.0**」，
   **不是**「装出来的程序在关于框里显示 3.8.0」。后者要有产物才核得动。
2. **`monitor.exe` / `monitor` 里内嵌的那份后端**是不是同一个身份 —— 同理，拿不到那两个文件。
   今天有的只是上游那一环：**进安装包之前**那两份 daemon 字节，身份是对的。

## 八 · 写区外逐处 · 对外动作逐处

**对外动作，今天一共三次**（分母 = 我这一轮跑过的、会改动 `origin` 或发到别人手上的命令；
只读的 `gh run view` / `gh api …/logs` / `gh run download` / `git fetch` 不算）：

| # | 动作 | 为什么 | 可逆吗 |
|---|---|---|---|
| 1 | `git push origin track/k-r119` | 演练在**构造上**要求这个 ref 在远端（`workflow_dispatch --ref`）；`K-R114` 走的是同一条路（它推的是 `track/k-r114`，那个分支今天还在 `origin` 上） | 可（删分支） |
| 2 | `gh workflow run release.yml --ref track/k-r119 -f publish=false` | `KR119D1` 点名要的演练 | 不适用（没写任何东西） |
| 3 | **`git push origin v3.8.0`** | 本件唯一被用户点过头的对外动作（`R77`「发3.8」＋ `R80`「不验，直接推 tag」） | 可（`git push origin :v3.8.0`），**但我没删** —— 见下 |

**没做的对外动作，逐处写清**：

- **没推 `main`。** `doc/RELEASING.md § 2` 的 SOP 逐字是「`git push origin main` ← **先推 main，等 CI 绿**」，
  而本件的硬边界逐字是「**推 tag 是本件唯一的对外动作**」⇒ 我按边界走，没推。
  代价现打就兑现了：`ci-gate` 在 `6bba991` 上查不到任何**已经绿过**的 CI run，
  只能等 tag 那一趟新起的 CI —— 而那一趟是红的。
  ⚠ **但别把因果读反**：先推 main 也一样红（同一棵树、同一份 `ci.yml`），
  **先推 main 唯一能买到的是「在推 tag 之前就知道它红」。**
- **没删 `v3.8.0` 这个 tag。** 理由两条：① 删 tag 是**另一次**对外动作，没人点头；
  ② `doc/RELEASING.md § 2.1` 给的恢复路逐字是「修 CI，然后**重跑 `ci-gate` 这一个 job**，**不必重打 tag**」。
  ⚠ **诚实边界**：那条恢复路是给「CI 偶发红」写的。**今天这五条不是偶发** ——
  修它们要落新提交，而新提交一落，`v3.8.0` 就指着一棵**没有那些修复**的树
  ⇒ 到那时 tag 要么移、要么换号。**这一步归用户 / PM 拍，我不替他拍。**
- **没改 GitHub Release 的正文**（没有 Release 可改；即便有，`generate_release_notes: true` 自动生成的
  那份要换成本件的文案也是**另一次对外动作**）。
- **没碰 `.github/**` 一个字节**（只用不改）· **没碰 `CHANGELOG.md` 与那七处版本号**（只核不改）·
  **没碰任何生产代码** —— 包括上面那五条 CI 红，**一条都没去修**。
