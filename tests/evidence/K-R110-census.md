# `K-R110` 普查原文 —— 两把尺子的读数

> 量于 **2026-09-13**，工作树 `.claude/worktrees/k-r110`、分支 `track/k-r110`、基点 `b0953e5`。
> 量具住址：`<本工作树>/evidence/K-R110-ruler.py`（**只读**，四个模式 `strip` / `backtest` / `census` / `probe`）。
> 🔴 **被测对象 = 本文件所在的那棵树**（`ROOT = 量具的父目录的父目录`），不是别的工作树 —— 别照这个住址去别的树上复跑然后拿这里的数对。
> ⚠ 尺子里的词法掩码与几处谓词都是 Rust 那一份的**逐条 Python 移植**；**权威源是 Rust**，本文件只出读数、不判红。

---

## 一 · `KR110D1` 的普查：今天有几份文件被提前收尾

**量法**：把 `guard_core::test_module_ranges` 的收尾针（`close = "\n}"`，列 0 的右大括号）
在**两份文本**上各跑一遍 —— ① 裸源码（09-13 之前的口径）· ② 词法掩码后的源码
（块注释 / 行注释 / 串·原始串·字节串内容 / 字符字面量一律抹成等长空格）。
两口径切出的区间**不一致**的文件，就是被「不是代码的那个 `}`」提前收尾的文件。

```
$ python3 evidence/K-R110-ruler.py strip
── 尺子 A `strip`：列 0 收尾针「裸文本 vs 词法掩码」对拍 ──
分母：`git ls-files '*.rs'` 现打 **235** 份
      其中落在 `assert_tree_strips_clean` 三个调用根之下的 **201** 份
      （根：remote-daemon-proto/src, src-tauri/src），射程外 **34** 份
      现行口径下**切出至少一个测试模块区间**的 **190** 份
      词法掩码走兜底（模型对不上、一个字不抹）的 **0** 份 []
两口径**不一致**的：**1** 份

  · remote-daemon-proto/src/plugin/mod.rs（`wc -l` 口径 688 行，在树遍历射程内）
      现行区间 [(107, 657)]
      掩码区间 [(107, 688)]
      ⇒ 提前收尾于第 657 行（逐字 `}"#;`），真收尾在第 688 行
        ⇒ **31 行**测试代码漏进生产段、同样这 31 行从测试段里少掉
```

### 读数怎么读

| 问题 | 答 | 分母怎么数的 |
|---|---|---|
| 今天有几份文件被提前收尾 | **1 份** | `git ls-files '*.rs'` 的 **235** 份逐份跑两口径对拍 |
| 各漏几行 | `plugin/mod.rs` **31 行**（658–688；657 行那一行从 `"#;` 起也在生产段里） | 两口径收尾行号之差 |
| 反方向（收尾挪晚、吞掉生产代码）有没有 | **0 份** | 同一张对拍表：区间只有 1 处不一致，方向是「挪晚到真收尾」 |
| 词法掩码自己崩掉（兜底）几份 | **0 份** | 同上 235 份 |

⚠ **「31 行」这个数有两个方向，别只记一个**：
- **测试段少了 31 行** ⇒ 以测试段为人群的判据（`guard_core::test_source` 的消费者）**够不着它们** —— 这一半是 **fail-open**；
- **生产段多了 31 行** ⇒ 以生产段为人群的判据在这份文件上多扫了 31 行测试代码 —— 这一半是**假红面**（今天恰好没红）。

🔴 **两条老守门人为什么看不见**（现打）：`assert_no_test_code` 数残留的测试属性、
`assert_no_unstripped_test_module` 数残留的「测试期 `cfg` ＋ 带花括号体的 `mod`」，
而漏出去的那 31 行里**测试属性 0 个、`mod` 行 0 行**。

### 回测（纪律 ⑳）—— 尺子数不到已知样本就作废

```
$ python3 evidence/K-R110-ruler.py backtest
  ① 已知阳性样本 `remote-daemon-proto/src/plugin/mod.rs`：尺子 A **数到了**
  ② 合成阴性（测试模块里没有列 0 `}`）：现行 [(2, 10)] · 掩码 [(2, 10)] ⇒ 判为一致，期望 一致 ⇒ OK
  ③ 合成阳性（`r#"…"#` 内容含列 0 `}`）：现行 [(2, 9)] · 掩码 [(2, 12)] ⇒ 判为提前收尾，期望 提前收尾 ⇒ OK
```

### 新判据的人群（修完之后，同一把尺子换个问法）

「`test_module_ranges` 切出的每一段，按词法数的 `{` 与 `}` 一样多吗」——
现打（235 份 · 切出 **249** 段 · 含区间的文件 **190** 份）：

| 口径 | 不配平的段数 |
|---|---|
| **修前**（锚点在裸文本上找） | **1**（`plugin/mod.rs` 第 107–657 行那一段：`{` 41 个 / `}` 39 个） |
| **修后**（锚点在词法掩码上找） | **0** |

⇒ 新判据 `guard_core::assert_test_module_ranges_are_brace_balanced` 今天在两棵树上零误红，
而它的**匹配单位是计数**、剥法的匹配单位是「第一处命中」—— 剥法的盲区不是它的盲区。

---

## 二 · `KR110D2` 的普查：「匹配单位是行」的 48 处

### 分母：我数出来的是 **48**，而 `K-R103` 那句「另外 47 处」复不出来

`K-R103` 的原话（住 `features/K-R103-K-R87交回的三条跟进（判据射程与同形重复）.md:282`）逐字是：
「现打分母：`.lines()` 在 `remote-daemon-proto/src/*.rs` ＋ `src/platform/*.rs` 里 **48** 处
（扣掉 `no_timer_guard.rs`）。**另外 47 处**里哪些的事实真的跨行，我没查」。

**我按它写的那个人群重打**（`python3 evidence/K-R110-ruler.py census`）：

| 口径 | 处数 |
|---|---|
| `.lines()` 文本出现次数 · `remote-daemon-proto/src/*.rs`（**顶层，不递归**）＋ `platform/*.rs`（现打 **28** 份文件） | **50** |
| 其中 `no_timer_guard.rs`（`K-R103` 治的那一处 `f09` 住在这里） | **2** |
| ⇒ 扣掉之后 | **48** |

🔴 **差在哪**：`48` 这个数**已经**把 `no_timer_guard.rs` 扣掉了，`K-R103` 又减了一次
（它治的 `f09` 就在被扣掉的那份文件里，而那份文件有 **2** 处 `.lines()`，不是 1 处）。
⇒ **今天待查的是 48 处，不是 47 处**。本文件按 **48** 出读数。

⚠ **这个人群本身的边界，两侧都写出来**（`K-R103` 定的，我沿用、不擅自扩）：
- 它**不含**子目录 —— 现打 `control/` 18 处 · `observe/` 19 处 · `plugin/` 6 处 · `relay/` 11 处；
  daemon crate 递归全量是 **112** 处 `.lines()`。
- 它只认 `.lines()` 这一种写法。同 crate 递归现打还有 `for line in` **31** 处 · `split('\n')` **4** 处 ·
  `.split_whitespace()` **5** 处 —— 这些**不在本轮分母里**，是另一张单子。
- 「`.lines()` 出现一次」≠「一处按行匹配的判据」：下面逐处定性时会指出哪些只是切分/取值。

### 三档定性（48 处，逐处）

判的问题逐字是：**「它的匹配单位是行 ⇒ 有没有跨行的真实形状够不着它」**。

- **够得着（无事）** —— 事实本来就落在一行上，或那份输入是**行式格式**（TOML/markdown 表/程序输出/`/proc`），
  或匹配根本不在行上做，或跨行时方向是 **fail-closed**（会红，不会静默）。
- **够不着且有真实反例（真漏）** —— 事实能跨行，而**盘上今天真有那种写法**。
- **够不着但今天零实例（预防性）** —— 事实能跨行，盘上今天没有那种写法。**这一档一律带回测**。

| # | 住址 | 它在按什么匹配 | 档 |
|---|---|---|---|
| 1 | `agent_boundary_guard.rs:205` | `production_lines` 切行，针是**单个字面量** | 够得着 |
| 2 | `agent_locality_guard.rs:493` | 单针 `contains` | 够得着 |
| 3 | `agent_locality_guard.rs:630` | 单针 `contains` | 够得着 |
| 4 | `agent_locality_guard.rs:700` | 单针，**按行计数**（针集刻意重叠 ⇒ 按行才不重复计） | 够得着（另见 §三） |
| 5 | `alloc_probe.rs:227` | `.lines()` 只用来剥整行注释，匹配在**整份串**上 `find` | 够得着（不是按行匹配） |
| 6 | `build_id_guard.rs:513` | `--ccm-probe` 输出的 `build=` 行（**行式契约**） | 够得着 |
| 7 | `build_id_guard.rs:558` | `Cargo.toml` 的 `version = ` 行（TOML 行式） | 够得着 |
| 8 | `build_id_guard.rs:581` | 数 `const BUILD_ID` 声明行（名字必在首行） | 够得着 |
| 9 | `inbound.rs:804` | **两针合取同一行**：`tokio::main` × `current_thread` | **够不着 · 零实例** |
| 10 | `inbound.rs:1363` | 单针 `observe::` | 够得着 |
| 11 | `inbound.rs:1393` | **两针合取**：`id.` × `.parse`／`.split`… | **够不着 · 零实例**（与 `K-R103` 治的 `f09` **同形**） |
| 12 | `listen.rs:470` | `refusal_line(`（`(` 紧跟名字） | 够得着 |
| 13 | `listen.rs:528` | `//!` 头注按行切（行式） | 够得着 |
| 14 | `listen.rs:540` | 同上，数 `//!` 行数 | 够得着 |
| 15 | `main.rs:548` | `starts_with("unavailable:")`（键必在首行） | 够得着 |
| 16 | `main.rs:852` | 行窗抽取，带 `2..=12` 反空真 | 够得着 |
| 17 | `main.rs:997` | 单针 ＋ 向上找 cfg（「属性归属是文本近似」**头注已逐字登记**） | 够得着（另一族已登记边界） |
| 18 | `main.rs:2201` | `split_once("=>")` 抽调度臂；臂体跨行 ⇒ 臂体成 `{` ⇒ `::`+`(` 断言当场红 | 够得着（**fail-closed**） |
| 19 | `panorama_locus_guard.rs:123` | `Cargo.lock` 整行相等（行式） | 够得着 |
| 20 | `panorama_locus_guard.rs:284` | `declares_engine_dep`：行首那个词按 `=` 切 | **够不着 · 零实例**（`[dependencies.<名>]` 分节头形） |
| 21 | `plugin_walk_fixture.rs:1351` | 词表 `contains` | 够得着 |
| 22 | `plugin_walk_fixture.rs:1750` | 子进程输出按行过滤（行式 `println`） | 够得着 |
| 23 | `plugin_walk_fixture.rs:1808` | `KEY=VALUE` env dump 按行（值里带换行 ⇒ 多出幽灵键，方向是**假红**不是漏） | 够得着 |
| 24 | `protocol_doc_guard.rs:301` | 「左花括号之前那段的**最后一行**」= 类型声明 | **够不着 · 零实例**（多行泛型声明） |
| 25 | `protocol_doc_guard.rs:478` | **两针合取**：`starts_with("#[serde(")` × `contains("rename")` | **够不着 · 零实例**（fail-open 方向：线上字段改名） |
| 26 | `protocol_doc_guard.rs:982` | 变体名按行 ＋ **深度跟踪** | 够得着 |
| 27 | `protocol_doc_guard.rs:1029` | markdown 表格行（行式） | 够得着 |
| 28 | `ratchet_guard.rs:181` | `PINS` 是**整行逐字指纹**，单位就是行；断行 ⇒ 计数变 0 ⇒ 红 | 够得着（**fail-closed**） |
| 29 | `ratchet_guard.rs:455` | 同上（存在性那一半） | 够得着（**fail-closed**） |
| 30 | `readonly_guard.rs:648` | `is_hatch`：`use ` × `std::fs`/`tokio::fs` **合取同一行** | **够不着 · 零实例**（fail-open 方向：只读红线） |
| 31 | `readonly_guard.rs:676` | `fs::`/`File::` **之后紧跟**的动词，只在同一行上找；取空就 `continue` | **够不着 · 零实例** |
| 32 | `readonly_guard.rs:2035` | `starts_with(mark)` 文档标记行（行式） | 够得着 |
| 33 | `readonly_guard.rs:2254` | 命中按行，而窗口**已放宽到 3 行** | 够得着 |
| 34 | `readonly_guard.rs:2680` | `dep_entries`：节名必须逐字以 `dependencies]` 收尾 | **够不着 · 零实例**（`[dependencies.<名>]` 分节头形） |
| 35 | `readonly_guard.rs:2865` | `write_surface_hits`：单串 `contains`（原文行） | 够得着 |
| 36 | `sidecar_fetch_guard.rs:318` | 结构体字段行（rustfmt 一字段一行） | 够得着 |
| 37 | `sidecar_fetch_guard.rs:402` | `allow(` 单串（换行后 `#[allow(` 仍在首行） | 够得着 |
| 38 | `wire.rs:674` | 单针，**按行计数** | 够得着（另见 §三） |
| 39 | `wire.rs:739` | `contains(ctor)` ＋ 同行负向过滤 `!contains("struct ")` | 够得着 |
| 40 | `wire.rs:759` | `impl` 块行窗 ＋ 缩进判据 | 够得着 |
| 41 | `wire.rs:795` | `contains("struct HelloFlushed")` | 够得着 |
| 42 | `wire.rs:941` | 变体行按缩进数，且与产出 kind **对拍** | 够得着 |
| 43 | `wire.rs:1149` | `starts_with("homes:")` | 够得着 |
| 44 | `platform/cfgless_guard.rs:809` | `fabricated_success`：块体**最后一个非空行**是不是 `Some(`/`Ok(` 打头 | **够不着 · 零实例**（fail-open） |
| 45 | `platform/fallback_guard.rs:284` | `block_tail`：块的值 = 最后一个非空非注释非裸花括号的**那一行** | 🔴 **够不着 · 有真实反例** |
| 46 | `platform/fallback_guard.rs:423` | 同 45（**同一份判定的第二处内联副本**） | 🔴 **够不着 · 有真实反例** |
| 47 | `platform/fallback_guard.rs:688` | 文档标记行（行式） | 够得着 |
| 48 | `platform/proc.rs:229` | `/proc/stat` 的 `btime ` 行（内核行式契约） | 够得着 |

**合计：够得着 37 · 真漏 2 · 预防性 9 = 48。**

### 「今天零实例」那一档的回测（纪律 ⑳ · `K-R103` 的活体先例）

> `K-R103` 那一处的头注写着「daemon 侧今天零实例」，而真相是**人群够不着** ——
> 两者在终端上长得一模一样。所以这一档一律喂**两个**样本：
> ① 一个**已知会命中**的单行形（它不命中 ⇒ 我的谓词复刻写错了，这一行读数作废）；
> ② 同一个事实换成 rustfmt / 人手会写出来的**跨行形**。

`python3 evidence/K-R110-ruler.py probe` 现打 **10 条**（9 处预防性 ＋ 真漏那一族的谓词 45/46 合成一条），
**10 条全部「①命中、②不命中」** ⇒ 「够不着」这个判词在这 10 条上成立，而不是「我的尺子瞎了」。
逐条原文见量具输出（每条都印着谓词、两个样本、两次命中与否）。

### 🔴 真漏那两处：建议的匹配单位 与 预估真红/假红

> **本条明令只出读数，我一处都没放宽。** 下面是**建议**，由 PM 决定另立几件。
> （理由是 `K-R103` 现打的：放到最宽买到 1 真 4 假、只换匹配单位买到 1 真 0 假；净变宽会让人绕开判据。）

| | 真漏 45/46 |
|---|---|
| **住址** | `remote-daemon-proto/src/platform/fallback_guard.rs:284`（`block_tail`）＋ `:423`（同一判定的第二处**内联副本**） |
| **事实是什么** | 「这个平台块的**值**是什么」—— 它是一个**表达式**，可以跨行 |
| **今天的匹配单位** | 块体里最后一个「非空、非 `//`、非裸花括号」的**行** |
| **真实反例（盘上现有）** | `remote-daemon-proto/src/platform/proc.rs:43` 的**多行 `unimplemented!(`**（43–46 行）。取最后一行拿到的是 `)` ⇒ 认不出这是「诚实空壳」 |
| **后果今天为什么没爆** | `:421` 有一条**特例** `has_macro = body.contains("unimplemented!(") \|\| body.contains("todo!(")`（看**整个块体**）把它接住了。那条特例的注释里逐字记着第一版就是这么误判的、「变异实测确认过」 |
| **所以它算真漏吗** | 算 —— **匹配单位没换，只是给已知的那一形打了个补丁**。下一个多行块尾（例如 `Ok(\n    Status::Alive,\n)`）照样被读成「块的值是 `)`」⇒ 落进「真正的平台实现」那一档，而那正是这条判据要拦的 |
| **建议的匹配单位** | 「块体里的**最后一个表达式**」——`no_timer_guard::expr_end`（`K-R103` 09-13 现打的那条原语）已经是这一形，**先把它抬进 `guard_core` 再共用**，别在 `fallback_guard` 里写第三份 |
| **预估真红** | **0 条**（唯一那处多行块尾已被 `has_macro` 中和 ⇒ 换单位后结论不变）。⚠ 它的价值不是「今天多逮一条」，是**把补丁换成判据** |
| **预估假红** | **0 条**（现打：全 daemon 递归里行尾恰好是 `unimplemented!(` / `todo!(` 的**只有 1 处**；`fallback_guard` 那条采集面的自检是「数量相等」，实测 7 个块） |
| **顺带一件** | 45 与 46 是**同一份判定的两份副本**（`block_tail` 一份、`:423` 内联一份）。E3：一个事实一个权威源 ⇒ 换单位那一拍应当把 `:423` 改成调 `block_tail`，否则改一边不改另一边必漂 |

### 九处「预防性」里，方向最贵的三条（供 PM 排期时看）

| 处 | 为什么它比别的贵 |
|---|---|
| `readonly_guard.rs:648`（#30） | 守的是**只读红线**，方向是 fail-open。而**多行 brace `use` 的形状盘上今天已有 2 处**（`observe/accounts_query.rs:67` · `sidecars/codepicture/acquire.rs:62`）—— 只是它们导入的不是 `fs`。这条离「真漏」只差一次 rustfmt 断行 |
| `protocol_doc_guard.rs:478`（#25） | 守的是**线上字段名**（跨仓契约）。把 `#[serde(rename = "…")]` 手动折成三行，这条判据当场瞎，而线上字节真的变了 |
| `inbound.rs:1393`（#11） | 与 `K-R103` 治的 `f09` **完全同形**（针在上一行、事实在续行的链式调用）。同形第二处 |

---

## 三 · 顺带逮到的**同族异形**（不在本轮三档口径里，单列）

**「匹配单位是行」的另一半：一行上有好几处，按行数会少数。**（`K-R106` 那一族：匹配单位是**计数**。）

- 现打：`agent_locality_guard::adapter_call_sites_measured`（#4）的人群里，
  **命中行 28 行 / 命中处 58 处**，其中 **`agents/mod.rs:19` 一行上有 3 处彼此不重叠的适配层直呼**
  （`claudecode::AGENT_KIND` · `claudecode::home` · `claudecode::paths::CONFIG_DI…`）。
  它被 `ADAPTER_CALL_SITES` 那张**逐条相等**的登记表按 **1** 计。
  ⇒ 往已有命中行上再挂一处适配层直呼，那张表**不会红**。
- ⚠ **别顺手改成「数出现次数」**：那张针表**刻意重叠**（`agents::codex::` 与 `codex::` 会命中同一段文本），
  按出现次数会当场把 58 里的一多半读成重复。正确的单位是「**去重叠之后的匹配位置**」。
- 同形还有 `wire.rs:674`（#38，按行计数，登记表逐条相等）。
- 🔴 **这两处我一个字没动** —— 它不在 `KR110D2` 的三档口径里（那一档问的是「跨行」），交 PM 裁要不要立件。

---

## 四 · 诚实边界

1. **本文件的每一个「跨行/不跨行」判词，都是我读那处判据的代码得出的**，不是全量变异实测出来的。
   变异实测只做了 `KR110D1` 那一族（四刀 ＋ 两刀补充，读数在 `evidence/K-R110-deathvalue.md`）。
   `KR110D2` 这一节**只出读数、没切刀**（DoD 明令只出读数）。
2. **回测台上的谓词是 Python 复刻**。复刻与 Rust 那一份对不上，读数就错 —— 每条都附了「已知会命中的单行样本」
   正是为了让复刻错的时候当场露馅（10 条全部 ①命中 ⇒ 复刻在这 10 条上站得住）。
3. **「今天零实例」的分母是本轮那个人群**（28 份文件的 48 处）。子目录里那 54 处 `.lines()`、
   以及 `for line in` / `split('\n')` / `.split_whitespace()` 那三张单子，**我没查** —— 不是「查过没有」，是**没查**。
4. 尺子 A 的**射程**是 `git ls-files '*.rs'` 的 235 份；`assert_tree_strips_clean` 的射程只有其中 **201** 份，
   剩下 **34** 份（`src-tauri/vendor/` · `src-tauri/crates/*-core` · `src-tauri/build.rs`）
   **今天没有任何树遍历在看它们的生产段** —— 这是 `K-R75` `KR75D3` 已登记的读数，本轮只是重打了一次分母（它从 230/198 涨到了 235/201）。
