# 状态 / STATUS — 恢复入口（每次先读这里）

> 工作区 `audit-fixes`。分支 `account-ux`。跨轮靠此文件,不靠记忆。

- **当前阶段**：⛔ **loop 暂停在 F03 门口**（§6 开放问题需用户拍板）→ F02 已 commit
- **当前功能**：F02 已完成 ✅；F03 待用户决策后再开
- **当前步骤**：—（等 F03 开放问题答复）
- **已完成功能**：F01 follow-resume 账号安全；**F02 kill 白名单**（kill_remote_tmux 补 is_ccm_tmux_name + 订正假注释 + Rust 回归测/变异验证）
- **下一个功能**：**F03 统一 tmux 管线** —— 但需先答 §6 开放问题
- **阻塞 / 待用户确认**：⛔ **F03 开放问题**：tmux 载荷「claude 退出即**收**会话（治孤儿，但退出后不能 re-attach）」 vs 「**保留**空 shell（可 re-attach 回去，但留孤儿，靠 F05 手动清理）」——二选一由用户拍。
- **最近一次计划回看**：2026-07-25（F02 完成，主计划 rev 04）
- **自动模式（/loop）**：**全自动**，但**已按规则停 loop**（撞 F03 开放问题）——省略 ScheduleWakeup，等用户答复
- **本轮 loop 目标**：（已达成 F02）+ 停在 F03 门口交用户
- **loop 停止条件**：**命中 = F03 开放问题需决策**
- **基线**：cargo fmt 0 / 353 Rust 测 / tsc 0 / npm test 570 / build ✓（F02 完成后）
- **恢复方式**：用户答 F03 开放问题后，从 F03 继续（C→F），完再 F04→F05…→Phase G。
- **loop 节奏**：ScheduleWakeup 60s（用户要求）
- **红线（每轮核对）**：daemon 零行为改动（只用现成 p1p 能力）· 不改 TMUX_LS_FMT 双写点 · 不碰 ~/.bashrc（F14 用户自跑）· 不改 cc-<sid8> 语义 · 不 push/发版/bump · 孤儿仅手动回收
- **feature 拆分记**：F01 三步——①pin 现读(本轮) ②基座可选 ③resume 账号下拉。picker 组件是 F03/F04 共享面。
- **备注**：全自动档下 loop 可自过「不碰新共享面」的功能计划；碰账本外新共享面或 §6 开放问题（如 F03 退出即收 vs 保留）则停下问用户。
