# 状态 / STATUS — 恢复入口（每次先读这里）

> 工作区 `audit-fixes`。分支 `account-ux`。跨轮靠此文件,不靠记忆。

- **当前阶段**：C 实现（F03 步骤 1 完成 + commit）→ 下轮 F03 步骤 2
- **当前功能**：F03 统一 tmux 管线 + 三态生命周期（步骤 1/4 完成）
- **当前步骤**：F03 步骤 1（idle 就地复用 resume）完成；下一步 = 步骤 2（灰灯 UI）
- **已完成功能**：F01 账号安全；F02 kill 白名单；**F03 步骤1**（idle-tmux 就地复用 resume，治 #76 根因 + #75 一条）
- **下一个功能**：F03 步骤 2（灰灯 UI，reconcile→活动灯灰状态）→ 步骤 3（attach-idle）→ 步骤 4（rbind 标题建时设 #74/#41）
- **阻塞 / 待用户确认**：无 —— §6 开放问题已由用户拍板「B 保留 + 三态 idle-tmux」关闭。
- **最近一次计划回看**：2026-07-25（F03 步骤1 后，主计划 rev 05）
- **自动模式（/loop）**：**全自动** —— 连续 B→G，只在阻塞/计划≠现实/同一步失败≥2/新决策/全部完成时停
- **本轮 loop 目标**：下轮 F03 步骤 2（灰灯）——会碰 reconcile/live-state 显示面（历史 bug 高发），若牵动新共享面或需设计决策则停下确认
- **loop 停止条件**：未命中
- **基线**：cargo fmt 0 / 353 Rust 测 / tsc 0 / npm test 573 / build ✓（F03 步骤1 后）
- **F03 剩余步骤**：2 灰灯 UI · 3 attach-into-idle · 4 rbind 标题建时设（#74/#41）
- **loop 节奏**：ScheduleWakeup 60s（用户要求）
- **红线（每轮核对）**：daemon 零行为改动（只用现成 p1p 能力）· 不改 TMUX_LS_FMT 双写点 · 不碰 ~/.bashrc（F14 用户自跑）· 不改 cc-<sid8> 语义 · 不 push/发版/bump · 孤儿仅手动回收
- **feature 拆分记**：F01 三步——①pin 现读(本轮) ②基座可选 ③resume 账号下拉。picker 组件是 F03/F04 共享面。
- **备注**：全自动档下 loop 可自过「不碰新共享面」的功能计划；碰账本外新共享面或 §6 开放问题（如 F03 退出即收 vs 保留）则停下问用户。
