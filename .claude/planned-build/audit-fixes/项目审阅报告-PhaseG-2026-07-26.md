# 项目全面审阅报告 — Phase G 最终验收（cc-monitor / account-ux 分支）

> planned-build audit-fixes 收官验收。4 并行独立 agent（整体设计 / 文档 / 代码工程 / 实现细节）+ 主线程文档-代码交叉对比。
> 日期 2026-07-26。**审阅发现的「立即修」项已在本轮修完**（多为本轮自身残留），见文末「Phase G 已修」。

## 摘要
- **整体健康度**：健康、可干净合入。三层架构（TS 前端 ↔ Tauri Rust ↔ 只读 daemon）边界清晰；本轮 13 功能改动保持架构自洽，无半成品、无破坏性回归。
- **阻塞 0 / 重要 4 / 建议 ~10**。重要项无一是「新引入的 bug」——3 项是本轮文档/注释半更新残留（已修），1 项是刻意保守取舍的已知残留（daemon-bound，记档延后）。
- **门禁基线全绿**：tsc 0 · vitest 595 · src-tauri 365(+1 ignored) · vendor code-picture-core 25 · daemon 125 · fmt 0 · build ✓ · CI 4 job。

## 整体设计（Agent 1）
- **阻塞 0**。§24/§24bis 单写者、F12 分层修复在设计层成立：三个 `remote_active` 生产者（daemon 帧 / 断连 flush / 收帧 tmux 收割器）全经唯一 emitter `session_changes.send`，reconcile 只读；F12 非 UI 模块全改依赖纯数据层 `remote-config`（仅 UI 父件 `settings/panel.ts` 仍 import `remote-section`，正确）。
- **重要**：① §24bis「REMOTE_IDLE 单写者=emitter」原靠注释约定、非类型保证 → **本轮已机器化**（见「已修」）。② ARCHITECTURE §2 未收录已落地的 Codex 适配轨道 → **已修**。
- 建议：把 `REMOTE_ANNOUNCED/REMOTE_TMUX_RAW/REMOTE_IDLE + remote_active` 收敛进注入式 `RemoteState`（让单写者靠所有权而非注释，长期）；统一 parking_lot/std 两处锁库；勿再往 `tabs.ts` god-object 塞新态。

## 文档工程（Agent 2）
- **阻塞 0**，但发现 F10/F11 是「半更新」：README 头部 stats 更新了、下游没跟 → 全库数字打架（backend 365↔308、vitest 595↔308、CI 四 job↔三 job，README 单文件内即自相矛盾）；`[未发布]/[Unreleased]` CHANGELOG 指针悬空；README.en 停在 22 action、缺 G/Ctrl+K；doc 索引表 e2e 行错列 + 漏 remote-daemon-proto；两审阅报告散落仓根。→ **均已修**。
- 好评：文档分层与深度做得好（ARCHITECTURE 只讲数据流+分层、细节下沉子 README + 33 条 INVARIANTS；设计草案明确标注）；F11 的 ARCHITECTURE 账号子系统、INVARIANTS §32、各子 README 都正确落地。

## 代码工程（Agent 3）
- **阻塞 0 / 重要 0**。CI 四 job 各跑其所声称；`cargo test -p code-picture-core` 是必需补口（vendor 非 workspace 成员）；覆盖率地板是真·阻断门禁（当前 S41.85 对阈 40 有余量）；14 个 tsx node 测 1:1 挂 `npm test`、无孤儿、`throw` 触发真失败（无伪造全绿）；lint 分级合理。
- 建议：① `vitest.config.ts` 头部注释误述门禁语义（写「不设 thresholds/advisory」但实际设了地板+CI 阻断）——对吃过「门禁没真门禁」亏的团队是陷阱 → **已修**。② CI frontend 的 jsdom 套件跑两遍（npm test + coverage 步）——可去冗余（未改，低优）。③ cards/index↔subagent 有一条 **type-only** import 环（isolatedModules 擦除、运行时无环，无害）。

## 实现细节（Agent 4）
- **阻塞 0**。F03.2 灰灯 + F08/F12 实现正确、365 测绿；§24 单写者逐调用点核实（mark_idle 仅 lib.rs:606、clear_idle 仅 565/619）；`pre_bound=idle` 播种正确闭合跨线程缝；TMUX_LS_FMT 双写断言锚定 const 定义行、正确。
- **重要（已知残留，记档）**：**空 backend 保守跳过 → 「被杀的是最后一个 tmux 会话」时灰灯卡到断连**。刻意取舍（挡 `tmux ls` 抖动批量误灰）、断连自愈、仅「最后一个」中招。干净修=daemon 回 `NO_SESSIONS` 确定性哨兵——**动 daemon 破红线，留 daemon 版本批次**（已记 INVARIANTS §24bis）。
- 建议：`readonly_guard.strip_cfg_test` 对「`#[cfg(test)]` 修饰 use/const」有未来假阴性（当前 daemon 全在 mod/fn 上、无 bug）；`pendingTmuxIdle`/`coerceHost 纯空白 label` 微小项（同既有模式、非回归）。

## 文档-代码漂移（主线程交叉对比）
- 交叉核实：代码**实现了**更新后文档所声称的（28 action ✓ actions.ts、4 CI job ✓ ci.yml、§32 暗色 ✓ styles.css:71、F12 分层 ✓ 8 importer）。漂移**纯在未更新的文档侧**（Agent 2 那批 + Codex 未进 ARCHITECTURE），非代码撒谎。→ 已修。
- 无新增代码-文档矛盾。

## 优先级建议
- **立即修（本轮已全修）**：README 数字自相矛盾 + 悬空 [未发布] 指针 + README.en 参差 + doc 索引表 + 散落报告归位 + ARCHITECTURE Codex 轨道 + vitest 注释门禁语义 + §24bis 单写者机器护栏。
- **近期处理（记档，未改）**：空 backend 最后会话卡灰（daemon 哨兵，红线待版本批次）；readonly_guard 剥离启发式加固；CI jsdom 双跑去冗余。
- **可选/长期**：RemoteState 收敛全局态；锁库统一；cards/types.ts 拆 type 环；勿再扩 TabManager。

## Phase G 已修（提交见 git）
README 中英数字统一(365/595/25/四 job) + 删悬空 [未发布] + README.en 补 28/G/Ctrl+K/6-unbound + doc 索引补 remote-daemon-proto/修 e2e 行 + CONTRIBUTING/RELEASING/DEVELOPMENT 数字同步 + ARCHITECTURE §2 补 Codex 适配 + vitest.config 注释改对门禁语义 + 两报告移入 audit-fixes/ + **§24bis 单写者机器护栏（cargo 测，变异验证过）** + INVARIANTS §24bis 记单写者机器化 + 空 backend 残留。全绿复验通过。
