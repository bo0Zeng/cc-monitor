# C04 — 命令半边：Phase B 的结论是**拆开**

> 主计划：`../MASTERPLAN.md` · 前置：C01 · C05 · C02 · C03 均已闭环签收
> **本文件不是一个功能的实现计划，而是「C04 该拆成四个功能」的论证 + C04a 的计划。**

## 1. 重取的承重数字（不照抄 Phase G，全部本轮实测）

| 项 | 实测 | Phase G 记的 | 差异说明 |
|---|---|---|---|
| Tauri 命令 | **119** 个唯一 fn 名（声明 = 注册，两边差集都空） | 119 | **一致**。**补一条易误判**：`#[tauri::command]` 属性实际出现 **120** 次——`bring_monitor_to_front` 有 `#[cfg(windows)]`/`#[cfg(not(windows))]` 一对（`lib.rs:1376`/`lib.rs:1475`）。守卫用 `Set` 去重是对的，拿 `grep -c` 复核的人会以为差了一个 |
| 跨边界 `Serialize` struct | **77** | 「128 个 Rust struct」 | Phase G 那个数应是 `pub struct` 总数或含别的口径；**真跨边界的是 77** |
| 其中已生成 | **14** | — | ⇒ **剩 63** |
| TS 侧 `import { invoke }` 文件 | **29** | 29 | 一致。**C04a 之后仍是 29**：`data-section.ts` 出列 (-1)，包装层 `src/ipc/commands.ts` 入列 (+1)，净持平；真正的下降从 C04d 第二个模块起。现由等号机检 |
| `invoke` 调用点 | **143** | — | **Phase D 审计 I5 订正**：原写 161。生产 + 剥注释 + 按**调用表达式** = **143**（按行 140）。**161 两种口径都复现不出**（含测试 + 不剥 + 按行我量到 154）⇒ 143 是承重数，C04d 的范围基准据此下修 |
| ~~TS 侧 `type`/`interface`~~ | **删掉** | 209 | **Phase D 审计 J9**：8 种口径都复现不出 211（col0-only-export 143 / col0-any 205 / any-indent 244 / 含测试 270）⇒「漂了 +2」这个结论没有可复现的基础。它不承重，留着只会成为下一个被照抄的错数 |

**命令分布**（前 5）：`lib.rs` 23 · `panorama.rs` 21 · `sftp_pool.rs` 11 · `mcp.rs` 10 · `history.rs` 9
**调用点分布**（前 8）：`panorama/api.ts` 21 · **`tabs.ts` 15** · `views/history.ts` 14 ·
`sftp/panel.ts` 12 · `settings/mcp-section.ts` 10 · `settings/remote-section.ts` 9 ·
`main.ts` 9 · `settings/cc_integration.ts` 7

### 一条我自己制造又收回的假警报，记下来

我第一遍量出「126 个声明 vs 119 个注册 ⇒ **7 个命令声明了没注册、前端调不到**」，
并准备把它当 C04 的一个真发现。**两处测量错误叠出来的**：
① `src-tauri/src/*.rs` 只匹配顶层，漏了 `adapter/` 子目录；
② 正则在 `#[tauri::command]` 后取「下一个 `fn`」，而**注释里提到 `#[tauri::command]`** 的地方
也被计入 —— 抓出来的 4 个「未注册命令」里有 3 个是普通 helper、
1 个直接是**测试函数名**（`remote_probe_uses_each_kind_of_evidence_precisely`）。

剥注释 + 递归全仓后：**119 = 119，两边差集都空**，Phase G 那句成立。
⇒ **报数字前先数一遍，而这次是第二遍才对。**

## 2. 结论：C04 必须拆，理由是它违反 planned-build 自己的粒度准则

planned-build 的「功能粒度准则」原文：
> 一个「功能」应能在一份 feature 计划里讲清、**几步内实现完**、有独立可验证的 DoD。
> 太大 → 在主计划里拆成多个功能。

C04 当前的范围是 **63 个 struct + 119 个命令签名 + 161 个调用点**
——**比 C01+C02+C03 加起来还大**（那三个合计：14 个生成物、约 12 处调用点替换）。
一份计划讲不清，也没法「几步内实现完」。

**而且它有一个今天就到不了的成功标准**：主计划 §0.1 的
「全仓 `import { invoke }` 从 29 个文件降到 1」——**`tabs.ts` 单独占 15 处调用点，
而「不碰 `tabs.ts`」是本会话在册的红线**。不松那条红线，这个标准结构性达不到。

### 拆法（四个功能，各有独立可验证的 DoD）

| ID | 功能 | 独立 DoD | 依赖 / 阻塞 |
|---|---|---|---|
| **C04a** | **类型化 `invoke` 包装层 + 钉死 119 个命令** | 包装层存在且**至少一个模块**改用它；「钉死 119 个命令名」的守卫（把今天只覆盖 3/119 的白名单扩到 119）；变异：删/改一个命令名 → 红 | 无 |
| **C04b** | **两处已登记的内联字面量** | `main.ts:744` 的 `ActiveSessionPayload` 换成生成物 + `tabs.ts:1632` 的 `SessionActivityPayload`（**后者卡红线**） | **`tabs.ts` 红线** |
| **C04c** | **`JsonlLine`/`JsonlBatch`** | 两个 payload 生成；`seq: u64` 用 C03 的策略；`message` 用逃生口 `#[ts(type = "import('../cards').JsonlRecord")]`；**当场暴露的 4 处 `cards/index.ts` 缺口如实登记不顺手修** | C03（**已解除**） |
| **C04d** | **按模块分批迁移剩余 63 个 struct + 调用点** | 每批一个模块、一个 commit、一次全门禁；`import { invoke }` 文件数逐批下降 | C04a；`tabs.ts` 那批卡红线 |

**为什么 C04a 先做**：包装层与守卫是**机制**，C04b/c/d 都在它上面走。
先建机制再批量迁移，避免 63 次迁移各自决定「怎么调 invoke」。

## 3. C04a 的计划（本轮要实现的就是这个）

### 3.1 DoD

- [x] **步骤 1 先定「返回值没人用类型的命令」怎么办**——C03 给出的答案是保持裸 `invoke`
      （不为假想消费者生成），但 C04a 面对 119 个命令**需要一条成文规则**并写进主计划 §5
- [x] 建**手写的**类型化 `invoke` 包装层（`ts-rs` 不生成命令签名，见主计划 §8）
- [x] **至少一个模块**改用包装层（`src/settings/data-section.ts`）（选调用点少、类型已生成的那个做样板）
- [x] **钉死 119 个命令名的守卫**：Rust 侧 `#[tauri::command]` 的 fn 名（剥注释后递归全仓抽取）
      ↔ `invoke_handler` 注册表 ↔ 包装层。计数自检用等号
- [x] **变异验收**（见 §3.3；**其中变异 ② 第一版没真正达成，审计抓到了，已补第 4 条守卫**）：① 删一个 `invoke_handler` 注册项 → 守卫红；
      ② 改包装层里一个命令名的字面量 → 守卫红。两次都先 diff 确认落位 + 确认编译得过再判色
- [x] 反向自检：不变异时全绿
- [x] 全门禁绿且数字不降（基线：cargo 550 · npm 821/54 · **clippy 无新增**（lib 36 + lib-test 44 warnings，0 error——**审计 I6 订正**：原写「clippy 0」，那是 C01 审计 S7 已订正过的误导口径）· tsc 0 · C05 门禁 rc=0）
- [x] 8 套真机套件全绿、条数与基线一致（26/44/12/15/13/21/14/7 = 152）

**明确不做**：不迁移 161 个调用点（C04d）· 不碰 `tabs.ts`（红线）·
不碰 `JsonlLine`/`JsonlBatch`（C04c）· 不生成剩余 63 个 struct（C04d）· 不改 `ci.yml`

### 3.2 守卫的形状（延续 C01/C02/C03 那两条的经验）

三条已被变异打磨过的经验直接套用：
1. **打在源上**（C03）：从 Rust 源抽命令名，不从生成物或包装层单侧断言。
2. **剥注释，两侧都剥**（C01/C02/C03 各栽一次）：本轮**我自己又栽了一次**
   （注释里提到 `#[tauri::command]` 被当成声明）——抽取器必须剥注释。
3. **计数自检用等号**（C02 时它抓到我新逻辑的 bug，C03 时抓到假红）。

**一条不能写的断言**：「每个命令都必须经过包装层」——C04a 只迁一个模块做样板，
其余 118 个还走裸 `invoke`，写了就是假红。范围该等于**性质**的范围。

## 4. 需要用户决定的一条（不阻塞 C04a）

**`tabs.ts` 红线要不要松。** 影响面：
- `tabs.ts` 有 **15 处** `invoke` 调用点（全仓第二多）
- C04b 的一半（`tabs.ts:1632` 的 `SessionActivityPayload`）卡在这里
- 主计划 §0.1 的成功标准「29 个文件降到 1」**不松红线就结构性达不到**

**未获授权时的处置**：C04b 只做 `main.ts:744` 那一半、`tabs.ts` 那一半标注
「已跳过，等授权」；C04d 的 `tabs.ts` 批同样跳过；主计划 §0.1 那条成功标准
**改写成「降到 1 + `tabs.ts`（等授权）」而不是假装达成**。

## 3.3 变异验收记录（**审计 Z3 点名要求落盘：此前只在会话里跑过，仓库里查不到**）

| # | 变异 | diff 落位 | 编译/跑得起来 | 守卫 | 结论 |
|---|---|---|---|---|---|
| **①** | `lib.rs` 删掉注册项 `            data_paths::get_data_paths,` | `git diff` 确认删 1 行 | `cargo build --lib` rc=0 | **红**，点名 `get_data_paths` | 成立 |
| **①′** | 同上，但第一次锚点写成 `\n(\s+)get_data_paths,` | **没落位** | — | 3 绿 | **那次的「3 绿」是无效结果，不算「没牙」**。真实行是带 `data_paths::` 模块前缀的。已用正确锚点重做 |
| **②** | 包装层里命令名改成**不存在**的 `get_data_pathz` | 确认 | `tsc` 0 错 | **红** | 成立**但抓错了地方**——见下方订正 |
| **②′** | 键不动、只把字面量抄成另一个**真实存在**的 `"open_log_file"` | 确认 | `tsc` **0 错** | **假绿（10/10）** | **审计的阻塞项**。已补第 4 条守卫 |
| ③ | 加一个 `#[tauri::command] fn audit_probe_cmd()` 并注册 | 确认 | cargo check 干净 | **红**：「期望恰好 119，实得 120」 | 等号计数有牙 |
| ④ | 去掉 Rust 侧的剥注释 | 确认 | 跑得起来 | **红**：4 个幻影名，含一个**测试函数名** | 剥注释是承重的（§1 那条假警报被守卫原样复现）|
| ⑤ | 把 TS walk 缩到 3 个子目录（模拟扫描器丢 26/29 个文件） | 确认 | 跑得起来 | **假绿（3/3）**，可见名 112→81 | `toBeGreaterThan(50)` 是安慰剂，已改等号 |
| ⑥ | 两个注册项写在同一行 / 最后一项漏尾逗号 / `#[tauri::command(rename_all=…)]` | 确认；**再跑 `cargo fmt` 一字未动，`--check` rc=0** | 干净 | **假红**，且诊断说反 | rustfmt **不进** `generate_handler!`；已去掉行锚、属性正则放宽 |
| ⑦ | `data-section.ts` 回退成裸 `invoke`，**注释故意留着不动** | 确认 | `tsc` 0 错 | **红** | 新断言**不是**被自己的注释喂饱的；且 `tsc` 完全不管，守卫是唯一的牙 |

**变异 ② 的订正（必须写清，否则下一个人会以为「包装层」那条守着它）**：
改成**不存在**的名字时红的是**第三条**断言（「TS 全仓字面量 ⊆ Rust 集」——它恰好也扫到了
包装层这个文件），**不是**「包装层」那条。而改成**另一个真实存在**的名字时**全绿**。
所以 DoD 原文写的「改包装层里一个命令名的字面量 → 守卫红」**第一版并没有真正达成**，
是审计的变异 ②′ 把它打掉的。现在由新增的第 4 条守卫（键名 == 该条目传给 `invoke` 的字面量）覆盖。

## 5. 代码审计结果（Phase D）

两个视角并行：**守卫是否真有牙 + 正确性**（做了 9 次变异实验）· **计划符合度 + 架构符合度**。
合计 **3 阻塞 + 6 重要 + 11 建议，全部处置**。三条阻塞都属同一类——
**守卫自己没牙**，而那正是本工作区在治的病。

### 阻塞（3/3 已修）

- **Z1 · 键名 ↔ 传给 `invoke` 的字面量无人对拍。** 见变异 ②′。**新机制原封不动地把
  「手写镜像静默漂移」这个病带了进来**，而 C04d 要在它上面手抄 118 次。
  修法：第 4 条守卫按**行首的键**切分条目（不按逗号——C04d 的带参条目会被 prettier 折成多行、
  泛型里也有逗号），断言每个条目里出现的字面量命令名集合 `toEqual([键名])`。
  **没采纳审计给的「换成单写形状」建议**，理由与实测数字记进主计划账本第 7 行。
- **Z2 · §5 规则少一个桶**（两桶 → 三桶 + 动态名第四类）。独立复核：返回 `()`/`Result<(),_>` 的
  命令 **34** 个、真有 payload 的 **85** 个；`unknown` 桶只该有 **4** 个（3%）。
  今天已有 **11 处** `invoke<void>`，判成 `unknown` 是净退化。
- **Z3 · 变异记录没落盘**，无法核实 ⇒ §3.3 补上，含变异 ② 的订正。

### 重要（6/6 已修）

- **I1 · `stripComments` 被字符串字面量骗走。** `config_surface.rs:775` 的 `"~/.local/*\/bin"`
  含 `/*`，非贪婪匹配一路吃到 `:1296`，**521 行真 Rust 代码对所有守卫隐形**（我自己复现了）。
  当时计数还全对纯属运气（被吞区间整段落在 `#[cfg(test)]` 里）。按断言方向分两档：
  肯定式过剥 ⇒ 假红会叫；**否定式（`.not.toContain("bigint")`、本轮新加的 `.not.toMatch(import invoke)`）
  过剥 ⇒ 假绿，永远不叫**。
  修法：**重写成状态机**（认字符串/模板/Rust 原始字符串，Rust 侧刻意不把 `'` 当定界符
  ——本仓 85 处 `&'static`、16 处 `&'a`），逐字符保留行结构，`lang` 参数必填，
  并**给它配了 9 条自己的测试**（它是两个守卫唯一的判据前处理，根软了整条链都软）。
  **一个附带的教训**：我在它的头注里引用了触发 bug 的原文 `"~/.local/*\/bin"`，
  那个 `*\/` 当场把我的 JSDoc 提前关掉、编译失败——bug 演示了自己。
- **I2 · `toBeGreaterThan(50)` 是安慰剂。** 见变异 ⑤。改成 `toBe(112)`，
  并把「7 个动态名盲区」从散文钉成 `toEqual` 断言（盲区不许静默变大）。
- **I3 · 三种 `cargo fmt` 认可的 Rust 形状让守卫假红。** 见变异 ⑥。
  **最反直觉的一条**：`cargo fmt` 根本**不进** `generate_handler!` 的内容
  ⇒「本仓 fmt 统一风格所以免疫」这条理由被实测推翻。
- **I4 · 「29 → 28」是假的**（净持平仍 29）。修法不止改准，还**把这个数机检掉**——
  它是主计划成功标准 4 的度量，而全仓唯一被写错的数字恰好是唯一没被机检的数字。
- **I5 · 成功标准 4 的改写没做，而 `STATUS.md` 已声称做了。** 已真改，
  并登记第二个此前无人记录的结构性障碍：**7 个动态命令名分布在 3 个文件**。
- **I6 · 数字订正三处**：调用点 161 → **143**（161 两种口径都复现不出）· 删掉不可复现的
  「211 个 type/interface」· DoD 的「clippy 0」改回「无新增（36+44 warning，0 error）」。

### 建议（11/11 已处置）

窗口 400 → **120**（实测属性到 fn 名最大真实距离 **65**）· `invoke` 前加 `(?<![A-Za-z0-9_$])`
防 `myinvoke(`/`this.invoke(` 误匹配 · 泛型参数放宽成 `[\s\S]{0,200}?`（含 `(` 的泛型会让整条漏掉 ⇒ 假绿）·
断言 `generate_handler![` 全仓恰好 1 处 · 删掉 `WRAPPED_COMMANDS`（唯一消费者是测试却住生产模块）·
两个守卫的 `ROOT` 深度约定统一到 `test-support/repo-root.ts` · coverage exclude 掉 `src/test-support/**` ·
**加一条「生产代码不许 import test-support」的机检**（原来那是条约定，而 eslint 在本仓是 advisory）·
头注三处过期/漏说的声明改准（「此前唯一的契约门禁」→「Phase G 时唯一」· 守卫清单从 2 条补成 4 条 ·
「调用点最少」→「并列最少，真正的区分是类型已生成」）· 注释里记下 120 属性/119 名字这个易误判 ·
`#[tauri::command(...)]` 带参形式已支持并注明。

## 6. 工程审计结果（Phase E）

## 6. 工程审计结果（Phase E）

**新共享面已登记**：`src/ipc/commands.ts` 进主计划 §3 账本第 **7** 行，写死四条形状约束
（扁平映射 · 键名==字面量由守卫兑现 · 三桶返回类型 · 每加一条就换掉裸 `invoke` 不许两路并存），
并把审计实测的「单写形状 tsc 数字」记进去留给 C04d 按数字决策。这一条正是账本存在的理由：
C04b/C04c/C04d 三个功能都要往这个面上写东西。

**账本预见的重叠、现在就做掉的**（planned-build 铁律 6）：`stripComments` 的重写不是 C04a 的
范围内工作，但它是**两个守卫唯一的判据前处理**、且主计划说这个守卫形状要复制 127 次。
留到 C04d 再修等于让 127 份拷贝建在一个已实测会假绿的根上 ⇒ 现在修 + 配测试。

**对后续功能的影响**：
- **C04b** 不受影响（两处内联字面量，与包装层正交）。
- **C04c** 会给包装层加第一个**带参数**的条目 ⇒ 第 4 条守卫的「按行首的键切分」已经为多行条目准备好了。
- **C04d** 的范围基准从「161 个调用点」下修到 **143**；且它现在有两条等号进度条
  （包装层条目数 `toBe(1)` · 直接 import invoke 的生产文件数 `toBe(29)`），每迁一个模块必须让它们动。

**没有引入新的隐藏耦合**：`src/test-support/` 只被 `*.vitest.ts` 引用（进不了 vite 的入口模块图），
这一条从**约定**升级成了**机检**。

## 7. 签收

## 7. 签收

- [x] 通过代码审计（两份并行审计：3 阻塞 + 6 重要 + 11 建议，全部处置；9 次变异实验）
- [x] 通过工程审计（新共享面进账本第 7 行；`stripComments` 按铁律 6 提前重构）
- [x] 主计划已据此更新（C04 拆成 C04a/b/c/d；§0.1 标准 4 真改写 · §5 三桶 · §3 账本第 7 行 · §1 状态列 · 变更记录 08）
