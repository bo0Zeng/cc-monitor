# 状态 / STATUS — rust-ts-boundary（恢复工作的入口，每次先读这里）

- **当前阶段**：**A 主规划已落盘，等用户审批**
- **当前功能**：无（C01 待批准后开工）
- **当前步骤**：n/a
- **已完成功能**：无
- **下一个功能**：C01（样板：一条命令走通全链）
- **阻塞 / 待用户确认**：
  - **[待批准] 主计划**
  - **[待定] `Result<_,String>` 的形状若会变怎么办** —— 建议加一层保持 throw 语义的包装，
    不改 29 个文件的调用形状（硬判据是行为不变）
  - **[待定] 大整数策略** —— 建议 `sftp_pool` 那批保留 `number` 但写下上限论证
    （文件大小/时间戳，2^53 = 8PB 够用），其余一律 `bigint` 或 `string`
  - **[待定] C04 是否迁完 128 个** —— 建议迁完（部分生成且看不出来 = 比统一手写更坏）
  - **[待定] 生成物进 git 吗** —— 建议进（否则 CI 与本地分叉，且门禁立不住）
- **最近一次计划回看时间**：2026-07-29（Phase A 落盘）
- **自动模式（/loop）**：**全自动**（用户 2026-07-29）。本区**无需任何外部授权**，可连续跑完 C01-C05。
- **备注**：
  - **路线图第 ① 项，是 ③ 与 ④ 的共同地基。**
  - **C01 的验收是变异验收，不是「跑通了」**：删一个 Rust 字段，`tsc` 必须报错。
    不报错 ⇒ 生成物没被真正消费 ⇒ 119 个命令迁过去也是白迁。
  - **本工作区不碰 IR 类型**（`launch-plan.ts` 的 `LaunchPlan`/`LaunchAccount`/`EnvOp` 等）。
    判断：IR 是前端的意图模型，Rust 侧只收渲染好的命令串，**别把它拖过边界**。
    那些类型由 `account-zero` Z02 与 `local-as-remote` L1/L2 改。
  - **冲突协议**：`src/accounts.ts` 让 `account-zero` 先改 · `ci.yml` 让 `gate-integrity` 先改。
  - 这一项做完，「要不要用 Rust GUI 重写前端」的收益少一半——**那时再谈重写才谈得清**。
    参考量：前端生产 **29300 行** TS + 测试 **14128 行**（814 个测试，本轮实测有牙）。
