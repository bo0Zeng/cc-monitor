# 状态 / STATUS — integrate-toolchain（恢复工作的入口，每次先读这里）

- **当前阶段**：P 段执行中（R 段 9/9 + B 段 B01-B04 + L2 已完成并 push）
- **当前功能**：**T03 待贴文本统一组件**（已实现并 push `632ba56`，Phase D 对抗性审计进行中）
- **已完成功能**：
  - **T01 受管工具注册表内核**（六步 + 审计 + 两轮收口；`46abd84`/`b0e0b1a`/`00800e6`/`a91a3dd`/`a6d4b63`）
  - **T02 配置面审计视图**（`5e85334` + 审计闭环 `006b4a9`）
- **下一个功能**：T04 第二步审计闭环 → **T07（设置面板 IA 重构）** → **Phase G**
  · **T06（code-picture 搬进本仓）已搁置**——用户 2026-07-29「code picture 先不做」。
    计划文件保留，但 §0 那份实测有时效（翻转零成本窗口在上游下一个 commit 关闭），重启时先重跑实测
- **阻塞 / 待用户确认**：无
- **最近一次计划回看时间**：2026-07-29（T03 §9 落地记录）

## T04 开工前必须先处理的硬约束（T02 Phase E 记进账本）

`TouchedFile` 的 **`host` 维度 + `origin` 模型要一起做**，现状三处都在等它：
- `~/.cc-bus/` 被本页当**本机**，而 `cc_bus.rs` 是按 `origin` 在**可能是远端**的主机上读它；
- `$DAEMON_PATH` / `$ACCT_ISO_DEST` 只是"配置项占位符"，不是真落点。

**「远端性没有门禁」已如实登记在 `TouchedFile` 的文档里**（原先那条"钉子"测试是同义反复，已删）。
一个 `const destination` 表达不了"按运行期 origin 跨主机"——**别在别的功能里顺手补，会又造一个半成品。**

## T01 结束时留下的**硬约束**（T02 必须处理，不是可选项）

`tool_registry.rs` 目前**零生产消费者**——`TOOLS` 只有自己的测试在读。这是 T01 审计 I2，
我已如实登记并**明写了处置条件**：**T02 收工时若 `TOOLS` 仍无生产消费者，就删掉该模块。**
不拿"以后会用"自动豁免（同一轮我以"只有测试在用"为由删了 `WriteVerdict::is_ok`、
把 `build_online_cmd` 零调用点判为**阻塞**——尺子必须一致）。
存根不靠记忆：`cargo clippy` 现在对该模块 6 个类型各报一条 `never used`，
**T02 做对了这 6 条会自己消失**；做完还剩着，就是没做对。

## 用户 2026-07-28 的排序指令（**已两次细化，以本节为准**）

> 「现在重心或者说先做的还是重构之前 sonnet 写的那些东西，先不要管 code-picture。
> 然后做完后直接接 cc-bus，**因为 cc-bus 有 spawn 即有起会话的功能**。然后下面再是 code-picture。
> 即我们现在的任务排的下面还是治的是「起一个会话」被写死成 15 套实现。」

**关键定性**：cc-bus **不是"又一个要集成的工具"，它是"起会话被写死成 N 套实现"的又一个病灶**——
`cc-spawn` 内部自己 `tmux new-session` + 送环境 + 送任务，是第三/四套起会话实现。
它属于 **unify-launch 主线**，不属于本工作区的集成线。
这也正是 unify-launch 账本里**未达成的那一行**（「cc-spawn 建会话/送环境/送任务改经 ccm」，
当时因「文件在仓外、需用户另行授权」收窄；用户 2026-07-28 已授权 + 要求搬进仓）。

### 三段总排序

1. **R 段（unify-launch 工作区）**：重构 Sonnet 写的 F03-F10 + 信号修复。**当前重心。**
2. **B 段（unify-launch 工作区）**：cc-bus 搬进仓 + `cc-spawn` 收编 + 驾驶舱。仍是「起会话」主线。
3. **P 段（本工作区）**：code-picture 搬进仓 + 受管工具注册表 + 设置面板重构。

### 对本工作区（integrate-toolchain）的影响

- **T05（cc-bus）与 T09（驾驶舱）移出本工作区**，转由 unify-launch 的 B 段承接——
  它们是起会话主线，不是集成主线。
- **T01-T04（注册表内核 + 收编五套机制）推迟到 P 段**。理由（并非拖延，是设计判断）：
  注册表的正当性来自「有 ≥2 个真实工具要求同一套机制」。cc-bus 走主线进来之后，
  code-picture 才是第二个——那时抽象才被真实需求证成。
  提前建等于为假想需求设计（unify-launch R12 踩过的同型错误）。
- 本工作区暂时**只保留** T06（code-picture）/ T07（设置重构）/ T08（skill 管理）+ T01-T04，
  全部等 R 段与 B 段完成后再启动。

## 自动模式（/loop）

用户 2026-07-28 指定：**全自动连续跑**。具体设计决策由本席自行判断，
**每个功能的 Phase D 必须开一个独立的对抗性 agent**（论证"不该这么做/不该做"，
不是复核实现），UX 视角与实现视角都要覆盖。
只在真阻塞 / 计划≠现实 / 同一步≥2 失败 / 全部完成时停。

## 已决策（2026-07-28，用户拍板）

- **D1 code-picture `tests/`**：同批搬。删 `build.rs::check_vendor_freshness`，VENDOR.md 改写为「本体，非镜子」，旧 sibling 仓退役。
- **D2 `code-picture-mcp` 二进制**：**由本仓构建+部署**，进受管工具注册表。cc-monitor 产出第二个二进制，CI 多一个构建目标。
- **D3 cc-bus 基线**：先原样固化盘上版本为仓内基线，**不趁搬家重构**；之后由 cc-monitor 部署覆盖，走「备份→写→读回比对→回滚」。
- **D4 信号修复**：**先做**（G01-G03），且用户追加指令要求 unify-launch 全部遗留一并闭环。
- **D5 设置面板**：**重构信息架构**（按受管工具组织 + 全局配置面审计页）。只碰 `src/settings/`，**不碰 `tabs.ts`**（K4）。
- **D6 驾驶舱**：**同批做**（T09 从 P3 提到 P2）。注意 K7（与「不新增轮询」的张力）与 K8（应顺带完成 `cc-spawn` 收编，而非再造第四套起会话实现）。

## 决策原文（存档）

### D1. code-picture 搬进来 = vendor 语义反转，`tests/` 要不要一起搬
今天 `src-tauri/vendor/code-picture-core`（4378 行）是**上游的镜子**，VENDOR.md 写着
SS-10 铁律「只照上游改，绝不在副本里改出自己的版本」，上游在
`/home/zbl/文档/project/self项目/code-picture`。当初 vendor **刻意没搬 `tests/`**（"测试留上游"）。
反转成本体后，不搬测试 = 接手一个无测试的内核。
→ **建议**：同批搬 `tests/`，删 `build.rs::check_vendor_freshness`（语义已反转，留着误导），
改写 VENDOR.md 为「本体，非镜子」，旧 sibling 仓标记退役。

### D2. `code-picture-mcp` 二进制归谁
它现在在 `~/.local/bin/code-picture-mcp`，源码 `src/mcp.rs` 已在 vendored core 里。
→ **建议**：进注册表，由本仓构建+部署（cc-monitor 产出第二个二进制）。
这样"改代码分析工具"才真的只需要动本仓。

### D3. cc-bus 搬进来后，`~/.claude/skills/cc-bus/` 那份怎么办
盘上有 3 个 `scripts.bak-*` 目录 + `cc-spawn`/`cc-whoami` 各一份 `.bak`——一直在手改、无版本管理。
→ **建议**：先原样固化当前盘上版本为仓内基线（**不趁机重构**），之后由 cc-monitor 部署覆盖；
部署走「备份→写→读回比对→回滚」。

### D4. 前置信号修复（G01-G03）要不要先做
`cargo fmt` 28 处红（CI 唯一阻断性 Rust 门）、分支领先 main 15 commit/13204 行**从未跑过 CI**、
7 套真机 e2e 共 126 条断言**既不在 CI 也不在 npm test**、已确认 3 条伪测试、F10 未 commit。
→ **建议**：先做。工作量小，且集成主线立刻受益。

### D5. 设置面板重构的激进程度（T07）
- (a) 小改：现有分节不动，加一个「受管工具」节 + 一个审计页
- (b) 重构信息架构：按受管工具组织 + 全局配置面审计页（`panel.ts` 826 行 + 5 个手写 section 改成遍历注册表渲染）
→ **建议 (b)**，但**只碰 `src/settings/`，不碰 `tabs.ts`**（那里有 F09 的 1 阻塞+5 重要修复，
重写极易丢，见 K4）。

### D6. cc-monitor × cc-bus 的集成深度（T09）
- (a) 只做装卸管理（cc-bus 是又一个受管工具）
- (b) 驾驶舱：cc-monitor 里看见 bus 上的 agent、派活、读 inbox、`cc-spawn` 图形化
→ (b) 工程量大得多，但正是"多 agent 联调"的落点。**建议先 (a)，把 T09 留作独立后续**。

## loop 停止条件

真阻塞 / 计划≠现实 / 同一步≥2 失败 / 全部完成 → Phase G。

## 上一轮（unify-launch）遗留

- F10（账号用量探测）**已实现未 commit**，工作区改动清单见 `git status`；
  Phase D 审计项已修完（含 3 条伪测试中的 2 条 + `exact_target` 绕过 + `disown` 非 POSIX）。
  欠：`launchPayload` 内容断言、`account_usage.rs` 11 处 fmt、Phase E/F 文档、commit。
- 账本 2 行最终形态未达成：`remote-launch-run.ts` 单一 `runLaunch`（理由经三方独立证伪，
  实际代价约 100 行）、`cc-spawn` 本体收编（本轮 T05 承接）。
- `INVENTORY.md` 冻结在 F01 → 成功标准1 从未验收（G03 承接）。
