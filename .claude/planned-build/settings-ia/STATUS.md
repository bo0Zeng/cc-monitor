# 状态 / STATUS — settings-ia（恢复入口，每次先读这里）

> **2026-07-31 Phase G 修正**：本文件此前**同时挂着两条互斥的「当前阶段」**（一条停在
> Phase A，一条写着收官中），且「已完成功能」只列到 S4b-3a、「下一个」还指着早已做完的 S5。
> 一个按本文件恢复的接手者会**重做已完成的工作**。这正是 `项目审阅报告-PhaseG-2026-07-29.md`
> 点名过的失败模式，在同一仓库内复发 —— 已重写，并把「已完成功能」改成**指向主计划功能表**
> 而不是在这里抄第二份（抄一份就多一个会漂的副本）。

- **当前阶段**：**✅ 全区收官（2026-07-31）**。终账见 `PHASE-G-收尾汇报.md`；主计划 §1 功能表是唯一权威，本文件不复述
- **本区已收尾**：11 个功能全部过 D+E+F 并各自一个 commit；共享面账本终账见 `MASTERPLAN.md §3`
  （7 条里 5 条达最终形态，2 条**如实标黄未改绿**）
- **下一个**：无。Phase G 做完即关 `#47`、本工作区归档

## 自动模式

**全自动**（用户 2026-07-31「全面开搞」）：loop 连续 B→G，只在阻塞 / 需新决策 / 全部完成时停。

## 用户 2026-07-31 拍板的四条

1. **cc-bus 驾驶舱移出设置** → 要（S6；前置：顶栏已 6 个图标，得先想怎么收）
2. **「改动足迹」给顶层槽** → 要（S5）
3. **按多台机器设计** → 是。⚠ **我此前读错了配置**（那是开发机的 e2e 配置、1 台 localhost；用户实际在 Windows）⇒ 对抗 agent 基于「只有 1 台」的论证**作废**
4. **panorama 远端代码图谱** → **做，但前置 = code-picture CLI（现在还没做）** ⇒ `parity_ledger` 里它从 `Undecided` 改判 `ParityDebt`

**另**：用户「**claude 全都是远端的**」⇒ 机器是中心对象，本地是边角。

## 用户 2026-07-31 追加拍板（本区之外，已登记 BACKLOG）

| 事项 | 决定 | 落点 |
|---|---|---|
| 点击某条消息 fork 对话 | **从指定消息分叉**（不是末尾）；入口 = 渲染界面每条消息旁小按钮 | **E52**。第一步是**查证 CC 支不支持从指定 uuid 分叉**，不是设计。查不到就退回末尾 fork 并如实说明，**不驱动 TUI 按键假装做到** |
| 终端 `cc` 开会话归谁管 | **改调 daemon** | **E53**。与 `#77` 同方向。开工前先答：daemonless 机器怎么降级、两道护栏边界怎么重写（E50） |
| v3.4.1 发版 | **等 settings-ia 跑完再发** | 攒着：`--account-trust-zero` 调度修复 · E42 用量 · S0 灰点（需 daemon 重部署）· ccm 新建会话（需 ccm 重部署） |

**顺带**：`#47「设置面板重组」`就是本工作区，收官时该关掉它。

## 阻塞 / 待用户确认

| # | 事项 | 状态 |
|---|---|---|
| 1 | ~~主计划审批~~ | **✅ 2026-07-31 已批准** |
| 2 | ~~`tabs.ts` 是否解禁~~ | **✅ 2026-07-31 用户「解禁」** ⇒ S8/S6 可直接动 `tabs.ts` |
| 4 | ~~一份真实 `/usage` 抓屏文本~~ | **✅ 2026-07-31 用户以两张截图提供，E42 已修完（`18a10fa`）** |
| 3 | **顶栏怎么收** | S6 的前置。顶栏已 6 个 28px 按钮，`main.ts:515` 自陈「顶栏已拥挤」。移 cc-bus 出来 = 第 7 个 |

## 必须做但刻意延后的（不许忘）

| # | 事项 | 排在哪 |
|---|---|---|
| 1 | **`info-icon` 加 `destroy()`** | **必须先于任何页面化**（16 个调用点、无销毁路径，页面级构造/销毁会稳定泄漏） |
| 2 | **6 条文件级源码扫描扩到目录级** | 拆文件的**同一个 commit** 里做，否则等于悄悄拆护栏 |
| 3 | **新的隔离测试要能被变异打红** | 替换 `panel-block-isolation` 那 5 条时，必须用「把 `build()` 求值移出 try」验一次 |

## loop 停止条件

- 撞到需新决策的阻塞（含要动 `tabs.ts`、顶栏收纳方案）→ 停
- 同一步 ≥2 次失败 → 停
- 门禁红且非在途变异 → 停
- 全部完成 → Phase G → 停

## 与其他工作区的关系

- **`account-zero` Z02**：「『基座』一词从 UI 消失」那半卡 `tabs.ts`。本区 S8 做掉**不卡**的四处，`tabs.ts` 那处留给 Z02 或解禁后
- **`local-as-remote`**：L3a 的本机账号枚举**后端已就绪、缺 UI 入口** —— 正好由本区 S4「本机」那一行的「账号」栏接上
- **`integrate-toolchain` T07**：「不拆 `panel.ts`」的结论本区**沿用**

## 时间线

- 2026-07-31 Phase A 落盘 + 用户批准（本文件 + `MASTERPLAN.md`）
