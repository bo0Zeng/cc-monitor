# S4 — 机器详情页：四栏 + 吸收四处 origin 选择器

本区最大一块。**拆成两个 commit**：先接线（S4a），后搬 UI（S4b）。

## §0 要治的病（主计划 §5-4 逐字记录）

> `accounts` / `mcp` / `cc-bus` / `cc-bus-hooks` **各自维护 `this.origin`，`events.ts` 无任何
> origin 广播** ⇒ 四份不同步。

复核属实，且四份的**形状还各不相同**：

| 分节 | 选择器形态 | 状态字段 |
|---|---|---|
| accounts | `<select>` | `origin: string \| null` |
| mcp | 一排按钮（tab 式） | `origin: string \| null` |
| cc-bus | `<select>` | 读 `originSel.value` |
| cc-bus-hooks | `<select>` | 读 `originSel.value` |

用户在「账号」里切到 aya，转头看「MCP」还停在上一台 —— 而这两块讲的都是**同一台机器**。
主计划给的最终形态是：**由「当前在哪台机器页」这个上下文取代**。

## §1 S4a（本 commit）：先立上下文，让四份同步

**做**：新建 `src/settings/machine-context.ts` —— 「当前在看哪台机器」的**单一事实来源**
（`null` = 本机），带订阅。四处选择器改为**读写同一个 store**。

**为什么先做这一步而不是直接上详情页**：这样每一步都独立成立，不留投机代码。

- 立刻可用：今天四块堆在「机器」页上，切一处四处同步 —— 这正是目标语义
  （「你在看哪台机器」），是**用户可感知的修复**，不是为了以后铺路。
- S4b 只需把「谁写这个 store」从四个选择器换成一个页面上下文，**一处改动**。
- 不做投机 API：store 现在就有真实的读者和写者（L1 那次为消灭 dead_code 造 API 的教训）。

**不做**（留 S4b）：详情页四栏、工具条整合、点行进详情页、`resumeCommandRemote`
per-machine 化、`MachineCard` 提取。

## §2 S4a 方案

```ts
// null = 本机；字符串 = 某台远端的 origin
getCurrentMachine(): string | null
setCurrentMachine(origin: string | null): void
subscribeMachine(fn: (origin: string | null) => void): () => void
```

- **不持久化**。「我现在在看哪台」是会话内的导航状态，不是配置。
  存下来会让下次打开设置停在一台可能已被删掉的机器上。
- 通知**只在值真变时**发（同值重复 set 不通知）—— 否则四个订阅者会互相激起
  一串无意义的 reload（每次 reload 都是一次 ssh 往返）。
- 订阅者用 `try/catch` 隔离：一个订阅者抛异常不能让其余三个收不到通知
  （同 `panel.ts::safeBlock` 的思路）。

四处改法一致：选择器 `change` → `setCurrentMachine(v)`；订阅 → 值变了就同步自己的
选择器显示 + reload。**reload 仍然只在值变时发生**，不新增任何轮询。

## §3 S4a DoD

- [x] store 本体 7 条：读/写/订阅/退订/同值不通知/异常隔离/遍历快照。
- [x] 四处都接上了；**跨分节同步有行为测试**（`machine-sync.vitest.ts`：真起两块分节，
      在一块里 dispatch `change`，断言另一块的 `<select>` 跟着变，双向各一条）。
- [x] 同值重复切换**不重复发请求**（数 `invoke` 调用次数，不是看代码）。
- [x] 变异 5 条（**退出码判定**）：同值也广播 / 异常不隔离 / hooks 不跟随 /
      cc-bus 不跟随 / 本机时乱选一台 —— 逐条改坏见红、还原见绿。
- [x] 门禁全绿：vitest 941 / tsc / build / coverage / lint 零告警。

## §4 S4b-1（已完成）：每台机器一页 + 点行进详情页

S4b 太大，按 loop 授权再拆。**本轮 = S4b-1**：路由器支持动态增删与一层子项，
每台机器开一页（编辑表单搬过去），列表里只留一行（名字 + 状态 + 点进去）。

- 路由器加 `parentId`（**只支持一层子项** —— 够用，且不必去想「孙子项怎么缩进 /
  折叠状态存哪」这些本轮没有的问题）与 `removeRoute`。
  **注销当前页时会切到父页**，否则用户停在一个已被摘掉的页面上看空白。
- `RemoteSection` 收一个 `MachinePagesHost`（开页 / 收页 / 跳过去三件事）。
  **抽成接口而不是直接传 router**：分节不该知道路由器长什么样，也让它在没有路由器的
  场合照常工作 —— **不传就是老形态**，既有单测与任何不带路由器的宿主零影响。
- 状态条从卡片 legend **移到列表行**（§2.3 里状态就是列表的一列；详情页上用户看的是
  动作按钮本身的结果，不需要再来一份缓存结论）。本机行与机器行共用同一份渲染
  （`renderStatusCells`），免得两处慢慢长歪。
- 详情页上的卡片去掉折叠箭头与删除按钮（一页只有它，没有可折的必要；
  删除入口在列表行上，那里才看得见「删的是哪一台」）。

## §4bis S4b-2（已完成）：四块分节跟着当前机器页走 + 本机也有一页

**S4a 那个半截状态就此消掉**：那时三块的下拉只列远端、表示不了本机，收到 `null`
只能原地不动；现在**本机页上它们压根不出现**，「表示不了」这件事也就不存在了。

- 路由器加 `onNavigate`（切页有两个入口 —— 点导航项 / 点列表行，只有路由器这一层
  两条都覆盖得到）与 `pageContentOf`。`navigate` 同页早退，**不重复通知**
  （订阅者要做搬 DOM 这类有代价的事）。
- 本机也注册成一页（`LOCAL_MACHINE_PAGE_ID`），列表第一行点得进去 ——
  §40「本地 = 不走 ssh 的远端」不只是说法，它在导航里就该和别的机器长得一样。
- 四块 per-machine 分节收进一个 slot，按当前机器页整块搬 DOM。
  **单例 + 搬 DOM，不给每台机器各起一份**：起 N 份意味着 N 倍构造开销与 N 份互不相干
  的缓存，而同一时刻只有一台机器的页面可见。
- 每块标注 `appliesTo`（依据主计划 §2.4 那张表）：
  「账号」= 远端（本机的多账号入口是 L3a 的欠账）；
  「终端集成」= 本机（PowerShell `$PROFILE`；远端的对应物是详情页上的「装/卸 ccm」）；
  「MCP」「cc-bus 钩子」= 两边都有意义。

### ★ 审计逮到一个我自己打破的隔离

`RemoteSection` 是唯一活的同步 throw 宿主（T07 审计阻塞 1）。它挂掉 ⇒ 没有任何机器页
被注册 ⇒ slot 无处安放 ⇒ **那四块会一起从界面上消失**。那就是「一块坏，五块没」，
把 T07 好不容易建立的隔离又打破了。**是真造它抛才发现的**（既有的 block-isolation
测试当场变红）。已给 slot 一个**兜底落点**（先挂列表页），最坏情况只是它们位置不理想，
但都还在、都能用。并补了一条测试钉住这个兜底。

### §5-4 的 6 条文件级守卫：本轮**不需要动**

那条注意事项针对的是**跨文件搬代码**。本轮只改了 DOM 挂载位置，四个分节仍在各自的
文件里，守卫的范围一字未缩。真正要处理它的是 `MachineCard` 提取 —— **那件事仍未做**。

### S4b-3a（已完成）：`resumeCommandRemote` per-machine 化

主计划 §5-1 记的建模缺陷：resume 命令是**全局单值**，而「装 ccm 助手」是**每台机器
一个按钮** ⇒ A 机装了 ccm、B 机没装时，数据模型根本表达不出来。于是「装完 ccm 却忘了
改 resume 命令」是个**结构性陷阱**（那两处此前还隔着两个顶层组）。

- `RemoteHostConfig` 加 `resumeCommand`；**空 = 沿用全局默认**。
  刻意**不做数据迁移** —— 迁移会静默改写用户已有的设置；回退让「没填过 = 行为一字不变」。
- `pickResumeCommand`（纯函数）+ `resolveResumeCommand`（IO 包装）。
  6 个消费点全部改为按 origin 解析 —— 它们**本来就手握 origin**，只是没拿它查值。
- UI 输入框放在「装/卸 ccm」按钮**紧邻处**，这正是 §5-1 说的那两处不该隔开。

**S1 那套编译期穷尽检查当场兑现了价值**：加字段后 `tsc` 一次性点出全部 6 个构造点
（含 3 个测试夹具），不用靠人去找。

### S4b-3b-1（已完成）：工具条整合 + tmux 命名反转

**工具条**（主计划 §2.3 逐字给的顺序：+添加 · ssh config 导入 · 批量导入 · 端口转发 ·
[x] 启用远端模式）。此前这几个散在列表**上下两侧**，空列表提示还得写「点**下方**添加机器，
或从**上方**下拉导入」——一句提示同时指两个方向，本身就是布局在报警。
归拢判据与 §2.1 同源：**它们改的都不是某一台机器，而是这份列表本身**。
「启用远端模式」刻意**留在条上不收折叠**：关着时整个列表都不生效，藏起来会让人对着
一列配好的机器纳闷为什么没连上（`INVARIANTS §12`）。

**命名反转 `cc-<X>` → `<X>-cc`**（用户 2026-07-31 追加）。三处产名 + 一处识别：
- `shared/ccm::derive_tmux_name` 与 `src/remote-launch.ts::deriveTmuxName`（跨语言双写点，
  真值对拍钉着，见 E49）→ `<safe>-cc` / `session-cc`。
- `pickFreshTmuxName` → `<sid8>-cc`，撞名 `<sid8>-cc-2`（「第几个」留在末段）。
- `src-tauri/src/tmux.rs::is_ccm_tmux_name`：**加**后缀形态，**绝不删老前缀** ——
  它自己的头注就写着「F02 之前的老 `cc-*` 会话没有 `@ccm_sid`，只靠这条前缀判据仍必须可
  kill/send-keys」。删了等于把用户**正在跑的**会话变成 issue #76 那种「失管会话」。
  两个方向都有测试（丢新后缀 / 丢老前缀，各自变异见红）。

**没做血洗式改名**：`cc-bus` / `cc-monitor` / `cc-acct` 是产品名，`cc-badge` / `cc-modal` /
`cc-stat` 是 CSS 类名 —— 全表扫一遍后只动了真正的会话名夹具（实际失败面只有 1 个文件 6 条，
远小于我最初的估计）。

### S4b-3b-2（已完成）：机器详情页四栏

远端机器页拆成横向四栏 **连接 / 组件 / 账号 / 工具**（主计划 §2.3、§2.4）。

- **复用 `SettingsRouter`（加 `orientation: "horizontal"` + `hidePageHeader`），不另造 tab 原语。**
  分栏要的「同一时刻只有一栏可见 + aria + 方向键 + 不重复注册」与左侧导航**逐条相同**，
  差的只是排布方向与要不要页头。另造等于把这套逻辑抄第二遍，然后两份各自漂。
- `MachineCard` 的 body 拆成 `connectionPart` / `componentsPart` 两块，经
  `MachinePagesHost.addMachinePage(..., parts)` 交给 panel；panel 拿它们当前两栏，
  「账号 / 工具」两栏接住 per-machine 那几块单例分节（按每块的 `tab` 归位）。
- **顺带纠正 S4b-3a 的一处不准确**：那轮我把 resume 命令插在 daemonless 之后，
  commit 却说它「放在装/卸 ccm 按钮紧邻处」—— 实际隔着 `installInfo` 等约 120 行。
  §5-1 要的正是这两者相邻（装完 ccm 就该顺手改 resume 命令）。这次分栏的分界线
  **就定在 resume 命令那一行**，它现在真的和装/卸 ccm 在同一栏、紧挨着。
- 本机页**不拆栏**（它没有卡片、不带 `parts`），per-machine 分节仍整块搬 —— 兜底落点
  （`RemoteSection` 挂掉时那几块留在列表页）也走这条路，未被破坏。

**变异 7 条**（退出码判定）：① 字段全跑进组件栏 · ② resume 命令漂回字段区 ·
③ 不交出 parts · ④ 远端机器页不分栏 · ⑤ 少一栏 · ⑥ 分节不进栏 · ⑦ 账号塞进工具栏。
其中 ④⑥⑦ **首轮未覆盖**（panel 的 stub 只注册本机页，走不到分栏那条路），
已让 stub 补注册一台带 `parts` 的远端机器页后再验红。

### S4b-3b-3（已完成）：`MachineCard` 提取 + 守卫扩到**组件**级

`remote-section.ts` 2110 行 → `remote-section.ts` 1029 + `machine-card.ts` 1109。

**我先质疑过这次搬迁值不值**（S4b-1/2 已让 `MachineCard` 在功能上成为详情页主体，
剩下的只是文件位置；而本仓的规矩是「拆分看缺陷不看行数」）。**用户拍板要搬**，照做。
搬完的实际收益：文件边界追上了早已成型的职责边界，两个类不再混住。

**三个契约逐条保住，各有变异见红**：`persistedKey`（卡片身份，S1）·
`parts()`（连接/组件两块，详情页分栏的接口，S4b-3b-2）· `setPageMode()`。

#### 守卫扩容：按**组件**而不是按目录

主计划 §5-4 说「扩到目录级」，实做时改成**组件级**，理由写清：
那些不变量是**针对某个组件**的 —— 「不许写盘」对 `cc-bus-hooks` 成立，
对 `accounts-section` / `remote-section` 根本不成立（它们本来就要写配置）。
一刀切扫整个 `src/settings/` 会得到一堆假红，然后守卫被放宽或删掉 —— **比范围缩小更糟**。

新增 `src/test-support/component-sources.ts`：按前缀收集组件的全部生产源文件并剥注释。
`cc-bus` 前缀今天真实覆盖 2 个文件（有测试钉住 `length > 1` —— 否则「扩容」可能只是
换了个写法、仍只看一个文件）。各守卫加反向自检（扫到 0 个文件即红）。

**局限如实说**：拆出去的文件若不带该前缀（叫 `hooks-diag.ts`）仍会漏。没有纯静态方案
能完全解决「代码搬到哪儿了」；这一条堵的是最常见的那种漂移。

详情页四栏的**分栏**本身（现在是一页平铺）· 工具条整合 ·
`MachineCard` 提取（连带把 §5-4 那 6 条文件级守卫扩到目录级）。

**已知的三个坑，先记下**：

1. `persistedKey`（S1 加的**卡片身份**）随 `MachineCard` 一起搬，它不是渲染细节。
2. S3 的本机行**刻意不进 `this.cards`**，这条边界不能破。
3. 主计划 §5-4：**6 条文件级源码扫描守卫**（`cc-bus-section` 无定时器 /
   `cc-bus-hooks` 绝无写入 / `config-surface` 不得写 / `accounts-section` 零写盘 …）
   —— 搬走被守的代码后它们**仍会全绿，但守的范围缩小了**。拆文件必须同步扩到目录级。

## §5 代码审计结果（Phase D，S4a）

**做法如实记**：未开并行审计 agent，主线程自审 + 变异兜底（退出码判定，不再 grep 文本）。

### 复核时发现四块的病比记录的还重一档

主计划记的是「四份不同步」。实际查下来，**`cc-bus-hooks` 连 `change` 监听都没有** ——
它的下拉只在用户点「检查」时被读一次。也就是说那一块的「当前机器」从来就不是一个状态，
只是一个待读的 DOM 值。这解释了为什么四块连「怎么算切换了」都对不齐。

### 三个刻意的设计决定

1. **同值不通知**。四个订阅者收到通知就 reload，而每次 reload 是一次 ssh 往返。
   同值也广播的话，四块之间会互相激起一串无意义的往返 —— 那就是变相轮询，撞 §1-2。
   有测试直接数 `invoke` 次数。
2. **订阅者异常隔离**。一块抛异常不能让其余三块收不到通知。这里的后果比白屏更隐蔽：
   其余分节会**停在上一台机器**上，界面看不出异常，用户以为在看 aya 其实在看 nano。
3. **切换不自动发诊断**（`cc-bus-hooks`）。那一块的既有语义就是「点了才发」
   （只读诊断，不替用户改 `~/.claude/settings.json`）。跟着切换自动发就成了变相轮询。

### 一个如实登记的半截状态

`accounts` / `cc-bus` / `cc-bus-hooks` 的下拉**只列远端**，表示不了「本机」。
收到 `null` 时它们**原地不动**，不乱选一台 —— 乱选比不动更糟（用户以为在看本机，
其实在对某台远端下命令）。这是半截状态，**S4b 从根上解决**：本机那一页压根不会包含
只对远端有意义的分节。已写成测试（连同「乱选一台」的变异）。

## §6 工程审计结果（Phase E，S4a）

- **没有投机 API**：store 现在就有真实的四个读者和四个写者，本轮即产生用户可感知的修复
  （在一处切机器，另外几处跟着走）。S4b 只需把「谁写这个 store」从四个选择器换成
  一个页面上下文，**一处改动**。
- **没碰任何 section 的业务逻辑**，只把「谁持有当前机器」这件事收口。
- **S4b 的三个坑已写进 §4**（`persistedKey` 要跟着搬 / 本机行不进 `this.cards` /
  6 条文件级源码扫描守卫搬完要扩到目录级）。

## §7 签收（S4a）

- [x] 过代码审计（自审 + 5 条变异）
- [x] 过工程审计
- [x] 主计划已更新
- [x] **S4b-1 完成**（每台机器一页 + 点行进详情页 + 路由器动态增删/子项）
- [x] **S4b-2 完成**（四块分节跟着机器页走 + 本机也有一页；S4a 的半截状态已消）
- [x] **S4b-3a 完成**（`resumeCommandRemote` per-machine 化）
- [x] **S4b-3b 完成**（三步：`cdeeeec` 分栏骨架 · `1471983` 详情页横向四栏 · `4e489df` `MachineCard`
      提取 2110 → 1029 + 1109 并把源码守卫从「单文件」扩到「组件」）

> **2026-07-31 Phase G 更正**：上面这一行此前一直写着「**未开工**」，而三个 commit 早已合入、
> 主计划也已把 S4 记为完成。**签收链条断了一环** —— feature 文件是最细一层的验收凭据，
> 它与主计划反向时，任何「按 feature 文件复核」的人都会得出错误结论。

## §8 S4b-1 审计与变异

**自审逮到一个自己引入的静默失效**：`removeCard` 里用 `CSS.escape` 拼选择器找列表行 ——
`CSS` 在 jsdom 里不一定有，于是那句直接抛，**`save()` 再也没被调到**，删除功能整个哑掉。
表现是 5 条既有测试同时变红（幸好它们在）。已改成扫 `dataset`：既不依赖宿主实现，
也顺带避开「机器名含任意字符、拼进选择器会炸」这个更根本的问题。

**变异 9 条（退出码判定）**：① 子项跑到导航末尾 · ② 子项没有缩进标记 ·
③ 注销当前页后停在空白上 · ④ 不给机器开页 · ⑤ 删机器不收页 · ⑥ 点机器名不跳页 ·
⑦ 详情页仍带折叠/删除 · ⑧ 重建列表不收旧页（**首轮未覆盖，已补测试**）·
⑨（router 既有 9 条一并复跑）。逐条改坏见红、还原见绿。

## §9 S4b-2 变异（退出码判定）

① 本机页不接管那四块 · ② 本机页也显示「账号」· ③ 去掉兜底落点（一块坏五块没）·
④ 同页也重复通知（**首轮未覆盖，已补测试**）· ⑤ 不给本机开页 · ⑥ 本机行点不进去。
逐条改坏见红、还原见绿。

## §10 S4b-3a 变异（退出码判定）

① per-machine 值被忽略 · ② 空串不回退全局 · ③ 序列化清单漏 `resumeCommand` ·
④ 读盘丢 `resumeCommand`（**首轮未覆盖，已补 write→read 往返测试** —— `jump` 与
`daemonless` 都栽在这同一处：S1 的编译期检查挡住了「漏写清单」，却挡不住
「读盘那侧忘了解析」）。逐条改坏见红、还原见绿。
