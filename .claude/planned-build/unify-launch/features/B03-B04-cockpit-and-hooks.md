# 功能计划 — B03 驾驶舱 + B04 settings.json 钩子诊断

> B03 依赖 B02（`cc-spawn` 收编）。B04 与两者正交，写在一起是因为它们同属"cc-monitor 认识 cc-bus"这件事。

## 0. B03 的两条硬约束（先说清楚，它们决定了方案形状）

**约束一：不新增轮询（红线）。** cc-bus 的状态全在 aya 本机 `~/.cc-bus/`，而 cc-monitor 跑在
Windows、只能经 SSH 看。两条备选：复用 daemon 既有 inotify watcher（**违反 daemon 零改红线**，
且要新增协议帧）；或**按需刷新**。→ 取按需刷新。

**已有现成范式，照抄不另发明**：`src-tauri/src/mcp.rs::read_remote_mcp_servers`
——用户在设置页点开 MCP 分节时才 `connect_and_exec_cmd` 一条**定值** `cat` 命令读远端 dotfile，
宽容解析（缺/坏 → None），30s 超时 + 32MB 上限，大解析进 `spawn_blocking`。
前端 `settings/mcp-section.ts:300` 在**打开分节**时 invoke 一次。B03 逐条对齐这个形状。
（同 F10 用量探针的懒加载精神：重操作只在用户真的要看时才做。）

**约束二：`cc-spawn` 图形化必须建立在 B02 之上。** 否则等于在 cc-monitor 侧再造第四套起会话
实现——亲手制造本工作区刚消灭的病（账本 K8）。B02 没落地前，B03 只做**只读**部分。

## 1. 盘面实测：这些状态文件**不干净**，读它们必须防御性解析

2026-07-28 直接看了 `~/.cc-bus/`，发现三类脏数据。**这不是假想风险，是现状**：

| 现象 | 实例 | 对 B03 的含义 |
|---|---|---|
| **陈旧登记** | `agents.tsv` 里最早的条目是 `2026-07-18`（10 天前），进程早不在了 | 登记 ≠ 在线。必须与"进程是否真在"分开呈现，否则驾驶舱会列一堆幽灵 |
| **TSV 被内嵌换行破坏** | `spawned.tsv` 有一行是 `x_y_cc\t/tmp/tmp.o6LGcLq9Qq/x⏎y\t...`——目录名里含换行，把一条记录劈成两行 | **按行 split 会解析出畸形记录**。必须容忍并跳过，不能让一条坏数据把整个面板搞崩 |
| **垃圾 id** | `inbox/` 里有 `--help.jsonl`、`282.jsonl`——有人敲了 `cc-send --help`，把 `--help` 当成了收件人 | id 不可信，渲染前要过一遍字符集；也说明 cc-bus 自己的入参校验有洞（**记录，但不在本功能修**） |

→ **DoD 里必须有一条"喂脏数据不崩"的测试**，用上面三个真实样本当输入。

## 2. B03 DoD（分两批，第二批依赖 B02）

### 批一：只读驾驶舱（不依赖 B02，可先做）
- [ ] 新增 Rust IPC `read_cc_bus_state(origin)`——**照 `read_remote_mcp_servers` 的形状**：
      定值命令（零用户输入拼接 → 零注入面）、超时 + 大小上限、宽容解析、只读。
      一次读回 `agents.tsv` + `spawned.tsv`（一条命令 `cat` 两个文件，用分隔标记切开，省一次往返）。
- [ ] 防御性解析：字段数不足/id 字符集非法/时间戳解析失败 → **跳过该行并计数**，不抛。
      解析结果带一个 `skipped: number`，UI 如实显示"N 条无法解析"而不是假装干净。
- [ ] UI：设置页新增「cc-bus」分节，**打开分节时才 invoke 一次**（不在应用启动时预取）。
      列出：agent id / 工作目录 / 登记时间 / 是否 spawn 的（来自 `spawned.tsv`）。
- [ ] **"登记"与"在线"分开呈现**：`agents.tsv` 只证明它登记过。要判在线需另查 tmux
      （`tmux has-session`），那是**第二次往返**——放在用户点某一行的"检查"上，不默认全量查。
- [ ] 手动"刷新"按钮（同 F10 用量探针的 `force`）。**无 `setInterval`、无后台定时任务。**

### 批二：派活 / 读 inbox / 图形化 spawn（**必须在 B02 之后**）
- [ ] 给某个 agent 发消息 = 远端跑 `cc-send <id> "<text>"`（经既有 launch 路径，不另造）
- [ ] 读某个 agent 的 inbox = `cat ~/.cc-bus/inbox/<id>.jsonl`，同样定值命令 + id 过字符集
- [ ] 图形化 spawn = 调收编后的 `cc-spawn`（它内部已改经 `ccm`）——**不在 cc-monitor 侧重写起会话**

**不做什么**：
- **不**在 cc-monitor 侧实现 cc-bus 的投递管线（ACL/限流/去重/灭环）——那是 `cc-bus-lib.sh` 的活
- **不**做实时消息流（要么轮询要么长连，两条都违反约束一）
- **不**替用户装/改 cc-bus 的钩子（那是 B04，且只读+生成）

## 3. B04 DoD：settings.json 钩子的读 + 诊断 + 生成待贴文本

用户 2026-07-28 定调：**不写** `~/.claude/settings.json`。cc-bus 自己的安装脚本第 3 行
同样拒绝改它（"只做可逆的本地安装：不改全局 settings.json、不 systemctl"）——**两边一致，不是我的保守**。

- [ ] 读 `~/.claude/settings.json` 的 `hooks` 段（远端与本机两侧；解析失败宽容降级）
- [ ] 诊断 cc-bus 需要的两个钩子是否装好：`SessionStart` → `cc-register`、`Stop` → `cc-bus-stop-hook`
- [ ] 三态如实呈现：**已装 / 未装 / 装了但指向别的路径**（第三态最容易被误报成"已装"）
- [ ] 生成待贴 JSON 片段 + 一键复制（同 F08 别名生成器的既有形状）
- [ ] **绝不写入**。文案里说清"请自己粘贴"，并说明为什么不代劳（这是共享的全局配置，
      cc-monitor 不该单方面改它）

## 4. 测试策略

- Rust 侧解析器纯函数化后单测：喂 §1 那三个**真实脏样本**（内嵌换行行 / `--help` 这种垃圾 id /
  10 天前的陈旧条目），断言不 panic、坏行被跳过并计数、好行仍被解出。
- 定值命令的零注入面：断言命令串里不含任何来自 `origin` 之外的插值
  （同 `mcp.rs` 那条 `CMD` 常量的形状）。
- UI 侧：断言**不存在** `setInterval`/后台定时器（守红线，用 grep 断言 + code review）。
- B04：三态各一条测试，重点是第三态（指向别的路径）不能被误判成已装。

## 5. 风险

| # | 风险 | 缓解 |
|---|---|---|
| K1 | 驾驶舱列出一堆幽灵 agent（登记≠在线） | §2 批一显式分开"登记/在线"，在线是按需第二次往返 |
| K2 | 脏数据把面板搞崩 | §1 三个真实样本进测试；解析器只跳过不抛 |
| K3 | 为了"实时"偷偷加轮询 | DoD 显式禁；测试里 grep 断言无定时器 |
| K4 | 批二先于 B02 做 → 再造第四套起会话实现 | 批二明确标注依赖 B02（账本 K8） |
| K5 | 本机住着正在跑的 cc-bus 实例，测试可能干扰它 | 一律隔离 `-L` socket + 临时 `CC_BUS_HOME`；**绝不碰默认 socket 与真实 `~/.cc-bus/`** |

## 6. 代码审计结果（Phase D）
（待填）

## 7. 工程审计结果（Phase E）
（待填）

## 8. 签收
- [ ] 通过代码审计（无阻塞项）
- [ ] 通过工程审计
- [ ] 主计划已据此更新（含变更记录）

---

## 9. B03 批一落地记录（2026-07-28）

**后端**（`src-tauri/src/cc_bus.rs`，commit `7556109`）：纯解析层 + 只读 IPC
`read_cc_bus_state(origin)`，形状逐条照抄 `mcp.rs::fetch_remote_claude_json`
（定值命令常量 → 零注入面、30s 超时、32MB 上限、宽容解析、`spawn_blocking`）。
一条 `cat` 读回两文件用 `CC_BUS_SPLIT_MARKER` 切开，省一次往返。
另加 `check_cc_bus_agent_online(origin, id)`——**刻意的第二次往返**，只查用户点的那一行。
id 进命令串前必须过 `is_valid_bus_id`（盘上真有 `--help` 这种 id），目标用 `=<名>:` 精确形态。

**前端**（`src/settings/cc-bus-section.ts` + `.vitest.ts`）：挂进 `panel.ts` 的集成组。

**与 §2 批一措辞的一处偏离（更严格，非放宽）**：原写「打开分节时才 invoke 一次」。
`CollapsibleGroup` 没有展开回调，而为一个消费者去改这个共享 UI 原语正是 R12/R15 拒绝的
"为假想需求建抽象"。改成**用户点「读取」才发请求**——一次 30s 超时的远端往返，显式触发
比"展开即偷偷发"更诚实，且天然满足"启动时不预取"。

**复用而非新增**：列远端直接用既有 `list_remote_mcp_origins`（它其实是通用的"列远端配置
标签"，名字带 mcp 只是历史）。为同一件事再加一条 IPC 是无谓重复。

**变异验收**（11 项 DOM 测试，每条先 diff 打出实际改动行再判色）：默认就说"在线" → 红 ·
不显示 skipped → 红 · 查失败当成不在线 → 红 · 偷偷加 setInterval → 红 ·
预取 → **第一次变异存活**，因为插在构造函数里时 `originSel.value` 还是空、`reload()`
撞上 `if (!origin) return` 空转（失效模式②：变异语义无效）；换到可达点（`loadOrigins`
末尾）后即红。**这条记下来：变异存活先判它可不可达，别急着改测试。**

## 10. B02 审计留下、判为「登记不改」的三条

| # | 事项 | 为什么现在不改 |
|---|---|---|
| L1 | `--tmux-size` 越界值（`0x0` / `99999x99999` / `4294967296x50`）能过校验，真跑时 `tmux new-session` 失败但 stderr 被 `2>/dev/null`（为幂等建闸吞 duplicate session）一起吞掉 → **诊断黑洞** | 注入面已关严（`x50`/`220x`/`1x2x3`/`$(id)x50` 全被拒）；cc-spawn 恒传 `220x50`。只影响 ccm 作为公共 CLI 的体验。修法是加上下界或只透出非 duplicate 的 stderr |
| L2 | spawn 出的 agent 现在会落 manifest 的 `isDefault` 账号（旧实现是 `CFGDIR=<unset>`） | 方向上是改进（cc-acct-iso 之后基座常没凭据），但**是可观测的行为变更**，且会消耗默认号额度。此处登记，B03 批二做图形化 spawn 时给用户显式选择 |
| L3 | 「上总线」时序从"紧跟 send-keys"变成"排在信任框轮询之后"，最坏晚 3s | 消息落 inbox 不丢，只影响即时性；且在阻塞-1 修好后这条路径本就少见 |

## 11. B03 批二待办（依赖已满足，B02 已完成）

- 派活 `cc-send <id> "<text>"`、读 inbox `cat ~/.cc-bus/inbox/<id>.jsonl`（id 过同一套校验）
- 图形化 spawn → 调收编后的 `cc-spawn`（**不在 cc-monitor 侧重写起会话**）
- **补回 `inbox_id_from_filename`**：批一因它只有测试在用（dead_code 告警）按 R12/R15 删了，
  批二真读 inbox 时连同测试一起加回
- **这里才是成功标准② 的第二次架构验收**：TS 侧第一次真需要发新维度时才加
  `LaunchDimension` + `requiredCaps`；若发现仍要改 9 个签名/6 个调用点 → R03 回炉

## 12. B03 批二落地记录（2026-07-28）+ **成功标准② 的诚实结论**

**做了什么**：后端加 `read_cc_bus_inbox` / `cc_bus_send` / `cc_bus_spawn`；UI 加每行的
「收件箱」「发送」与一个「派生新 agent」表单（**两步确认**——起真 agent 会消耗额度）。
图形化 spawn 调的是收编后的 `cc-spawn`，**cc-monitor 侧不碰建会话逻辑**（账本 K8）。

### 成功标准② 在这里**同样没有验收场地**——这是结论，不是借口

计划里我两次预判它会落地：先说在 B02，实测发现 B02 是纯 shell（`grep CC_BUS_ID src/
--include=*.ts` 零命中）后改口说「挪到 B03 批二，那时 cc-monitor 图形化 spawn 会第一次
真的需要发新维度」。**批二做完，这个预判也不成立。**

实测依据：
- `tryRenderCli` 全仓**只有一个真实调用点**（`src/remote-launch-run.ts:46`），
  服务的是「把会话开进 cc-monitor 的终端标签页」。
- 图形化 spawn 是 **fire-and-forget 的远端 exec**：不开标签页、不要 pane、不回填身份，
  自然不经 IR。`src/settings/cc-bus-section.ts` 因此**零引用 launch 模块**（有测试守着）。

所以「加一个维度 → 零改 builder/renderer/调用点」这条标准，在 B 段全程**找不到第二个
真实消费者**。诚实的处置是三选一，我选第三条：

1. ~~在 B02/B03 造一个用不上的 TS 维度来"验收"~~ —— 那是自己给自己出空题，且正是
   R12/R15 一路拒绝的「为假想消费者建抽象」。
2. ~~把标准② 悄悄划掉~~ —— 它是主计划四条成功标准之一，删掉等于粉饰。
3. **如实记为「一次验收（F07）+ 一次未获场地」**，并写清它下一次可能的落点：
   真正会新增 TS 维度的是**P 段**（code-picture / managed-tool registry 要给远端工具
   传新修饰），到那时再验；若 P 段也不需要，那说明这条标准本身设得过窄——它假定了
   「新维度会持续出现」，而实际演进方向是 **shell 侧长能力**（`--detach`/`--tmux-size`
   都是 shell-only），TS 的 IR 反而稳定了。**IR 稳定是好事，不该被记成"验收失败"。**

→ **行动**：Phase G 的终账里，成功标准② 记为「F07 一次通过；B 段无场地（已实证）；
留待 P 段，若仍无则改写标准措辞」。**不伪造第二次验收。**

### 顺带：`inbox_id_from_filename` 批二也没补回

计划 §11 写「批二真读 inbox 时连测试一起加回」。实测批二**按 id 直接 `cat`**，不列目录，
所以仍然没有消费者。继续不加——理由同批一（dead_code 告警就是信号）。真要列 inbox
目录（比如「谁有未读」）时再加。

### 变异验收

后端 3 条 / 前端 5 条全部由绿转红。其中两条是**修好了我自己的门禁缺口**：
- `build_send_cmd` 去掉 id 校验，上一轮**存活**——因为断言打在 `is_valid_bus_id` 这个
  **谓词**上而非调用点（失效模式③ 门禁太窄）。把「校验+拼串」抽成 `build_*_cmd` 纯函数、
  断言改打这些函数之后，同一变异立刻红。**async 命令要 SSH 连接没法单测，就把该测的
  纯逻辑摘出来，而不是降低断言强度。**
- `shell_quote` 那条变异第一次"通过"是**假信号**：Python 语法错导致变异根本没写进文件。
  改用 heredoc 落文件后重跑，6 条断言红。**变异前后必须 diff 确认真改到了。**

## 13. B04 落地记录（2026-07-28）

**后端** `src-tauri/src/hooks_diag.rs`（commit `c2dea4d`）+ **UI** `src/settings/cc-bus-hooks-section.ts`。
挂进 `panel.ts` 集成组，标题「cc-bus 钩子」。**全程只读，零写入路径**（两侧各有结构性守卫）。

### 计划的「三态」订正为四态

计划 §3 写「已装 / 未装 / 装了但指向别的路径」，并提醒第三态"最容易被误报成已装"。
实测发现**方向反了**，且三态不够用——见 `features/B04-hook-states-from-real-disk.md`：
用户盘上装的是 `"$HOME/.local/bin/cc-register" >/dev/null 2>&1 || true`，规范片段是裸
`cc-register …`，**功能等价但字符串不等**。按等值比较会把这套**完全正确的安装**报成第三态。

→ 「已装」必须拆开才说得清哪种才是问题：
`NotInstalled` / `InstalledViaPath`（裸命令走 PATH）/ `InstalledAtPath`（显式路径**且存在**
——**用户当前状态，不是问题**）/ `PathMissing`（显式路径但**不存在**——**真正的第三态**）。

### 不代劳写入的理由要讲给用户听

文案不只说"请手动粘贴"，而是说清：`~/.claude/settings.json` 是**共享的全局配置**，
用户自己的编辑器、别的工具、别的 skill 都可能在动它，单方面改有可能覆盖掉他人的改动；
且 cc-bus 自己的安装脚本同样拒绝碰它。复制成功的提示里额外强调「**合并**进 hooks 段，
不是整份覆盖」——那里可能还有别的工具的钩子。

### 本轮修掉的两个自己的缺陷

1. **结构性守卫 `this_module_never_writes` 第一版是坏的**：用 `include_str!` 扫整个文件，
   而禁用词清单本身就是测试源码里的字面量 → **扫到了自己、必红**。改成只扫
   `#[cfg(test)]` 之前的部分（标记用 `concat!` 拼接免自匹配），并加**反向自检**断言
   「守卫真的看到了代码、不是切成空串在空转」——正是 B01 审计教的那一课，这次自己先接住。
2. **`invoke` resolve 成 `undefined` 会让分节崩掉**（真 bug，测试跑出来的未捕获 rejection）。
   原代码只 `catch` 不校验形状，`origins.length` 直接抛。本工作区一路在守「脏数据不能把
   面板搞崩」，**对自己的 IPC 返回值同样适用**。`cc-bus-section.ts` 有同一处模式，一并修。

### 变异验收

后端 5 条（不剥引号 / 存在的显式路径也判 PathMissing / 显式路径当裸命令 / 不比对程序名 /
不优先返回能用的那条）全红。
前端 5 条：PathMissing 说成已装 → 红 · InstalledAtPath 报成有问题 → 红 · 吞掉 note → 红 ·
回退成不校验形状 → 红 · **预取远端第一次变异存活**——插在构造函数里时 `originSel.value`
还是空、`checkRemote` 撞上 `if (!origin) return` 空转（失效模式②，与 B03 那次同型）；
换到可达点（`loadOrigins` 末尾）后即红。**变异存活先判可达性，别急着改测试。**

## 14. B03 代码审计结果（Phase D，2026-07-28）

独立对抗性审计（95 分钟，73 次工具调用）。**两个阻塞项 + 六个重要项，我独立复核后全部认账。**
审计诚实标注了它有一批验证因权限分类器中途拦截而**未能执行**（逐条列了哪些结论是静态推演），
这点按其标注的成色采信。

### 阻塞项

| # | 发现 | 我的独立复核 | 处置 |
|---|---|---|---|
| 阻塞-1 | 头条计数骗人：`其中 spawn 的` 数的是 spawned **全集**，而 `render()` 只遍历 `agents` → **spawned-only 的条目既被计入又不渲染** | **属实**。真实盘面 agents=37 / spawned=7 / **交集仅 2** → 面板会说"其中 spawn 的 7 个"而只有 2 行带标签，另外 **5 个连同工作目录一行都不显示** | **已修**：spawned-only 条目**并进列表**并标注「未在 agents.tsv 登记」；计数改为交集 + 另计未登记数。取"并进列表"而非"只改计数"——那 5 条有 dir 和时间，信息量比 agents.tsv 还大 |
| 阻塞-2 | `check_cc_bus_agent_online` **没调** `build_online_cmd`，内联复制了一份校验 → 前者是**零生产调用点的死代码**，而真正在跑的校验**零测试覆盖** | **属实**（grep 铁证） | **已修**：改走 `build_online_cmd`；并加结构性守卫断言「`tmux has-session -t` 在非测试代码里只准出现一次」+「`check_cc_bus_agent_online` 必须调它」 |

**阻塞-2 的教训值得单列**：我做那次「把校验抽成 `build_*_cmd` 纯函数」正是为了修
「断言打在谓词而非调用点」这个失效模式，**结果抽完没接上去**——在修复动作本身里原样复现了
同一个病。§12 里那句「删掉任何一处校验，对应测试立刻红」因此对 online 路径**是假的**。
新加的守卫直接守「构造逻辑只有一份」，比守单个函数更难被绕过。

### 重要项

- **重要-1 两步确认可绕过（已修）**：`spawnArmed` 只在成功执行时复位 → 武装后改目录/改 tool
  再点会**用新值执行**，用户确认过的那句话描述的是从未发生的操作；更糟的是 `if (!dir) return`
  在武装判断**之前**，于是「武装→清空→点击→填新值→点一次」= 一击直达且全程无确认文案。
  UI 文案还写着「点别处不算」而代码没实现——**对用户做代码不兑现的承诺比不承诺更坏**，已删。
  改成**存参数快照比对**（不只是布尔），任一参数变化即作废。
- **重要-2 `parse_agents_tsv` 的 id 校验无会红断言（已修）**：`never_panics_...` 用 `let _ =`
  丢结果，只守 panic 不守语义。对照 `parse_spawned_tsv` 有守——两个同构解析器只守了一个。
- **重要-3 我文档里的数字是错的（已修 4 处）**：「15 行里 8 行坏、53%、多数派」——8 = 5 畸形
  + 3 真空行，而「空行不计入 skipped」恰是我自己在代码里立的契约。**写文档时用了代码里明令
  禁止的口径**。实际 33%（非空行口径 42%），非多数派。设计结论不变，错的是记录。
  连带把单测名 `bad_lines_being_majority_...` 改掉（真实数据上不成立）。
- **重要-4 `skipped` 漏报（部分已修）**：只含制表符的行凭空蒸发 → **已修**（判据改为"去空白后
  为空**且**不含制表符"）；末字段被制表符截断 → **已修**（`f[3..].join("\t")`）；
  4 字段续行残骸变幽灵 agent → **结构性缺陷，行级解析堵不干净**，已在计划登记，不谎称已修。
- **重要-5 L2 被跳过且 §12 漏评（登记）**：见 §15。
- **重要-6 零插值断言打在常量上（已修）**：加守卫断言 `CC_BUS_CAT_CMD` 原样到达
  `connect_and_exec_cmd`，且非测试代码里只出现两次（定义 + 唯一调用点）。

### 建议项处置

- **`build_spawn_cmd` 的 `dir` 没挡前导 `-`（已修）**：`cc-spawn` 的旗标循环跑在位置参数之前，
  `dir="--new"` 会被吃成旗标。加 `--` 显式结束选项即可（比再加一套白名单干净）。
- 长度上界 / `split_combined` 标记冲突 / inbox `text` 非字符串 / `flush()` 余量 /
  `pane` 解析了不用 —— **登记未改**，见 §16。

### 修复过程中我自己又踩的坑（记下来）

**三条结构性守卫，我写的第一版都是坏的**：
① B04 的 `this_module_never_writes` 用 `include_str!` 扫整文件 → **扫到了自己的禁用词清单**；
② 本轮 `online_check_has_exactly_one_command_construction` **把注释里提到的命令名也数进去**（3≠1）；
③ 本轮 `cat_command_reaches_ssh_unmodified` 断言「函数体内无 `format!`」→ **过宽**，
错误消息用 `format!` 是正当的。
→ 固化成一条纪律：**扫源码的守卫必须先剥注释、必须有反向自检（"我确实看到了代码"）、
断言的性质必须正好是要守的那一个**。已抽出 `non_test_code()` 助手并给它自己配了自检测试。

**变异存活两次，都不是测试弱**：指纹比对与解除武装监听器**互相掩盖**（测试里派发了 input
事件，于是监听器先解了武装，指纹那条测不出来）。判明**指纹比对才是承重机制**后，补了三条
「程序化改值（不派发事件）」的隔离测试；解除武装那条则用「按钮文案不得与实际状态不符」
给它一个可观测的门禁——它在有指纹的前提下确实不产生绕过，但会让按钮撒谎。

## 15. 重要-5：L2 被跳过，且 §12 的结论要打折（如实记）

计划 §10 的 L2 写明「spawn 出的 agent 会落 manifest 默认号、消耗默认号额度，**批二做图形化
spawn 时给用户显式选择**」。**批二没做，§12 的落地记录也没提。** 结果是：面板上点两下就在
默认账号上起真 agent 烧额度，二次确认文案里也没写会用哪个账号。

更要紧的是审计指出的这一点：**L2 恰好是 B 段里唯一可能构成成功标准② 验收场地的候选**
（"给远端启动增加一个账号维度"）。§12 断言「B 段全程找不到第二个真实消费者」时，
**没有把这个候选摆出来评估就下了结论**。这不是伪造，但结论成色因此打折。

**订正 §12 的措辞**：应为「唯一候选（L2 的账号选择）被推迟未做，故本段**仍**无场地」，
而不是让读者以为候选集是空的。**L2 提升为 P 段的待办**，做它的时候顺带就是成功标准② 的验收。

另附审计的一条观察，值得记进终账：`cc_bus_spawn` 事实上开了**第二处远端启动命令构造点**
（Rust 侧直接拼 `cc-spawn --tool ...`）。它委托给 `cc-spawn` 而非重写 tmux/会话逻辑，符合
账本 K8 的字面；但"启动一个远端 agent"这件事现在有 IR 与 cc_bus 两条**不共享校验**的路径
——`--` 缺失就是这条新路径独有的毛病，IR 那边不会犯。

## 16. 登记未改（连同 §10 的 L1-L3 一并）

| # | 事项 | 为什么不改 |
|---|---|---|
| M1 | 4 字段续行残骸变幽灵 agent | 行级解析一个"记录内可含 `\n`"的文件，这个洞是**结构性**的；真正的修法是让 cc-bus 写 TSV 时转义换行（**上游的事**，不在本仓）。已在此明记，不谎称已修 |
| M2 | id / text 无长度上界 | 不是安全问题；远端 `ARG_MAX` 撞上后的报错难懂。低优先 |
| M3 | `split_combined` 按标记**首次**出现切分 | 内容里含该标记才会错，低危；可改成只认独占一行的标记 |
| M4 | inbox `text` 非字符串时静默变空且不计 skipped | 类型错误与"字段缺失"被混为一谈，低危 |
| M5 | `flush()` 8 tick 余量偏紧 | 当前断言都落在链前半段，不 flaky；新增看 `reload()` 之后 DOM 的断言时要改 `vi.waitFor` |
| M6 | `CcBusAgent.pane` 解析了从不展示 | 要么显示（`tmux attach -t <pane>` 对用户有用），要么按 R12/R15 同一把尺子删掉。**下轮处理** |

## 17. B04 代码审计结果（Phase D，2026-07-28）

独立对抗性审计（140 分钟，138 次调用）。**无阻塞项**——它逐行核过两个文件，确认
「绝不写入」这条红线**在当前代码里确实成立**。问题全出在**守住它的机制**上。

### 修了什么

| # | 发现 | 处置 |
|---|---|---|
| **B04-1** | **`this_module_never_writes` 可被绕过**。审计往非测试段插了真会写盘的 `fs::rename` + `File::options().write()` + `symlink` + `remove_dir_all`，**15 项测试全绿**。禁用词表只有 6 个字面量，漏掉最自然的几种写法；且它是朴素子串扫（注释里写 `File::create` 会假红）、只扫第一个 `#[cfg(test)]` 之前（之后的非测试代码是盲区） | **已修，且换了范式**：黑名单改**白名单**——非测试代码里 `std::fs::` 只准是 `read_to_string`，其余一律红。**黑名单永远漏，因为写盘的写法列不完。** 另加反向自检（"确实扫到了那处读，否则白名单在空转"）+ 剥注释。**用审计当初绕过它的那五种写法逐一验证：全部拦下；注释里提 `File::create` 不假红。** |
| **B04-4** | **四态里没有「无法判断」**。`program_of` 只取第一个词，于是 `sh -c "cc-register"` / `bash -lc` / `exec` / `env` / `timeout` / `nohup` / `true &&` 全被判成确定性的**「未装」**。源码注释写着"看不懂就返回 None 而不是猜"，但 `None` 落到 `NotInstalled`——**"说不知道"这个设计意图根本没有对应的状态** | **已修**：加第五态 `Unknown`。命令里出现了目标程序但不是被直接执行的那个 → 判"无法判断"，UI 渲染成**中性**而非红。**猜"未装"和猜"已装"一样是猜。** |
| **B04-3** | `${HOME}/...` 花括号形态被判成「装了但路径不存在」——**正是本模块文档头声称要避免的那件事，换个花括号就重现了** | **已修**：`exists` 展开时同时认 `$HOME/`、`${HOME}/`、`~/` |
| **B04-2** | UI「只允许三条只读 invoke」的正则**漏 6 种写法**（不带类型参数 / 单引号 / 模板串 / 嵌套泛型 / 驼峰 / 常量间接）。新增一条 IPC 只要不用那唯一一种写法，白名单完全看不见 | **已修**：扫描器重写；并加一条**守扫描器自身**的测试（喂 7 种写法，都必须被看见）。变异实测四种写法各插一条 evil invoke，全部被抓 |
| **B04-5** | `REMOTE_HOOKS_CMD` **一条守卫都没有**。对比 `cc_bus.rs` 的 `CC_BUS_CAT_CMD` 既有常量守卫又有调用点守卫——「绝不写远端任何文件」在 B04 里曾是纯口头的 | **已修**：常量守卫（零插值、无写动词、含分隔标记、以 `true` 收尾）+ 调用点守卫（必须原样交给 SSH，常量只准出现两次） |
| **B04-7** | 远端 exists 探测面只有 `$HOME/.local/bin`，且 `HAS_PATH_*` 标记的 basename 与目标不等、**等于没参与匹配**。装在 `/usr/local/bin` 且在 PATH 上的**能用的安装**被报成"指不到" | **已修**：改成 `command -v` 直接吐真实路径进清单 |
| **B04-6** | 「推荐，与本机现状一致」是**写死的**，而 `snippet()` 根本不接收诊断结果。用户的 cc-register 只在 `/usr/local/bin` 时，面板仍推荐 `$HOME/.local/bin/...` 并宣称"与现状一致"——贴上去就是一个 `PathMissing` 的钩子 | **已修**：删掉那句承诺，改成只描述两种形态各自的取舍。**与其给一个可能是错的承诺，不如让用户按诊断结果自己选** |
| 建议 | `describeState` 无 `default`，后端加第六态时 `d.ok` 当场抛、整个 `renderDiag` 炸掉 | **已修**：加 `default` 兜底 + 测试（故意传一个类型上不存在的 kind） |

### 登记未改

- **只看 `~/.claude/settings.json` 一处**：还可能有 `settings.local.json`、项目 `.claude/settings.json`；且本机在用 `CLAUDE_CONFIG_DIR`（恰好软链所以对得上，换个没软链的账号库会诊断错文件、而 `source` 字段还会言之凿凿地报错误路径）。**cc-monitor 自己就出多账号隔离，这条值得收**——但它是 B04 的范围扩张，登记进 T02/T07。
- `snippet_is_valid_json_and_round_trips` 用 `&always` 做闭环，**结构上测不到"片段指向不存在的路径"**。要真闭环得让 `snippet()` 接收诊断结果——那是 B04-6 的完整修法，本轮只做了"不再撒谎"，没做"按诊断推荐"。登记。
- `flush()` 对**否定式断言**有假绿风险（预取若藏在第 9 层 await 之后会静默通过）。当前不 flaky。登记。
- `program_of` 的 `trim_matches` 剥两端所有引号，`''x''` 会被吃平。低危，登记。

### 审计确认为真的部分

`unquote_posix` 助手**可信**（它用真 bash + 11 个桩 + canary 文件跑了 29 组 send × 196 组 spawn 组合，全部逐字到达、canary 全空；又用 `shlex.split(posix=True)` 独立还原 259 例零失配）· 真实 `settings.json` 判成能用 · 解析器计数与人工数吻合（37/0 与 7/5）· **成功标准② 的结论诚实** · `632f865` 的 `--` 修复确实堵住了它原本要报的参数注入。

### 一条关于我自己的观察

**本会话我写坏过四条结构性守卫**（B04 这条 + cc_bus 的两条 + 计数那条）。共同点：我总在写"检查代码里有没有坏东西"的黑名单，而**黑名单要求我预先想全所有坏写法**。这次换成白名单（"只准出现这一种好写法"）之后，审计当初绕过它的五种写法一次全被拦下。
→ **纪律更新：结构性守卫优先写成白名单；黑名单只在白名单不可行时用，且必须配一条"用已知绕过手法验证"的测试。**

## 18. L2 落地：spawn 必须表态用哪个账号（B03 审计重要-5）

**问题**：图形化 spawn 没有账号概念 → ccm 落 manifest 的 `isDefault` → 从驾驶舱点两下就在
默认号上起真 agent **烧额度**，用户既没选过也不知道用了哪个号。二次确认文案里也没写。

**改法**：`cc-spawn` 加 `--account/--base` **转发给 ccm**（ccm 本来就支持这两个，
**不改 ccm 本体**——账本红线）；Rust 侧 `cc_bus_spawn` 收 `account: Option<String>`；
UI 加账号下拉。

**刻意不提供"什么都不传"这一档**：`None`/空串 → 转发 `--base`（**显式**不注入）。
让调用方必须表态——选一个号，或明说"就用基座"。默认项是「基座」而不是某个具体账号：
**不替用户默认花掉某个号的额度**。

**复用而非新写**：账号列表走既有 `fetchAccounts` + `selectableAccounts`
（`accounts.ts:130` 的注释明写"可选账号的单一判据，别各处再 filter 一遍"）。
未登录 / `in-place` 逃生口的号不进下拉。拿不到账号时**只留「基座」**——
宁可少一个选项，也不能让用户以为选了号而其实没生效。

**账号名仍过字符集校验**：它来自我们自己维护的 manifest，但 B03 审计的 `--help` 教训摆在
那里（盘上真会出现没人预料的 id）——"这是我们自己的数据"不是免检理由。

**确认文案点名账号**：「消耗额度」不说清是哪个号的额度等于没说。指纹里也含账号——
**换号 = 换花谁的钱**，武装后改账号必须重新确认。

### 成功标准② 第三次落空（如实记）

L2 是 B03 审计指出的**唯一候选场地**。做下来发现：`ccm` 本来就支持 `--account`，
cc-spawn 只需转发，**整条链零涉及 TS 的 launch IR**。加上此前 B02（纯 shell）与
B03 批二（fire-and-forget 远端 exec），**我对这条标准的落点预判已经错了三次**。

`tryRenderCli` 全仓仍只有一个真实调用点。这条标准假定「新维度会持续出现在 IR 里」，
而实际演进方向是**能力长在 shell 侧**（`--detach`/`--tmux-size`/`--account` 全是 shell-only），
TS 的 IR 反而稳定了。**IR 稳定是好事，不该记成"验收失败"。**
→ Phase G 终账按 `unify-launch/features/B03-B04 §12` 的措辞改写标准；**不伪造第二次验收**。
唯一还可能的场地是 P 段若出现"部署受管工具时要在 UI 里选修饰"的真实需求——**若没有就照实写**。

### 变异验收

不传 account → 红 · 默认项改成具体账号 → 红 · 不过滤可选性 → 红 ·
确认文案不点名账号 → 红 · 指纹不含账号 → 红。

**测试隔离**：`fetchAccounts` 带 TTL 缓存会跨测试泄漏（实测第一条的账号列表被后面
"取不到账号"那条读到），故 UI 测试 mock 掉 `../accounts` 模块——它自己有测试，
这里不该顺带重测它的缓存。

## 19. B04 登记项收掉一条：尊重 `CLAUDE_CONFIG_DIR`

**登记时的说法**：诊断只看 `~/.claude/settings.json` 一处，而本机在用 `CLAUDE_CONFIG_DIR`
（恰好软链所以对得上），换个没软链的账号库会诊断错文件、而 `source` 还会报出错误路径。

**实测核实**（只读）：
```
CLAUDE_CONFIG_DIR=/home/zbl/.claude-accts/z
账号 b: settings.json 是软链 → /home/zbl/.claude/settings.json
账号 z: settings.json 是软链 → /home/zbl/.claude/settings.json
~/.claude/settings.local.json: 不存在
```
→ 本机确实**恰好**对得上（cc-acct-iso 把 settings 软链回共享库是它的设计）。
**但那是巧合而非保证**：某个账号库没做软链、或用户手工维护 `CLAUDE_CONFIG_DIR`（R13 提过
这种边缘配置）时，旧写法会读错文件，**而 `source` 字段还会言之凿凿地报出那个没被读的路径
——诊断错了还给出一个看着很确定的来源，比说不知道更坏**。
cc-monitor 自己就出多账号隔离这套东西，不该假设只有一个 config dir。

**改法**：抽出纯函数 `settings_path(cfg_dir_env, home, is_dir)`——
`CLAUDE_CONFIG_DIR` 存在**且确实是个目录** → 用它；否则回落 `~/.claude`。
「确实是个目录」这道判定不能省：环境变量里留一个已删目录或一个文件路径都真实会发生，
那时回落比读一个不存在的路径更有用。

**为什么抽成纯函数**：这段逻辑原本写在 async 命令里，`cargo test` 19 项**没有一条走得到**。
本会话反复吃过这个亏（B03 阻塞-2：抽了 `build_online_cmd` 却没接上，真校验零覆盖）。
抽出来 + 注入 `is_dir` 之后它有了门禁，且已确认**调用点真的走了它**（`:294`）。

**变异验收**：忽略 `CLAUDE_CONFIG_DIR`（退回旧写法）→ 红 · 不校验它真是目录 → 红。

**仍未收的三条**（B04 登记）：`settings.local.json` 与项目级 `.claude/settings.json`
（本机前者不存在，后者要先定「诊断哪个项目」的语义，留 T02/T07）· `snippet()` 不接收诊断
结果故闭环测不到"片段指向不存在的路径" · `program_of` 的 `trim_matches` 剥两端所有引号。
