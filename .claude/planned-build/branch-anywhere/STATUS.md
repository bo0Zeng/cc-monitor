# 状态 / STATUS — branch-anywhere（恢复入口，每次先读这里）

- **当前阶段**：**Phase A 已批准（2026-07-31）**，且**已按真机语料订正一次**（见 `MASTERPLAN.md` §0.3bis 与变更记录 02）
- **已完成功能**：无（尚未开工）
- **下一个**：**G0（缩水版）**——不改行为，只把「我们的落盘格式 == 原生 CC fork」钉成测试 + 子 agent 防御性 reject + 注释写清实证与 SDK 分歧

## 自动模式

**未定**。用户说「审批后开做」，默认按半自动：每个功能的 Phase B 计划仍回来问一次。
用户若说「全自动」再连续跑。

## 用户已拍板的四条 + 一条

| 事项 | 决定 |
|---|---|
| 远端也能 branch | **要**。走 **daemon 新增命令**（不走 ssh 拉整份 jsonl） |
| branch 后旧会话怎么办 | **不杀，两个都活着**。不走 CC 的 `/branch`（那是原地换 sid），走「复制 jsonl + 另起一个」 |
| 新会话的参数 | **全照搬，只换 sid**（账号 / cwd / 是否 tmux 都跟原会话；tmux 名另取，否则撞上原会话） |
| 入口 | **实时会话里也要有**，不只历史查看器 |
| 被 ESC 废弃掉的分支能不能分叉 | **能，要给路口**。理由：ESC 掉的那段数据只剩这一个出口，CC 的 TUI 没有任何路回去。但**呈现要区分**（弱化样式 + 文案说清「这条是被回退掉的」）—— 缺的是信息不是限制 |

## 阻塞 / 待用户确认

| # | 事项 | 状态 |
|---|---|---|
| 1 | ~~主计划审批~~ | **✅ 2026-07-31 已批准**（并已订正一次，见 §0.3bis） |
| 2 | **G2 要收窄 `readonly_guard`**（撞「护栏不动」红线） | **仍等**（G2 开工前要）。方案见 `MASTERPLAN.md §4`：收窄不删，且比今天更强 |
| 3 | G3 若查明历史会话还原不出账号 | 到时如实回报，降级成「分叉时选一次」，不猜 |
| 4 | G3 若需要改 `shared/ccm`（红线「先问」） | 尽量不改；真要改就停下来问 |

## 最高风险项（开工前先看）

1. **账号从哪来** —— `TMUX_LS_FMT` 不含账号且**不许改**；`--session-accounts` 按 PID 扫
   `sessions/<PID>.json`，**对已退出的历史会话可能答不出**。G3 的第一步是查清这件事，不是设计。
2. **G1 的共享方式** —— daemon 刻意不在 workspace（Linux-only 不能拖累 Windows CI）。
   `path` 依赖 / 复制 + 漂移守卫 / 提第三个 crate，三条路要拿实测说话。
3. ~~G0 改的是已发布行为~~ **已消解**——真机语料证明不用改行为。**新的第 3 条**：`logicalParentUuid`（compact 边界回指）真语料里有 4 处，而祖先回溯不跟这条边 ⇒ 「在 compact 之后分叉会不会缺上下文」**没查清**。不猜，要一份「compact 之后 fork」的原生样本才能答。

## loop 停止条件

- 撞到需新决策的阻塞（含护栏、`shared/ccm`、账号还原不出）→ 停
- 同一步 ≥2 次失败 → 停
- 门禁红且非在途变异 → 停
- 全部完成 → Phase G → 停

## 与其他工作区的关系

- **`settings-ia`**：已收官。本区的远端入口会落在它建的「机器」页语义下
- **BACKLOG E52**（消息级 fork）**由本区取代**——且 E52 原文的第一步「查证 CC 支不支持从
  指定 uuid 分叉」是**多余的**：F62 根本不依赖 CC 支持，分支 jsonl 是我们自己写的
- **BACKLOG E50**（daemon 写盘边界）**由本区 G2 解掉**，见 `MASTERPLAN.md §4`
- **BACKLOG E67**（main 的 CI 连红两版）**与本区正交**，但 G2 要动 daemon 侧护栏，
  开工前最好先把 CI 弄绿，否则分不清是不是自己弄红的

## 时间线

- 2026-07-31 Phase A 落盘（本文件 + `MASTERPLAN.md`），等审批
