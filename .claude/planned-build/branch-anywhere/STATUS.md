# 状态 / STATUS — branch-anywhere（恢复入口，每次先读这里）

- **当前阶段**：**全自动执行中**。G0/G1/G2/G3a/G4/G5 完成，G3b-1 完成，下一个 G3b-2（前端接线 + 追问 UI）→ G6
- **已完成功能**：**G0**（格式一致性钉成测试）· **G1**（共享 crate `branch-core`）· **G2**（daemon `--fork-session` + 护栏收窄，E50 已解）· **G3a**（推断内核）· **G4**（实时会话入口）· **G5**（废弃分支呈现区分）· **G3b-1**（本地拉起补账号注入）
- **下一个**：**G3b**（接进 spawn 路：远端走 IR 可直接做；**本地 PS/POSIX 缺 `CLAUDE_CONFIG_DIR` 注入**要补；unknown 的格子弹一次小窗）→ **G6**（远端入口解禁）

## 自动模式

**全自动**（用户 2026-07-31）：连续 B→G 跑完 G0-G6，再做 Phase G，**然后发版**。
只在**真阻断**时停。用户同时授权：「能自定的自定」——例如账号还原不出就按计划降级成
「分叉时让用户选一次」，`shared/ccm` 尽量不改、实在必须改就改并在汇报里说清。

## 用户已拍板

| 事项 | 决定 |
|---|---|
| 远端也能 branch | **要**，走 **daemon 新增命令**（不走 ssh 拉整份 jsonl） |
| branch 后旧会话怎么办 | **不杀，两个都活着**。不走 CC 的 `/branch`（那是原地换 sid），走「复制 jsonl + 另起一个」 |
| 新会话的参数 | **全照搬，只换 sid**（账号 / cwd / 是否 tmux 跟原会话；tmux 名另取，否则撞上原会话） |
| 入口 | **实时会话里也要有**，不只历史查看器 |
| 被 ESC 废弃掉的分支能不能分叉 | **能，要给路口**。那段数据只剩这一个出口，CC 的 TUI 没有任何路回去。但**呈现要区分**（弱化样式 + 文案说清是被回退掉的）—— 缺的是信息不是限制 |
| 要不要改跟官方 SDK 的格式 | **不改**。实证表明我们与官方 TUI 一致，SDK 才是偏离的那个（`MASTERPLAN.md §0.2/§0.3`） |

## 阻塞 / 待用户确认

| # | 事项 | 状态 |
|---|---|---|
| 1 | 主计划审批 | **✅ 已批准** |
| 2 | ~~G2 收窄 `readonly_guard`~~ | **✅ 已批准并已完成**（G2）。两层护栏落地，E50 已解，边界进 `INVARIANTS §41.6` |
| 3 | G3 若查明历史会话还原不出账号 | **已授权自定**：降级成「分叉时选一次」，不猜；汇报里说清 |
| 4 | G3 若需要改 `shared/ccm` | **已授权自定**：尽量不改；实在必须改就改并在汇报里说清 |

## 开工前先看

1. ~~账号从哪来~~ **已查清（G3a）**：**还原不出**。pidfile 随进程消失 · 各账号 `projects` 是软链到
   共享目录（同 inode）· jsonl 里没有账号字段。⇒ 已退出的会话一律 `unknown`，**不猜**，由 G3b 追问。
2. ~~G1 的共享方式~~ **已定**：共享 crate `branch-core` + 单向 path 依赖（实测不破坏 daemon 的独立性）。
   **记住**：path 依赖不进 `cargo test --all`，新增此类 crate 必须同时在 CI 与发版 checklist 加 `-p`。
3. **CI 目前是红的**（BACKLOG E67，连红两个版本）。G2 动 daemon 侧护栏之前最好先弄绿，
   否则分不清是不是自己弄红的。
4. **别拿 SDK 当规范** —— 规划本区时栽过一次，详见 `MASTERPLAN.md §0.3` 的警示。

## loop 停止条件

- 撞到需新决策的阻塞（含护栏、`shared/ccm`、账号还原不出）→ 停
- 同一步 ≥2 次失败 → 停
- 门禁红且非在途变异 → 停
- 全部完成 → Phase G → 停

## 与其他工作区的关系

- **`settings-ia`**：已收官。本区的远端入口落在它建的「机器」页语义下
- **BACKLOG E52**（消息级 fork）**由本区取代**。E52 原文的第一步「查证 CC 支不支持从指定
  uuid 分叉」是多余的：F62 不依赖 CC 支持，分支 jsonl 是我们自己写的
- **BACKLOG E50**（daemon 写盘边界）**由本区 G2 解掉**，见 `MASTERPLAN.md §4`
- **BACKLOG E67**（CI 连红两版）与本区正交，但见「开工前先看」第 3 条

## 时间线

- 2026-07-31 Phase A 落盘 → 用户批准 → 按真机语料重写技术核心（见 `MASTERPLAN.md §8-02`）
