# cc-monitor 项目全面审阅报告

> 日期：2026-07-25 · 分支：`account-ux`（10 commits，未 push / 未合并 / 未发版）
> 方法：full-audit —— 4 视角并行独立排查（整体设计 / 文档工程 / 代码工程 / 实现细节）+ 主线程文档↔代码交叉验证。
> 基线：**tsc 0 error · npm test 566 passed（41 vitest + 14 tsx）· vite build ✓**（唯一非阻塞警告：主 JS chunk 897 kB > 500 kB，历史既有）。

## 摘要

- **整体健康度**：骨架稳健、文档化程度罕见地高、失败语义严谨、daemon 只读边界干净——但账号子系统留了**一个合入前必修的正确性漏洞**，且**对外门面文档（README 中/英）对已发版 3.2.0 系统性过期**。
- **阻塞 1 项**（合入 `account-ux` 前必修）· **重要 8 项** · **建议若干**。
- 值得强调：4 个视角里**只有实现细节视角发现了 1 个真正的阻塞级 bug**；设计/工程视角都明确、诚实地判定"无阻塞、均为债务"。这个 bug 此前已被项目自己的 `BACKLOG.md` 以 G4-2 记为"重要"——本轮把它**升级为阻塞并给出确定性复现路径**。

---

## 一、阻塞（合入前必修）

### 🔴 B1 · follow-resume 用内存镜像覆盖磁盘真实 pin，静默破坏"会话粘性账号"核心不变量
**位置**：`src/tabs.ts:1855`、`src/tabs.ts:1911`（两处）+ `src/accounts.ts:483` + `src/main.ts:233-240`
**已交叉验证**（读源码逐行确认）：
- `withAccount` 的不-clobber 守卫（`accounts.ts:480-484`）判据 `priorPin` 来自**调用方传入**的值，而非磁盘真相。
- **history 入口是对的**：`views/history.ts:1488-1490` 每次**现读** `invoke("list_last_accounts")` 拿 pin 再传给 `follow`。
- **tabs 两处是错的**：`resumeTab`(1855) 与 `resumeTabTmux`(1911) 传的是内存表 `this.accountLastByS.get(sid)`。
- 该内存表由 `refreshSessionAccounts`（`main.ts:202`）在**启动时 + 每 10s** 重建；`list_last_accounts` 抛错时（`main.ts:237-239`）`lastByS` 保持空 Map 并在 `:240` **无条件**推给 tabs。

**确定性触发路径**（任一）：
1. app 启动后首轮 refresh 完成前的数秒——内存表是构造时的空 `Map`；
2. 任一次 `list_last_accounts` invoke 抛错——空表被无条件覆盖进 tabs；
3. 刚用"用账号 z resume"显式钉 pin（写盘）后 **10s 内**对同一会话做普通 resume——内存表还没轮询到新 pin。

**后果**：`accountLastByS.get(sid)` 返回 `undefined` → `priorPin=null` → `resolveFollowAccount` 下沉到 `current`（当前工作账号）→ 因 `!priorPin` 为真，`recordName=followName`（`accounts.ts:483`）→ `recordLastAccount` 把磁盘 pin 从真实的 `z` 静默改写成 `current`。**持久、无提示、不自愈**（之后徽章/follow 都跟着错账号走）。

**严重性定级说明（抗迎合）**：分支未合并/未发版，所以这是"**合入前必修**"而非"生产事故"。但它直接击穿了 U3 审计**已经为 history 路径修好**的同一条不变量——同一契约在一个入口对、另一个入口错，这种非对称是"真缺陷"而非"有意取舍"的强指纹。

**建议修法**：把 `tabs.ts` 两处对齐 history 的做法——resume 前**现读** `invoke("list_last_accounts")` 拿该 sid 的 pin 传给 `follow.lastAccount`，不要读 `accountLastByS`。与 U3 给 history.ts 的修法逐字一致。改完加"内存表为空/陈旧时不得 clobber 磁盘 pin"的回归测 + 变异验证。

---

## 二、重要

### 🟠 I1 · `kill_remote_tmux` 缺 `cc-*` 白名单，可 kill 掉用户自己的整个 tmux 会话（+ 注释撒谎）
`src-tauri/src/tmux.rs:140`。**已交叉验证**：`tmux_send_keys`(`:192`) 有 `is_ccm_tmux_name` 门控，`kill_remote_tmux`(`:140`) **没有**。触发：用户 `tmux new -s work` 里跑 claude（非 `cc-` 名 / 无 `@ccm_sid`），右键"杀死会话"经 cwd 回退命中 `name="work"` → `tmux kill-session -t work` 端掉整个 `work` 会话所有 window。破坏性重启/对齐路径**安全**（有 `live.sid===sid` @ccm_sid 精确守卫）。附带 doc-code drift：`account-restart.ts:21` 注释"后端白名单只认 cc-*"对 kill **是假的**。建议给 `kill_remote_tmux` 补同一条白名单（防御纵深），并订正注释。

### 🟠 I2 · README（中/英）对已发版 3.2.0 系统性过期——版本号 + 悬空引用 + 零账号覆盖
`README.md:5` / `:259` / `README.en.md:5` 停在 `v3.0.0`（实际 `package.json`/`Cargo.toml`/`tauri.conf.json` 三处一致 `3.2.0`，CHANGELOG 四段齐全）；`README.md:9` / `README.en.md:9` 指向 CHANGELOG 不存在的「[未发布]段」（grep 0 命中）且"上一发布 v3.0.0"错；README 全文对 `账号/account/CLAUDE_CONFIG_DIR` grep **0 命中**，而多账号是 3.2.0 头号新增。对外第一站在版本、状态、功能三点上误导。

### 🟠 I3 · 发版 checklist 结构性缺 README 两条 = 门面过期的机制性根因
`doc/RELEASING.md:7` 与 `doc/CONTRIBUTING.md:77-83` 只列"版本三处对齐 + CHANGELOG"，**没有** README.md(×2)/README.en.md 版本行、也没有"更新 README 功能列表"行。不补这条，修完 I2 下次发版仍会重现（BACKLOG 自称"连续三次漏改")。

### 🟠 I4 · `refreshSessionAccounts` 无重入/顺序保护，慢的旧快照可覆盖新快照
`src/main.ts:202-248`。`onDefaultChanged`（切号即触发）/chip 即时重算 与 10s 定时器并发，某次 SSH 轮询慢后完成 → `setSessionAccounts` 无条件替换 → 晚到旧快照覆盖新的 → chip 显新账号但 tab 对齐/⚠k 仍按旧账号（重开"切号反向窗口"）。概率偏低、10s 自愈，但正落在"刚切号"敏感时刻。加 in-flight 序号门即可。

### 🟠 I5 · 切默认账号只失效主 origin 缓存，多远端下其它远端最长 30s 用旧账号
`account-chip.ts:299-304` / `settings/accounts-section.ts:489-490` 只 `invalidateAccountsCache(this.origin)`；`defaultName` 是全局单值但缓存按 origin，其它 origin ≤30s（TTL）判"不一致"/对齐目标错，且 follow 会按旧账号持久化 pin。仅多远端命中、30s 自愈。

### 🟠 I6 · 两个"上帝文件"是脊柱级维护风险
`src-tauri/src/ssh_source.rs` **4512 行**（传输/快照子系统/协议解析/导入工具 ~10 职责混居，远端功能族脊柱）；`src/tabs.ts` **2934 行 / 73 私有字段**（TabManager 前端 god object，账号子系统又加 ~350 行 15 方法，把徽章/mismatch/align 状态判定塞进来而非抽 `AccountBadgeController`）。重编排已干净外置到 `account-restart.ts`（带 ADR 式注释），只差把徽章状态也移出。这是"vanilla-DOM 无框架在此规模是否撑得住"的答案：撑得住但已明显吃力。

### 🟠 I7 · daemon 只读性无结构护栏，且 daemon 整体会 `sh -c` shell out
`remote-daemon-proto/src/watcher.rs:78-93` `run_tmux_ls` 用 `Command::new("sh").arg("-c")` 跑 `tmux ls`。**交叉验证正面结论**：账号查询 `accounts_query.rs` 确为纯只读（仅 read/read_dir/metadata、不 shell out、不读 stdin 改状态、`.credentials.json` 只 stat、`session_process_identity_ok` 防 PID 复用误归属，实现质量高）。**但**：INVARIANTS §1「零写、不 shell out」只对 accounts 路径成立，daemon 整体已 shell out；只读性纯靠人工评审维持（无沙箱/只读挂载/能力裁剪），随手一个 edit 就能加写路径而 CI 全绿。这是"最重要的不变量却唯一纯靠人审"。

### 🟠 I8 · TS/CSS 侧零 lint / 零 formatter / 零覆盖率门禁 + ~2000 行零测试加载
全仓无 eslint/prettier/stylelint/biome/editorconfig；唯一强制风格门禁是 Rust `cargo fmt --check`。`src/main.ts`（1053 行组装根）+ 另 8 个源文件（合计 ~1990 行）从未被任何测试加载。`styles.css` 5964 行无类名对账（类名以字符串散在 TS 里，tsc/vitest 看不见死类/拼写错）。110 个 `#[tauri::command]` vs TS 30 个裸字符串 `invoke`，IPC 契约靠字符串裸对、重命名只在运行时炸。

---

## 三、建议（近期 / 可选）

- **测试基建**：`vitest.config.ts` 无 `mockReset`/`restoreMocks`（23 vitest 用 `vi.mock` 仅 11 自清 → spy 跨用例泄漏）；14 个 tsx `*.test.ts` 手工 `&&` 串联 + vitest glob 两套发现模型并存（当前无破损，风险在未来漏挂）；建议纯函数测试并入 vitest 统一 runner + 上覆盖率。
- **vendor `code-picture-core` 25 个单测 CI 永不运行**（非 workspace 成员，`cargo test --all` 跑不到）；`e2e/` 未接入 CI；clippy 仅警告无棘轮。
- **`remote-section.ts`(1801) 分层倒挂**：数据层唯一真相源（`readRemoteConfig`/`writeRemoteConfig` 等）住在 UI 模块里、被 7 个含非 UI 的模块依赖，测试被迫整体 mock。抽 `src/remote-config.ts`。
- **架构脊柱两处**：`lib.rs` `setup()` ~500 行 god function（内联 23 命令 + spawn 7 State），按域搬回模块；跨二进制契约（`TMUX_LS_FMT` 双写点、`wire::Frame` ↔ `InboundFrame` 两份定义）加机器化对账。
- **文档卫生**：`resumeTab` 显式选号缺 `onUnselectable`（静默落基座，不 clobber 故不升级，`tabs.ts:1852`）；`ARCHITECTURE.md`/`STATE-MATRIX.md`/`src/README.md`/`src-tauri/README.md` 补账号子系统；仓库级事实（无浅色主题/`color-scheme:dark`/TOKENS 11 token）从 `account-ux/MASTERPLAN.md:122` 上移 `INVARIANTS.md`；`doc/` 两份"设计草案未写码"文档移入 `proposals/`；`.claude/planned-build/` 加索引 README；README「26 个 action」订正为实际 ~30 + 快捷键表补 `Acct`/`G`。

---

## 四、文档-代码漂移（交叉验证专项）

| 文档声称 | 代码实况 | 结论 |
|---|---|---|
| README `v3.0.0` | package.json/Cargo.toml/tauri.conf.json = `3.2.0` | ❌ 过期 |
| README「Batch15 开发中未发布」「上一发布 v3.0.0」 | CHANGELOG [3.1.0] 已发 Codex+全景，最新 [3.2.0] | ❌ 系统性过期 |
| README/ARCHITECTURE 无账号功能 | `lib.rs` 已注册 `accounts::list_remote_accounts`/`list_remote_session_accounts`/`check_account_trust` + 前端 `account-*.ts` | ❌ 已交付功能门面隐身 |
| `account-restart.ts:21`「后端白名单只认 cc-*」 | `kill_remote_tmux` 无白名单（只有 `tmux_send_keys` 有） | ❌ 注释撒谎 + 安全缺口（见 I1） |
| INVARIANTS §1「daemon 零写、不 shell out」 | accounts 路径确实只读；daemon 整体 `run_tmux_ls` 会 `sh -c` | ⚠️ 仅对 accounts 成立（见 I7） |
| STATE-MATRIX 自订「IPC 变化→更新§2」 | 4 条新账号 IPC 命令未登记 | ❌ 违反其自身规则 |
| BACKLOG §D 文档债 / §A2 G4-* | 独立复核确属真实、大多未修 | ✅ 登记准确（登记≠修复）|

**正面**：三层契约有单一来源（`bridge.rs`）、ADR 式注释、`every_capability_token_is_strippable` 把约定拉成代码强制、release.yml 4 路版本一致性门禁 + daemon build_id 双向核验、Rust 测试深度（monitor ~354 / daemon 124 内联）——这些都是同规模项目里少见的工程严谨度。

---

## 五、优先级建议

**立即修（合入 `account-ux` 前）**
1. **B1 pin-clobber**：tabs.ts 两处改为现读磁盘 pin（对齐 history.ts）+ 回归测 + 变异验证。← 唯一阻塞。

**近期处理（合入时 / 发版时一起）**
2. I1 `kill_remote_tmux` 补 `cc-*` 白名单 + 订正 account-restart.ts:21 注释。
3. I2+I3 README 中英修版本/删悬空引用/补账号小节 + RELEASING/CONTRIBUTING checklist 补 README 两条（先修根因，否则复发）。
4. I4 refreshSessionAccounts 加 in-flight 序号门；I5 切号失效全 origin 缓存。
5. 文档漂移批量修（ARCHITECTURE/STATE-MATRIX/子 README/INVARIANTS 上移）。

**可选优化（立棘轮，别一次做完）**
6. I8 TS/CSS lint + 覆盖率门禁；I6/I7 脊柱拆分（`AccountBadgeController` / ssh_source 分模块）+ daemon 只读机器化护栏 + 跨 crate 契约对账；vendor crate 进 CI；vitest mock 卫生。

---

*报告完。审阅≠修复：以上未改动任何代码。B1 与 I1 是本轮"新增/升级"的实质发现，其余多数与项目自建的 `BACKLOG.md` 吻合（说明遗留项登记机制在起作用）。*
