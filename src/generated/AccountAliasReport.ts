// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { AccountAliasRc } from "./AccountAliasRc";

/**
 * 一次生成的结果 —— **预览与真写走同一个返回形状**，差别只在 `wrote_*` 那两格。
 */
export type AccountAliasReport = { 
/**
 * 生成文件的绝对路径。
 */
aliasPath: string, 
/**
 * 这一份文件里会有哪几条命令（按传进来的顺序）。
 */
names: Array<string>, 
/**
 * 名字撞了的那几条，每条一句人话。**只出声，不拦** —— 见模块头注。
 */
collisions: Array<string>, 
/**
 * 这台机器上找得到的 shell 配置候选。
 */
rcCandidates: Array<AccountAliasRc>, 
/**
 * 真写了生成文件吗（预览恒 false）。
 */
wroteAliasFile: boolean, 
/**
 * 内容与盘上那份逐字相同 ⇒ **一个字节都没写**。
 */
aliasFileUnchanged: boolean, 
/**
 * 往用户 rc 里加了那一行 `source` 吗（已经有了 ⇒ false，且 `notes` 里说明）。
 */
wroteRc: boolean, 
/**
 * 给人看的补充说明，一条一句。
 */
notes: Array<string>, };
