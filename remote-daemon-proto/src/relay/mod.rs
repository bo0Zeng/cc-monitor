//! `K-H1` 甲半：**HTTP 中转 · 搬字节**。纯基础设施 —— 它不懂任何 agent 的语义。
//!
//! # 它做四件事（`K9` 裁定二那四条硬要求，逐字）
//!
//! 1. **一个进程** —— 一个监听面服务 N 个会话，不是每会话一个。
//! 2. **按路径前缀分流** —— 路由键塞在 base URL 的路径里
//!    （`/s/<agent>/<account>/<key>/<真路径>`；`<account>` 那一段是 `K-H2` 加的，
//!    理由与代价逐条住 `route.rs` 头注）。
//! 3. **逐块透传绝不缓冲** —— 上游每给一块就立刻写下游并 flush，从不攒整个响应体。
//!    这是本方案**唯一真正的技术点**：缓冲了 TUI 会卡住不出字。
//! 4. **一边流回 CLI 一边 tee** —— 同一批字节既原样写回下游，又抄一份进 tee 流。
//!
//! # 三条**有意的偏离**，别当成移植漏了
//!
//! ## ㈠ tee 落在 **stdout**，不是文件（`裁-2` 在今天的代码上做不到）
//!
//! `裁-2`（PM 08-25）要 tee 走「每次响应一个新文件（`O_EXCL` 新建）」，理由是
//! 「新建一个此前不存在的文件不违反只读判据」。**那句话住在只读护栏的白名单层头注里，
//! 而白名单层只作用于唯一一个模块**（一个按仓库相对路径全等匹配的 `&str` 常量）。
//! 本模块落在**默认层**，而默认层的模式表把「以选项方式打开」与「新建句柄」那两个
//! 标准库入口的名字整个禁掉了 —— 标准库里「O_EXCL 新建」的每一种写法都含其中之一。
//! ⇒ 「判据一个字节不动」与「本模块自己新建文件」**不可能同时成立**。
//!
//! ⚠ 上一段**刻意不写出那两个入口的字面名字**：只读护栏是子串扫描、**不剥注释**，
//! 把它们原样写进注释，本模块会被自己的文档判成违规（写这份注释的第一版就是这么红的，
//! 而 `control/fork_write.rs` 的头注早把这条纪律逐字写着：**要改就改措辞，别去放宽护栏**）。
//!
//! 三条能绕开子串扫描的写法（把调用放进那个白名单模块 / 用类型别名改写调用 /
//! 直接走 `libc` 的系统调用）**一条都不走**：它们守住了护栏的字面、守不住它的意图。
//! ⇒ 本轮 tee 写 **stdout**。
//!
//! ⚠ 订正〔回修轮 08-25，承接 D1 `建议-4`/`建议-5`：这两句先前把话说大了一格〕：
//! - 先前写的是「那是护栏头注**逐字承认的唯一合法写**」。**不逐字。**
//!   护栏头注那句带**两个限定**，中转这一条**两个都不满足**：它说的是「把 **wire 帧** 写 stdout
//!  （`main.rs` 里那**一个**异步调用点）」，而 tee 写的是自己的 **NDJSON 行**、走的是
//!   `relay/tee.rs` 里**另一个**同步调用点。
//!   ⚠ 再订正一格〔回修轮之四 08-25，承接 D2 `建议-4`〕：先前这里写「**护栏承认「写 stdout」
//!   这一类**」—— 那仍是**推论，不是引文**。护栏头注的字面是「**唯一**合法的「写」是把 wire 帧
//!   写 stdout」，它**没有**承认「写 stdout」这一类。
//!   **机制上确实放行，但理由不在那句话里**：我读了它的实现 —— 它扫的是**文件系统变更调用**，
//!   **stdout 根本不在它的扫描面里**。⇒ 准确说法只有两句：①那句引文**不覆盖**本处；
//!   ②本处今天不撞，靠的是护栏的**扫描面**与下面那句「独立进程」，**不是**那句引文。
//! - 先前写的是「本进程**一个字节也不写文件系统**」。**这条设计的用法就是让启动方把 stdout
//!   重定向到文件** ⇒ 那些字节确实经由本进程的 fd 落到文件系统。
//!   准确说法：**本进程不自己打开 / 创建任何文件**，落不落盘由启动方的重定向决定。
//! - 行为面为什么今天不撞：`--relay` 住**一次性子命令**分派臂（`main.rs`），是**独立进程**，
//!   它的 stdout 不与流式那条 wire 流共享。⚠ 而「谁在同一个进程里既跑流式又跑中转」
//!   **今天没有任何判据挡着**（那条「只有一个写者」的判据人群只有三个文件，**不含 `relay/`**）。
//!
//! ## ㈡ 每行**不带 `t_ns`**（`裁-3`）
//!
//! 参考实现每行带一个跨响应单调的 `t_ns`（`Instant::now` 的等价物），而
//! `Instant::now` 在零定时器护栏的禁用清单里**且没有登记口**。⇒ 第一刀不带它。
//! **如实说这丢了什么**：`t_ns` 在参考实现里是一条**修过的 bug**（跨响应单调），
//! 不带它 = 暂时放弃那个修复面 ⇒ **任何要按时间对齐 tee 行的下游都得等这条解锁**。
//! 解锁条件：真出现需要它的消费者时，单独立一件去处置那个守卫。
//!
//! ## ㈢ 上游 `Accept-Encoding` 收窄成 `identity`，不是 `gzip`
//!
//! 参考实现收窄成 `gzip` 并自己流式解压，因此要一条解压依赖。Rust 重写改成收窄成
//! `identity`：tee 侧拿到的直接是明文 SSE，**整条解压依赖不需要**。
//! 代价是上游那一跳不再压缩（真实带宽，不是回环）。
//!
//! # 还有一条**没做**的，写在这里免得被读成「做了」
//!
//! ⚠ 订正〔回修轮之六 08-26〕：这一节先前的第一条逐字写着「**不设任何读写期限**」——
//! **那句话今天是假的，已收口**。经过：`阻-3(D3)` 的后半段（期限）在回修轮之五因为
//! `no_timer_guard.rs` 不在写区而交回 PM（§8.20.4），PM 收 R5 时**扩了写区一格**并派了 R6（§8.21.3）。
//! 今天的形状是**顶不满 + 拒绝有声 + 顶住的会自己散**，三样齐了：
//! - 上界与出声：`server.rs::INFLIGHT_CONNECTIONS`（超了回 503）；
//! - 会自己散：`server.rs::DOWNSTREAM_DEADLINE`（本机对端，**30 秒**）与
//!   `upstream::UPSTREAM_DEADLINE`（模型在想是正常的，**600 秒**，就是这里记的那个参考数），
//!   两条 socket 的**读写两个方向**都装。
//!   写法上没有走 `Duration::from_secs`（它在零定时器护栏的**禁用清单**里），
//!   走的是 `Duration::from_millis` + **往 `no_timer_guard.rs::REGISTERED_DURATION_USES` 加两行**
//!   并逐条写明「socket 期限是一次阻塞的上限，不是周期性唤醒」。
//!   ⚠ 往一张「**恰好相等**」的表里加行是**收紧**（逼你说清那个 `Duration` 干什么用），
//!   与放宽那条断言方向相反 —— 断言本身**一个字节都没动**。
//! - ⚠ **仍然没有端到端等满 30 秒的判据**（那要跑 30 秒）：这个结论由两格拼出来 ——
//!   `both_peers_really_carry_their_read_and_write_deadline_on_the_socket`（那个数真装上了）
//!   + `a_socket_deadline_makes_a_half_open_read_return_instead_of_wedging_the_thread`（装上之后读会报错返回、无人重试）。
//!
//! - **`connect` 那一步没有期限**。`upstream::connect` 用 `TcpStream::connect((host, port))`。
//!   故意不做：读/写是**真无界**（对端不发就永不返回），而 connect 有内核 SYN 重试上限
//!   与解析器自己的上限兜着 —— **有上限**。⚠ 那个上限具体多少**我没量** ⇒
//!   只敢说「不是无界」，不敢说「够小」。真要收紧得先量它（且 `connect_timeout` 要 `SocketAddr`，
//!   得先自己解析），是另一件活。
//! - **不碰凭据**。请求头原样转发，**一个都不落进 tee、不落进日志**。
//!   ⚠ 订正〔回修轮之五 08-25，D3 `重要-3(D3)`〕：这一条先前引 `K11 裁定一`「不记录 ≠ 不持有」
//!   当依据，而 `K11 裁定一` **08-25 已被改判**，现行正文逐字要的是
//!   「中转在转发时**替换** `Authorization` 头」。⇒ 那条裁定支持的是「不记录」，
//!   **不支持**「原样转发」。准确说法两句：①本件（甲半·搬字节）**原样转发、不替换**；
//!   ②「换头」是**下一件**的活，而 `K11 裁定一` 自己写着硬前置，逐字「**没有那条新判据之前，
//!   不许把 key 接进中转**」。逐条订正住 `server.rs::render_upstream_request` 的头注。

mod bind_guard; // `DoD-4㈠`：零命中守卫单住一个文件（理由见它的头注）
mod creds; // `K-H2a`：中转从哪儿拿 key（**只读**）+ 读之前查一次权限
mod creds_guard; // `K-H2a` `KS2`/`KS4`：明文出口恰好一处 · 记日志走白名单（整体 #[cfg(test)]）
mod http1;
mod nodelay_guard; // `重要-5`：Nagle 零命中守卫，同样单住一个文件
mod route;
mod server;
mod table; // `K-H2`：路由表 —— 账号段 → **上游与 key 焊死的一个值**（`Row` 住私有 `mod sealed`）
mod table_guard; // `K-H2` `KH1`：决定点三条腿里的两条（焊接点 · 开上游连接点），整体 #[cfg(test)]
mod tee;
mod upstream;

pub(crate) use server::run;
