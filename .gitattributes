# 统一行尾：仓库与工作树都用 LF，消除 Windows 上每次 commit 的
# "LF will be replaced by CRLF the next time Git touches it" 警告。
# 源码（rs / ts / js / css / html / json / md / toml / yml…）全部 text=auto + eol=lf。
* text=auto eol=lf

# Windows 批处理脚本若存在，必须 CRLF（否则部分 cmd.exe 解析异常）。PowerShell .ps1 用 LF 没问题。
*.bat text eol=crlf
*.cmd text eol=crlf

# 二进制资源：不做行尾转换、不当文本 diff。
*.png   binary
*.jpg   binary
*.jpeg  binary
*.gif   binary
*.ico   binary
*.icns  binary
*.ttf   binary
*.otf   binary
*.woff  binary
*.woff2 binary
*.exe   binary
*.msi   binary
*.dll   binary

# C01（rust-ts-boundary）：ts-rs 生成物，diff 默认折叠。源是 src-tauri/src/**.rs 的
# #[cfg_attr(test, derive(ts_rs::TS))]，重新生成走 `npm run gen:types`。
#
# **别把这条保证挂在 vitest 守卫上**（Phase D 审计 B2 实测：手改生成物、甚至插一个 Rust 侧
# 根本不存在的字段，5 条守卫 + tsc **全绿**）。真正抓它的是 **C05**：`ci.yml` 的 `rust` job
# 在 `cargo test --all` 之后跑 `git diff --exit-code -- ../src/generated/`
# ——CI 从 commit checkout，重新生成产出正确版 ⇒ **已提交的**手改必然 diff 非空。
# （未提交的本地手改会被重新生成静默冲掉，从来到不了仓库。）
src/generated/** linguist-generated=true
