# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
coverage
*.local

# Rust build artifacts
src-tauri/target/
src-tauri/gen/schemas/
target/

# Batch15：code-picture 索引任何仓时生成的派生索引侧车（含索引 cc-monitor 自身时）
.codepicture/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Phase G 收尾：vendor 的 code-picture 副本里 cargo 生成的 Cargo.lock 是游离产物
# （被 workspace 根 lock 架空、构建不用它），不进版本控制。re-vendor（F68）时别提交它。
src-tauri/vendor/**/Cargo.lock
# U0（2026-08-01）：`src-tauri/crates/` 下的 path 依赖 crate 同理 —— 单独
# `cargo test -p <crate>` 会在那里落一个被上层架空的游离 Cargo.lock。
# ⚠ **G2（2026-08-04）订正**：那时它们**确实不是** workspace 成员；
# 现在 `src-tauri/Cargo.toml` 有 `[workspace]`，六个 crate 都是**真成员**，
# cargo **不会再**在子目录落 lock（已删掉存量的 6 个）。
# 这条 ignore **刻意留着**：它零成本，且 re-vendor / 有人临时在子目录跑 cargo 时仍兜得住。
# 本仓红线是「显式 `git add` 文件清单、绝不 `git add -A`」，别让它出现在 `git status` 里。
src-tauri/crates/**/Cargo.lock
__pycache__/

# planned-build agent worktrees (transient, never commit)
.claude/worktrees/
